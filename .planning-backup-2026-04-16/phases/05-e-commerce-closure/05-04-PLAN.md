---
phase: 05-e-commerce-closure
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/backend/src/modules/cart/cart.service.ts
  - apps/backend/src/modules/cart/cart.controller.ts
  - apps/backend/src/modules/order/order.service.ts
  - apps/backend/src/modules/order/order.controller.ts
autonomous: true
requirements:
  - COMM-03
  - COMM-06
  - COMM-08

must_haves:
  truths:
    - "Cart validates stock before adding items and shows discount with coupon"
    - "Order supports coupon application, confirm receipt, structured tracking"
    - "Auto-confirm cron job confirms shipped orders after 15 days"
    - "Frontend payment flow connects to existing PaymentService"
  artifacts:
    - path: "apps/backend/src/modules/cart/cart.service.ts"
      provides: "Stock validation, coupon calculation, batch operations"
      contains: "getCartSummary"
    - path: "apps/backend/src/modules/order/order.service.ts"
      provides: "Coupon integration, confirm receipt, structured tracking, auto-confirm"
      contains: "confirmReceipt"
  key_links:
    - from: "apps/backend/src/modules/cart/cart.service.ts"
      to: "apps/backend/src/modules/coupon/coupon.service.ts"
      via: "CouponService.validateCoupon + calculateDiscount"
      pattern: "couponService\\.(validate|calculate)"
    - from: "apps/backend/src/modules/order/order.service.ts"
      to: "apps/backend/src/modules/coupon/coupon.service.ts"
      via: "CouponService.useCoupon on order creation"
      pattern: "couponService\\.useCoupon"
---

<objective>
Enhance CartService with stock validation, coupon integration, batch operations (COMM-03 per D-12/D-13/D-14). Enhance OrderService with coupon application, confirm receipt, structured tracking timeline, auto-confirm cron (COMM-06/08 per D-15/D-16/D-18).

Purpose: Cart becomes a full-featured e-commerce cart with pricing and promotions. Order lifecycle becomes complete with coupon support and auto-confirmation.
Output: Enhanced cart.service.ts, order.service.ts, their controllers
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e-commerce-closure/05-CONTEXT.md
@.planning/phases/05-e-commerce-closure/05-RESEARCH.md
@apps/backend/src/modules/cart/cart.service.ts
@apps/backend/src/modules/order/order.service.ts
@apps/backend/src/modules/payment/payment.service.ts
</context>

<interfaces>
<!-- CouponService from Plan 01 -->

From Plan 01 coupon.service.ts:
- `validateCoupon(code, userId, orderAmount, categoryIds?, brandId?)` -- returns validation result
- `calculateDiscount(coupon, orderAmount)` -- returns discount amount
- `useCoupon(userCouponId, orderId)` -- marks coupon as used in transaction
- `getApplicableCoupons(userId, cartItems)` -- returns coupons valid for cart
- `getUserCoupons(userId, status?)` -- lists user coupons

From payment.service.ts:
- `createPayment(orderId, provider, amount)` -- creates payment record, returns payment params
- AlipayProvider and WechatProvider handle actual payment flow
- Callback verification with signature validation
- Redis idempotency lock for callbacks
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Cart enhancement -- stock validation, coupon, batch ops</name>
  <files>
    apps/backend/src/modules/cart/cart.service.ts,
    apps/backend/src/modules/cart/cart.controller.ts
  </files>
  <read_first>
    - apps/backend/src/modules/cart/cart.service.ts
    - apps/backend/src/modules/cart/cart.controller.ts
    - apps/backend/src/modules/coupon/coupon.service.ts
    - .planning/phases/05-e-commerce-closure/05-CONTEXT.md (D-12, D-13, D-14)
  </read_first>
  <action>
Enhance `apps/backend/src/modules/cart/cart.service.ts`:
Inject CouponService (via forwardRef if circular dependency exists).

1. **Add stock validation to `addItem`**: Before adding, query ClothingItem.stock. If stock < requested quantity, throw BadRequestException("库存不足"). If item isActive=false or isDeleted=true, throw BadRequestException("商品已下架").

2. **Enhance `getCartSummary` with coupon support**: Add optional `couponCode` parameter. If provided:
   - Call couponService.validateCoupon(code, userId, totalAmount)
   - Call couponService.calculateDiscount(coupon, totalAmount)
   - Return summary with: { totalItems, selectedItems, totalAmount, selectedAmount, discountAmount, shippingFee, finalAmount }
   - Shipping fee: free if selectedAmount >= 99, otherwise 10 yuan

3. **Add `getInvalidItems(userId)`**: Find cart items where ClothingItem.isActive=false OR ClothingItem.isDeleted=true. Return list with item details for frontend to display.

4. **Add `checkPriceChanges(userId)`**: Compare cart item prices (stored at add time or current) with current ClothingItem prices. Return [{cartItemId, oldPrice, newPrice}].

5. **Add `batchDelete(userId, cartItemIds: string[])`**: Delete multiple cart items in one transaction.

6. **Add `moveToFavorites(userId, cartItemIds: string[])`**: In transaction: create Favorite entries for each item, then delete cart items.

7. **Add `updateItemSku(userId, cartItemId, color?, size?)`**: Change color/size of a cart item. Validate new SKU stock. If same SKU already exists in cart, merge quantities.

Add to cart.controller.ts:
- GET /api/v1/cart/invalid -- Get invalid items
- GET /api/v1/cart/price-changes -- Check price changes
- DELETE /api/v1/cart/batch -- Batch delete (body: { cartItemIds })
- POST /api/v1/cart/move-to-favorites -- Move to favorites (body: { cartItemIds })
- PATCH /api/v1/cart/:id/sku -- Update item SKU (body: { color?, size? })
- GET /api/v1/cart/summary?couponCode=XXX -- Enhanced summary with coupon
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `cart.service.ts` addItem checks ClothingItem.stock before adding
    - `cart.service.ts` getCartSummary returns discountAmount and finalAmount when couponCode provided
    - `cart.service.ts` has getInvalidItems method
    - `cart.service.ts` has batchDelete method
    - `cart.service.ts` has moveToFavorites method
    - `cart.service.ts` has updateItemSku method
    - `cart.controller.ts` has /cart/invalid, /cart/batch, /cart/move-to-favorites, /cart/:id/sku endpoints
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Cart service enhanced with stock validation, coupon discount calculation, invalid item detection, batch operations, and inline SKU editing.</done>
</task>

<task type="auto">
  <name>Task 2: Order enhancement -- coupon, confirm receipt, tracking, auto-confirm</name>
  <files>
    apps/backend/src/modules/order/order.service.ts,
    apps/backend/src/modules/order/order.controller.ts
  </files>
  <read_first>
    - apps/backend/src/modules/order/order.service.ts
    - apps/backend/src/modules/order/order.controller.ts
    - apps/backend/src/modules/coupon/coupon.service.ts
    - apps/backend/src/modules/payment/payment.service.ts
    - .planning/phases/05-e-commerce-closure/05-CONTEXT.md (D-15, D-16, D-17, D-18)
  </read_first>
  <action>
Enhance `apps/backend/src/modules/order/order.service.ts`:
Inject CouponService (via forwardRef if needed).

1. **Add coupon to `create` method**: Accept optional `couponCode` in CreateOrderDto. If provided:
   - Validate coupon via couponService.validateCoupon
   - Calculate discount via couponService.calculateDiscount
   - Set order.discountAmount = discount
   - Set order.finalAmount = totalAmount + shippingFee - discountAmount
   - Mark UserCoupon as USED via couponService.useCoupon
   - All within the existing transaction

2. **Add `confirmReceipt(userId, orderId)`**: Validate: order belongs to userId, order status is SHIPPED. Set order status to COMPLETED, set completedAt timestamp. Emit ORDER_COMPLETED event.

3. **Enhance `softDeleteOrder`**: Allow soft delete for COMPLETED and CANCELLED orders only. Set isDeleted=true, deletedAt=now.

4. **Enhance `getTracking` method** to return structured timeline per D-18:
   ```json
   [
     { "status": "pending", "time": "2026-04-14 10:00", "description": "订单已创建" },
     { "status": "paid", "time": "2026-04-14 10:05", "description": "支付成功" },
     { "status": "shipped", "time": "2026-04-15 09:00", "description": "商家已发货", "trackingNumber": "SF1234567890", "carrier": "顺丰速运" }
   ]
   ```
   Build timeline from order status change timestamps (createdAt, paidAt, shippedAt, completedAt). Include tracking info for shipped status.

5. **Add auto-confirm cron**: `@Cron('0 0 * * *')` -- daily at midnight. Find orders where status=SHIPPED and shippedAt < now() - 15 days. For each, set status=COMPLETED. Use batch update in transaction.

6. **Add `getOrdersByTab(userId, tab, page, limit)`**: Tab values: 'all', 'pending', 'paid', 'shipped', 'completed', 'refund'. Filter by status accordingly. For 'refund' tab, find orders with existing RefundRequest records.

Add to order.controller.ts:
- PATCH /api/v1/orders/:id/confirm -- Confirm receipt (auth required)
- DELETE /api/v1/orders/:id -- Soft delete (auth required, COMPLETED/CANCELLED only)
- GET /api/v1/orders/tab/:tab -- Get orders by tab with pagination
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `order.service.ts` create method accepts couponCode and applies discount in transaction
    - `order.service.ts` has confirmReceipt method that validates SHIPPED status
    - `order.service.ts` getTracking returns structured timeline with status/time/description
    - `order.service.ts` has auto-confirm cron for 15-day shipped orders
    - `order.service.ts` has getOrdersByTab method
    - `order.controller.ts` has PATCH /orders/:id/confirm, DELETE /orders/:id, GET /orders/tab/:tab
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Order service enhanced with coupon integration on creation, confirm receipt, structured tracking timeline, daily auto-confirm cron, and tab-based order listing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Cart API | Untrusted quantities, coupon codes |
| client -> Order API | Untrusted order operations, confirm receipt |
| coupon -> Order | Coupon discount applied in transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | T (Tampering) | Cart price | mitigate | All price calculation server-side, client sends only itemIds and quantities |
| T-05-16 | T (Tampering) | Order discount | mitigate | Discount calculation atomic in transaction with coupon validation |
| T-05-17 | R (Repudiation) | Confirm receipt | mitigate | confirmReceipt records completedAt timestamp for audit |
| T-05-18 | T (Tampering) | Cart stock | mitigate | Stock validated server-side on addItem, quantity capped at stock |
</threat_model>

<verification>
1. `cd C:/AiNeed/apps/backend && npx tsc --noEmit` -- TypeScript must compile
2. `cd C:/AiNeed/apps/backend && npx jest --passWithNoTests` -- Existing tests must pass
3. Verify cart.service.ts addItem checks stock
4. Verify cart.service.ts getCartSummary calculates discount when couponCode provided
5. Verify order.service.ts create applies coupon discount
6. Verify order.service.ts confirmReceipt validates SHIPPED status
7. Verify order.service.ts getTracking returns structured timeline
8. Verify auto-confirm cron exists
</verification>

<success_criteria>
- Cart: stock validation, coupon discount, invalid items, batch delete, move to favorites, inline SKU
- Order: coupon application, confirm receipt, structured tracking, auto-confirm, tab listing
- Both services integrate with CouponService from Plan 01
- All TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-e-commerce-closure/05-04-SUMMARY.md`
</output>
