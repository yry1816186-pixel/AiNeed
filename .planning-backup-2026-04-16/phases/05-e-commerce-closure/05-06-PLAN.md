---
phase: 05-e-commerce-closure
plan: 06
type: execute
wave: 4
depends_on: ["05-05"]
files_modified:
  - apps/mobile/src/screens/ClothingDetailScreen.tsx
  - apps/mobile/src/screens/SearchScreen.tsx
  - apps/mobile/src/components/ProductImageCarousel.tsx
  - apps/mobile/src/components/SKUSelector.tsx
  - apps/mobile/src/components/AISizeBadge.tsx
  - apps/mobile/src/components/OutfitRecommendationCards.tsx
  - apps/mobile/src/components/CategoryNavigation.tsx
  - apps/mobile/src/components/SubcategoryTabs.tsx
  - apps/mobile/src/components/FilterTags.tsx
  - apps/mobile/src/components/SortBar.tsx
autonomous: true
requirements:
  - COMM-01
  - COMM-02
  - COMM-04
  - COMM-11

must_haves:
  truths:
    - "Product detail page shows images, price, SKU selector, AI size rec, outfit recs"
    - "AI推荐 badge appears next to recommended size when body profile exists"
    - "AI recommendation hidden when no body profile data"
    - "Search has camera icon, filter tags, sort bar, category navigation"
  artifacts:
    - path: "apps/mobile/src/screens/ClothingDetailScreen.tsx"
      provides: "E-commerce product detail page"
      contains: "ProductDetailScreen"
    - path: "apps/mobile/src/components/SKUSelector.tsx"
      provides: "Color/size selection with AI badge"
      contains: "AI"
    - path: "apps/mobile/src/screens/SearchScreen.tsx"
      provides: "Enhanced search with filters and camera"
      contains: "camera"
  key_links:
    - from: "apps/mobile/src/screens/ClothingDetailScreen.tsx"
      to: "apps/mobile/src/services/api/commerce.api.ts"
      via: "getSizeRecommendation, getRelatedItems, cartApi.addItem"
      pattern: "commerce\\.api"
    - from: "apps/mobile/src/components/SKUSelector.tsx"
      to: "apps/mobile/src/stores/sizeRecommendationStore.ts"
      via: "useSizeRecommendationStore"
      pattern: "sizeRecommendationStore"
---

<objective>
Transform ClothingDetailScreen into a full e-commerce product detail page (COMM-01 per D-01/D-02/D-03/D-05/D-06). Enhance SearchScreen with camera icon, category navigation, filter tags, and sort bar (COMM-02 per D-19/D-20/D-21, COMM-11 per D-22/D-23). Create supporting components.

Purpose: Product detail page is the core e-commerce experience. Enhanced search enables product discovery.
Output: Rewritten ClothingDetailScreen, enhanced SearchScreen, 7 new components
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e-commerce-closure/05-CONTEXT.md
@.planning/phases/05-e-commerce-closure/05-UI-SPEC.md
@apps/mobile/src/screens/ClothingDetailScreen.tsx
@apps/mobile/src/screens/SearchScreen.tsx
@apps/mobile/src/services/api/commerce.api.ts
@apps/mobile/src/stores/sizeRecommendationStore.ts
</context>

<interfaces>
<!-- From Plan 05: commerce.api.ts methods -->

API methods available:
- `getSizeRecommendation(itemId)` -- returns { recommendedSize, confidence, reasons } or null
- `getRelatedItems(itemId)` -- returns outfit recommendations
- `cartApi.addItem(itemId, color, size, quantity)` -- add to cart
- `getSubcategories(category?)` -- returns [{name, count}]
- `getFilterOptions(category?)` -- returns { brands, colors, sizes, priceRange }
- `searchByImage(image)` -- existing image search
- `subscribeStockNotification(itemId, color?, size?)` -- subscribe to out-of-stock

Store available:
- `useSizeRecommendationStore` -- fetchRecommendation(itemId), getRecommendation(itemId)

UI-SPEC Design Contract (Screen 1 - Product Detail):
- Dominant: #FFFFFF background
- Accent: #FF4D4F for "立即购买", sale tags, AI推荐
- Success: #52C41A for AI推荐 badge
- Price: 20px/700 weight red, Original: 14px/400 strikethrough #CCCCCC
- Bottom bar: Left (Favorite + Try-on), Right (Add to Cart + Buy Now)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Product detail page + supporting components</name>
  <files>
    apps/mobile/src/screens/ClothingDetailScreen.tsx,
    apps/mobile/src/components/ProductImageCarousel.tsx,
    apps/mobile/src/components/SKUSelector.tsx,
    apps/mobile/src/components/AISizeBadge.tsx,
    apps/mobile/src/components/OutfitRecommendationCards.tsx
  </files>
  <read_first>
    - apps/mobile/src/screens/ClothingDetailScreen.tsx
    - apps/mobile/src/services/api/commerce.api.ts
    - apps/mobile/src/stores/sizeRecommendationStore.ts
    - .planning/phases/05-e-commerce-closure/05-UI-SPEC.md (Screen 1)
    - .planning/phases/05-e-commerce-closure/05-CONTEXT.md (D-01, D-02, D-03, D-05, D-06)
  </read_first>
  <action>
Rewrite `apps/mobile/src/screens/ClothingDetailScreen.tsx` as e-commerce product detail page. The existing file is for user wardrobe items -- replace entirely with e-commerce version.

Create supporting components first:

1. **`ProductImageCarousel.tsx`**: ScrollView horizontal with pagingEnabled. Show 3-8 product images with dot indicators below. Support tap-to-zoom via Modal.

2. **`AISizeBadge.tsx`** per D-05: Green (#52C41A) "AI推荐" badge. Props: recommendation { recommendedSize, confidence, reasons }. Tap to expand showing recommendation reason (confidence score + body match description + purchase history reference). Per D-06: if recommendation is null/undefined, render nothing.

3. **`SKUSelector.tsx`**: Bottom sheet modal with:
   - Color selection: Circular color swatches (30px), selected has 2px accent border
   - Size selection: Square buttons (48px), out-of-stock sizes grayed out (#F5F5F5 bg + #CCCCCC text)
   - AISizeBadge rendered next to the recommended size
   - Quantity selector: -/N/+ buttons, min=1, max=stock for selected SKU
   - "该尺码暂时缺货，到货通知我" button for out-of-stock sizes -> calls subscribeStockNotification
   - Props: item (with colors[], sizes[], stock), selectedColor, selectedSize, quantity, onChange, aiRecommendation?

4. **`OutfitRecommendationCards.tsx`** per D-03: Horizontal ScrollView with 3-5 outfit cards. Each card shows top+bottom+shoes thumbnails (50px each) arranged horizontally. Compact card: ~200px wide. Tap to navigate to item detail.

5. **Rewrite `ClothingDetailScreen.tsx`** as e-commerce product detail per D-01:
   - Top: ProductImageCarousel (full width, ~300px height)
   - Price bar: Current price in red (20px/700) + original price with strikethrough (14px, #CCCCCC) + discount badge if applicable
   - SKU selector: Triggered by tapping color/size text. Shows SKUSelector in Modal.
   - Product info: Name (18px/600), brand, description, material, care instructions in scrollable section
   - Outfit recommendations: OutfitRecommendationCards with 3-5 sets (D-03)
   - Bottom fixed bar (D-02): Left side -- Favorite button (heart icon) + "虚拟试衣" button. Right side -- "加入购物车" (outlined, secondary) + "立即购买" (filled, Accent #FF4D4F)
   - Data loading: GET /api/v1/clothing/:id for item data, getSizeRecommendation for AI rec, getRelatedItems for outfit recs
   - Actions: Add to cart calls cartApi.addItem. Buy now creates order. Favorite calls favorites API.
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `ProductImageCarousel.tsx` exists with horizontal ScrollView and dot indicators
    - `AISizeBadge.tsx` exists with green "AI推荐" badge that expands on tap
    - `SKUSelector.tsx` exists with color swatches, size buttons, quantity selector, AI badge integration
    - `OutfitRecommendationCards.tsx` exists with horizontal ScrollView and outfit cards
    - `ClothingDetailScreen.tsx` shows image carousel, price bar, SKU selector trigger, product info, outfit recs, bottom bar
    - Bottom bar has Favorite + Try-on + Add to Cart + Buy Now buttons
    - AISizeBadge only renders when recommendation data exists (D-06)
    - TypeScript compiles without new errors
  </acceptance_criteria>
  <done>Product detail page complete with image carousel, SKU selector with AI size recommendation, outfit recommendations, and fixed bottom action bar.</done>
</task>

<task type="auto">
  <name>Task 2: Search enhancement -- camera, filters, sort, categories</name>
  <files>
    apps/mobile/src/screens/SearchScreen.tsx,
    apps/mobile/src/components/CategoryNavigation.tsx,
    apps/mobile/src/components/SubcategoryTabs.tsx,
    apps/mobile/src/components/FilterTags.tsx,
    apps/mobile/src/components/SortBar.tsx
  </files>
  <read_first>
    - apps/mobile/src/screens/SearchScreen.tsx
    - apps/mobile/src/services/api/commerce.api.ts
    - .planning/phases/05-e-commerce-closure/05-UI-SPEC.md (Screen 2)
    - .planning/phases/05-e-commerce-closure/05-CONTEXT.md (D-19, D-20, D-21, D-22, D-23)
  </read_first>
  <action>
Create supporting components first:

1. **`CategoryNavigation.tsx`** per D-22: Horizontal ScrollView with 8 category icons. Each icon: MaterialCommunityIcons icon + text label below (12px). Categories: 上装(tshirt-crew), 下装 (pants), 连衣裙 (hanger), 外套 (coat-rain), 鞋履 (shoe-formal), 配饰 (necklace), 运动装 (run), 泳装 (swim). Props: selectedCategory, onSelectCategory.

2. **`SubcategoryTabs.tsx`** per D-23: Horizontal ScrollView with subcategory labels as Chips. Props: subcategories [{name, count}], selectedSubcategory, onSelectSubcategory. Appears below CategoryNavigation when a category is selected.

3. **`FilterTags.tsx`** per D-20: Horizontal ScrollView with filter tags: 价格/品牌/颜色/尺码/风格. Each tag tap opens a Modal/BottomSheet with filter options. Props: filterOptions (from getFilterOptions API), activeFilters, onApplyFilter.

4. **`SortBar.tsx`** per D-21: Row with sort options: 综合(default) / 价格↑ / 价格↓ / 销量. Active sort highlighted with Accent color (#FF4D4F). Props: activeSort, onSortChange.

Enhance `apps/mobile/src/screens/SearchScreen.tsx`:

5. **Camera icon** per D-19: Add MaterialCommunityIcons `camera-outline` to right side of search bar. Tap -> ActionSheet with "拍照搜索" and "从相册选择". After image selection, call searchByImage API and display results.

6. **Layout structure** (top to bottom):
   - Search bar (with camera icon)
   - CategoryNavigation (horizontal scroll)
   - SubcategoryTabs (conditional, shown when category selected)
   - FilterTags (horizontal scroll)
   - SortBar
   - Results grid (existing)

7. **Data flow**:
   - Category tap -> fetch getSubcategories(category) -> show SubcategoryTabs
   - Subcategory tap -> update search filter -> re-fetch results
   - Filter tap -> show filter options modal -> apply filter -> re-fetch
   - Sort tap -> update sortBy -> re-fetch
   - Camera -> image search -> display results
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `CategoryNavigation.tsx` exists with 8 category icons in horizontal scroll
    - `SubcategoryTabs.tsx` exists with horizontal Chip list
    - `FilterTags.tsx` exists with 5 filter tags and modal for options
    - `SortBar.tsx` exists with 综合/价格/销量 sort options
    - `SearchScreen.tsx` has camera icon on search bar right side
    - `SearchScreen.tsx` integrates CategoryNavigation, SubcategoryTabs, FilterTags, SortBar
    - TypeScript compiles without new errors
  </acceptance_criteria>
  <done>Search screen enhanced with camera icon for image search, 8-category navigation with subcategory tabs, multi-dimension filter tags (price/brand/color/size/style), and sort bar (comprehensive/price/sales).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> Product Detail | Tapping buy/cart triggers order/cart API calls |
| user -> Search | Free-text search input, image upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-21 | I (Info Disclosure) | Product description XSS | mitigate | Use Text component for description, not WebView |
| T-05-22 | T (Tampering) | Search params | accept | Search params sanitized by backend Prisma queries |
</threat_model>

<verification>
1. `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` -- TypeScript must compile
2. Verify ClothingDetailScreen has image carousel, price, SKU selector, bottom bar
3. Verify AISizeBadge conditionally renders based on recommendation data
4. Verify SearchScreen has camera icon, categories, filters, sort bar
5. Verify all 7 new components exist and export correctly
</verification>

<success_criteria>
- Product detail page: image carousel, price display, SKU selector with AI size badge, outfit recommendations, bottom action bar
- AI recommendation hidden when no body data (D-06)
- Search: camera icon, 8 categories with subcategories, 5 filter dimensions, 3 sort options
- All TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-e-commerce-closure/05-06-SUMMARY.md`
</output>
