---
phase: 05-e-commerce-closure
plan: 07
type: execute
wave: 4
depends_on: ["05-05"]
files_modified:
  - apps/mobile/src/screens/CartScreen.tsx
  - apps/mobile/src/screens/CheckoutScreen.tsx
  - apps/mobile/src/components/CouponSelector.tsx
  - apps/mobile/src/components/EmptyCartView.tsx
  - apps/mobile/src/components/FreeShippingProgress.tsx
  - apps/mobile/src/components/InlineSKUSelector.tsx
  - apps/mobile/src/components/PaymentWaitingScreen.tsx
autonomous: true
requirements:
  - COMM-03
  - COMM-06
  - COMM-07

must_haves:
  truths:
    - "Empty cart shows recommended items and go-shop button"
    - "Cart supports inline SKU editing and edit mode with batch operations"
    - "Coupon entry shows available coupons with discount preview"
    - "Checkout shows two payment buttons (Alipay + WeChat)"
    - "Payment waiting page polls every 2 seconds with 2-minute timeout"
  artifacts:
    - path: "apps/mobile/src/screens/CartScreen.tsx"
      provides: "Full-featured cart with coupon, edit mode, inline SKU"
      contains: "去逛逛"
    - path: "apps/mobile/src/screens/CheckoutScreen.tsx"
      provides: "Checkout with dual payment and coupon"
      contains: "支付宝"
    - path: "apps/mobile/src/components/PaymentWaitingScreen.tsx"
      provides: "Payment polling with timeout"
      contains: "pollPaymentStatus"
  key_links:
    - from: "apps/mobile/src/screens/CheckoutScreen.tsx"
      to: "apps/mobile/src/services/api/commerce.api.ts"
      via: "createPayment -> pollPaymentStatus"
      pattern: "createPayment"
    - from: "apps/mobile/src/components/CouponSelector.tsx"
      to: "apps/mobile/src/stores/couponStore.ts"
      via: "useCouponStore"
      pattern: "couponStore"
---

<objective>
Enhance CartScreen with empty state, coupon support, inline SKU editing, edit mode with batch operations, and free shipping progress (COMM-03 per D-12/D-13/D-14). Enhance CheckoutScreen with dual payment buttons and payment waiting with polling (COMM-06 per D-15/D-16, COMM-07 per D-07/D-08).

Purpose: Cart and checkout are the core purchase flow. These enhancements complete the buying experience.
Output: Enhanced CartScreen, CheckoutScreen, 5 new components
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e-commerce-closure/05-CONTEXT.md
@.planning/phases/05-e-commerce-closure/05-UI-SPEC.md
@apps/mobile/src/screens/CartScreen.tsx
@apps/mobile/src/screens/CheckoutScreen.tsx
@apps/mobile/src/services/api/commerce.api.ts
@apps/mobile/src/stores/couponStore.ts
</context>

<interfaces>
<!-- From Plan 05: API layer + stores -->

API methods available:
- `getCartSummary(couponCode?)` -- returns { totalAmount, discountAmount, shippingFee, finalAmount }
- `batchDeleteCartItems(ids)` -- batch delete
- `moveCartToFavorites(ids)` -- move to favorites
- `updateCartItemSku(id, color?, size?)` -- inline SKU change
- `getInvalidCartItems()` -- get unavailable items
- `validateCoupon(code, orderAmount)` -- validate coupon
- `createPayment(orderId, provider)` -- initiate payment
- `pollPaymentStatus(orderId)` -- check payment status

Store available:
- `useCouponStore` -- availableCoupons, validateCoupon, selectCoupon, fetchUserCoupons

UI-SPEC Design Contract (Screen 3 - Cart, Screen 4 - Checkout):
- Cart empty: icon + "购物车空空如也" + "去逛逛" button
- Free shipping: "再买¥{amount}免运费" progress bar
- Payment: "支付宝支付" (blue) + "微信支付" (green) side by side
- Payment waiting: spinner + "正在查询支付结果..."
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Cart enhancement with coupon, edit mode, inline SKU</name>
  <files>
    apps/mobile/src/screens/CartScreen.tsx,
    apps/mobile/src/components/EmptyCartView.tsx,
    apps/mobile/src/components/FreeShippingProgress.tsx,
    apps/mobile/src/components/InlineSKUSelector.tsx,
    apps/mobile/src/components/CouponSelector.tsx
  </files>
  <read_first>
    - apps/mobile/src/screens/CartScreen.tsx
    - apps/mobile/src/services/api/commerce.api.ts
    - apps/mobile/src/stores/couponStore.ts
    - .planning/phases/05-e-commerce-closure/05-UI-SPEC.md (Screen 3)
    - .planning/phases/05-e-commerce-closure/05-CONTEXT.md (D-12, D-13, D-14)
  </read_first>
  <action>
Create supporting components first:

1. **`EmptyCartView.tsx`** per D-12:
   - Cart-off icon (MaterialCommunityIcons `cart-off`, 64px, #CCCCCC)
   - Heading: "购物车空空如也" (18px/600, #333)
   - Body: "去发现喜欢的穿搭吧" (14px, #999)
   - "去逛逛" button (Accent #FF4D4F) -> navigate to Home
   - Below: "猜你喜欢" section with 4-6 recommended items in horizontal FlatList

2. **`FreeShippingProgress.tsx`**:
   - Props: currentAmount, threshold (default 99)
   - ProgressBar with green (#52C41A) fill
   - Text: "再买¥{remaining}免运费" when below threshold, "已免运费" when above
   - Progress = currentAmount / threshold, capped at 1

3. **`InlineSKUSelector.tsx`** per D-13:
   - Modal bottom sheet with color swatches and size buttons
   - Props: item (colors[], sizes[], stock), currentColor, currentSize, onChange
   - Compact layout, no quantity selector (only color/size change)
   - On selection, calls updateCartItemSku API

4. **`CouponSelector.tsx`**:
   - Modal with list of available coupons
   - Each coupon shows: discount amount/type, min order, expiry, applicable scope
   - Selected coupon has checkmark
   - "不使用优惠券" option at top
   - Unavailable coupons grayed out with reason
   - Props: coupons[], selectedCouponId, onSelect

Enhance `apps/mobile/src/screens/CartScreen.tsx`:

5. **Empty state** (D-12): When cart is empty, render EmptyCartView instead of cart items.

6. **Coupon entry**: Above summary bar, show "使用优惠券" row. Tap -> CouponSelector modal. Show "{N}张优惠券可用" if coupons exist.

7. **Free shipping progress**: Below coupon entry, show FreeShippingProgress component.

8. **Inline SKU editor** (D-13): When user taps size/color text on cart item, show InlineSKUSelector modal.

9. **Edit mode** (D-14): Top-right "编辑"/"完成" toggle button:
   - Normal mode: Select all checkbox + "结算(N)" button
   - Edit mode: Bottom bar changes to "全选" + "删除" + "移入收藏" batch action buttons
   - Items show checkboxes for multi-select in edit mode

10. **Stock validation**: Show "库存不足" warning badge on items where stock < quantity. Gray out unavailable items.

11. **Invalid items section**: Show inactive items in separate grayed-out section with "已失效" tag and delete option.
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `EmptyCartView.tsx` exists with cart-off icon, "购物车空空如也", "去逛逛" button
    - `FreeShippingProgress.tsx` exists with progress bar and remaining amount text
    - `InlineSKUSelector.tsx` exists with color/size selection modal
    - `CouponSelector.tsx` exists with coupon list and select functionality
    - `CartScreen.tsx` renders EmptyCartView when cart empty
    - `CartScreen.tsx` has coupon entry section
    - `CartScreen.tsx` has edit mode toggle with batch actions
    - `CartScreen.tsx` shows "库存不足" warning badge
    - TypeScript compiles without new errors
  </acceptance_criteria>
  <done>Cart screen enhanced with empty state + recommendations, coupon selection, free shipping progress bar, inline SKU editing, edit mode with batch operations, stock warnings, and invalid items section.</done>
</task>

<task type="auto">
  <name>Task 2: Checkout with dual payment + payment waiting</name>
  <files>
    apps/mobile/src/screens/CheckoutScreen.tsx,
    apps/mobile/src/components/PaymentWaitingScreen.tsx
  </files>
  <read_first>
    - apps/mobile/src/screens/CheckoutScreen.tsx
    - apps/mobile/src/services/api/commerce.api.ts
    - .planning/phases/05-e-commerce-closure/05-UI-SPEC.md (Screen 4)
    - .planning/phases/05-e-commerce-closure/05-CONTEXT.md (D-15, D-16)
  </read_first>
  <action>
Create supporting component:

1. **`PaymentWaitingScreen.tsx`** per D-16:
   - Props: orderId, onSuccess callback, onTimeout callback
   - Loading animation: ActivityIndicator (large, Accent color)
   - Text: "正在查询支付结果..." (16px, centered)
   - Auto-poll: Call pollPaymentStatus(orderId) every 2 seconds
   - Max 60 polls (2 minutes total)
   - On success: Navigate to OrderDetailScreen with success message "支付成功！"
   - On timeout: Show "支付超时，请手动查看订单状态" with "查看订单" button
   - Use useEffect with setInterval for polling, clearInterval on unmount

Enhance `apps/mobile/src/screens/CheckoutScreen.tsx`:

2. **Coupon section**: Show selected coupon with discount amount. "更换" button opens CouponSelector modal. Price breakdown: 商品合计 + 运费 + 优惠(discount in green -¥XX) + 实付(final amount in red, 20px/700)

3. **Payment buttons** per D-15: Replace existing payment preference with two prominent buttons:
   - "支付宝支付" button: Blue (#1677FF) background, Alipay icon, full width or half width
   - "微信支付" button: Green (#07C160) background, WeChat icon, full width or half width
   - Both buttons side by side at bottom
   - Remove any "银行卡" option (no backend support)

4. **Payment flow**:
   - User taps payment button
   - Call createPayment(orderId, provider) via commerce.api.ts
   - On success: Navigate to PaymentWaitingScreen
   - PaymentWaitingScreen polls and handles success/timeout

5. **First-order auto-discount**: After order creation, check if coupon was auto-applied. If discount > 0 and coupon is first-order type, show notification "首单优惠已自动应用 -¥{discount}".
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `PaymentWaitingScreen.tsx` exists with ActivityIndicator and pollPaymentStatus call
    - `PaymentWaitingScreen.tsx` has 2-second polling interval with 60-poll max
    - `CheckoutScreen.tsx` has "支付宝支付" button with blue styling
    - `CheckoutScreen.tsx` has "微信支付" button with green styling
    - `CheckoutScreen.tsx` has coupon section with discount display
    - `CheckoutScreen.tsx` shows price breakdown: 合计 + 运费 + 优惠 + 实付
    - `CheckoutScreen.tsx` calls createPayment API on button tap
    - TypeScript compiles without new errors
  </acceptance_criteria>
  <done>Checkout screen enhanced with dual payment buttons (Alipay blue + WeChat green), coupon selection, price breakdown, and payment waiting screen with 2-second polling and 2-minute timeout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Payment API | Payment creation via HTTPS, params returned from server |
| client -> Cart summary | All prices calculated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-23 | T (Tampering) | Payment params | mitigate | Payment params from server response, not client-constructed |
| T-05-24 | I (Info Disclosure) | Payment data | mitigate | Use official SDK deep links, never handle card data |
| T-05-25 | D (Denial of Service) | Payment polling | accept | 2-minute timeout prevents infinite polling |
</threat_model>

<verification>
1. `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` -- TypeScript must compile
2. Verify CartScreen has empty state, coupon entry, edit mode, inline SKU
3. Verify CheckoutScreen has Alipay + WeChat buttons, coupon, price breakdown
4. Verify PaymentWaitingScreen has polling with 2-second interval
5. Verify all 5 new components exist
</verification>

<success_criteria>
- Cart: empty state with recommendations, coupon support, inline SKU editing, edit mode, batch operations, free shipping progress
- Checkout: dual payment buttons, coupon selection, price breakdown, payment waiting with polling
- All TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-e-commerce-closure/05-07-SUMMARY.md`
</output>
