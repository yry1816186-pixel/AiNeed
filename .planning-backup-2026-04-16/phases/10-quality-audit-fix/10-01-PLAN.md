---
phase: 10-quality-audit-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/navigation/RouteGuards/VipGuard.tsx
  - apps/mobile/src/screens/PaymentScreen.tsx
  - apps/mobile/src/screens/consultant/ChatScreen.tsx
  - apps/mobile/src/screens/consultant/BookingScreen.tsx
  - apps/mobile/src/screens/consultant/AdvisorProfileScreen.tsx
  - apps/mobile/src/screens/consultant/AdvisorListScreen.tsx
  - apps/mobile/src/screens/home/HomeScreen.tsx
  - apps/mobile/src/screens/AiStylistChatScreen.tsx
autonomous: true
requirements:
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05

must_haves:
  truths:
    - "VIP users can access CustomDesign, CustomEditor, Booking, Chat features"
    - "PaymentScreen displays all text in Chinese (no English visible)"
    - "Consultant screens adapt to device safe area insets"
    - "Weather data fetched using device GPS coordinates, not hardcoded Beijing"
    - "AI Stylist chat messages persist across page exits and app restarts"
  artifacts:
    - path: "apps/mobile/src/navigation/RouteGuards/VipGuard.tsx"
      provides: "VIP guard reading real user state from useAuthStore"
      contains: "useAuthStore((state) => state.isVip)"
    - path: "apps/mobile/src/screens/PaymentScreen.tsx"
      provides: "Fully localized payment screen"
      min_lines: 280
    - path: "apps/mobile/src/screens/consultant/ChatScreen.tsx"
      provides: "Safe area insets integration"
      contains: "useSafeAreaInsets"
    - path: "apps/mobile/src/screens/AiStylistChatScreen.tsx"
      provides: "Persisted chat messages via Zustand store"
      contains: "useAiStylistChatStore"
  key_links:
    - from: "VipGuard.tsx"
      to: "useAuthStore"
      via: "state.isVip selector"
      pattern: "useAuthStore.*isVip"
    - from: "AiStylistChatScreen.tsx"
      to: "aiStylistChatStore.ts"
      via: "useAiStylistChatStore hook"
      pattern: "useAiStylistChatStore"
---

<objective>
Fix all P0 blocking issues that prevent core functionality from working.

Purpose: These 5 issues block real user journeys - VIP features inaccessible, payment page unreadable for Chinese users, inconsistent safe areas, wrong weather location, and lost chat messages.

Output: All P0 issues resolved, verified by TypeScript compilation and functional checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quality-audit-fix/10-CONTEXT.md
@.planning/phases/10-quality-audit-fix/10-REQUIREMENTS.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/mobile/src/stores/index.ts (AuthState):
```typescript
interface AuthState {
  user: User | null;
  token: string | null;
  isAuthenticated: boolean;
  isLoading: boolean;
  onboardingCompleted: boolean;
  isVip: boolean;  // <-- This is what VipGuard should read
  // ...methods
}
export const useAuthStore = create<AuthState>()(persist(...));
```

From apps/mobile/src/stores/aiStylistChatStore.ts:
```typescript
export interface ChatMessage {
  id: string;
  role: "user" | "assistant";
  content: string;
  timestamp: string;
}

interface AiStylistChatState {
  messages: ChatMessage[];
  addMessage: (message: ChatMessage) => void;
  setMessages: (messages: ChatMessage[]) => void;
  clearMessages: () => void;
}

// Already has AsyncStorage persistence via zustand/middleware persist
export const useAiStylistChatStore = create<AiStylistChatState>()(persist(...));
```

From react-native-safe-area-context:
```typescript
function useSafeAreaInsets(): EdgeInsets;
interface EdgeInsets { top: number; bottom: number; left: number; right: number; }
```

From @react-native-community/geolocation (already imported in HomeScreen.tsx):
```typescript
Geolocation.getCurrentPosition(
  success: (position: GeolocationResponse) => void,
  error?: (error: GeolocationError) => void,
  options?: GeolocationOptions
): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix VipGuard to read real user VIP status</name>
  <read_first>
    - apps/mobile/src/navigation/RouteGuards/VipGuard.tsx
    - apps/mobile/src/stores/index.ts (lines 25-60 for AuthState interface and isVip selector)
  </read_first>
  <files>apps/mobile/src/navigation/RouteGuards/VipGuard.tsx</files>
  <action>
VipGuard.tsx already correctly uses `useAuthStore((state) => state.isVip)` at line 15. The original audit report mentioned `isVip = false` hardcoded, but the current code reads from the store.

Verify the store correctly computes isVip. In stores/index.ts, the isVip is derived from `user?.subscriptionTier === 'vip'` (line 56, 121-122, 145).

If VipGuard is working correctly, document this in the summary. If there is still a hardcoded value, remove it and ensure the component uses only the store state.

The VipGuard component should:
1. Read `isVip` from `useAuthStore((state) => state.isVip)`
2. Show children when `isVip === true`
3. Show VIP upgrade prompt when `isVip === false`
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && grep -n "isVip" src/navigation/RouteGuards/VipGuard.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - VipGuard.tsx line 15 contains: `const isVip = useAuthStore((state) => state.isVip);`
    - No hardcoded `isVip = false` or `const isVip = false` anywhere in the file
    - `grep -c "isVip = false" src/navigation/RouteGuards/VipGuard.tsx` returns 0
  </acceptance_criteria>
  <done>VipGuard reads isVip from useAuthStore, no hardcoded false value exists</done>
</task>

<task type="auto">
  <name>Task 2: Localize PaymentScreen to Chinese</name>
  <read_first>
    - apps/mobile/src/screens/PaymentScreen.tsx
  </read_first>
  <files>apps/mobile/src/screens/PaymentScreen.tsx</files>
  <action>
Review PaymentScreen.tsx and identify all English strings. Based on my read, the file is already mostly in Chinese. Verify and fix any remaining English strings.

Expected Chinese strings (verify these exist):
- Header title: "支付" (line 119, 135)
- Section titles: "订单摘要" (line 143), "支付方式" (line 161)
- Labels: "订单号", "商品", "合计" (lines 145, 148, 153)
- Payment methods: "支付宝", "微信支付" (lines 172, 189)
- Button: "立即支付" (line 222)
- Alerts: "支付成功", "您的订单已提交", "查看订单", "返回首页" (lines 78-81)
- "支付已发起", "请通过...完成支付" (lines 96-98)
- "支付失败", "无法发起支付" (line 101)
- "错误", "网络异常，请重试" (line 104)
- Polling text: "等待支付确认中..." (line 201)

If any English strings remain, replace them:
- "Order Summary" -> "订单摘要"
- "Payment" -> "支付"
- "Pay Now" -> "立即支付"
- "Total" -> "合计"
- "Alipay" -> "支付宝"
- "WeChat Pay" -> "微信支付"
- "Order ID" -> "订单号"
- "Items" -> "商品"
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && grep -E "(Payment|Order|Summary|Pay Now|Total|Alipay|WeChat)" src/screens/PaymentScreen.tsx | grep -v "//" | grep -v "PaymentScreen" | grep -v "PaymentRoute" | grep -v "PaymentProvider" | grep -v "paymentApi" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All user-visible strings are in Chinese
    - `grep -c '"支付"' src/screens/PaymentScreen.tsx` returns at least 2
    - `grep -c '"支付宝"' src/screens/PaymentScreen.tsx` returns at least 2
    - `grep -c '"微信支付"' src/screens/PaymentScreen.tsx` returns at least 2
    - No English-only UI strings like "Pay Now", "Order Summary" visible (type names and code identifiers are okay)
  </acceptance_criteria>
  <done>PaymentScreen displays all user-facing text in Chinese</done>
</task>

<task type="auto">
  <name>Task 3: Fix safe area hardcoding in consultant screens</name>
  <read_first>
    - apps/mobile/src/screens/consultant/ChatScreen.tsx
    - apps/mobile/src/screens/consultant/BookingScreen.tsx
    - apps/mobile/src/screens/consultant/AdvisorProfileScreen.tsx
    - apps/mobile/src/screens/consultant/AdvisorListScreen.tsx
  </read_first>
  <files>
    - apps/mobile/src/screens/consultant/ChatScreen.tsx
    - apps/mobile/src/screens/consultant/BookingScreen.tsx
    - apps/mobile/src/screens/consultant/AdvisorProfileScreen.tsx
    - apps/mobile/src/screens/consultant/AdvisorListScreen.tsx
  </files>
  <action>
In each of the 4 consultant screens, find any hardcoded `paddingTop: 56` (or similar fixed values) and replace with dynamic safe area insets.

For ChatScreen.tsx (already has useSafeAreaInsets at line 21, uses insets.top at line 157):
- Verify line 157: `paddingTop: insets.top` is used in header style
- If there's any remaining hardcoded paddingTop, remove it

For BookingScreen.tsx:
1. Import `useSafeAreaInsets` from `react-native-safe-area-context` if not already imported
2. Call `const insets = useSafeAreaInsets();` at the start of the component
3. Find `paddingTop: 56` in StyleSheet or inline styles
4. Replace with `paddingTop: insets.top` in the header/container style

For AdvisorProfileScreen.tsx:
1. Same pattern as above
2. Find `paddingTop: 56` around line 160
3. Replace with dynamic `insets.top`

For AdvisorListScreen.tsx:
1. Same pattern as above
2. Find `paddingTop: 56` around line 201
3. Replace with dynamic `insets.top`

Pattern to apply in each screen's header style:
```typescript
// Before
header: { paddingTop: 56, ... }

// After
<View style={[styles.header, { paddingTop: insets.top }]}>
```
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && grep -rn "paddingTop: 56" src/screens/consultant/ && echo "--- If no output above, all hardcoded values removed ---"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "paddingTop: 56" src/screens/consultant/*.tsx` returns 0
    - `grep -c "useSafeAreaInsets" src/screens/consultant/ChatScreen.tsx` returns at least 1
    - `grep -c "useSafeAreaInsets" src/screens/consultant/BookingScreen.tsx` returns at least 1
    - `grep -c "useSafeAreaInsets" src/screens/consultant/AdvisorProfileScreen.tsx` returns at least 1
    - `grep -c "useSafeAreaInsets" src/screens/consultant/AdvisorListScreen.tsx` returns at least 1
    - Each file uses `insets.top` in header styling
  </acceptance_criteria>
  <done>All 4 consultant screens use useSafeAreaInsets for paddingTop, no hardcoded values</done>
</task>

<task type="auto">
  <name>Task 4: Verify weather uses device location (already fixed)</name>
  <read_first>
    - apps/mobile/src/screens/home/HomeScreen.tsx (lines 1-100)
  </read_first>
  <files>apps/mobile/src/screens/home/HomeScreen.tsx</files>
  <action>
Based on my read of HomeScreen.tsx, it already uses `@react-native-community/geolocation` to get device coordinates:
- Line 10: Imports Geolocation
- Lines 77-78: State for coords
- Lines 82-96: useEffect that calls `Geolocation.getCurrentPosition()` and sets coords from device
- Lines 50-51: FALLBACK_LATITUDE/LONGITUDE (35.8617, 104.1954) used only on error - this is center of China, not Beijing

The original audit mentioned `fetchWeather(39.9042, 116.4074)` (Beijing coords), but the current code uses dynamic coords from the device.

Verify the fetchWeather call uses the dynamic coords state:
- Around line 104: `fetchWeather(coords.latitude, coords.longitude)` should be called
- The Beijing coordinates (39.9042, 116.4074) should NOT appear anywhere

If Beijing coords are still present, replace with the coords state. Otherwise, document that this is already fixed.
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && grep -n "39.9042\|116.4074" src/screens/home/HomeScreen.tsx | head -5 && echo "--- If no output above, Beijing coords removed ---"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "39.9042" src/screens/home/HomeScreen.tsx` returns 0
    - `grep -c "116.4074" src/screens/home/HomeScreen.tsx` returns 0
    - `grep -c "Geolocation.getCurrentPosition" src/screens/home/HomeScreen.tsx` returns at least 1
    - `grep -c "coords.latitude" src/screens/home/HomeScreen.tsx` returns at least 1
  </acceptance_criteria>
  <done>HomeScreen fetches weather using device GPS coordinates, no hardcoded Beijing location</done>
</task>

<task type="auto">
  <name>Task 5: Verify chat message persistence (already implemented)</name>
  <read_first>
    - apps/mobile/src/screens/AiStylistChatScreen.tsx (lines 1-100)
    - apps/mobile/src/stores/aiStylistChatStore.ts
  </read_first>
  <files>apps/mobile/src/screens/AiStylistChatScreen.tsx</files>
  <action>
Based on my read of the codebase:

1. aiStylistChatStore.ts (lines 1-36) already implements persistence:
   - Uses `zustand/middleware` persist
   - Uses `AsyncStorage` for storage
   - Has `name: "ai-stylist-chat-storage"`
   - Partializes `messages` array

2. AiStylistChatScreen.tsx (lines 20-22, 50) already uses the store:
   - Line 21: `import { useAiStylistChatStore } from '../stores/aiStylistChatStore';`
   - Line 50: `const { messages, addMessage } = useAiStylistChatStore();`

The original audit mentioned messages were managed with `useState` and lost on page exit. This appears to have been fixed already.

Verify by checking:
- AiStylistChatScreen uses `useAiStylistChatStore` (not local useState for messages)
- aiStylistChatStore has persist middleware configured

If useState is still used for messages, migrate to the store. Otherwise, document that this is already fixed.
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && grep -n "useAiStylistChatStore\|useState.*messages" src/screens/AiStylistChatScreen.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useAiStylistChatStore" src/screens/AiStylistChatScreen.tsx` returns at least 1
    - `grep -c "useState.*messages" src/screens/AiStylistChatScreen.tsx` returns 0 (no local messages state)
    - aiStylistChatStore.ts contains `persist` and `AsyncStorage`
    - Messages are read from and written to the Zustand store, not local state
  </acceptance_criteria>
  <done>AI Stylist chat messages persist via Zustand store with AsyncStorage, survive page exits</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only fixes UI/UX issues, no security-sensitive changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I (Info Disclosure) | VipGuard | accept | VIP status is user data, already protected by auth; UI guard is UX only |
| T-10-02 | T (Tampering) | ChatStore | accept | AsyncStorage is local device storage, same trust level as app |
</threat_model>

<verification>
After completing all tasks:
1. `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` - TypeScript compiles with no new errors
2. Verify no hardcoded `paddingTop: 56` in consultant screens
3. Verify no Beijing coordinates (39.9042, 116.4074) in HomeScreen
4. Verify PaymentScreen has Chinese text for all UI strings
</verification>

<success_criteria>
- [ ] VipGuard reads isVip from useAuthStore (verified)
- [ ] PaymentScreen displays all Chinese text
- [ ] All 4 consultant screens use useSafeAreaInsets
- [ ] HomeScreen uses Geolocation for weather coordinates
- [ ] AiStylistChatScreen uses persisted Zustand store for messages
- [ ] TypeScript compilation passes with no new errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-quality-audit-fix/10-01-SUMMARY.md`
</output>
