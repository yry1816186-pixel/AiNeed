import React from "react";
import { render } from "@testing-library/react-native";
import { Button } from "../Button";

describe("Button minimal", () => {
  it("renders", () => {
    expect(1).toBe(1);
  });
});
