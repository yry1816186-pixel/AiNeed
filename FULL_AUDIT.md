# AiNeed (寻裳) 全项目审计报告

> 审计日期: 2026-04-20
> 审计范围: 后端架构、移动端前端、AI/ML服务、基础设施与安全
> 项目状态: 功能完整度极高，AI层超出预期，前端需打磨，运维待加固

---

## 一、总体评价

AiNeed 项目的 **功能完整度远超预期**。35+ 后端模块、65个移动端屏幕、17个 Python AI 服务全部是真实实现，没有 placeholder。AI 推荐引擎是6策略集成而非随机返回，虚拟试穿是4阶段管线而非单次API调用。

**核心问题不是"缺功能"，而是"从能用到好用"的差距**：前端视觉割裂、部分文件过大、测试覆盖率低、运维自动化不足。

---

## 二、按模块分类的问题清单

### A. 前端体验 (最高优先级)

#### A1. 超大文件需拆分

| 文件                           | 行数 | 问题                   | 修复方式                                                   |
| ------------------------------ | ---- | ---------------------- | ---------------------------------------------------------- |
| CartScreen.tsx                 | 1004 | 购物车逻辑+UI混合      | 提取 CartItem、CartSummary、CouponInput 子组件             |
| AiStylistChatScreen.tsx        | 933  | 聊天+消息列表+输入混合 | 提取 ChatMessageList、ChatInput、QuickActions 子组件       |
| CheckoutScreen.tsx             | 918  | 地址+支付+确认混合     | 提取 AddressSelector、PaymentSelector、OrderSummary 子组件 |
| CustomizationScreen.tsx        | 781  | 编辑器+预览混合        | 提取 DesignCanvas、DesignToolbar、DesignPreview 子组件     |
| OnboardingScreen.tsx           | 758  | 4步引导全在一个文件    | 按Step拆分（已有Step组件但逻辑未分离）                     |
| RecommendationDetailScreen.tsx | 746  | 详情+推荐+反馈混合     | 提取 RecommendationHeader、ItemGrid、FeedbackPanel         |

**适合Trae**: 每个文件拆分都是机械性工作，给出明确的组件接口定义即可。每个文件一个独立任务。

#### A2. 视觉风格割裂

**问题**: 虽然有完整的设计系统（design-tokens.ts），但各屏幕之间视觉一致性不足。部分屏幕使用了硬编码颜色值而非 theme tokens。

**验证方式**: 在 `apps/mobile/src/` 中搜索硬编码的 `#` 颜色值和 `rgb/rgba` 值，对比 design-tokens.ts 中定义的色板。

**修复**:

- 全局搜索替换硬编码颜色 → theme tokens
- 统一所有屏幕的间距使用 spacing scale
- 确保 Card、Button、Tag 等组件的使用方式一致
- **适合Trae**: codemod 脚本 + 逐文件替换

#### A3. Profile 标签页过载

Profile 标签承载了 27 个屏幕（购物车、订单、订阅、顾问、定制等），导航层级过深。

**修复方案**:

- 考虑将"购物"（Cart/Orders/Checkout）提升为独立 Tab 或在 Home 中突出入口
- 顾问(Consultant)和定制(Customization)可以在 Stylist Tab 中有入口
- 减少用户心智负担

#### A4. 缺失的高价值功能

| 功能                  | 重要度 | 实现难度 | 说明                                             |
| --------------------- | ------ | -------- | ------------------------------------------------ |
| AR 实时试穿           | 高     | 极高     | 当前仅支持照片上传，缺少实时摄像头 AR 叠加       |
| 外部分享(微信/小红书) | 高     | 中       | SharePosterScreen 仅生成图片，未接入社交平台 SDK |
| 尺码管理独立页面      | 中     | 低       | SizeRecommendation 类型已存在但无独立管理页      |
| 穿搭日历/计划         | 中     | 中       | SessionCalendarScreen 仅显示历史，无前瞻性规划   |
| 好友/关注系统         | 低     | 中       | 社区有博主体系但无普通用户社交图谱               |

#### A5. 性能隐患

- `WardrobeStore.recalculateStats()` 每次变更都遍历全量数据，衣柜大时会卡顿 → 改为增量更新或 memoized selector
- `RootNavigator.handleStateChange` 每次导航遍历整棵路由树 → 缓存结果
- `CartStore` 中 `addItem/removeItem/updateItem` 重复 reduce 计算 → 抽取 `computeTotals()` 统一调用

---

### B. 后端架构

#### B1. any 类型泛滥

- 后端 135 处 `any`，250 个文件有 `eslint-disable` 注释
- 主要原因: Prisma 动态 where 子句 + 测试 mock
- **修复优先级**: 中。功能不受影响，但长期维护性差
- **策略**: 先修 Prisma 动态查询层（用泛型封装），测试文件的 any 可暂缓
- **适合Trae**: 按模块分批修复，每批5-10个文件

#### B2. 数据库约束缺失

Prisma schema 2539 行，但缺少数据库级别的 CHECK 约束（Prisma 原生不支持）:

- 订单价格范围: `minPrice < maxPrice`
- 用户年龄范围: 13-120
- 库存非负: `stock >= 0`

**修复**: 编写 raw SQL migration 文件添加 CHECK 约束

- **适合Trae**: 纯 SQL 工作

#### B3. JSON 字段无 Schema 验证

`ClothingItem.attributes`、`ClothingItem.filters`、`Order.metadata` 等 JSON 字段仅在应用层验证，数据库层无约束。

**修复方案**: 使用 PostgreSQL JSON Schema 扩展 或 在 migration 中添加触发器验证

- **优先级**: 低（应用层已有验证）

#### B4. 软删除覆盖不一致

`isDeleted` 字段存在于部分模型，SoftDeleteMiddleware 尝试全局应用，但新模型可能遗漏。

**修复**: 在 Prisma middleware 中统一检查，或添加 linter 规则

- **适合Trae**: 检查所有模型，补充缺失的 isDeleted 字段

#### B5. 测试覆盖率极低

- 后端 ~15%，移动端 ~5%
- 无自动化端到端集成测试验证关键路径（如结账流程）

**修复策略**:

1. 先为关键业务路径写集成测试: 注册→登录→浏览→加购→下单→支付
2. 为推荐引擎写单元测试验证各策略权重
3. 为 AI 服务写 mock 测试验证 prompt 构建逻辑

- **Claude 分析**: 测试策略设计（哪些路径最关键）
- **Trae 执行**: 按策略逐模块编写

#### B6. WebSocket 事件覆盖不足

WS 模块存在但仅用于通知推送。以下场景也应该用 WebSocket:

- 订单状态变更实时更新
- 聊天消息实时推送
- 试穿任务进度推送

---

### C. AI/ML 服务

#### C1. AI 层整体评价: 优秀

所有 17 个 Python 服务都是真实实现:

- **推荐引擎**: 6策略集成（内容、协同过滤、知识图谱、搭配理论、SASRec序列、热度），含冷启动权重自适应
- **AI 造型师**: 多轮对话，10轮历史，Redis 持久化，RAG 上下文增强
- **虚拟试穿**: 4阶段管线（预处理→提示词生成→API调用→后处理质量验证），Doubao/GLM 双供应商降级
- **体型分析**: MediaPipe 33关键点，5种体型分类
- **色彩分析**: 12季色彩理论，CIELAB + CIEDE2000
- **FashionCLIP**: 视觉相似度嵌入

#### C2. 推荐引擎需验证的问题

- SASRec transformer 有 PyTorch 和 NumPy 双后端 → 生产环境应锁定 PyTorch 后端
- 协同过滤依赖用户行为数据 → 冷启动阶段效果差（虽有自适应权重，但需要真实用户数据验证）
- **Claude 分析**: 用 A/B 测试框架验证各策略实际效果

#### C3. Prompt 工程质量

- `stylist_prompts.py` (540行) 覆盖全面: 体型规则、色彩理论、场合规则、注入防御
- 20+ 正则模式防注入，中英文都覆盖
- **改进点**: 缺少 prompt 版本管理和 A/B 测试机制

#### C4. AI 服务运维短板

- 无 GPU 使用监控（虽然用 API 不需要本地 GPU，但 API 调用延迟/成本需要追踪）
- 无 API 调用成本追踪（GLM/Doubao 按量计费，需要每用户成本控制）
- 无 prompt 效果评估 pipeline（推荐点击率、试穿满意度等指标未闭环）

**修复**: 在 Analytics 模块中添加 AI 成本和效果追踪

- **Claude 分析**: 指标体系设计
- **Trae 执行**: 实现 tracking 代码

---

### D. 基础设施与安全

#### D1. [严重] .env 密钥过于规律

```
JWT_SECRET=a1b2c3d4e5f6a7b8c9d0...     # 交替十六进制模式
ENCRYPTION_KEY=0123456789abcdef...       # 顺序十六进制
POSTGRES_PASSWORD=aineed_dev_2026        # 可猜测模式
```

**修复**: 生成真正的随机密钥

```bash
openssl rand -hex 64  # 生成 128 字符随机密钥
```

- **优先级**: 立即

#### D2. [高] 生产环境 Docker 端口暴露

主 `docker-compose.yml` 将所有数据库端口(5432, 6379, 9000等)暴露到宿主机。生产环境正确使用 `expose`，但 main compose 不应用于服务器部署。

**修复**: 文档明确说明各 compose 文件的使用场景，或从 main compose 中移除 ports 映射

#### D3. [中] Nginx server_name 使用通配符

```nginx
server_name _;  # 应替换为实际域名
```

#### D4. [中] Staging 环境使用弱 CSRF 密钥默认值

```yaml
CSRF_SECRET: ${CSRF_SECRET:-xuno-csrf-secret} # staging
```

应改为 `${CSRF_SECRET:?CSRF_SECRET is required}`

#### D5. [中] Vault 使用开发模式

生产 compose 中 Vault 的 `VAULT_DEV_ROOT_TOKEN_ID` 是开发模式 token，应使用正式初始化的 Vault。

#### D6. [低] 缺少 IaC

没有 Terraform/K8s/Pulumi，基础设施完全靠 Docker Compose。适合 MVP，但扩展时需要 IaC。

#### D7. [低] Observability 网络名不匹配

`docker-compose.observability.yml` 使用 `ainetwork`，主 compose 使用 `xuno-network`，独立启动会失败。

---

### E. 数据完整性

#### E1. Seed 数据质量: 优秀

11步 seed 覆盖所有领域，15个测试账号覆盖多样化用户画像（时尚达人、运动员、街头风、极简主义者等）。

#### E2. 缺失的测试数据

- 聊天/顾问预约流程无 seed 数据
- 无负面测试数据（已被举报的帖子、退款中的订单等）

---

### F. 国际化准备

#### F1. 中文字符串管理

前端大量硬编码中文字符串散落在各屏幕组件中。虽然目前只做中国市场，但集中管理有利于:

- 后续扩展其他市场
- 统一文案修改
- A/B 测试不同文案

**修复**: 建立 i18n 配置文件，提取所有中文字符串

- **适合Trae**: 机械性提取工作

---

## 三、问题关联图

```
前端文件过大(A1) ──→ 视觉割裂(A2) ──→ 用户体验差
     │                                        │
     └──→ 组件复用率低 ──→ 开发效率低          │
                                               ↓
测试覆盖低(B5) ←── 业务逻辑复杂 ──→ 线上故障风险高
     │
     └──→ 无集成测试 ──→ 重构信心不足 ──→ 技术债累积

any类型(B1) ──→ 类型安全弱 ──→ 重构风险高 ──→ 不敢改代码

密钥规律(D1) ──→ 安全隐患 ──→ 数据泄露风险
```

---

## 四、修复路线图

### Phase 1: 紧急修复 (1-2天)

**目标: 消除安全隐患，确保基础可用**

| 任务                                   | 负责人                 | 依赖         |
| -------------------------------------- | ---------------------- | ------------ |
| D1: 替换 .env 中所有规律密钥为随机密钥 | Claude 指导 + 手动执行 | 无           |
| D3: Nginx server_name 替换为实际域名   | Trae                   | 需要域名信息 |
| D4: Staging CSRF 密钥改为必填          | Trae                   | 无           |

### Phase 2: 前端打磨 (3-5天) ★ 最影响用户感知

**目标: 前端视觉统一，体验流畅**

| 任务                                  | 负责人                  | 可并行                   |
| ------------------------------------- | ----------------------- | ------------------------ |
| A1: 拆分6个超大屏幕文件               | Trae (6个并行任务)      | ✅ 全部并行              |
| A2: 统一颜色/间距 → theme tokens      | Trae (codemod + 手动)   | ✅ 与A1并行              |
| A5: 修复 WardrobeStore/CartStore 性能 | Claude 分析 + Trae 执行 | 与A1串行(可能改同一文件) |

### Phase 3: 后端加固 (3-5天)

**目标: 类型安全、数据完整性**

| 任务                                | 负责人                  | 可并行   |
| ----------------------------------- | ----------------------- | -------- |
| B2: 添加数据库 CHECK 约束 migration | Trae                    | ✅       |
| B4: 统一软删除覆盖                  | Trae                    | ✅       |
| B6: 扩展 WebSocket 事件覆盖         | Claude 分析 + Trae 执行 | 与B2并行 |
| B1: 消除 Prisma 动态查询中的 any    | Trae (按模块分批)       | ✅       |

### Phase 4: 测试建设 (5-7天)

**目标: 关键路径测试覆盖 >80%**

| 任务                           | 负责人 | 依赖       |
| ------------------------------ | ------ | ---------- |
| 设计测试策略（哪些路径最关键） | Claude | 无         |
| 结账流程集成测试               | Trae   | 策略确定后 |
| 推荐引擎单元测试               | Trae   | 策略确定后 |
| AI 服务 mock 测试              | Trae   | 策略确定后 |

### Phase 5: AI 效果优化 (持续)

**目标: AI 推荐质量和成本控制**

| 任务                     | 负责人        | 依赖                 |
| ------------------------ | ------------- | -------------------- |
| C4: AI 成本追踪系统设计  | Claude        | 无                   |
| C4: AI 效果指标 pipeline | Claude + Trae | 成本追踪完成后       |
| C3: Prompt 版本管理机制  | Claude        | 无                   |
| A4: 外部分享 SDK 接入    | Trae          | 需要各平台开发者账号 |

### Phase 6: 运维成熟 (2-3天)

**目标: 生产环境可靠运行**

| 任务                           | 负责人 | 可并行 |
| ------------------------------ | ------ | ------ |
| D7: 修复 observability 网络名  | Trae   | ✅     |
| D6: 评估是否需要 Terraform/K8s | Claude | ✅     |
| E2: 补充负面测试 seed 数据     | Trae   | ✅     |
| F1: 提取硬编码中文字符串       | Trae   | ✅     |

---

## 五、适合 Trae vs Claude 的分工

### Trae 负责 (机械性、可批量执行)

1. 文件拆分 (A1) — 给出接口定义，逐文件拆分
2. 颜色/间距统一 (A2) — codemod 脚本
3. 数据库约束 SQL (B2) — 纯 SQL
4. any 类型消除 (B1) — 按模块批量
5. 测试编写 (B5) — 按策略逐模块
6. 中文字符串提取 (F1) — 机械提取
7. Seed 数据补充 (E2) — 按模板填充
8. Nginx/CSRF/网络名修复 (D3/D4/D7) — 配置修改

### Claude 负责 (需要分析、判断、设计)

1. 前端组件接口设计 (指导 A1 的拆分方式)
2. 测试策略设计 (哪些路径最关键)
3. AI 效果指标体系设计
4. 架构决策 (Profile Tab 拆分方案, IaC 评估)
5. WebSocket 事件覆盖方案设计
6. 推荐引擎效果验证方案

---

## 六、商业发布 Checklist

| 维度          | 状态        | 阻塞项                     |
| ------------- | ----------- | -------------------------- |
| 核心功能完整  | ✅ 完成     | -                          |
| 用户注册/登录 | ✅ 完成     | -                          |
| AI 造型师对话 | ✅ 完成     | -                          |
| 虚拟试穿      | ✅ 完成     | 照片式，非AR               |
| 衣橱管理      | ✅ 完成     | -                          |
| 购物车+支付   | ✅ 完成     | 需要真实支付商户号         |
| 社区          | ✅ 完成     | -                          |
| 推荐系统      | ✅ 完成     | 需真实用户数据验证效果     |
| 安全合规      | ⚠️ 基本完成 | 密钥需替换(D1)             |
| 性能达标      | ⚠️ 需验证   | 负载测试待执行             |
| 测试覆盖      | ❌ 不足     | 需补关键路径测试           |
| 监控告警      | ⚠️ 框架在   | 需实际接入和调优           |
| 应用商店      | ⚠️ 资料在   | 需审核和提交               |
| 隐私合规      | ✅ 完成     | PRIVACY.md + TERMS.md 已有 |

---

## 七、结论

AiNeed 项目在功能层面已经达到"能用"的标准。AI 层的质量尤其出色，推荐引擎和造型师对话都是真实的高水平实现。

**距离"可上架"的核心差距**:

1. **前端视觉打磨** — 这是用户第一眼看到的，需要投入最多精力
2. **安全基线加固** — 密钥替换、配置审查
3. **测试保障** — 关键路径必须有自动化测试
4. **运维就绪** — 监控、告警、灰度发布需要实际跑通

建议执行顺序: **Phase 1 (安全) → Phase 2 (前端) → Phase 3 (后端) → Phase 4 (测试) → Phase 5+6 (持续优化)**

前端和安全的修复可以并行启动，由 Trae 执行前端打磨任务，Claude 负责安全审查和架构决策。
