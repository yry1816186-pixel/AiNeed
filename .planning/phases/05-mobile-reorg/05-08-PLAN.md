---
phase: 5
plan: 08
type: refactor
wave: 2
depends_on: ["05-PLAN-03"]
files_modified:
  - apps/mobile/src/shared/stores/uiStore.ts
  - apps/mobile/src/shared/stores/app.store.ts
  - apps/mobile/src/shared/stores/index.ts
  - apps/mobile/src/features/commerce/stores/cart.store.ts
  - apps/mobile/src/features/commerce/stores/orderStore.ts
  - apps/mobile/src/features/commerce/stores/index.ts
  - apps/mobile/src/features/home/stores/index.ts
  - apps/mobile/src/stores/index.ts
  - apps/mobile/src/stores/__tests__/uiStore.test.ts
  - apps/mobile/src/stores/__tests__/clothingStore.test.ts
  - apps/mobile/src/stores/__tests__/notificationStore.test.ts
  - apps/mobile/src/stores/__tests__/wardrobeStore.test.ts
  - apps/mobile/src/stores/__tests__/onboardingStore.test.ts
  - apps/mobile/src/stores/__tests__/orderStore.test.ts
  - apps/mobile/src/stores/__tests__/photoStore.test.ts
  - apps/mobile/src/stores/__tests__/quizStore.test.ts
  - apps/mobile/src/stores/__tests__/recommendationFeedStore.test.ts
  - apps/mobile/src/stores/__tests__/couponStore.test.ts
  - apps/mobile/src/stores/__tests__/aiStylistChatStore.test.ts
  - apps/mobile/src/stores/__tests__/appStore.test.ts
  - apps/mobile/src/stores/__tests__/authStore.test.ts
autonomous: true
requirements:
  - MOBL-02
  - MOBL-05
---

<objective>
统一项目中重复定义的 Store，消除命名冲突和运行时风险。每个 Store 只保留一个权威来源，所有 barrel 文件只做 re-export，不做内联定义。修复跨 feature 的错误 import 路径和测试文件断裂 import。
</objective>

<tasks>

<task id="5-08-01" type="refactor">
<read_first>
- apps/mobile/src/shared/stores/uiStore.ts (version A: ModalType 含 share|sizeGuide, activeTab: string, 有 helper hooks)
- apps/mobile/src/shared/stores/index.ts (version B: ModalType 缺 share|sizeGuide, ActiveTab 为 union type, 有 reset())
- apps/mobile/src/stores/index.ts (主 barrel: 从 uiStore.ts re-export，注意 useUiStore 命名 bug)
- apps/mobile/src/shared/stores/app.store.ts (循环 re-export from ./index)
</read_first>
<action>
统一 useUIStore：将两个定义合并为一个权威版本，写入 `shared/stores/uiStore.ts`。

**合并策略（取两个版本的超集）：**

1. ModalType 取超集：`"none" | "filter" | "sort" | "share" | "settings" | "confirm" | "imagePicker" | "sizeGuide"`
2. ActiveTab 使用 version B 的 union type：`"home" | "explore" | "heart" | "cart" | "wardrobe" | "profile"`
3. 保留 version B 的 `reset()` 方法
4. 保留 version A 的 helper hooks（useTheme, useModal, useLoading, useNotifications, useOnline）
5. 保留 version A 的 `@deprecated` 注解
6. Notification 接口含 `timestamp` 字段
7. `addNotification` 使用 version A 实现（自动移除超时通知 + 限制最多 10 条），ID 用 `notif_` 前缀
8. persist partialize 持久化 `theme` 和 `activeTab`
9. `initialState` 常量提取便于 reset()

**行为变更说明（相比 version B）：**

- `addNotification` 签名从 `Omit<UINotification, "id">` 变为 `Omit<Notification, "id" | "timestamp">`
- 通知现在会自动移除（version B 没有此行为）

用上述合并结果覆盖 `shared/stores/uiStore.ts` 的全部内容。

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>

- `grep -c "export const useUIStore" apps/mobile/src/shared/stores/uiStore.ts` 返回 1
- `grep -c "export const useUIStore" apps/mobile/src/shared/stores/index.ts` 返回 0
- `apps/mobile/src/shared/stores/uiStore.ts` 包含 `export type ModalType` 含 "share" 和 "sizeGuide"
- `apps/mobile/src/shared/stores/uiStore.ts` 包含 `export type ActiveTab` 类型定义
- `apps/mobile/src/shared/stores/uiStore.ts` 包含 `reset: () => void`
- `apps/mobile/src/shared/stores/uiStore.ts` 包含 `export const useModal`
- `cd apps/mobile && npx tsc --noEmit` 通过
  </acceptance_criteria>
  </task>

<task id="5-08-02" type="refactor">
<read_first>
- apps/mobile/src/shared/stores/index.ts (包含 useUIStore 和 useAppStore 内联定义)
- apps/mobile/src/shared/stores/app.store.ts (当前是循环 re-export from ./index)
- apps/mobile/src/shared/stores/uiStore.ts (task 5-08-01 完成后的权威版本)
</read_first>
<action>
将 useAppStore 从 `shared/stores/index.ts` 提取到 `shared/stores/app.store.ts`，然后将 `shared/stores/index.ts` 转为纯 barrel re-export。

**Step 1: 重写 `shared/stores/app.store.ts`**

将 `shared/stores/index.ts` 中 useAppStore 的完整内联定义（从 `type NetworkType` 到文件末尾的 `useAppStore` create 调用）移入 `shared/stores/app.store.ts`。这是从现有 `index.ts` 中**复制**代码，不是从零编写。确保包含：

- `type NetworkType`
- `interface AppState`（含所有字段和方法签名）
- `const appInitialState`
- `export const useAppStore = create<AppState>()(persist(...))`

**Step 2: 重写 `shared/stores/index.ts` 为纯 barrel re-export**

```typescript
export { useUIStore } from "./uiStore";
export type { ModalType, ActiveTab, ThemeMode } from "./uiStore";
export { useTheme, useModal, useLoading, useNotifications, useOnline } from "./uiStore";
export { useAppStore } from "./app.store";
```

注意：`useTheme` 虽然标记为 deprecated 但仍需导出以保持向后兼容。

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>

- `grep -c "export const useAppStore" apps/mobile/src/shared/stores/app.store.ts` 返回 1
- `grep -c "export const useAppStore" apps/mobile/src/shared/stores/index.ts` 返回 0
- `grep -c "export const useUIStore" apps/mobile/src/shared/stores/index.ts` 返回 0
- `grep -c "create<" apps/mobile/src/shared/stores/index.ts` 返回 0
- `apps/mobile/src/shared/stores/index.ts` 包含 `export { useTheme` (deprecated 但仍导出)
- `cd apps/mobile && npx tsc --noEmit` 通过
  </acceptance_criteria>
  </task>

<task id="5-08-03" type="refactor">
<read_first>
- apps/mobile/src/features/commerce/stores/index.ts (包含过时的内联 useCouponStore, useSizeRecommendationStore, useOrderStore)
- apps/mobile/src/features/commerce/stores/couponStore.ts (已存在的权威版本)
- apps/mobile/src/features/commerce/stores/sizeRecommendationStore.ts (已存在的权威版本，接口与内联版完全不同)
- apps/mobile/src/features/commerce/stores/orderStore.ts (已存在的权威版本，接口与内联版完全不同，且有断裂 import)
- apps/mobile/src/features/commerce/stores/cart.store.ts (独立文件，已有)
</read_first>
<action>
删除 `commerce/stores/index.ts` 中的过时内联定义，保留已存在的独立文件为权威版本，将 index.ts 转为 barrel re-export。

**重要：独立文件已存在且实现更完善，不可覆盖！**

- `couponStore.ts` 已存在，内容与内联版相同
- `sizeRecommendationStore.ts` 已存在，使用 `Record<string, SizeRecommendation | null>` + `sizeRecommendationApi`（内联版用 Array + stub，已过时）
- `orderStore.ts` 已存在，使用 `ordersByTab` + `orderEnhancementApi`（内联版用 `orders: Order[]` + stub，已过时）

**Step 1: 修复 `orderStore.ts` 的断裂 import**

当前第 4 行：`import type { Order } from "../types"` — `features/commerce/types.ts` 不存在。

修改为从 `@xuno/types` 或 `shared/types` 导入 Order 类型。如果 `@xuno/types` 未配置，则从 `../../../types/api` 导入（检查 `apps/mobile/src/types/api.ts` 是否导出 Order 类型）。如果均不可用，则在 `orderStore.ts` 文件内定义 `Order` interface（与现有实现一致）。

**Step 2: 重写 `features/commerce/stores/index.ts` 为纯 barrel re-export**

```typescript
export { useCartStore } from "./cart.store";
export { useCouponStore } from "./couponStore";
export { useSizeRecommendationStore } from "./sizeRecommendationStore";
export { useOrderStore } from "./orderStore";
```

删除 index.ts 中所有内联的 interface 定义和 create 调用。

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>

- `grep -c "create<" apps/mobile/src/features/commerce/stores/index.ts` 返回 0
- `grep -c "export const use" apps/mobile/src/features/commerce/stores/index.ts` 返回 0
- `apps/mobile/src/features/commerce/stores/index.ts` 包含 `export { useCartStore } from "./cart.store"`
- `apps/mobile/src/features/commerce/stores/index.ts` 包含 `export { useCouponStore } from "./couponStore"`
- `apps/mobile/src/features/commerce/stores/orderStore.ts` 不包含 `from "../types"`（断裂 import 已修复）
- `cd apps/mobile && npx tsc --noEmit` 通过
  </acceptance_criteria>
  </task>

<task id="5-08-04" type="refactor">
<read_first>
- apps/mobile/src/features/home/stores/index.ts (包含内联 useHomeStore, useRecommendationFeedStore)
- apps/mobile/src/features/home/stores/homeStore.ts (已存在的权威版本，内容与内联版相同)
- apps/mobile/src/features/home/stores/recommendationFeedStore.ts (已存在的权威版本，内容与内联版相同)
- apps/mobile/src/features/home/stores/heart-recommend.store.ts (独立文件，已有)
- apps/mobile/src/features/home/stores/recommendation.store.ts (独立文件，已有)
</read_first>
<action>
删除 `home/stores/index.ts` 中的内联定义，保留已存在的独立文件为权威版本，将 index.ts 转为 barrel re-export。

**重要：独立文件已存在，不可覆盖！**

- `homeStore.ts` 已存在，内容与内联版相同
- `recommendationFeedStore.ts` 已存在，内容与内联版相同

**重写 `features/home/stores/index.ts` 为纯 barrel re-export**

```typescript
export { useHomeStore } from "./homeStore";
export type { WeatherData } from "./homeStore";
export { useProfileCompletion, useWeatherData, useBannerState } from "./homeStore";
export { useRecommendationFeedStore } from "./recommendationFeedStore";
export { useHeartRecommendStore } from "./heart-recommend.store";
export type { LikedItemData } from "./heart-recommend.store";
export { useRecommendationStore } from "./recommendation.store";
```

删除 index.ts 中所有内联的 interface 定义、函数和 create 调用。

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>

- `grep -c "create<" apps/mobile/src/features/home/stores/index.ts` 返回 0
- `grep -c "export const use" apps/mobile/src/features/home/stores/index.ts` 返回 0
- `apps/mobile/src/features/home/stores/index.ts` 包含 `export { useHomeStore } from "./homeStore"`
- `apps/mobile/src/features/home/stores/index.ts` 包含 `export type { WeatherData } from "./homeStore"`
- `apps/mobile/src/features/home/stores/index.ts` 包含 `export type { LikedItemData } from "./heart-recommend.store"`
- `cd apps/mobile && npx tsc --noEmit` 通过
  </acceptance_criteria>
  </task>

<task id="5-08-05" type="fix">
<read_first>
- apps/mobile/src/features/commerce/stores/cart.store.ts (第 4 行: `import type { ClothingItem } from "./clothingStore"`)
- apps/mobile/src/features/wardrobe/stores/clothingStore.ts (ClothingItem 的实际位置)
- apps/mobile/src/stores/index.ts (主 barrel，第 27 行有 useUiStore 命名 bug)
</read_first>
<action>
修复两个 bug：

**Bug 1: cart.store.ts 断裂 import**

当前（错误）：`import type { ClothingItem } from "./clothingStore"`
`clothingStore.ts` 位于 `features/wardrobe/stores/`，不在 `features/commerce/stores/`。

修改为：

```typescript
import type { ClothingItem } from "../../wardrobe/stores/clothingStore";
```

同时检查 `ClothingItem` 是否已从 `clothingStore.ts` 导出。如果未导出（只是 interface 定义没有 export），需要添加 `export` 关键字。

**Bug 2: stores/index.ts 中 useUiStore 命名不一致**

第 27 行：`export { useUiStore } from "../shared/stores/uiStore"` — 导出名 `useUiStore`（小写 i）
但实际导出的是 `useUIStore`（大写 I），导致：

- 从 barrel 导入 `useUiStore` 得到 undefined
- `clearAllStores()` 中 `ui` 变量为 undefined

修改第 27 行为：

```typescript
export { useUIStore } from "../shared/stores/uiStore";
```

修改第 52 行 clearAllStores 中的：

```typescript
const { useUiStore: ui } = require("../shared/stores/uiStore");
```

改为：

```typescript
const { useUIStore: ui } = require("../shared/stores/uiStore");
```

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>

- `grep "from.*clothingStore" apps/mobile/src/features/commerce/stores/cart.store.ts` 包含 `../../wardrobe/stores/clothingStore`
- `grep "useUiStore" apps/mobile/src/stores/index.ts` 返回空（旧命名已移除）
- `grep "useUIStore" apps/mobile/src/stores/index.ts` 返回至少 2 行（export + require）
- `cd apps/mobile && npx tsc --noEmit` 通过
  </acceptance_criteria>
  </task>

<task id="5-08-06" type="fix">
<read_first>
- apps/mobile/src/stores/__tests__/uiStore.test.ts (import from "../uiStore")
- apps/mobile/src/stores/__tests__/clothingStore.test.ts (import from "../clothingStore")
- apps/mobile/src/stores/__tests__/notificationStore.test.ts (import from "../notificationStore")
- apps/mobile/src/stores/__tests__/wardrobeStore.test.ts (import from "../wardrobeStore")
- apps/mobile/src/stores/__tests__/onboardingStore.test.ts (import from "../onboardingStore")
- apps/mobile/src/stores/__tests__/orderStore.test.ts (import from "../orderStore")
- apps/mobile/src/stores/__tests__/photoStore.test.ts (import from "../photoStore")
- apps/mobile/src/stores/__tests__/quizStore.test.ts (import from "../quizStore")
- apps/mobile/src/stores/__tests__/recommendationFeedStore.test.ts (import from "../recommendationFeedStore")
- apps/mobile/src/stores/__tests__/couponStore.test.ts (import from "../couponStore")
- apps/mobile/src/stores/__tests__/aiStylistChatStore.test.ts (import from "../aiStylistChatStore")
- apps/mobile/src/stores/__tests__/appStore.test.ts (import from "../app.store")
- apps/mobile/src/stores/__tests__/authStore.test.ts (import from "../authStore" 或类似)
</read_first>
<action>
修复 `stores/__tests__/` 下所有 13 个测试文件的断裂 import 路径。所有 `from "../xxxStore"` 需要改为指向正确的 feature/shared 目录。

**具体修改映射：**

| 测试文件                        | 旧 import                           | 新 import                                                      |
| ------------------------------- | ----------------------------------- | -------------------------------------------------------------- |
| uiStore.test.ts                 | `from "../uiStore"`                 | `from "../../shared/stores/uiStore"`                           |
| clothingStore.test.ts           | `from "../clothingStore"`           | `from "../../features/wardrobe/stores/clothingStore"`          |
| notificationStore.test.ts       | `from "../notificationStore"`       | `from "../../features/notifications/stores/notificationStore"` |
| wardrobeStore.test.ts           | `from "../wardrobeStore"`           | `from "../../features/wardrobe/stores/wardrobeStore"`          |
| onboardingStore.test.ts         | `from "../onboardingStore"`         | `from "../../features/onboarding/stores/onboardingStore"`      |
| orderStore.test.ts              | `from "../orderStore"`              | `from "../../features/commerce/stores/orderStore"`             |
| photoStore.test.ts              | `from "../photoStore"`              | `from "../../features/tryon/stores/photoStore"`                |
| quizStore.test.ts               | `from "../quizStore"`               | `from "../../features/style-quiz/stores/quizStore"`            |
| recommendationFeedStore.test.ts | `from "../recommendationFeedStore"` | `from "../../features/home/stores/recommendationFeedStore"`    |
| couponStore.test.ts             | `from "../couponStore"`             | `from "../../features/commerce/stores/couponStore"`            |
| aiStylistChatStore.test.ts      | `from "../aiStylistChatStore"`      | `from "../../features/stylist/stores/aiStylistChatStore"`      |
| appStore.test.ts                | `from "../app.store"`               | `from "../../shared/stores/app.store"`                         |
| authStore.test.ts               | 检查实际 import 路径                | 指向 `../../features/auth/stores/authStore`                    |

同时检查每个测试文件中的其他 import（如 `from "../../services/..."`、`from "../../types"`），确保它们也指向正确位置。如果测试文件中 import 的 API service 或 type 文件也已迁移到 feature 目录，需要同步更新。

**uiStore.test.ts 特殊处理：**

- 合并后的 UIState 新增了 `reset()` 方法，测试可能需要添加 reset 相关测试用例
- `activeTab` 类型从 `string` 变为 `ActiveTab` union type，现有测试中 `setActiveTab("explore")` 等仍然合法
- `ModalType` 新增了 "share" 和 "sizeGuide"，现有测试不受影响

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>

- `grep -rn 'from "\.\./' apps/mobile/src/stores/__tests__/ --include="*.ts"` 返回 0（无旧路径 import）
- `cd apps/mobile && npx tsc --noEmit` 通过
  </acceptance_criteria>
  </task>

<task id="5-08-07" type="verify">
<read_first>
- apps/mobile/src/stores/index.ts (主 barrel)
- apps/mobile/src/shared/stores/index.ts (shared barrel)
- apps/mobile/src/features/commerce/stores/index.ts (commerce barrel)
- apps/mobile/src/features/home/stores/index.ts (home barrel)
</read_first>
<action>
最终验证：确认所有 Store 只有一个权威来源，所有 barrel 文件只做 re-export。

1. **全局搜索重复定义：**

   ```bash
   grep -rn "export const use" apps/mobile/src/shared/stores/ apps/mobile/src/features/*/stores/ --include="*.ts" | grep -v "index.ts" | sort
   ```

   每个 store name 应该只出现一次。

2. **确认所有 barrel 文件无内联定义：**

   ```bash
   grep -l "create<" apps/mobile/src/shared/stores/index.ts apps/mobile/src/features/*/stores/index.ts
   ```

   应该返回空。

3. **确认主 barrel 的 re-export 路径正确：**
   检查 `stores/index.ts` 中每个 re-export 的目标文件是否存在。

4. **确认 useUIStore 命名一致：**

   ```bash
   grep -rn "useUiStore" apps/mobile/src/ --include="*.ts"
   ```

   应该返回 0（全部使用 useUIStore 大写 I）。

5. **运行 TypeScript 编译：**

   ```bash
   cd apps/mobile && npx tsc --noEmit
   ```

6. **运行测试：**
   ```bash
   cd apps/mobile && npx jest --passWithNoTests
   ```
   </action>
   <acceptance_criteria>

- `grep -rn "export const useUIStore" apps/mobile/src/ --include="*.ts"` 只返回 1 个结果（shared/stores/uiStore.ts）
- `grep -rn "export const useAppStore" apps/mobile/src/ --include="*.ts"` 只返回 1 个结果（shared/stores/app.store.ts）
- `grep -c "create<" apps/mobile/src/shared/stores/index.ts` 返回 0
- `grep -c "create<" apps/mobile/src/features/commerce/stores/index.ts` 返回 0
- `grep -c "create<" apps/mobile/src/features/home/stores/index.ts` 返回 0
- `grep -rn "useUiStore" apps/mobile/src/ --include="*.ts"` 返回 0（命名已统一）
- `cd apps/mobile && npx tsc --noEmit` 通过
- `cd apps/mobile && npx jest --passWithNoTests` 通过
  </acceptance_criteria>
  </task>

</tasks>

<verification>
1. `cd apps/mobile && npx tsc --noEmit` — 必须通过
2. `cd apps/mobile && npx jest --passWithNoTests` — 必须通过
3. 每个 Store name 全局搜索只出现一次定义：`grep -rn "export const use" apps/mobile/src/ --include="*.ts" | grep -v index.ts | grep -v __tests__ | sort`
4. 所有 barrel index.ts 不含 `create<` 调用
5. `stores/index.ts` 主 barrel 所有 re-export 路径可解析
6. 无 `useUiStore`（小写 i）残留
</verification>

<success_criteria>

- useUIStore 只有 1 个定义（shared/stores/uiStore.ts），合并了两个版本的超集
- useAppStore 只有 1 个定义（shared/stores/app.store.ts），从 index.ts 提取
- 所有 barrel 文件（shared/stores/index.ts, commerce/stores/index.ts, home/stores/index.ts）只包含 re-export
- cart.store.ts 的 ClothingItem import 指向正确路径（../../wardrobe/stores/clothingStore）
- orderStore.ts 的 Order import 断裂已修复
- stores/index.ts 中 useUiStore 命名 bug 已修复为 useUIStore
- 所有 13 个测试文件 import 路径已更新
- TypeScript 编译通过
- 测试通过
  </success_criteria>
