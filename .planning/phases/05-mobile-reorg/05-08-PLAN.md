---
phase: 5
plan: 08
type: refactor
wave: 2
depends_on: ["05-PLAN-03"]
files_modified:
  - apps/mobile/src/shared/stores/uiStore.ts
  - apps/mobile/src/shared/stores/app.store.ts
  - apps/mobile/src/shared/stores/index.ts
  - apps/mobile/src/features/commerce/stores/cart.store.ts
  - apps/mobile/src/features/commerce/stores/couponStore.ts
  - apps/mobile/src/features/commerce/stores/sizeRecommendationStore.ts
  - apps/mobile/src/features/commerce/stores/orderStore.ts
  - apps/mobile/src/features/commerce/stores/index.ts
  - apps/mobile/src/features/home/stores/homeStore.ts
  - apps/mobile/src/features/home/stores/recommendationFeedStore.ts
  - apps/mobile/src/features/home/stores/index.ts
  - apps/mobile/src/stores/index.ts
  - apps/mobile/src/stores/__tests__/uiStore.test.ts
autonomous: true
requirements:
  - MOBL-02
  - MOBL-05
---

<objective>
统一项目中重复定义的 Store，消除命名冲突和运行时风险。每个 Store 只保留一个权威来源，所有 barrel 文件只做 re-export，不做内联定义。修复跨 feature 的错误 import 路径。
</objective>

<tasks>

<task id="5-08-01" type="refactor">
<read_first>
- apps/mobile/src/shared/stores/uiStore.ts (version A: ModalType 含 share|sizeGuide, activeTab: string, 有 helper hooks)
- apps/mobile/src/shared/stores/index.ts (version B: ModalType 缺 share|sizeGuide, ActiveTab 为 union type, 有 reset())
- apps/mobile/src/stores/index.ts (主 barrel: 从 uiStore.ts re-export)
- apps/mobile/src/shared/stores/app.store.ts (循环 re-export from ./index)
</read_first>
<action>
统一 useUIStore：将两个定义合并为一个权威版本，写入 `shared/stores/uiStore.ts`。

**合并策略（取两个版本的超集）：**

1. ModalType 取超集 — 合并 version A 和 version B 的所有值：
   ```typescript
   export type ModalType =
     | "none"
     | "filter"
     | "sort"
     | "share"
     | "settings"
     | "confirm"
     | "imagePicker"
     | "sizeGuide";
   ```

2. ActiveTab 使用 version B 的 union type（更类型安全）：
   ```typescript
   export type ActiveTab = "home" | "explore" | "heart" | "cart" | "wardrobe" | "profile";
   ```
   UIState 中 `activeTab: ActiveTab`，`setActiveTab: (tab: ActiveTab) => void`

3. 保留 version B 的 `reset()` 方法（version A 没有）

4. 保留 version A 的 helper hooks（useTheme, useModal, useLoading, useNotifications, useOnline）

5. 保留 version A 的 `@deprecated` 注解（theme 相关字段标记为 deprecated，引导使用 ThemeContext）

6. Notification 接口取超集 — 包含 version A 的 `timestamp` 字段和 version B 的结构

7. `addNotification` 使用 version A 的实现（自动移除超时通知 + 限制最多 10 条），但 ID 生成使用 version B 的 `notif_` 前缀格式

8. persist partialize 取超集 — `theme` 和 `activeTab` 都持久化

9. `initialState` 常量提取（参考 version B 的模式，便于 reset()）

**最终 `shared/stores/uiStore.ts` 的完整内容：**

```typescript
import { create } from "zustand";
import { persist, createJSONStorage } from "zustand/middleware";
import AsyncStorage from "@react-native-async-storage/async-storage";

export type ModalType =
  | "none"
  | "filter"
  | "sort"
  | "share"
  | "settings"
  | "confirm"
  | "imagePicker"
  | "sizeGuide";

export type ActiveTab = "home" | "explore" | "heart" | "cart" | "wardrobe" | "profile";

/**
 * @deprecated 使用 `import { ThemeMode } from '../../contexts/ThemeContext'` 代替。
 */
export type ThemeMode = "light" | "dark" | "system";

interface Notification {
  id: string;
  type: "success" | "error" | "warning" | "info";
  title: string;
  message?: string;
  duration?: number;
  timestamp: number;
}

interface UIState {
  /** @deprecated 使用 ThemeContext (contexts/ThemeContext) 管理主题状态 */
  theme: ThemeMode;
  activeModal: ModalType;
  modalData: Record<string, unknown>;
  isLoading: Record<string, boolean>;
  notifications: Notification[];
  isOnline: boolean;
  isScrolling: boolean;
  activeTab: ActiveTab;
  searchQuery: string;
  isSearchFocused: boolean;

  /** @deprecated 使用 ThemeContext 的 setMode 代替 */
  setTheme: (theme: ThemeMode) => void;
  showModal: (modal: ModalType, data?: Record<string, unknown>) => void;
  hideModal: () => void;
  setLoading: (key: string, loading: boolean) => void;
  addNotification: (notification: Omit<Notification, "id" | "timestamp">) => void;
  removeNotification: (id: string) => void;
  clearNotifications: () => void;
  setOnline: (online: boolean) => void;
  setScrolling: (scrolling: boolean) => void;
  setActiveTab: (tab: ActiveTab) => void;
  setSearchQuery: (query: string) => void;
  setSearchFocused: (focused: boolean) => void;
  reset: () => void;
}

const initialState = {
  theme: "light" as const,
  activeModal: "none" as const,
  modalData: {},
  isLoading: {},
  notifications: [],
  isOnline: true,
  isScrolling: false,
  activeTab: "home" as const,
  searchQuery: "",
  isSearchFocused: false,
};

export const useUIStore = create<UIState>()(
  persist(
    (set, get) => ({
      ...initialState,

      setTheme: (theme) => set({ theme }),

      showModal: (modal, data = {}) => set({ activeModal: modal, modalData: data }),

      hideModal: () => set({ activeModal: "none", modalData: {} }),

      setLoading: (key, loading) =>
        set((state) => ({
          isLoading: { ...state.isLoading, [key]: loading },
        })),

      addNotification: (notification) => {
        const id = `notif_${Date.now()}_${Math.random().toString(36).slice(2)}`;
        const newNotification: Notification = {
          ...notification,
          id,
          timestamp: Date.now(),
          duration: notification.duration ?? 3000,
        };
        set((state) => ({
          notifications: [...state.notifications, newNotification].slice(-10),
        }));

        if (newNotification.duration && newNotification.duration > 0) {
          setTimeout(() => {
            get().removeNotification(id);
          }, newNotification.duration);
        }
      },

      removeNotification: (id) =>
        set((state) => ({
          notifications: state.notifications.filter((n) => n.id !== id),
        })),

      clearNotifications: () => set({ notifications: [] }),

      setOnline: (isOnline) => set({ isOnline }),

      setScrolling: (isScrolling) => set({ isScrolling }),

      setActiveTab: (activeTab) => set({ activeTab }),

      setSearchQuery: (searchQuery) => set({ searchQuery }),

      setSearchFocused: (isSearchFocused) => set({ isSearchFocused }),

      reset: () => set(initialState),
    }),
    {
      name: "ui-storage",
      storage: createJSONStorage(() => AsyncStorage),
      partialize: (state) => ({
        theme: state.theme,
        activeTab: state.activeTab,
      }),
    }
  )
);

/** @deprecated 使用 `useTheme` from `../../../../contexts/ThemeContext` 代替 */
export const useTheme = () => useUIStore((state) => state.theme);
export const useModal = () =>
  useUIStore((state) => ({
    activeModal: state.activeModal,
    modalData: state.modalData,
  }));
export const useLoading = (key: string) => useUIStore((state) => state.isLoading[key] ?? false);
export const useNotifications = () => useUIStore((state) => state.notifications);
export const useOnline = () => useUIStore((state) => state.isOnline);
```

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>
- `grep -c "export const useUIStore" apps/mobile/src/shared/stores/uiStore.ts` 返回 1
- `grep -c "export const useUIStore" apps/mobile/src/shared/stores/index.ts` 返回 0
- `apps/mobile/src/shared/stores/uiStore.ts` 包含 `ModalType` 含 "share" 和 "sizeGuide"
- `apps/mobile/src/shared/stores/uiStore.ts` 包含 `ActiveTab` 类型定义
- `apps/mobile/src/shared/stores/uiStore.ts` 包含 `reset: () => void`
- `apps/mobile/src/shared/stores/uiStore.ts` 包含 `useModal` helper hook
- `cd apps/mobile && npx tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="5-08-02" type="refactor">
<read_first>
- apps/mobile/src/shared/stores/index.ts (包含 useUIStore 和 useAppStore 内联定义)
- apps/mobile/src/shared/stores/app.store.ts (当前是循环 re-export from ./index)
- apps/mobile/src/shared/stores/uiStore.ts (task 5-08-01 完成后的权威版本)
</read_first>
<action>
将 useAppStore 从 `shared/stores/index.ts` 提取到 `shared/stores/app.store.ts`，然后将 `shared/stores/index.ts` 转为纯 barrel re-export。

**Step 1: 重写 `shared/stores/app.store.ts`**

将 `shared/stores/index.ts` 中 useAppStore 的完整内联定义（从 `type NetworkType` 到文件末尾）移入 `shared/stores/app.store.ts`。内容为：

```typescript
import { create } from "zustand";
import { persist, createJSONStorage } from "zustand/middleware";
import AsyncStorage from "@react-native-async-storage/async-storage";

type NetworkType = "unknown" | "wifi" | "cellular" | "none";

interface AppState {
  isOnline: boolean;
  networkType: NetworkType;
  isFirstLaunch: boolean;
  hasCompletedOnboarding: boolean;
  pushPermissionGranted: boolean;
  hasRequestedPushPermission: boolean;
  appVersion: string | null;
  lastActiveAt: number | null;

  setOnline: (online: boolean) => void;
  setNetworkType: (type: NetworkType) => void;
  markFirstLaunchComplete: () => void;
  setOnboardingCompleted: (completed: boolean) => void;
  setPushPermission: (granted: boolean) => void;
  setHasRequestedPushPermission: (requested: boolean) => void;
  setAppVersion: (version: string) => void;
  updateLastActiveAt: () => void;
  resetApp: () => void;
}

const appInitialState = {
  isOnline: true,
  networkType: "unknown" as const,
  isFirstLaunch: true,
  hasCompletedOnboarding: false,
  pushPermissionGranted: false,
  hasRequestedPushPermission: false,
  appVersion: null as string | null,
  lastActiveAt: null as number | null,
};

export const useAppStore = create<AppState>()(
  persist(
    (set) => ({
      ...appInitialState,

      setOnline: (online) => set({ isOnline: online }),
      setNetworkType: (type) => set({ networkType: type }),
      markFirstLaunchComplete: () => set({ isFirstLaunch: false }),
      setOnboardingCompleted: (completed) => set({ hasCompletedOnboarding: completed }),
      setPushPermission: (granted) =>
        set({ pushPermissionGranted: granted, hasRequestedPushPermission: true }),
      setHasRequestedPushPermission: (requested) =>
        set({ hasRequestedPushPermission: requested }),
      setAppVersion: (version) => set({ appVersion: version }),
      updateLastActiveAt: () => set({ lastActiveAt: Date.now() }),
      resetApp: () =>
        set({
          ...appInitialState,
          isOnline: true,
          networkType: "unknown" as const,
        }),
    }),
    {
      name: "app-storage",
      storage: createJSONStorage(() => AsyncStorage),
      partialize: (state) => ({
        isFirstLaunch: state.isFirstLaunch,
        hasCompletedOnboarding: state.hasCompletedOnboarding,
        hasRequestedPushPermission: state.hasRequestedPushPermission,
        appVersion: state.appVersion,
      }),
    }
  )
);
```

**Step 2: 重写 `shared/stores/index.ts` 为纯 barrel re-export**

```typescript
export { useUIStore } from "./uiStore";
export type { ModalType, ActiveTab, ThemeMode } from "./uiStore";
export { useModal, useLoading, useNotifications, useOnline } from "./uiStore";
export { useAppStore } from "./app.store";
```

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>
- `grep -c "export const useAppStore" apps/mobile/src/shared/stores/app.store.ts` 返回 1
- `grep -c "export const useAppStore" apps/mobile/src/shared/stores/index.ts` 返回 0
- `grep -c "export const useUIStore" apps/mobile/src/shared/stores/index.ts` 返回 0
- `apps/mobile/src/shared/stores/index.ts` 只包含 re-export 语句（无 create/persist 调用）
- `cd apps/mobile && npx tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="5-08-03" type="refactor">
<read_first>
- apps/mobile/src/features/commerce/stores/index.ts (内联 useCouponStore, useSizeRecommendationStore, useOrderStore)
- apps/mobile/src/features/commerce/stores/cart.store.ts (独立文件，已有)
- apps/mobile/src/features/commerce/stores/couponStore.ts (如已存在则跳过)
- apps/mobile/src/features/commerce/stores/sizeRecommendationStore.ts (如已存在则跳过)
- apps/mobile/src/features/commerce/stores/orderStore.ts (如已存在则跳过)
</read_first>
<action>
将 `commerce/stores/index.ts` 中的内联 Store 定义提取到独立文件，然后转为纯 barrel re-export。

**Step 1: 创建 `features/commerce/stores/couponStore.ts`**

从 `index.ts` 中提取 `useCouponStore` 的完整定义（包括 `CouponStore` interface 和 `couponApi` import），写入独立文件。

**Step 2: 创建 `features/commerce/stores/sizeRecommendationStore.ts`**

从 `index.ts` 中提取 `useSizeRecommendationStore` 的完整定义（包括 `SizeRecommendationState` interface），写入独立文件。

**Step 3: 创建 `features/commerce/stores/orderStore.ts`**

从 `index.ts` 中提取 `useOrderStore` 的完整定义（包括 `OrderItem`, `Order`, `OrderState` interfaces），写入独立文件。

**Step 4: 重写 `features/commerce/stores/index.ts` 为纯 barrel re-export**

```typescript
export { useCartStore } from "./cart.store";
export { useCouponStore } from "./couponStore";
export { useSizeRecommendationStore } from "./sizeRecommendationStore";
export { useOrderStore } from "./orderStore";
```

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>
- `test -f apps/mobile/src/features/commerce/stores/couponStore.ts` exits 0
- `test -f apps/mobile/src/features/commerce/stores/sizeRecommendationStore.ts` exits 0
- `test -f apps/mobile/src/features/commerce/stores/orderStore.ts` exits 0
- `grep -c "export const use" apps/mobile/src/features/commerce/stores/index.ts` 返回 0 (无内联定义)
- `cd apps/mobile && npx tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="5-08-04" type="refactor">
<read_first>
- apps/mobile/src/features/home/stores/index.ts (内联 useHomeStore, useRecommendationFeedStore)
- apps/mobile/src/features/home/stores/homeStore.ts (如已存在则跳过)
- apps/mobile/src/features/home/stores/recommendationFeedStore.ts (如已存在则跳过)
- apps/mobile/src/features/home/stores/heart-recommend.store.ts (独立文件，已有)
- apps/mobile/src/features/home/stores/recommendation.store.ts (独立文件，已有)
</read_first>
<action>
将 `home/stores/index.ts` 中的内联 Store 定义提取到独立文件，然后转为纯 barrel re-export。

**Step 1: 创建 `features/home/stores/homeStore.ts`**

从 `index.ts` 中提取 `useHomeStore` 的完整定义（包括 `WeatherData`, `HomeState` interfaces, `calculateProfileCompletion` 函数, `useProfileCompletion`, `useWeatherData`, `useBannerState` helper hooks），写入独立文件。保留所有 import 语句（`profileApi`, `weatherService` 等）。

**Step 2: 创建 `features/home/stores/recommendationFeedStore.ts`**

从 `index.ts` 中提取 `useRecommendationFeedStore` 的完整定义（包括 `FeedState` interface, `PAGE_SIZE` 常量, `deduplicateItems` 函数, 以及 `recommendationFeedApi`, `recommendationsApi` imports），写入独立文件。

**Step 3: 重写 `features/home/stores/index.ts` 为纯 barrel re-export**

```typescript
export { useHomeStore } from "./homeStore";
export type { WeatherData } from "./homeStore";
export { useProfileCompletion, useWeatherData, useBannerState } from "./homeStore";
export { useRecommendationFeedStore } from "./recommendationFeedStore";
export { useHeartRecommendStore } from "./heart-recommend.store";
export type { LikedItemData } from "./heart-recommend.store";
export { useRecommendationStore } from "./recommendation.store";
```

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>
- `test -f apps/mobile/src/features/home/stores/homeStore.ts` exits 0
- `test -f apps/mobile/src/features/home/stores/recommendationFeedStore.ts` exits 0
- `grep -c "export const use" apps/mobile/src/features/home/stores/index.ts` 返回 0 (无内联定义)
- `cd apps/mobile && npx tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="5-08-05" type="fix">
<read_first>
- apps/mobile/src/features/commerce/stores/cart.store.ts (第 4 行: `import type { ClothingItem } from "./clothingStore"`)
- apps/mobile/src/features/wardrobe/stores/clothingStore.ts (ClothingItem 的实际位置)
</read_first>
<action>
修复 `cart.store.ts` 中错误的 ClothingItem import 路径。

当前（错误）：
```typescript
import type { ClothingItem } from "./clothingStore";
```

`clothingStore.ts` 位于 `features/wardrobe/stores/`，不在 `features/commerce/stores/`。

修改为正确的相对路径：
```typescript
import type { ClothingItem } from "../../wardrobe/stores/clothingStore";
```

同时检查 `ClothingItem` 是否已从 `clothingStore.ts` 导出（应为 `export interface ClothingItem` 或 `export type ClothingItem`）。如果未导出，需要在 `clothingStore.ts` 中添加 export。

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>
- `grep "from.*clothingStore" apps/mobile/src/features/commerce/stores/cart.store.ts` 返回包含 `../../wardrobe/stores/clothingStore` 的行
- `cd apps/mobile && npx tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="5-08-06" type="fix">
<read_first>
- apps/mobile/src/stores/__tests__/uiStore.test.ts (第 1 行: `import { useUIStore } from "../uiStore"`)
- apps/mobile/src/stores/__tests__/clothingStore.test.ts (第 1 行: `import { useClothingStore, ClothingItem, ClothingFilter } from "../clothingStore"`)
- apps/mobile/src/shared/stores/uiStore.ts (task 5-08-01 完成后的权威版本)
- apps/mobile/src/features/wardrobe/stores/clothingStore.ts
</read_first>
<action>
修复测试文件中的错误 import 路径。

**uiStore.test.ts：**
当前（错误）：`import { useUIStore } from "../uiStore"`
`stores/uiStore.ts` 已不存在（旧位置）。

修改为：
```typescript
import { useUIStore } from "../../shared/stores/uiStore";
```

同时更新测试中 `initialState` 的获取方式。如果测试文件中有 `const initialState = ...` 硬编码，需要确认与合并后的 UIState 一致。特别注意：
- `activeTab` 现在是 `ActiveTab` 类型而非 `string`
- `ModalType` 现在包含 "share" 和 "sizeGuide"
- 新增了 `reset()` 方法

**clothingStore.test.ts：**
当前（错误）：`import { useClothingStore, ClothingItem, ClothingFilter } from "../clothingStore"`
`stores/clothingStore.ts` 已不存在。

修改为：
```typescript
import { useClothingStore, ClothingItem, ClothingFilter } from "../../features/wardrobe/stores/clothingStore";
```

**其他测试文件：** 检查 `stores/__tests__/` 下所有测试文件的 import 路径，确保指向正确的 feature 目录位置。

**验证：** `cd apps/mobile && npx tsc --noEmit` 必须通过
</action>
<acceptance_criteria>
- `grep "from.*\.\./uiStore" apps/mobile/src/stores/__tests__/uiStore.test.ts` 返回空（旧路径已移除）
- `grep "from.*\.\./clothingStore" apps/mobile/src/stores/__tests__/clothingStore.test.ts` 返回空（旧路径已移除）
- `cd apps/mobile && npx tsc --noEmit` 通过
</acceptance_criteria>
</task>

<task id="5-08-07" type="verify">
<read_first>
- apps/mobile/src/stores/index.ts (主 barrel)
- apps/mobile/src/shared/stores/index.ts (shared barrel)
- apps/mobile/src/features/commerce/stores/index.ts (commerce barrel)
- apps/mobile/src/features/home/stores/index.ts (home barrel)
</read_first>
<action>
最终验证：确认所有 Store 只有一个权威来源，所有 barrel 文件只做 re-export。

1. **全局搜索重复定义：**
   ```bash
   grep -rn "export const use" apps/mobile/src/shared/stores/ apps/mobile/src/features/*/stores/ --include="*.ts" | grep -v "index.ts" | sort
   ```
   每个 store name 应该只出现一次。

2. **确认所有 barrel 文件无内联定义：**
   ```bash
   grep -l "create<" apps/mobile/src/shared/stores/index.ts apps/mobile/src/features/*/stores/index.ts
   ```
   应该返回空（无 barrel 文件包含 `create<` 调用）。

3. **确认主 barrel 的 re-export 路径正确：**
   检查 `stores/index.ts` 中每个 re-export 的目标文件是否存在。

4. **运行 TypeScript 编译：**
   ```bash
   cd apps/mobile && npx tsc --noEmit
   ```

5. **运行测试：**
   ```bash
   cd apps/mobile && npx jest --passWithNoTests
   ```
</action>
<acceptance_criteria>
- `grep -rn "export const useUIStore" apps/mobile/src/ --include="*.ts"` 只返回 1 个结果（shared/stores/uiStore.ts）
- `grep -rn "export const useAppStore" apps/mobile/src/ --include="*.ts"` 只返回 1 个结果（shared/stores/app.store.ts）
- `grep -c "create<" apps/mobile/src/shared/stores/index.ts` 返回 0
- `grep -c "create<" apps/mobile/src/features/commerce/stores/index.ts` 返回 0
- `grep -c "create<" apps/mobile/src/features/home/stores/index.ts` 返回 0
- `cd apps/mobile && npx tsc --noEmit` 通过
- `cd apps/mobile && npx jest --passWithNoTests` 通过
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `cd apps/mobile && npx tsc --noEmit` — 必须通过
2. `cd apps/mobile && npx jest --passWithNoTests` — 必须通过
3. 每个 Store name 全局搜索只出现一次定义：`grep -rn "export const use" apps/mobile/src/ --include="*.ts" | grep -v index.ts | grep -v __tests__ | sort`
4. 所有 barrel index.ts 不含 `create<` 调用
5. `stores/index.ts` 主 barrel 所有 re-export 路径可解析
</verification>

<success_criteria>
- useUIStore 只有 1 个定义（shared/stores/uiStore.ts）
- useAppStore 只有 1 个定义（shared/stores/app.store.ts）
- 所有 barrel 文件（shared/stores/index.ts, commerce/stores/index.ts, home/stores/index.ts）只包含 re-export
- cart.store.ts 的 ClothingItem import 指向正确路径
- 测试文件 import 路径全部正确
- TypeScript 编译通过
- 测试通过
</success_criteria>
