---
phase: 5
plan: 07
type: verification
wave: 4
depends_on: ["05-PLAN-05", "05-PLAN-06"]
files_modified:
  - apps/mobile/src/stores/index.ts (final cleanup)
  - apps/mobile/src/MIGRATION_MAP.md (remove after verification)
autonomous: true
requirements:
  - MOBL-01
  - MOBL-02
  - MOBL-03
  - MOBL-04
  - MOBL-05
  - MOBL-06
---

<objective>
Final verification and cleanup. Ensure the entire migration is complete, all UAT criteria pass, and the old directory structure is fully removed.
</objective>

<tasks>

<task id="5-07-01" type="verify">
<read_first>
- .planning/phases/05-mobile-reorg/05-RESEARCH.md (UAT criteria)
- .planning/phases/05-mobile-reorg/05-VALIDATION.md (validation strategy)
- apps/mobile/src/navigation/types.ts
</read_first>
<action>
Run comprehensive verification against all UAT criteria:

1. **All pages migrated to features/*/screens/ structure** (MOBL-01):
   - `find apps/mobile/src/features -name "*Screen.tsx" | wc -l` should be >= 50
   - `ls apps/mobile/src/screens/ 2>/dev/null` should be empty or not exist

2. **Store merge complete, no duplicate stores** (MOBL-02/03):
   - `test ! -f apps/mobile/src/stores/auth.store.ts` exits 0
   - `test ! -f apps/mobile/src/stores/user.store.ts` exits 0
   - `test ! -f apps/mobile/src/stores/quizStore.ts` exits 0
   - `test ! -f apps/mobile/src/stores/styleQuizStore.ts` exits 0
   - `grep -r "useUserStore" apps/mobile/src/ | grep -v node_modules` returns 0
   - `grep -r "useStyleQuizStore" apps/mobile/src/ | grep -v node_modules` returns 0

3. **Store migration to feature directories** (MOBL-04* — deviation, see PLAN-03):
   - `test -f apps/mobile/src/features/wardrobe/stores/clothingStore.ts` exits 0
   - `test -f apps/mobile/src/features/home/stores/homeStore.ts` exits 0
   - clothingStore and homeStore are in separate feature directories (not merged, per deviation)

3. **Navigation works (including deep links)** (MOBL-01):
   - All route names in types.ts are unchanged
   - All lazy imports in MainStackNavigator.tsx resolve to new paths
   - Deep link configuration unchanged
   - Route guard configuration unchanged

4. **@xuno/types referenced in mobile** (MOBL-06):
   - `grep -r "@xuno/types" apps/mobile/src/ | wc -l` >= 10
   - Backend integration status documented (exploratory, may be 0 references)

5. **Metro bundler starts** (MOBL-01):
   - `cd apps/mobile && npx react-native start --port 8081` starts without errors

6. **All core pages render** (MOBL-01):
   - Manual verification: navigate through all 5 tabs
   - Each tab's initial screen renders without crash

7. **TypeScript compilation**:
   - `cd apps/mobile && npx tsc --noEmit` exits 0
   - `cd apps/backend && npx tsc --noEmit` exits 0

8. **Tests pass**:
   - `cd apps/mobile && npx jest --passWithNoTests` exits 0
</action>
<acceptance_criteria>
- All 6 UAT criteria from the roadmap pass
- TypeScript compilation passes for both mobile and backend
- All tests pass
- Metro bundler starts without errors
- No stale file references remain
</acceptance_criteria>
</task>

<task id="5-07-02" type="cleanup">
<read_first>
- apps/mobile/src/ (verify old directories are empty)
- apps/mobile/src/MIGRATION_MAP.md
</read_first>
<action>
Final cleanup:

1. Remove empty old directories:
   - `src/screens/` (should be empty after all screens migrated)
   - `src/components/` (should be empty after all components migrated)
   - `src/stores/` (keep only `index.ts` barrel for backward compatibility)
   - `src/services/` (should be empty after all services migrated)
   - `src/hooks/` (should be empty after all hooks migrated)
   - `src/contexts/` (should be empty after all contexts migrated)
   - `src/types/` (should be empty after all types migrated)
   - `src/utils/` (should be empty after all utils migrated)
   - `src/theme/` (already removed by PLAN-02; verify it doesn't exist)

2. Remove `src/MIGRATION_MAP.md` (no longer needed)

3. Verify `src/stores/index.ts` barrel still works for backward compatibility

4. Verify no broken imports remain:
   - `grep -r "from.*['\"]\.\.\/screens\/" apps/mobile/src/ | grep -v node_modules` returns 0
   - `grep -r "from.*['\"]\.\.\/components\/" apps/mobile/src/ | grep -v node_modules | grep -v features | grep -v shared | grep -v design-system` returns 0
   - `grep -r "from.*['\"]\.\.\/stores\/[a-z]" apps/mobile/src/ | grep -v node_modules | grep -v "stores/index"` returns 0 (no direct store imports from old location)

5. Final TypeScript compilation check
6. Final test run
</action>
<acceptance_criteria>
- All old empty directories removed
- MIGRATION_MAP.md removed
- `cd apps/mobile && npx tsc --noEmit` exits 0
- `cd apps/mobile && npx jest --passWithNoTests` exits 0
- No stale import references
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Full TypeScript compilation: `cd apps/mobile && npx tsc --noEmit`
2. Full test suite: `cd apps/mobile && npx jest`
3. Metro bundler starts: `cd apps/mobile && npx react-native start --port 8081`
4. Backend compilation: `cd apps/backend && npx tsc --noEmit`
5. Package builds: `cd packages/types && pnpm build && cd ../shared && pnpm build`
</verification>

<success_criteria>
- All UAT criteria pass
- Old directories cleaned up
- No stale imports
- TypeScript compilation passes
- Tests pass
- Metro bundler starts
- @xuno/types actively used in both mobile and backend
</success_criteria>
