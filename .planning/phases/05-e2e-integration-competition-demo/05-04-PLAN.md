---
phase: 05
plan: 04
type: fix
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - apps/mobile/src/features/stylist/components/TryOnBottomSheet.tsx
  - apps/mobile/src/features/stylist/components/QuickReplyBar.tsx
  - apps/backend/prisma/schema.prisma
autonomous: true
requirements: D-12, D-13, D-14, D-15
---

# Plan 05-04: Zero Errors + Verification

<objective>
Systematically fix all TypeScript compilation errors. Execute Phase 4 human verification items. Fix TryOnBottomSheet save no-op and QuickReplyBar static buttons. Run Prisma schema push. Create E2E test checklist. Ensure zero compile errors across backend and mobile.
</objective>

<threat_model>
**Assets:** Database schema, user data integrity
**Threats:** Schema push with destructive changes could lose data; TS error suppression with @ts-expect-error could hide real bugs
**Mitigations:**

- Prisma db push reviewed before execution — `--accept-data-loss` only used if changes are additive
- No @ts-expect-error or any suppression allowed — all errors must be properly fixed
  **HIGH severity blockers:** Destructive schema migration without backup
  </threat_model>

<tasks>

<task id="05-04-01" type="fix">
<read_first>
- apps/mobile/src/features/stylist/components/TryOnBottomSheet.tsx
- apps/mobile/src/features/stylist/components/QuickReplyBar.tsx
- apps/mobile/src/services/api/tryon.api.ts
- apps/mobile/src/features/wardrobe/stores/wardrobeStore.ts
- apps/mobile/src/services/api/wardrobe.api.ts
</read_first>
<action>
1. In `TryOnBottomSheet.tsx`, fix the save no-op (Phase 4 Warning #1):
   - Read `wardrobeStore.ts` and `wardrobe.api.ts` to find the existing save method
   - Find the save button handler (currently just dismisses the bottom sheet)
   - Replace with actual save logic using the existing wardrobe API:
     - If `wardrobeStore` has an `addItem` or `saveOutfit` method: call it with the outfit data
     - If `wardrobeApi` has a `POST /wardrobe/items` or `POST /wardrobe/outfits` endpoint: call it
     - Show success toast/feedback after save using the existing toast/notification mechanism
     - Then dismiss the bottom sheet
   - Add loading state during save operation (isSaving boolean)
   - Add error handling: if save fails, show ErrorState with retry option inside the bottom sheet

2. In `QuickReplyBar.tsx`, fix static decoration buttons (Phase 4 Warning #2):
   - Read `QuickReplyBar.tsx` to find the hardcoded quick reply options
   - Replace with dynamic options from the dialog response:
     - The backend `processDialog` response includes `quickReplies` array
     - Use these server-provided options instead of static strings
   - If `quickReplies` is empty/undefined, fall back to default options: ["换一套", "不喜欢", "推荐其他风格"]
   - Each quick reply button press should send the text as a user message to the dialog API
     </action>
     <acceptance_criteria>

- `TryOnBottomSheet.tsx` save handler calls a wardrobe/outfit save API or store method (not just dismiss)
- `TryOnBottomSheet.tsx` shows loading state during save (isSaving boolean)
- `TryOnBottomSheet.tsx` shows success feedback after save
- `TryOnBottomSheet.tsx` shows error state with retry if save fails
- `QuickReplyBar.tsx` has fallback options when no server quickReplies available
- Quick reply button press sends the text as a user message
  </acceptance_criteria>
  </task>

<task id="05-04-02" type="fix">
<read_first>
- apps/mobile/tsconfig.json
- apps/backend/tsconfig.json
</read_first>
<action>
1. Run `cd apps/mobile && npx tsc --noEmit 2>&1 | head -200` to get current error list

2. Run `cd apps/backend && npx tsc --noEmit 2>&1 | head -200` to get current error list

3. For each TypeScript error in the mobile app:

   - Group errors by file
   - For each file, read the file and understand the error context
   - Fix the error properly (not with @ts-expect-error or any):
     - Missing imports: add the correct import
     - Type mismatches: fix the type annotation or the value
     - Missing properties: add the required property
     - Unused variables: remove them
     - Incorrect generic types: fix the type parameters
   - After fixing each file, run `npx tsc --noEmit` again to verify

4. For each TypeScript error in the backend:

   - Same approach as mobile — fix properly, no suppression

5. Common fix patterns:
   - `RecommendedItem` type mismatch: update to match the new `RecommendationOutput` structure from Plan 01
   - Missing `explanation` field on recommendation items: add it
   - `WeatherInfo` type not matching new API response: align types
   - Unused imports from deleted mock data: remove them
     </action>
     <acceptance_criteria>

- `cd apps/mobile && npx tsc --noEmit` exits with code 0 (zero errors)
- `cd apps/backend && npx tsc --noEmit` exits with code 0 (zero errors)
- No `@ts-expect-error` comments added during this task
- No `any` type assertions added to suppress errors
  </acceptance_criteria>
  </task>

<task id="05-04-03" type="feature">
<read_first>
- apps/backend/prisma/schema.prisma
- apps/backend/src/domains/platform/recommendations/dto/recommendation-output.dto.ts
</read_first>
<action>
1. Review `schema.prisma` for any changes needed by Phase 5:
   - Check if `RecommendationBreakdown` needs a Prisma model (likely NOT — it's a computed DTO, not persisted)
   - Check if `DemoProfile` needs a model (likely NOT — it's frontend-only via demoStore)
   - If no schema changes needed, skip to step 4

2. If schema changes ARE needed:

   - Add the new model/fields to `schema.prisma`
   - Run `cd apps/backend && npx prisma validate` to verify schema syntax
   - Run `cd apps/backend && npx prisma generate` to update Prisma client

3. **[BLOCKING]** Run Prisma schema push:

   - `cd apps/backend && npx prisma db push`
   - If prompted about data loss, review the changes carefully
   - If changes are additive only (new tables/columns, no drops), proceed
   - If changes are destructive, use `npx prisma db push --accept-data-loss` ONLY after confirming no production data is at risk
   - If push requires interactive prompts that cannot be suppressed, set `autonomous: false` for this step

4. Verify database is accessible:
   - `cd apps/backend && npx prisma db execute --stdin <<< "SELECT 1;"` or equivalent
     </action>
     <acceptance_criteria>

- `npx prisma validate` exits with code 0
- `npx prisma generate` exits with code 0
- `npx prisma db push` completes successfully (or confirmed no schema changes needed)
- Database is accessible after push
  </acceptance_criteria>
  </task>

<task id="05-04-04" type="test">
<read_first>
- apps/mobile/src/features/today/screens/TodayScreen.tsx
- apps/mobile/src/features/stylist/screens/AiStylistUnifiedScreen.tsx
- apps/mobile/src/features/onboarding/screens/OnboardingWizard.tsx
- .planning/phases/04-yiyi-agent-voice-onboarding-studio/04-VERIFICATION.md
</read_first>
<action>
1. Create E2E test checklist at `docs/e2e-test-checklist.md`:

```markdown
# E2E Test Checklist — 寻裳 XUNO

## Critical Path: New User Journey

- [ ] App launches without crash
- [ ] Registration screen loads
- [ ] User can register with email/password
- [ ] 4-step onboarding completes:
  - [ ] Step 1: Scene selection (1-3 cards selectable)
  - [ ] Step 2: Quick profile (age/height/weight/garmentPreference)
  - [ ] Step 3: Style expression (1 of 5 styles)
  - [ ] Step 4: "让伊伊搭第一套" (3 outfit options, select one)
- [ ] Today Tab shows Yiyi's proactive push
- [ ] Weather scene card displays real weather data
- [ ] Recommendation carousel shows outfits
- [ ] AI insight bubble shows explanation
- [ ] Quick reply buttons work (navigate to Stylist)
- [ ] Voice button triggers STT
- [ ] Chat with Yiyi in Stylist Tab
- [ ] Interview flow: company → position → budget → 3 outfits
- [ ] Try-on BottomSheet opens and shows result
- [ ] Save outfit to wardrobe works
- [ ] Calendar view shows saved outfits

## Phase 4 Human Verification Items

- [ ] Python dialog engine: 50 pytest tests pass
- [ ] Interview outfit flow end-to-end
- [ ] Voice button STT/TTS works on device
- [ ] Onboarding Step 4 "让伊伊搭第一套" works
- [ ] Edge-TTS audio playback works

## Visual Consistency

- [ ] No hardcoded colors visible (all use DesignTokens)
- [ ] ShimmerSkeleton uses warm camel brand colors
- [ ] Loading states on all screens
- [ ] Empty states on all screens
- [ ] Error states with retry on all screens

## Competition Demo

- [ ] Demo mode toggle works
- [ ] ProfileDebugPanel opens in **DEV** mode
- [ ] Profile switch triggers recommendation change
- [ ] RecommendationFunnel renders 6 layers
- [ ] Pre-cache populates for 3 profiles
- [ ] Demo script follows without issues

## Compile Check

- [ ] `cd apps/backend && npx tsc --noEmit` — zero errors
- [ ] `cd apps/mobile && npx tsc --noEmit` — zero errors
- [ ] `pnpm lint` — zero errors
```

2. Execute Phase 4 human verification items (D-13):
   - Run `cd ml && python -m pytest services/stylist/ -v` — verify 50 tests pass
   - Manually verify: interview outfit flow end-to-end
   - Manually verify: voice button STT/TTS
   - Manually verify: Onboarding Step 4
   - Manually verify: Edge-TTS audio playback
   - Document results in the checklist
     </action>
     <acceptance_criteria>

- `docs/e2e-test-checklist.md` exists with all critical path items
- Python pytest tests pass (50 tests in ml/services/stylist/)
- Phase 4 human verification items documented (pass/fail status)
- Checklist covers: new user journey, Phase 4 verification, visual consistency, competition demo, compile check
  </acceptance_criteria>
  </task>

<task id="05-04-05" type="verify">
<read_first>
- apps/mobile/tsconfig.json
- apps/backend/tsconfig.json
</read_first>
<action>
1. Run final compile check:
   - `cd apps/backend && npx tsc --noEmit` — MUST exit 0
   - `cd apps/mobile && npx tsc --noEmit` — MUST exit 0

2. Run lint check:

   - `pnpm lint` — MUST exit 0 (or only pre-existing warnings)

3. Run backend tests:

   - `cd apps/backend && pnpm test` — verify existing tests pass

4. Run Python tests:

   - `cd ml && python -m pytest services/stylist/ -v` — verify 50 tests pass

5. If any check fails:

   - Fix the specific error
   - Re-run the check
   - Do NOT proceed until all checks pass

6. Document final status:
   - Backend tsc: PASS/FAIL
   - Mobile tsc: PASS/FAIL
   - Lint: PASS/FAIL
   - Backend tests: PASS/FAIL
   - Python tests: PASS/FAIL
     </action>
     <acceptance_criteria>

- `cd apps/backend && npx tsc --noEmit` exits with code 0
- `cd apps/mobile && npx tsc --noEmit` exits with code 0
- `pnpm lint` exits with code 0
- Backend test suite passes
- Python test suite passes (50 tests)
  </acceptance_criteria>
  </task>

</tasks>

<verification>
1. `cd apps/backend && npx tsc --noEmit` — zero errors
2. `cd apps/mobile && npx tsc --noEmit` — zero errors
3. `pnpm lint` — zero errors
4. TryOnBottomSheet save actually persists outfit data
5. QuickReplyBar uses server-provided quick replies
6. Prisma schema push completed (or confirmed not needed)
7. E2E test checklist exists and is populated
8. Phase 4 human verification items documented
9. All test suites pass
</verification>

<success_criteria>

- Zero TypeScript compilation errors across entire monorepo
- Zero lint errors
- TryOnBottomSheet save works (not a no-op)
- QuickReplyBar uses dynamic server-provided options
- Prisma schema in sync with code
- E2E test checklist covers all critical paths
- Phase 4 verification items completed
- All test suites green
  </success_criteria>

<must_haves>

- tsc --noEmit zero errors on both backend and mobile (D-12, D-15)
- TryOnBottomSheet save is functional (Phase 4 Warning fix)
- QuickReplyBar uses server quickReplies (Phase 4 Warning fix)
- Prisma db push completed (D-12 schema sync)
- E2E test checklist created (D-14)
- Phase 4 human verification executed (D-13)
  </must_haves>
