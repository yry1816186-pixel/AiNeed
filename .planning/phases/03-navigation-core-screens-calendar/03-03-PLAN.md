---
phase: 03-navigation-core-screens-calendar
plan: 03
type: execute
wave: 3
depends_on: [01]
files_modified:
  - apps/mobile/src/design-system/theme/tokens/design-tokens.ts
  - apps/mobile/src/design-system/theme/tokens/colors.ts
  - apps/mobile/src/design-system/ui/YiyiAvatar.tsx
requirements: [VIS-01, VIS-02, VIS-03, VIS-04]
autonomous: true

must_haves:
  truths:
    - "DesignTokens.borderRadius.lg equals 12 (not 10)"
    - "No hardcoded hex colors remain in non-design-system component files"
    - "YiyiAvatar uses DesignTokens consistently and is referenced from Today/Stylist/Onboarding screens"
    - "WarmPrimaryColors (coral/mint/ocean) no longer imported anywhere outside the design-system theme layer"
  artifacts:
    - path: "apps/mobile/src/design-system/theme/tokens/design-tokens.ts"
      provides: "borderRadius.lg = 12"
      contains: "lg: 12"
  key_links:
    - from: "component files across src/features/"
      to: "DesignTokens.colors.*"
      via: "import { DesignTokens } from theme tokens"
      pattern: "DesignTokens\\.colors\\.(xuno|neutral|semantic|backgrounds|text|borders)"
---

<objective>
Unify design tokens: adjust borderRadius, replace hardcoded hex colors with DesignTokens references, clean up deprecated WarmPrimaryColors, and verify YiyiAvatar consistency.

Purpose: Per VIS-01/VIS-03/VIS-04, the app has 263+ hardcoded hex colors in 38 non-theme files. The borderRadius.lg token is 10 but should be 12 for buttons. WarmPrimaryColors (coral/mint/ocean) is deprecated but still referenced. This plan addresses all three token unification tasks.

Output: Unified design token usage across all component files. borderRadius.lg = 12. Zero hardcoded hex colors outside design-system/theme/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-navigation-core-screens-calendar/03-CONTEXT.md
@.planning/phases/03-navigation-core-screens-calendar/03-RESEARCH.md
</context>

<tasks>

<task id="T1" type="execute" title="Adjust borderRadius.lg + clean up WarmPrimaryColors + verify YiyiAvatar consistency">
<read_first>
- apps/mobile/src/design-system/theme/tokens/design-tokens.ts (borderRadius definition at line 194-204)
- apps/mobile/src/design-system/theme/tokens/colors.ts (WarmPrimaryColors definition)
- apps/mobile/src/design-system/ui/YiyiAvatar.tsx (YiyiAvatar component)
</read_first>

<action>
**Step 1: Change borderRadius.lg from 10 to 12 in design-tokens.ts**

In `apps/mobile/src/design-system/theme/tokens/design-tokens.ts` line 199:
Change `lg: 10,` to `lg: 12,`

Before making this change, grep for all usages of `borderRadius.lg` across the codebase:

```bash
grep -rn 'borderRadius\.lg\|borderRadius\["lg"\]' apps/mobile/src/ --include='*.ts' --include='*.tsx' | grep -v design-tokens.ts | grep -v darkTokens
```

If ALL usages are button-related, change the value. If any non-button components use `lg` and would be visually affected by 10->12, add a semantic alias `button: 12` to borderRadius instead and update button components to use `borderRadius.button`. Per Claude's Discretion in CONTEXT.md, this judgment call is acceptable.

**Step 2: Clean up WarmPrimaryColors usage**

1. Find all files importing WarmPrimaryColors:

```bash
grep -rn 'WarmPrimaryColors' apps/mobile/src/ --include='*.ts' --include='*.tsx'
```

2. For each file outside `design-system/theme/`, replace WarmPrimaryColors references with the equivalent DesignTokens.colors.xuno references:

   - `WarmPrimaryColors.coral` -> `DesignTokens.colors.xuno.warmOrange` (closest semantic match)
   - `WarmPrimaryColors.mint` -> `DesignTokens.colors.brand.sage` (closest semantic match)
   - `WarmPrimaryColors.ocean` -> `DesignTokens.colors.brand.slate` (closest semantic match)

3. After all external references are removed, verify WarmPrimaryColors is only referenced within `design-system/theme/tokens/colors.ts` (its definition). Consider adding `@deprecated` JSDoc if not already present.

**Step 3: Verify YiyiAvatar consistency**

1. Read `apps/mobile/src/design-system/ui/YiyiAvatar.tsx` -- confirm it:

   - Uses `DesignTokens.colors.xuno.warmCamel` for background
   - Uses SVG hanger icon
   - Accepts `size` prop

2. Search for all files that should be using YiyiAvatar:

```bash
grep -rn 'YiyiAvatar' apps/mobile/src/ --include='*.ts' --include='*.tsx'
```

3. For Today Screen, Stylist Screen, and Onboarding screens, verify they either:
   - Already import and use YiyiAvatar, OR
   - Have a placeholder/avatar area where YiyiAvatar should be added

This is a consistency audit per VIS-02. No new implementation needed -- just verify existing usage is correct and flag any screens missing the YiyiAvatar.
</action>

<acceptance_criteria>

- `grep 'lg:' apps/mobile/src/design-system/theme/tokens/design-tokens.ts | head -5` shows `lg: 12`
- `grep -rn 'WarmPrimaryColors' apps/mobile/src/ --include='*.ts' --include='*.tsx' | grep -v 'design-system/theme/' | grep -v '@deprecated'` returns ZERO matches
- `grep -rn 'YiyiAvatar' apps/mobile/src/ --include='*.ts' --include='*.tsx'` shows at least 1 usage (the component definition)
- `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` returns zero errors
  </acceptance_criteria>
  </task>

<task id="T2" type="execute" title="Replace hardcoded hex colors with DesignTokens references in component files">
<read_first>
- apps/mobile/src/design-system/theme/tokens/design-tokens.ts (full token map -- colors section)
- apps/mobile/src/shared/contexts/ThemeContext.tsx (23 hardcoded colors -- shared context)
</read_first>

<action>
This task replaces hardcoded hex colors in the ~38 non-theme component files. The design-system/theme/ files (221 occurrences in colors.ts, design-tokens.ts, season-colors.ts, index.ts) are DEFINITIONS and must be LEFT UNCHANGED.

**Step 1: Identify the target files (excluding design-system/theme/)**

```bash
grep -rln '#[0-9a-fA-F]\{6\}' apps/mobile/src/ --include='*.tsx' --include='*.ts' | grep -v 'design-system/theme/' | grep -v '__tests__' | grep -v 'node_modules'
```

**Step 2: Build the replacement mapping. For each hardcoded hex, map to the correct DesignTokens path:**

Core brand colors:

- `#C4956A` -> `DesignTokens.colors.xuno.warmCamel`
- `#2D3436` -> `DesignTokens.colors.xuno.charcoal`
- `#E17055` -> `DesignTokens.colors.xuno.warmOrange`
- `#FAFAF8` -> `DesignTokens.colors.neutral[50]`

Neutral palette:

- `#FFFFFF` -> `DesignTokens.colors.neutral.white`
- `#1A1A18` -> `DesignTokens.colors.neutral[900]`
- `#F5F5F3` -> `DesignTokens.colors.neutral[100]`
- `#EBEBE8` -> `DesignTokens.colors.neutral[200]`
- `#D4D4D0` -> `DesignTokens.colors.neutral[300]`
- `#8A8A85` -> `DesignTokens.colors.neutral[400]`
- `#73736D` -> `DesignTokens.colors.neutral[500]`
- `#52524D` -> `DesignTokens.colors.neutral[600]`
- `#3D3D39` -> `DesignTokens.colors.neutral[700]`
- `#282825` -> `DesignTokens.colors.neutral[800]`
- `#0D0D0C` -> `DesignTokens.colors.neutral.black`

Semantic colors:

- `#5B8A72` -> `DesignTokens.colors.semantic.success`
- `#D9A441` -> `DesignTokens.colors.semantic.warning`
- `#C44536` -> `DesignTokens.colors.semantic.error`
- `#7B8FA2` -> `DesignTokens.colors.semantic.info`

Backgrounds:

- `rgba(0, 0, 0, 0.4)` -> `DesignTokens.colors.backgrounds.overlay`

Brand palette:

- `#C67B5C` -> `DesignTokens.colors.brand.terracotta`
- `#8B9A7D` -> `DesignTokens.colors.brand.sage`
- `#B5A08C` -> `DesignTokens.colors.brand.camel`
- `#7B8FA2` -> `DesignTokens.colors.brand.slate`

Text:

- `rgba(0, 0, 0, 0.06)` -> `DesignTokens.colors.borders.light`
- `rgba(0, 0, 0, 0.1)` -> `DesignTokens.colors.borders.default`
- `rgba(0, 0, 0, 0.2)` -> `DesignTokens.colors.borders.strong`

**Step 3: For each target file, perform the replacements.** Process files in priority order:

1. `shared/contexts/ThemeContext.tsx` (23 colors) -- HIGH priority, shared context affects all screens
2. `features/today/components/WeatherSceneCard.tsx` (5 colors) -- HIGH, new Today screen
3. `features/stylist/screens/AiStylistUnifiedScreen.tsx` (8 colors) -- HIGH, main stylist screen
4. `features/home/screens/components/SceneCarousel.tsx` (9 colors) -- HIGH, scene carousel
5. `features/profile/screens/components/ColorSeasonCard.tsx` (17 colors) -- MEDIUM
6. All remaining files -- process each in turn

For each file:

1. Read the file
2. Replace each hardcoded hex with the corresponding DesignTokens reference
3. Ensure the file imports `DesignTokens` from the correct relative path (e.g., `import { DesignTokens } from "../../../design-system/theme/tokens/design-tokens"`)
4. If the file already uses `useTheme()` and accesses `colors` from the theme context, prefer `colors.xxx` over `DesignTokens.colors.xxx` (the theme context already wraps DesignTokens)
5. Write the updated file

**IMPORTANT: Files that use `useTheme()` hook should use the theme's `colors` object rather than directly importing DesignTokens. Only use `DesignTokens` directly in files that do NOT use the theme context.**

**Step 4: Verify no hardcoded hex colors remain in component files:**

```bash
grep -rn '#[0-9a-fA-F]\{6\}' apps/mobile/src/ --include='*.tsx' --include='*.ts' | grep -v 'design-system/' | grep -v '__tests__' | grep -v 'node_modules'
```

Remaining matches should only be in:

- Color season definitions (intentional seasonal palette colors)
- SVG path data (color values embedded in SVG markup)
- Any colors that genuinely have no DesignTokens equivalent (document these)
  </action>

<acceptance_criteria>

- `grep -rn '#C4956A\|#2D3436\|#E17055\|#FAFAF8' apps/mobile/src/ --include='*.tsx' --include='*.ts' | grep -v 'design-system/' | grep -v '__tests__'` returns ZERO matches in component files (or only in seasonal color definitions)
- `grep -rn '#FFFFFF\|#1A1A18' apps/mobile/src/ --include='*.tsx' --include='*.ts' | grep -v 'design-system/' | grep -v '__tests__' | wc -l` shows dramatic reduction from baseline
- `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` returns zero errors
- Metro bundler can resolve all DesignTokens imports
  </acceptance_criteria>
  </task>

</tasks>

<verification>
After ALL tasks complete:
1. `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` -- zero errors
2. `grep 'lg:' apps/mobile/src/design-system/theme/tokens/design-tokens.ts` -- shows `lg: 12`
3. Hardcoded hex count in non-theme files reduced by >80% from 263 baseline
4. No WarmPrimaryColors usage outside design-system/theme/
5. YiyiAvatar consistency verified across Today/Stylist/Onboarding
</verification>

<success_criteria>

1. borderRadius.lg changed from 10 to 12 in design-tokens.ts
2. WarmPrimaryColors no longer imported outside theme layer
3. Hardcoded hex colors in component files replaced with DesignTokens references (target: >80% reduction)
4. YiyiAvatar usage verified across all target screens
5. tsc --noEmit zero errors
   </success_criteria>

<output>
After completion, create `.planning/phases/03-navigation-core-screens-calendar/03-03-SUMMARY.md`
</output>
