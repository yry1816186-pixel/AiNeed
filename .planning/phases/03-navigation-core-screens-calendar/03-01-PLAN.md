---
phase: 03-navigation-core-screens-calendar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/shared/stores/clearAllStores.ts
  - apps/mobile/src/features/auth/stores/index.ts
  - apps/mobile/src/features/auth/stores/authStore.ts
  - apps/mobile/src/navigation/RootNavigator.tsx
  - apps/mobile/src/navigation/navigationService.ts
  - apps/mobile/src/navigation/RouteGuards/AuthGuard.tsx
  - apps/mobile/src/navigation/RouteGuards/ProfileGuard.tsx
  - apps/mobile/src/navigation/RouteGuards/VipGuard.tsx
  - apps/mobile/src/navigation/RouteGuards/useRouteGuard.tsx
  - apps/mobile/src/features/profile/screens/SettingsScreen.tsx
  - apps/mobile/src/features/profile/screens/ProfileScreen.tsx
  - apps/mobile/src/features/home/screens/RecommendationsScreen.tsx
  - apps/mobile/src/features/auth/screens/RegisterScreen.tsx
  - apps/mobile/src/features/auth/screens/PhoneLoginScreen.tsx
  - apps/mobile/src/features/auth/screens/LoginScreen.tsx
  - apps/mobile/src/features/style-quiz/screens/StyleQuizScreen.tsx
  - apps/mobile/src/features/wardrobe/screens/FavoritesScreen.tsx
  - apps/mobile/src/features/wardrobe/screens/ClothingDetailScreen.tsx
  - apps/mobile/src/features/profile/screens/SharePosterScreen.tsx
  - apps/mobile/src/features/commerce/screens/CheckoutScreen.tsx
  - apps/mobile/src/features/commerce/screens/CartScreen.tsx
  - apps/mobile/src/features/commerce/screens/OrdersScreen.tsx
  - apps/mobile/src/features/notifications/screens/NotificationsScreen.tsx
  - apps/mobile/src/features/notifications/screens/NotificationSettingsScreen.tsx
  - apps/mobile/src/features/community/screens/BloggerDashboardScreen.tsx
  - apps/mobile/src/features/consultant/screens/ChatScreen.tsx
  - apps/mobile/src/features/consultant/screens/BookingScreen.tsx
  - apps/mobile/src/features/consultant/screens/AdvisorProfileScreen.tsx
  - apps/mobile/src/features/consultant/screens/AdvisorListScreen.tsx
  - apps/mobile/src/features/customization/screens/CustomizationPreviewScreen.tsx
  - apps/mobile/src/features/customization/screens/CustomizationEditorScreen.tsx
  - apps/mobile/src/features/home/screens/HomeScreen.tsx
  - apps/mobile/src/features/home/screens/RecommendationFeedScreen.tsx
  - apps/mobile/src/features/home/components/heartrecommend/HeartRecommendScreen.tsx
  - apps/mobile/src/features/home/screens/components/WeatherGreeting.tsx
  - apps/mobile/src/features/stylist/screens/StylistScreen.tsx
  - apps/mobile/src/features/stylist/screens/AiStylistUnifiedScreen.tsx
  - apps/mobile/src/features/stylist/screens/ChatHistoryScreen.tsx
  - apps/mobile/src/features/stylist/screens/SessionCalendarScreen.tsx
  - apps/mobile/src/features/stylist/screens/OutfitPlanScreen.tsx
  - apps/mobile/src/features/stylist/components/AICompanionProvider.tsx
  - apps/mobile/src/features/stylist/components/ChatBubble.tsx
  - apps/mobile/src/features/stylist/components/ItemReplacementModal.tsx
  - apps/mobile/src/features/stylist/components/PresetQuestionsModal.tsx
  - apps/mobile/src/features/stylist/components/OutfitPlanView.tsx
  - apps/mobile/src/features/onboarding/screens/OnboardingWizard.tsx
  - apps/mobile/src/features/onboarding/screens/StyleStep.tsx
  - apps/mobile/src/features/onboarding/screens/PreferenceStep.tsx
  - apps/mobile/src/features/onboarding/screens/ResultStep.tsx
  - apps/mobile/src/features/onboarding/screens/SceneStep.tsx
  - apps/mobile/src/features/onboarding/screens/steps/BasicInfoStep.tsx
  - apps/mobile/src/features/onboarding/screens/steps/StyleTestStep.tsx
  - apps/mobile/src/features/profile/screens/ColorAnalysisScreen.tsx
  - apps/mobile/src/features/profile/screens/BodyAnalysisScreen.tsx
  - apps/mobile/src/features/profile/screens/ProfileEditScreen.tsx
  - apps/mobile/src/features/style-quiz/screens/QuizResultScreen.tsx
  - apps/mobile/src/features/style-quiz/screens/components/QuizImageCard.tsx
  - apps/mobile/src/features/tryon/screens/CameraScreen.tsx
  - apps/mobile/src/features/tryon/screens/components/PhotoQualityFeedback.tsx
  - apps/mobile/src/features/tryon/utils/image-quality.ts
  - apps/mobile/src/features/style-quiz/services/quizService.ts
  - apps/mobile/src/features/onboarding/services/onboardingService.ts
  - apps/mobile/src/hooks/useAuth.ts
  - apps/mobile/src/hooks/useSeasonAccent.ts
  - apps/mobile/src/shared/hooks/useSeasonAccent.ts
  - apps/mobile/src/shared/hooks/useAuth.ts
  - apps/mobile/src/services/deeplinkService.ts
  - apps/mobile/src/shared/services/deeplinkService.ts
requirements: [NAV-04]
autonomous: true

must_haves:
  truths:
    - "Zero files import from src/stores/index or src/stores/XXXStore"
    - "clearAllStores lives in src/shared/stores/clearAllStores.ts and is imported from there"
    - "tsc --noEmit returns zero errors after migration"
    - "All feature screens resolve store imports from feature-local paths"
  artifacts:
    - path: "apps/mobile/src/shared/stores/clearAllStores.ts"
      provides: "Centralized store cleanup function"
      contains: "export const clearAllStores"
    - path: "apps/mobile/src/stores/"
      provides: "Nothing -- entire directory deleted"
      contains: "SHOULD NOT EXIST"
  key_links:
    - from: "features/auth/stores/index.ts"
      to: "shared/stores/clearAllStores.ts"
      via: "dynamic import"
      pattern: "import.*clearAllStores.*from.*shared/stores"
---

<objective>
Delete the legacy src/stores/ directory (30 files) and migrate ALL imports to feature-local stores.

Purpose: src/stores/ contains 30 files that duplicate stores already present in src/features/\*/stores/. The 4 inline stores in index.ts (useAnalysisStore, useRecommendationStore, useCartStore, useHeartRecommendStore) have feature-local equivalents that are SUPERSETS (they include API fetch methods + error handling). The legacy barrel re-exports from feature stores, creating hidden coupling. Removing this eliminates a whole class of import confusion.

Output: Empty src/stores/ directory (deleted). All ~55+ files updated to import from feature-local paths. clearAllStores created at src/shared/stores/clearAllStores.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-core-screens-calendar/03-CONTEXT.md
@.planning/phases/03-navigation-core-screens-calendar/03-RESEARCH.md
</context>

<tasks>

<task id="T1" type="execute" title="Create clearAllStores + update auth store dynamic imports">
<read_first>
- apps/mobile/src/stores/index.ts (current clearAllStores implementation with inline store references)
- apps/mobile/src/features/auth/stores/index.ts (has dynamic import of clearAllStores from stores/index)
- apps/mobile/src/features/auth/stores/authStore.ts (has dynamic import of clearAllStores from stores/index)
- apps/mobile/src/shared/stores/index.ts (existing shared stores barrel)
</read_first>

<action>
1. Create `apps/mobile/src/shared/stores/clearAllStores.ts` with the clearAllStores function. This function must:
   - Import `useAuthStore` from `../../features/auth/stores`
   - Import `useAnalysisStore` from `../../features/profile/stores/analysis.store`
   - Import `useRecommendationStore` from `../../features/home/stores/recommendation.store`
   - Import `useCartStore` from `../../features/commerce/stores/cart.store`
   - Import `useHeartRecommendStore` from `../../features/home/stores/heart-recommend.store`
   - Export `clearAllStores` as async function wrapping each store reset in try/catch
   - Match the exact same function body as src/stores/index.ts lines 343-359

2. Update `apps/mobile/src/shared/stores/index.ts` barrel to re-export from `./clearAllStores`:

   ```
   export { clearAllStores } from './clearAllStores';
   ```

3. Update `apps/mobile/src/features/auth/stores/index.ts` line 270: change dynamic import from `"../../../stores/index"` to `"../../../shared/stores/clearAllStores"`

4. Update `apps/mobile/src/features/auth/stores/authStore.ts` line 304: change dynamic import from `"../../../stores/index"` to `"../../../shared/stores/clearAllStores"`
   </action>

<acceptance_criteria>

- `grep -rn 'clearAllStores' apps/mobile/src/shared/stores/clearAllStores.ts` returns the function definition
- `grep -rn 'stores/index.*clearAllStores\|clearAllStores.*stores/index' apps/mobile/src/features/auth/stores/` returns ZERO matches
- `grep -rn 'clearAllStores.*shared/stores' apps/mobile/src/features/auth/stores/` returns 2 matches (index.ts + authStore.ts)
- `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` returns zero errors
  </acceptance_criteria>
  </task>

<task id="T2" type="execute" title="Migrate all stores/index imports to feature-local paths">
<read_first>
- apps/mobile/src/navigation/RootNavigator.tsx (imports useAuthStore + useCartStore from stores/index)
- apps/mobile/src/navigation/navigationService.ts (imports useAuthStore from stores/index)
- apps/mobile/src/navigation/RouteGuards/AuthGuard.tsx (imports useAuthStore from stores/index)
- apps/mobile/src/navigation/RouteGuards/ProfileGuard.tsx (imports useAuthStore from stores/index)
- apps/mobile/src/navigation/RouteGuards/VipGuard.tsx (imports useAuthStore from stores/index)
- apps/mobile/src/navigation/RouteGuards/useRouteGuard.tsx (imports useAuthStore from stores/index)
- apps/mobile/src/features/profile/screens/SettingsScreen.tsx (imports useAuthStore from ../stores/index)
- apps/mobile/src/features/profile/screens/ProfileScreen.tsx (imports useAuthStore from ../stores/index)
- apps/mobile/src/features/home/screens/RecommendationsScreen.tsx (imports useAuthStore from ../stores/index)
- apps/mobile/src/features/style-quiz/screens/StyleQuizScreen.tsx (imports multiple stores from ../stores/index)
- apps/mobile/src/features/wardrobe/screens/FavoritesScreen.tsx (imports useAuthStore from ../../../stores)
- apps/mobile/src/features/auth/screens/RegisterScreen.tsx (imports useAuthStore from ../stores/index -- resolves to features/auth/stores, ALREADY OK)
- apps/mobile/src/features/auth/screens/PhoneLoginScreen.tsx (imports useAuthStore from ../stores/index -- resolves to features/auth/stores, ALREADY OK)
- apps/mobile/src/features/auth/screens/LoginScreen.tsx (imports useAuthStore from ../stores/index -- resolves to features/auth/stores, ALREADY OK)
</read_first>

<action>
Update each file's store import to use the correct feature-local path. The exact changes:

**Navigation layer (from src/ root context):**

- `RootNavigator.tsx` line 16: Replace `import { useAuthStore, useCartStore } from "../stores/index"` with TWO imports:
  - `import { useAuthStore } from "../features/auth/stores";`
  - `import { useCartStore } from "../features/commerce/stores/cart.store";`
- `navigationService.ts` line 14: Replace `"../stores/index"` with `"../features/auth/stores"`
- `RouteGuards/AuthGuard.tsx` line 2: Replace `"../../stores/index"` with `"../../features/auth/stores"`
- `RouteGuards/ProfileGuard.tsx` line 2: Replace `"../../stores/index"` with `"../../features/auth/stores"`
- `RouteGuards/VipGuard.tsx` line 3: Replace `"../../stores/index"` with `"../../features/auth/stores"`
- `RouteGuards/useRouteGuard.tsx` line 4: Replace `"../../stores/index"` with `"../../features/auth/stores"`

**Feature screens (from features/ context, importing from stores/index):**

- `profile/screens/SettingsScreen.tsx` line 22: Replace `"../stores/index"` with `"../../auth/stores"`
- `profile/screens/ProfileScreen.tsx` line 22: Replace `"../stores/index"` with `"../../auth/stores"`
- `home/screens/RecommendationsScreen.tsx` line 18: Replace `"../stores/index"` with `"../../auth/stores"`
- `style-quiz/screens/StyleQuizScreen.tsx` line 24: Read the full import to see which stores are imported. Update each import to its feature-local path. If it imports `useAuthStore` + quiz stores, split into:
  - `import { useAuthStore } from "../../auth/stores";`
  - `import { useQuizStore } from "../stores/quizStore";` (this resolves to features/style-quiz/stores, already feature-local)
- `wardrobe/screens/FavoritesScreen.tsx` line 21: Replace `"../../../stores"` with `"../../auth/stores"`

**Auth screens -- these resolve to features/auth/stores via ../stores/index which is features/auth/stores/index.ts. They are ALREADY feature-local. Verify they still work after src/stores/ deletion -- these should NOT be changed since `../stores/index` from `features/auth/screens/` resolves to `features/auth/stores/index.ts`, NOT `src/stores/index.ts`. Leave these files UNCHANGED.**

**Hooks layer:**

- `hooks/useAuth.ts` line 2: Replace `"../stores/auth.store"` with `"../features/auth/stores/auth.store"`
- `hooks/useSeasonAccent.ts` line 13: Replace `"../stores/profileStore"` with `"../features/profile/stores/profileStore"`
- `shared/hooks/useSeasonAccent.ts` line 13: Replace `"../../stores/profileStore"` with `"../../features/profile/stores/profileStore"`
- `shared/hooks/useAuth.ts` line 2: Already imports from `"../../features/auth/stores/auth.store"` -- no change needed

**Services layer:**

- `services/deeplinkService.ts` line 2: Replace `"../stores"` with `"../features/auth/stores"` (if this file exists at src/services/)
- `shared/services/deeplinkService.ts`: Already imports from `"../../features/auth/stores/authStore"` -- no change needed
  </action>

<acceptance_criteria>

- `grep -rn 'from.*stores/index' apps/mobile/src/ --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v '__tests__'` returns ZERO matches (except features/auth/screens which resolve locally)
- `grep -rn 'from.*\.\.\/\.\.\/\.\.\/stores[^/]' apps/mobile/src/ --include='*.ts' --include='*.tsx'` returns ZERO matches
- `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` returns zero errors
  </acceptance_criteria>
  </task>

<task id="T3" type="execute" title="Migrate ../stores/XXX imports + update feature-local stores barrel exports + delete src/stores/">
<read_first>
- apps/mobile/src/features/commerce/stores/index.ts (barrel exports for commerce)
- apps/mobile/src/features/notifications/stores/index.ts (barrel exports)
- apps/mobile/src/features/community/stores/index.ts (barrel exports)
- apps/mobile/src/features/consultant/stores/index.ts (barrel exports)
- apps/mobile/src/features/customization/stores/index.ts (barrel exports)
- apps/mobile/src/features/onboarding/stores/index.ts (barrel exports)
- apps/mobile/src/features/style-quiz/stores/index.ts (barrel exports)
- apps/mobile/src/features/tryon/stores/index.ts (barrel exports)
- apps/mobile/src/features/stylist/stores/index.ts (barrel exports)
- apps/mobile/src/features/profile/stores/index.ts (barrel exports)
</read_first>

<action>
**Step 1: Verify each feature stores barrel exports the correct stores.** For each feature directory, confirm that `index.ts` re-exports the stores that consumers need. If any barrel is missing an export that consumers depend on, add it.

The critical pattern: feature screens use `import { useXXXStore } from "../stores/XXXStore"`. From `features/stylist/screens/`, `../stores/` resolves to `features/stylist/stores/`. These are ALREADY feature-local and correct. They must NOT be changed.

However, some files outside features/ use `../stores/XXX` paths that resolve to `src/stores/`:

- `services/quizService.ts`: `import type { QuizQuestion, QuizAnswer, QuizResult } from "../stores/quizStore"` -- resolves to `src/stores/quizStore`. Change to `"../features/style-quiz/stores/quizStore"`
- `services/onboardingService.ts`: `import type { OnboardingFormData } from "../stores/onboardingStore"` -- resolves to `src/stores/onboardingStore`. Change to `"../features/onboarding/stores/onboardingStore"`
- `features/style-quiz/screens/components/QuizImageCard.tsx`: `import type { QuizImage } from "../../../../stores/quizStore"` -- resolves to `src/stores/quizStore`. Change to `"../../../stores/quizStore"` (feature-local)
- `features/onboarding/screens/steps/BasicInfoStep.tsx`: `import type { OnboardingFormData } from "../../../../stores/onboardingStore"` -- resolves to `src/stores/onboardingStore`. Change to `"../../stores/onboardingStore"` (feature-local)
- `features/wardrobe/screens/ClothingDetailScreen.tsx`: `import { useSizeRecommendationStore } from "../../../stores/sizeRecommendationStore"` -- resolves to `src/stores/sizeRecommendationStore`. Change to `"../../commerce/stores/sizeRecommendationStore"`

**Step 2: Delete the entire src/stores/ directory.**

```bash
rm -rf apps/mobile/src/stores/
```

**Step 3: Run tsc --noEmit to verify zero errors. If errors appear, each error will indicate a file still referencing the deleted stores/ directory -- fix each one by pointing to the correct feature-local path.**

Common patterns for fixing remaining errors:

- If a file at `features/X/screens/Y.tsx` shows `Cannot find module '../stores/ZZZ'`, and the file `features/X/stores/ZZZ` exists, the import is already correct and was resolving to `features/X/stores/` not `src/stores/`. No change needed.
- If the error says `Cannot find module '../../stores/XXX'` from a `features/X/screens/` file, check if this resolves to `src/stores/XXX`. If so, change to point to the correct feature directory.
  </action>

<acceptance_criteria>

- `ls apps/mobile/src/stores/ 2>/dev/null` returns error (directory does not exist)
- `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` returns zero errors
- `grep -rn 'from.*\.\./stores/' apps/mobile/src/ --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v features/` returns ZERO matches for src-level stores references
- `grep -rn 'from.*\.\./\.\./\.\./\.\./stores/' apps/mobile/src/ --include='*.ts' --include='*.tsx' | grep -v node_modules` returns ZERO matches pointing to src/stores/
  </acceptance_criteria>
  </task>

</tasks>

<verification>
After ALL tasks complete:
1. `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` -- must return zero errors
2. `ls apps/mobile/src/stores/ 2>&1` -- must return "No such file or directory"
3. `grep -rn "from.*stores/index" apps/mobile/src/ --include='*.ts' --include='*.tsx' | grep -v node_modules | grep -v features/auth/screens` -- must return zero matches
4. `cat apps/mobile/src/shared/stores/clearAllStores.ts` -- must contain "export const clearAllStores"
5. Verify Metro bundler can start: `cd C:/AiNeed/apps/mobile && npx react-native start --port 8081 --verify`
</verification>

<success_criteria>

1. src/stores/ directory completely deleted
2. All store imports resolve to feature-local paths
3. clearAllStores created at shared/stores/clearAllStores.ts
4. tsc --noEmit returns zero errors
5. No circular import warnings from Metro
   </success_criteria>

<output>
After completion, create `.planning/phases/03-navigation-core-screens-calendar/03-01-SUMMARY.md`
</output>
