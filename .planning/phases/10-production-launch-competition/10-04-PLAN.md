---
phase: 10-production-launch-competition
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
files_modified:
  - tests/load/basic.js
  - tests/load/ai-conversation.js
  - scripts/security-audit.sh
  - tests/load/package.json
autonomous: true
requirements:
  - PRD-04

must_haves:
  truths:
    - "50 并发用户下 API P95 延迟 < 2s"
    - "100 并发用户下 API 无 5xx 错误"
    - "AI 对话端点 P95 < 5s"
    - "npm audit 无 CRITICAL 级别漏洞"
    - "pip audit 无 CRITICAL 级别漏洞"
  artifacts:
    - path: "tests/load/basic.js"
      provides: "k6 基础负载测试脚本"
      contains: "http_req_duration"
    - path: "tests/load/ai-conversation.js"
      provides: "AI 对话端点压测脚本"
      contains: "dialog"
    - path: "scripts/security-audit.sh"
      provides: "安全审计一键脚本"
      min_lines: 20
  key_links:
    - from: "tests/load/basic.js"
      to: "backend:3001"
      via: "HTTP requests"
      pattern: "BASE_URL.*api"
    - from: "scripts/security-audit.sh"
      to: "npm audit + pip audit"
      via: "CLI commands"
      pattern: "npm audit.*critical"
---

<objective>
负载测试脚本 + 安全审计脚本

Purpose: 创建 k6 负载测试脚本验证生产环境性能 (P95 < 2s @50 users, AI < 5s)，运行 npm audit + pip audit 确保无 CRITICAL 漏洞 (per D-17, D-18)

Output: k6 负载测试脚本 + AI 对话压测脚本 + 安全审计一键脚本
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-production-launch-competition/10-CONTEXT.md
@.planning/phases/10-production-launch-competition/10-RESEARCH.md

<interfaces>
<!-- k6 API surface (v0.57.0) -->
- import http from 'k6/http' -- HTTP client
- import { check, sleep } from 'k6' -- assertions + pacing
- import { Rate, Trend } from 'k6/metrics' -- custom metrics
- export const options = { stages, thresholds } -- test config
- __ENV.API_URL -- environment variable injection
- http.get(url, { headers }) / http.post(url, body, params)

<!-- Backend API endpoints to test -->

- GET /api/v1/health -- health check (no auth)
- GET /api/v1/recommendations -- recommendations (auth required, JWT Bearer)
- POST /api/v1/dialog/process -- AI dialog (auth required)
- POST /api/v1/auth/login -- login endpoint (for obtaining test tokens)
- GET /api/v1/wardrobe -- wardrobe items (auth required)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: k6 负载测试脚本 + 安全审计脚本</name>
  <files>tests/load/basic.js, tests/load/ai-conversation.js, scripts/security-audit.sh, tests/load/package.json</files>
  <read_first>
    - .planning/phases/10-production-launch-competition/10-CONTEXT.md (D-17 压测标准, D-18 安全审计)
    - .planning/phases/10-production-launch-competition/10-RESEARCH.md (Pattern 4 k6 脚本结构)
    - .planning/phases/10-production-launch-competition/10-VALIDATION.md (验证策略)
  </read_first>
  <action>
1. 创建 tests/load/ 目录 (如不存在)

2. 创建 tests/load/basic.js -- 基础负载测试:

   ```javascript
   import http from "k6/http";
   import { check, sleep } from "k6";
   import { Rate, Trend } from "k6/metrics";

   const errorRate = new Rate("errors");
   const apiLatency = new Trend("api_latency");

   export const options = {
     stages: [
       { duration: "30s", target: 20 }, // ramp up to 20
       { duration: "1m", target: 50 }, // sustain 50 (D-17: P95 < 2s)
       { duration: "30s", target: 100 }, // spike to 100 (D-17: no 5xx)
       { duration: "30s", target: 0 }, // ramp down
     ],
     thresholds: {
       http_req_duration: ["p(95)<2000"], // P95 < 2s
       errors: ["rate<0.05"], // < 5% error rate
     },
   };

   const BASE_URL = __ENV.API_URL || "http://localhost:3001/api/v1";

   export default function () {
     // Test 1: Health check (no auth)
     const healthRes = http.get(`${BASE_URL}/health`);
     check(healthRes, {
       "health status 200": (r) => r.status === 200,
     });

     // Test 2: Login (obtain token)
     const loginRes = http.post(
       `${BASE_URL}/auth/login`,
       JSON.stringify({
         email: `test_user_${__VU}@xuno.test`,
         password: "TestPassword123!",
       }),
       { headers: { "Content-Type": "application/json" } }
     );

     if (loginRes.status === 200 || loginRes.status === 201) {
       const token = loginRes.json("access_token") || loginRes.json("data.access_token");

       // Test 3: Recommendations (auth required)
       const recRes = http.get(`${BASE_URL}/recommendations`, {
         headers: { Authorization: `Bearer ${token}` },
       });
       check(recRes, {
         "recommendations status 200": (r) => r.status === 200 || r.status === 401,
       });
       apiLatency.add(recRes.timings.duration);

       // Test 4: Wardrobe (auth required)
       const wardrobeRes = http.get(`${BASE_URL}/wardrobe`, {
         headers: { Authorization: `Bearer ${token}` },
       });
       check(wardrobeRes, {
         "wardrobe status 200 or 401": (r) => r.status === 200 || r.status === 401,
       });
       apiLatency.add(wardrobeRes.timings.duration);
     }

     errorRate.add(loginRes.status >= 500);
     sleep(1);
   }
   ```

3. 创建 tests/load/ai-conversation.js -- AI 对话端点压测:

   ```javascript
   import http from "k6/http";
   import { check, sleep } from "k6";
   import { Trend } from "k6/metrics";

   const aiLatency = new Trend("ai_latency");

   export const options = {
     stages: [
       { duration: "30s", target: 5 }, // gentle ramp (AI is expensive)
       { duration: "1m", target: 10 }, // sustain 10 concurrent
       { duration: "30s", target: 20 }, // spike
       { duration: "30s", target: 0 },
     ],
     thresholds: {
       http_req_duration: ["p(95)<5000"], // D-17: AI P95 < 5s
     },
   };

   const BASE_URL = __ENV.API_URL || "http://localhost:3001/api/v1";

   export default function () {
     const token = __ENV.TEST_TOKEN || "";
     const res = http.post(
       `${BASE_URL}/dialog/process`,
       JSON.stringify({
         message: "帮我搭一套面试穿搭",
         dialogState: "GREET",
       }),
       {
         headers: {
           "Content-Type": "application/json",
           Authorization: `Bearer ${token}`,
         },
         timeout: "30s",
       }
     );
     check(res, {
       "dialog status 200 or 401": (r) => r.status === 200 || r.status === 401,
       "dialog response < 5s": (r) => r.timings.duration < 5000 || r.status === 401,
     });
     aiLatency.add(res.timings.duration);
     sleep(2);
   }
   ```

4. 创建 tests/load/package.json (标记测试目录):

   ```json
   {
     "name": "xuno-load-tests",
     "version": "1.0.0",
     "private": true,
     "description": "Load tests for XUNO API"
   }
   ```

5. 创建 scripts/security-audit.sh:

   ```bash
   #!/bin/bash
   # 寻裳 XUNO 安全审计脚本
   # OWASP Top 10 快速扫描 + 依赖漏洞检查 (D-18)
   set -euo pipefail

   echo "========================================"
   echo "  寻裳 XUNO 安全审计"
   echo "========================================"
   echo ""

   EXIT_CODE=0

   # 1. Node.js 依赖审计
   echo "--- Node.js 依赖审计 (npm audit) ---"
   cd "$(dirname "$0")/.."
   if pnpm audit --audit-level=critical 2>&1; then
       echo "[PASS] 无 CRITICAL 级别 Node.js 漏洞"
   else
       echo "[FAIL] 发现 CRITICAL 级别 Node.js 漏洞"
       EXIT_CODE=1
   fi
   echo ""

   # 2. Python 依赖审计
   echo "--- Python 依赖审计 (pip audit) ---"
   if command -v pip-audit &>/dev/null; then
       cd ml
       if pip-audit --strict 2>&1; then
           echo "[PASS] 无 CRITICAL 级别 Python 漏洞"
       else
           echo "[FAIL] 发现 CRITICAL 级别 Python 漏洞"
           EXIT_CODE=1
       fi
       cd ..
   else
       echo "[WARN] pip-audit 未安装，跳过 Python 审计"
       echo "  安装: pip install pip-audit"
   fi
   echo ""

   # 3. OWASP Top 10 快速检查
   echo "--- OWASP Top 10 快速扫描 ---"

   # A01: Broken Access Control
   echo "[CHECK] A01 - 检查是否有公开的 .env 文件..."
   if git ls-files | grep -q "\.env$"; then
       echo "[FAIL] .env 文件被 git tracked"
       EXIT_CODE=1
   else
       echo "[PASS] .env 文件未被 git tracked"
   fi

   # A02: Cryptographic Failures
   echo "[CHECK] A02 - 检查 TLS 配置..."
   if grep -q "ssl_protocols TLSv1.2 TLSv1.3" infrastructure/nginx/nginx.conf; then
       echo "[PASS] Nginx TLS 配置正确 (TLS 1.2+)"
   else
       echo "[FAIL] Nginx TLS 配置缺失或不正确"
       EXIT_CODE=1
   fi

   # A03: Injection
   echo "[CHECK] A03 - 检查是否使用 ORM (防 SQL 注入)..."
   if grep -q "prisma" apps/backend/package.json; then
       echo "[PASS] 使用 Prisma ORM (参数化查询)"
   else
       echo "[WARN] 未检测到 Prisma ORM"
   fi

   # A04: Insecure Design
   echo "[CHECK] A04 - 检查 rate limiting..."
   if grep -q "limit_req" infrastructure/nginx/nginx.conf; then
       echo "[PASS] Nginx rate limiting 已配置"
   else
       echo "[FAIL] Nginx rate limiting 未配置"
       EXIT_CODE=1
   fi

   # A05: Security Misconfiguration
   echo "[CHECK] A05 - 检查 security headers..."
   if grep -q "X-Frame-Options DENY" infrastructure/nginx/nginx.conf; then
       echo "[PASS] Security headers 已配置"
   else
       echo "[FAIL] Security headers 缺失"
       EXIT_CODE=1
   fi

   # A07: Identification and Authentication Failures
   echo "[CHECK] A07 - 检查 JWT secret 不在代码中..."
   if grep -rq "JWT_SECRET.*=.*['\"]" apps/backend/src/ 2>/dev/null | grep -v "node_modules" | grep -v ".d.ts"; then
       echo "[FAIL] JWT_SECRET 可能硬编码在源码中"
       EXIT_CODE=1
   else
       echo "[PASS] JWT_SECRET 未在源码中硬编码"
   fi

   # A09: Security Logging and Monitoring Failures
   echo "[CHECK] A09 - 检查监控配置..."
   if test -f monitoring/prometheus/prometheus.yml && test -f monitoring/alerts/alert.rules.yml; then
       echo "[PASS] Prometheus + alert rules 已配置"
   else
       echo "[FAIL] 监控配置缺失"
       EXIT_CODE=1
   fi

   echo ""
   echo "========================================"
   if [ $EXIT_CODE -eq 0 ]; then
       echo "  安全审计通过: 无 CRITICAL 发现"
   else
       echo "  安全审计失败: 发现 CRITICAL 问题"
   fi
   echo "========================================"
   exit $EXIT_CODE
   ```

6. 确保脚本可执行: chmod +x scripts/security-audit.sh

注意: k6 需要单独安装。如果不便安装 k6，所有脚本保持与 Artillery 兼容的替代描述。
实际的 k6 执行需要在部署环境或有 API 访问的环境中运行。
</action>
<verify>
<automated>cd /c/AiNeed && ls tests/load/basic.js tests/load/ai-conversation.js scripts/security-audit.sh 2>&1 && grep "p(95)<2000" tests/load/basic.js && grep "p(95)<5000" tests/load/ai-conversation.js && grep "npm audit" scripts/security-audit.sh</automated>
</verify>
<acceptance_criteria> - tests/load/basic.js exists with stages: 20->50->100->0 users - tests/load/basic.js has threshold p(95)<2000 - tests/load/ai-conversation.js exists with stages for AI endpoint - tests/load/ai-conversation.js has threshold p(95)<5000 - scripts/security-audit.sh exists and is executable - security-audit.sh runs npm audit with --audit-level=critical - security-audit.sh checks OWASP A01-A09 categories - security-audit.sh checks TLS config in nginx.conf - security-audit.sh checks rate limiting in nginx.conf
</acceptance_criteria>
<done>k6 负载测试脚本 (基础 API 50 users P95<2s + AI 对话 20 users P95<5s) + OWASP Top 10 安全审计脚本创建完成</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                   |
| ------------------------------ | ------------------------------------------------------------- |
| Load test client -> API        | Simulated external traffic; tests rate limiting effectiveness |
| Security audit -> dependencies | Scanning for known CVEs in npm/pip packages                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                              |
| --------- | ---------------------- | -------------------------- | ----------- | ------------------------------------------------------------ |
| T-10-03   | Tampering              | Dependency vulnerabilities | mitigate    | npm audit + pip audit scanning; block on CRITICAL findings   |
| T-10-12   | Information Disclosure | OWASP A02 (crypto)         | mitigate    | TLS 1.2+ verified; security headers verified by audit script |
| T-10-13   | Denial of Service      | API under load             | mitigate    | k6 load test validates rate limiting and capacity            |

</threat_model>

<verification>
1. `ls tests/load/basic.js tests/load/ai-conversation.js scripts/security-audit.sh` all exist
2. `grep "p(95)<2000" tests/load/basic.js` confirms threshold
3. `grep "p(95)<5000" tests/load/ai-conversation.js` confirms threshold
4. `bash scripts/security-audit.sh` runs and produces report (may have WARN for pip-audit)
</verification>

<success_criteria>

- k6 load test scripts with correct thresholds (API P95<2s, AI P95<5s)
- Security audit script covers OWASP Top 10 categories
- npm audit execution included with --audit-level=critical
- OWASP checks reference actual config files (nginx.conf, prometheus.yml)
  </success_criteria>

<output>
After completion, create `.planning/phases/10-production-launch-competition/10-04-SUMMARY.md`
</output>
