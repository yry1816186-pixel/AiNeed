---
wave: 2
depends_on: ["05-01", "05-02", "05-03", "05-04"]
autonomous: true
files_modified:
  - scripts/demo-e2e-run.sh
  - docs/DEMO-SCRIPT-TEST-PLAN.md
requirements: [DEMO-01, DEMO-02, DEMO-13]
---

# Plan 05-05: E2E Automation + 3-Run Rehearsal

**Goal:** Automate core E2E checks and execute 3 consecutive zero-crash demo runs following the competition script.

## Tasks

### Task 1: Create Automated E2E Smoke Test Runner

<action>
Create `scripts/demo-e2e-run.sh`:

1. Pre-run gate (mandatory):

   - Run `bash scripts/demo-preflight.sh` — if FAIL, exit with code 2.
   - Run `bash scripts/demo-warmup.sh` — if FAIL, exit with code 3.
   - Verify `curl http://localhost:3001/api/v1/demo/pre-cache/status | jq '.allRequiredPresent'` returns `true`.

2. Automated checks (adopt existing `check()` / `record_result()` pattern):

   **Navigation smoke (4 checks):**

   - Backend health: `curl -sf http://localhost:3001/api/health` returns 200
   - AI service health: `curl -sf http://localhost:8002/health` returns 200
   - All 15 Docker services healthy: `docker compose -f docker-compose.production.yml ps | grep -c healthy` == 15
   - Demo mode enabled: `curl -s http://localhost:3001/api/v1/demo/pre-cache/status | jq '.allRequiredPresent'` == true

   **AI pipeline smoke (5 checks):**

   - Recommendation endpoint: `curl -s -X POST http://localhost:3001/api/v1/stylist/recommend -H 'Content-Type: application/json' -d '{"scene":"interview","profileId":"demo-001"}' | jq '.outfits | length'` returns >= 1
   - Try-on endpoint (async): `curl -s -X POST http://localhost:3001/api/v1/tryon -H 'Content-Type: application/json' -d '{"outfitId":"test","profileId":"demo-001"}' | jq '.jobId'` returns a string
   - TTS health: `curl -sf http://localhost:3001/api/v1/tts/precache/status` returns 200
   - Voice STT mock response time: measure with `curl -w '%{time_total}'` < 3s
   - RecommendationFunnel endpoint: `curl -s http://localhost:3001/api/v1/stylist/funnel/demo-001 | jq '.layers | length'` >= 1

   **Data integrity (3 checks):**

   - 10 seed profiles present: `ls docs/PRESENTATION/seed-user-data-v2.json` — parse and count profiles == 10
   - TTS cache files exist: iterate `PRECACHE_PHRASES` list from tts.service.ts, check each file on disk
   - Demo recording exists: `test -f docs/PRESENTATION/demo-recording.mp4` and `test -f docs/PRESENTATION/XUNO-FINAL.pptx`

3. Summary output: `PASS: X / FAIL: Y / SKIP: Z` with per-check line items.
   Exit code: 0 if PASS == total, 1 if any FAIL.

4. Append result to `demo-e2e-run-log.txt` with timestamp: `echo "$(date -Iseconds) | Run $RUN_NUM | PASS:$P | FAIL:$F | SKIP:$S" >> demo-e2e-run-log.txt`
   </action>
   <read_first>

- scripts/demo-preflight.sh
- scripts/demo-warmup.sh
- scripts/demo-fallback-test.sh (from Plan 05-04)
  </read_first>
  <acceptance_criteria>
- `test -f scripts/demo-e2e-run.sh` exits 0
- `bash -n scripts/demo-e2e-run.sh` exits 0 (valid bash)
- `grep "check \"" scripts/demo-e2e-run.sh | wc -l` returns >= 10
- `grep "record_result" scripts/demo-e2e-run.sh | wc -l` returns >= 10
- `grep "demo-e2e-run-log" scripts/demo-e2e-run.sh` matches
  </acceptance_criteria>

### Task 2: Execute and Document 3-Run Demo Rehearsal

<action>
Execute the 3-run rehearsal following `docs/DEMO-SCRIPT-TEST-PLAN.md`:

1. For each run (N=1,2,3):

   - Run preflight + warmup
   - Run `bash scripts/demo-e2e-run.sh`
   - Follow competition demo script (`docs/demo-script.md`) manually on device/emulator
   - Monitor crash log: `adb logcat | grep -E "FATAL|ReactNative|crash" > crash-log-run${N}.txt`
   - Record timing per segment (reference timing table in DEMO-SCRIPT-TEST-PLAN.md)
   - Record any issues, edge cases, or deviations

2. After 3 runs:

   - Verify zero FATAL/ReactNative/crash entries across all crash logs
   - Verify total time per run ≤ 150s (target ≤ 140s for margin)
   - Verify all 13 demo requirements (DEMO-01 through DEMO-13) are met on at least run 3

3. Fill in DEMO-SCRIPT-TEST-PLAN.md execution records for all 3 runs.

4. If any run has a crash: fix root cause → restart run counter from 1.

5. Update `docs/DEMO-CHECKLIST.md` with E2E verification results.
   </action>
   <read_first>

- docs/demo-script.md
- docs/DEMO-SCRIPT-TEST-PLAN.md
- docs/DEMO-CHECKLIST.md
  </read_first>
  <acceptance_criteria>
- 3 consecutive runs documented in DEMO-SCRIPT-TEST-PLAN.md
- Zero crashes in all crash log files: `grep -c "FATAL\|crash" crash-log-run*.txt` returns 0
- Each run total time ≤ 150s (verified from timing records)
- All 13 DEMO requirements checked off in DEMO-CHECKLIST.md
- `grep "RecommendationFunnel" docs/DEMO-CHECKLIST.md` — all 6 layers verified
  </acceptance_criteria>

## Verification

- `bash scripts/demo-e2e-run.sh` exits 0
- 3-run rehearsal log shows zero crashes
- Each run completes within 150s
- DEMO-SCRIPT-TEST-PLAN.md has completed execution records
