---
wave: 1
depends_on: []
autonomous: true
files_modified:
  - apps/mobile/src/shared/components/ErrorBoundary/
  - apps/mobile/src/features/today/screens/
  - apps/mobile/src/features/discover/screens/
  - apps/mobile/src/features/stylist/screens/
  - apps/mobile/src/features/profile/screens/
  - apps/mobile/src/features/onboarding/screens/
requirements: [DEMO-08]
---

# Plan 05-02: ErrorBoundary Coverage Extension

**Goal:** Extend ErrorBoundary wrapping to all 4 tab screens and onboarding wizard for zero-crash demo guarantee.

## Tasks

### Task 1: Diagnose Current ErrorBoundary Coverage

<action>
1. Search `apps/mobile/src/` for all usages of `withErrorBoundary`:
   `grep -r "withErrorBoundary" apps/mobile/src/ --include="*.tsx" -l`

2. Search for all screen default exports in the 4 tab features + onboarding:
   `grep -r "export default" apps/mobile/src/features/today/screens/ apps/mobile/src/features/discover/screens/ apps/mobile/src/features/stylist/screens/ apps/mobile/src/features/me/screens/ apps/mobile/src/features/onboarding/screens/ --include="*.tsx" -l`

3. Cross-reference: list screens without `withErrorBoundary` wrapping.

4. Report: list of unprotected screens as actionable items.
   </action>
   <read_first>

- apps/mobile/src/shared/components/ErrorBoundary/ScreenErrorBoundaries.ts
- apps/mobile/src/shared/components/ErrorBoundary/ErrorBoundary.tsx
- apps/mobile/src/shared/components/ErrorBoundary/index.ts
  </read_first>
  <acceptance_criteria>
- Task output contains an explicit list of `PROTECTED: N screens`, `UNPROTECTED: M screens`
- Each unprotected screen is listed with file path
  </acceptance_criteria>

### Task 2: Wrap All Unprotected Screens with ErrorBoundary

<action>
For each unprotected screen identified in Task 1:

1. Import `withErrorBoundary` and `ErrorFallback` from `@/shared/components/ErrorBoundary`

2. Create a screen-specific config entry in `ScreenErrorBoundaries.ts`:

   ```typescript
   {screenName}: {
     fallbackTitle: '{Human-readable title}',
     fallbackMessage: 'Something went wrong loading {feature}. Please try again.',
     reportToAnalytics: __DEV__ ? false : true,
   }
   ```

3. Replace `export default ScreenName` with:

   ```typescript
   export default withErrorBoundary(ScreenName, errorBoundaryConfigs.{screenName})
   ```

4. Target screens to wrap:

   - `apps/mobile/src/features/today/screens/TodayScreen.tsx`
   - `apps/mobile/src/features/discover/screens/DiscoverScreen.tsx`
   - `apps/mobile/src/features/stylist/screens/AiStylistUnifiedScreen.tsx` (already wrapped, verify)
   - `apps/mobile/src/features/me/screens/MeScreen.tsx` (or `ProfileScreen.tsx`, adjust to actual filename)
   - `apps/mobile/src/features/onboarding/screens/` — all screens in the onboarding flow

5. Verify: `grep -r "withErrorBoundary"` on all identified files shows the HOC wrapper on the default export.
   </action>
   <read_first>

- apps/mobile/src/shared/components/ErrorBoundary/ScreenErrorBoundaries.ts
- apps/mobile/src/shared/components/ErrorBoundary/ErrorBoundary.tsx
- Each target screen file (read before editing)
  </read_first>
  <acceptance_criteria>
- `grep "withErrorBoundary" apps/mobile/src/features/today/screens/*.tsx` matches
- `grep "withErrorBoundary" apps/mobile/src/features/discover/screens/*.tsx` matches
- `grep "withErrorBoundary" apps/mobile/src/features/me/screens/*.tsx` matches
- `grep "withErrorBoundary" apps/mobile/src/features/onboarding/screens/*.tsx` matches at least one file
- `grep -r "withErrorBoundary" apps/mobile/src/features/ --include="*.tsx" | wc -l` returns >= 5
- `npx tsc --noEmit -p apps/mobile/tsconfig.json` (mobile typecheck passes)
  </acceptance_criteria>

## Verification

- TypeScript compilation passes for mobile app
- All 4 tab screens + onboarding screens have ErrorBoundary HOC wrapping
- App navigates between all tabs without error propagation on mock failure
