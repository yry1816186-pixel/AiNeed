---
phase: 04-yiyi-agent-voice-onboarding-studio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ml/services/stylist/dialog_state.py
  - ml/services/stylist/dialog_engine.py
  - ml/services/stylist/rule_loader.py
  - ml/services/stylist/studio_signal_detector.py
  - ml/data/studio_directory.json
  - ml/tests/test_dialog_engine.py
  - ml/tests/test_rule_loader.py
autonomous: true
requirements:
  - YIYI-01
  - YIYI-02
  - YIYI-03
  - YIYI-07
  - YIYI-06
  - YIYI-05
  - RUL-01
  - RUL-02
  - RUL-03
  - WKS-01
  - WKS-03
  - ETH-01
  - ETH-02

must_haves:
  truths:
    - "DialogEngine processes GREET->CONTEXT->SCENE->GENERATE->ACTION->REFINE->WRAP with all transitions"
    - "Interview flow: company->position->budget->outfits has dedicated slot extraction"
    - "Yiyi personality prompt enforced: no '亲~', no '根据算法分析', describes clothes not body"
    - "Fashion rules loaded from 7 JSON files and filtered by bodyType+occasion+colorSeason"
    - "Studio signal detector triggers on premium budget, 3 rejections, special events, unique keywords"
    - "Exception handling: give up->gentle close, dislike all->guide preference, timeout->rule fallback"
  artifacts:
    - path: "ml/services/stylist/dialog_state.py"
      provides: "DialogState enum with SCENE, DIRECT, CHAT states added"
      contains: "SCENE"
    - path: "ml/services/stylist/dialog_engine.py"
      provides: "Full state machine with interview flow, personality, exception handling"
      exports: ["DialogEngine"]
    - path: "ml/services/stylist/rule_loader.py"
      provides: "FashionRuleLoader class with load_all_rules and get_filtered_rules"
      exports: ["FashionRuleLoader"]
    - path: "ml/services/stylist/studio_signal_detector.py"
      provides: "StudioSignalDetector class"
      exports: ["StudioSignalDetector"]
    - path: "ml/data/studio_directory.json"
      provides: "Manual 5-10 studio directory seed data"
      min_lines: 40
  key_links:
    - from: "ml/services/stylist/dialog_engine.py"
      to: "ml/services/stylist/rule_loader.py"
      via: "FashionRuleLoader.get_filtered_rules() in outfit generation"
      pattern: "FashionRuleLoader"
    - from: "ml/services/stylist/dialog_engine.py"
      to: "ml/services/stylist/studio_signal_detector.py"
      via: "StudioSignalDetector.detect() in GENERATE/REFINE handlers"
      pattern: "StudioSignalDetector"
---

<objective>
Extend Python DialogEngine with missing states (SCENE/DIRECT/CHAT), interview-specific flow, Yiyi personality prompt, fashion rules dynamic loading, studio signal detection, and exception handling.

Purpose: This is the AI brain of Yiyi. All conversation intelligence lives in Python. NestJS only persists what Python decides.
Output: DialogEngine that processes full state machine, loads rules from JSON, detects studio signals, and enforces Yiyi personality + body-positive language.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-yiyi-agent-voice-onboarding-studio/04-CONTEXT.md
@.planning/phases/04-yiyi-agent-voice-onboarding-studio/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From ml/services/stylist/dialog_state.py:

```python
class DialogState(str, Enum):
    GREET = "GREET"
    CONTEXT = "CONTEXT"
    GENERATE = "GENERATE"
    REFINE = "REFINE"
    ACTION = "ACTION"
    WRAP = "WRAP"
    # MUST ADD: SCENE = "SCENE", DIRECT = "DIRECT", CHAT = "CHAT"

class DialogSlot(BaseModel):
    occasion: Optional[str] = None
    body_type: Optional[str] = None
    style_preference: List[str] = []
    budget: Optional[Dict[str, float]] = None
    color_preference: List[str] = []
    avoid_items: List[str] = []
    temperature: Optional[float] = None

class DialogContext(BaseModel):
    state: DialogState = DialogState.GREET
    slots: DialogSlot = Field(default_factory=DialogSlot)
    turn_count: int = 0
    generated_outfits: List[Dict] = Field(default_factory=list)
    user_feedback: List[str] = Field(default_factory=list)
```

From ml/data/fashion_rules/body_type_rules.json (example structure):

```json
{
    "id": "bt_hourglass_interview",
    "body_type": "hourglass",
    "occasion": "interview",
    "strategy": "...",
    "recommended": { "tops": [...], "bottoms": [...], "shoes": [...] },
    "recommended_colors": ["#2C3E50", ...],
    "avoid": [...]
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend DialogState + DialogEngine with new states and interview flow</name>
  <files>ml/services/stylist/dialog_state.py, ml/services/stylist/dialog_engine.py, ml/tests/test_dialog_engine.py</files>
  <read_first>
    - ml/services/stylist/dialog_state.py (current DialogState enum and DialogSlot)
    - ml/services/stylist/dialog_engine.py (current handler_map and all _handle_* methods)
    - ml/data/fashion_rules/body_type_rules.json (first 40 lines, to understand JSON structure for rule loading)
  </read_first>
  <behavior>
    - Test 1: DialogState enum contains SCENE, DIRECT, CHAT values
    - Test 2: DialogEngine._handle_greet transitions to SCENE when user specifies a scene
    - Test 3: DialogEngine._handle_greet transitions to DIRECT when user gives full details
    - Test 4: DialogEngine._handle_greet transitions to CONTEXT when message is vague
    - Test 5: Interview flow: company slot -> position slot -> budget slot -> can_generate
    - Test 6: _handle_generate with sentiment "positive" transitions to ACTION
    - Test 7: _handle_generate with 3 consecutive "negative" feedback triggers studio signal
    - Test 8: _handle_wrap with "give up" intent returns gentle close message
    - Test 9: _handle_generate with empty outfits returns rule-based fallback message
  </behavior>
  <action>
    **DialogState enum (dialog_state.py):** Add three new enum values after CONTEXT:
    - `SCENE = "SCENE"` -- user specified a scene but needs detail collection
    - `DIRECT = "DIRECT"` -- user gave enough info for direct generation
    - `CHAT = "CHAT"` -- free-form conversation, not slot-filling

    Add to DialogSlot model:
    - `company: Optional[str] = None` -- interview target company
    - `position: Optional[str] = None` -- interview target position
    - `color_season: Optional[str] = None` -- user's color season

    Add to DialogContext:
    - `preference_memory: Dict[str, str] = Field(default_factory=dict)` -- cross-session preference recall (YIYI-05)
    - `negative_feedback_count: int = 0` -- tracks consecutive negative feedback for studio trigger

    **DialogEngine (dialog_engine.py):**

    1. Update `handler_map` in `process_message()` to include SCENE, DIRECT, CHAT handlers:
       ```python
       handler_map = {
           DialogState.GREET: self._handle_greet,
           DialogState.CONTEXT: self._handle_context,
           DialogState.SCENE: self._handle_scene,
           DialogState.DIRECT: self._handle_direct,
           DialogState.CHAT: self._handle_chat,
           DialogState.GENERATE: self._handle_generate,
           DialogState.REFINE: self._handle_refine,
           DialogState.ACTION: self._handle_action,
           DialogState.WRAP: self._handle_wrap,
       }
       ```

    2. Update `_handle_greet()` routing logic:
       - After slot extraction, check if user mentioned a specific scene (interview keywords: "面试", "interview") -> transition to SCENE
       - If user gave enough info for direct generation (occasion + style_preference filled) -> transition to DIRECT
       - If message is social greeting with no intent -> transition to CHAT
       - Otherwise -> CONTEXT (current behavior, ask for missing slots)

    3. Implement `_handle_scene(self, message, context)`:
       - Extract scene-specific slots (for interview: company, position, budget)
       - If interview: ask "什么公司？" if company missing, "什么岗位？" if position missing, "预算大概多少？" if budget missing
       - When all scene slots filled -> transition to GENERATE
       - Use BODY_POSITIVE_PROMPT in all LLM calls

    4. Implement `_handle_direct(self, message, context)`:
       - Skip to outfit generation immediately
       - Call _generate_outfits(context) directly

    5. Implement `_handle_chat(self, message, context)`:
       - Free-form response via LLM with Yiyi personality
       - If user shifts to a specific request (contains scene keywords), transition to SCENE or GENERATE
       - Always include YIYI_PERSONALITY_PROMPT in system message

    6. Add YIYI_PERSONALITY_PROMPT constant (before BODY_POSITIVE_PROMPT):
       ```python
       YIYI_PERSONALITY_PROMPT = """你是伊伊(Yiyi)，用户最信任的穿搭搭子。

       ## 性格
       你是温柔但有主见的朋友。你会给出明确建议，不是和稀泥。
       你有自己的审美判断，但永远尊重用户的选择。

       ## 禁止用语（绝对不能出现）
       - "亲~"、"亲爱的"、"宝子"
       - "根据算法分析"、"系统推荐"、"数据分析显示"
       - 任何描述身体缺点或身材缺陷的语言
       - "这个风格很适合你的体型"（改为"这个风格很适合你的气质"）

       ## 必须遵循
       - 用"适合你的风格"而非"适合你的体型"
       - 用"这件衣服的版型"而非"遮住/修饰你的XXX"
       - 推荐时从"衣服特点"出发，不从"身体缺点"出发
       - 试穿失败时归因于"这件衣服的剪裁可能不是最佳选择"
       - 语气像一个25-28岁的有品味的朋友，温暖但不甜腻

       ## 说话风格
       - 简短自然，不啰嗦
       - 可以用"我觉得"、"依我看"表达观点
       - 偶尔用"诶"、"嗯"等语气词让对话更自然
       """
       ```

    7. Update all LLM call sites to prepend YIYI_PERSONALITY_PROMPT as system message (replace generic prompts):
       - `_ask_for_slots()`: Use YIYI_PERSONALITY_PROMPT + BODY_POSITIVE_PROMPT as system messages
       - `_format_outfit_reply()`: Same
       - `_handle_chat()`: Use YIYI_PERSONALITY_PROMPT as primary system prompt
       - `_handle_wrap()`: Use personality for farewell message

    8. Add exception handling (YIYI-07):
       - In `_handle_generate()`: If `context.negative_feedback_count >= 3`, include studio signal in response
       - In `_handle_generate()`: If `len(context.generated_outfits) == 0` and `self._outfit_generator` raises exception, return rule-based fallback: "我先用基础规则帮你搭了一套，之后再细聊你的喜好"
       - In `_handle_refine()`: If user says keywords like ["算了", "不要了", "不想要了", "放弃"], transition to WRAP with gentle close: "没问题，下次想聊穿搭随时找我"
       - In all LLM call catch blocks: return a rule-based fallback response instead of generic error

    9. Update `_generate_outfits()` quick_replies to be state-aware (YIYI-06):
       - If interview scene: ["喜欢方案A", "喜欢方案B", "喜欢方案C", "换个价位", "换个风格", "都不喜欢"]
       - If date scene: ["喜欢方案A", "喜欢方案B", "喜欢方案C", "更甜一点", "更酷一点", "都不喜欢"]
       - Default: current behavior

    10. Update `_get_context_quick_replies()` to include interview-specific options:
        - When occasion="interview": ["互联网公司", "金融公司", "外企", "国企", "创业公司"]
        - When occasion="interview" and position missing: ["技术岗", "产品岗", "设计岗", "运营岗", "管理岗"]

    11. Write test file `ml/tests/test_dialog_engine.py` with pytest tests covering all behaviors listed above.

  </action>
  <verify>
    <automated>cd C:/AiNeed && python -m pytest ml/tests/test_dialog_engine.py -x -v</automated>
  </verify>
  <done>
    - DialogState enum has SCENE, DIRECT, CHAT values
    - DialogEngine has _handle_scene, _handle_direct, _handle_chat methods
    - Interview flow extracts company, position, budget slots
    - YIYI_PERSONALITY_PROMPT constant exists with forbidden phrases
    - BODY_POSITIVE_PROMPT used in all LLM calls
    - Exception handling: 3 negatives triggers studio signal, give-up transitions to WRAP, empty outfits gives rule fallback
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FashionRuleLoader + StudioSignalDetector + studio directory</name>
  <files>ml/services/stylist/rule_loader.py, ml/services/stylist/studio_signal_detector.py, ml/data/studio_directory.json, ml/tests/test_rule_loader.py</files>
  <read_first>
    - ml/data/fashion_rules/body_type_rules.json (full file, understand structure)
    - ml/data/fashion_rules/chinese_occasion_rules.json (first 30 lines, check structure)
    - ml/data/fashion_rules/color_season_rules.json (first 30 lines, check structure)
    - ml/services/stylist/full_outfit_engine.py (first 80 lines, see how it currently handles rules)
  </read_first>
  <behavior>
    - Test 1: FashionRuleLoader loads all 7 JSON files from ml/data/fashion_rules/
    - Test 2: FashionRuleLoader.get_filtered_rules(body_type="hourglass", occasion="interview") returns only matching rules
    - Test 3: FashionRuleLoader.get_filtered_rules(color_season="spring") returns color-matched rules
    - Test 4: FashionRuleLoader.get_filtered_rules() with no filters returns all rules
    - Test 5: StudioSignalDetector detects premium_budget signal when budget min >= 3000
    - Test 6: StudioSignalDetector detects luxury_budget signal when budget min >= 5000
    - Test 7: StudioSignalDetector detects unique_request when message contains "独一无二"
    - Test 8: StudioSignalDetector detects special_event when message contains "婚礼"
    - Test 9: StudioSignalDetector detects multiple_rejections when negative_feedback_count >= 3
    - Test 10: StudioSignalDetector returns None for normal messages
  </behavior>
  <action>
    **Create ml/services/stylist/rule_loader.py:**

    ```python
    class FashionRuleLoader:
        """Load and filter fashion rules from JSON files."""

        def __init__(self, rules_dir: str = "ml/data/fashion_rules"):
            self._rules = self._load_all_rules(rules_dir)

        def _load_all_rules(self, rules_dir: str) -> Dict[str, List[Dict]]:
            rules = {}
            for filepath in glob(f"{rules_dir}/*.json"):
                with open(filepath, encoding="utf-8") as f:
                    category = Path(filepath).stem  # e.g., "body_type_rules"
                    rules[category] = json.load(f)
            return rules

        def get_filtered_rules(
            self,
            body_type: Optional[str] = None,
            occasion: Optional[str] = None,
            color_season: Optional[str] = None,
        ) -> List[Dict]:
            matched = []
            for category, rule_list in self._rules.items():
                for rule in rule_list:
                    if body_type and rule.get("body_type") and rule.get("body_type") != body_type:
                        continue
                    if occasion and rule.get("occasion") and rule.get("occasion") != occasion:
                        continue
                    if color_season and rule.get("color_season") and rule.get("color_season") != color_season:
                        continue
                    matched.append(rule)
            return matched

        def get_category(self, category_name: str) -> List[Dict]:
            return self._rules.get(category_name, [])

        def get_all_rules(self) -> Dict[str, List[Dict]]:
            return self._rules
    ```

    **Create ml/services/stylist/studio_signal_detector.py:**

    ```python
    class StudioSignalDetector:
        """Detect conversation signals that trigger studio recommendation."""

        SIGNAL_PATTERNS = {
            "premium_budget": lambda slots: (slots.get("budget", {}) or {}).get("min", 0) >= 3000,
            "luxury_budget": lambda slots: (slots.get("budget", {}) or {}).get("min", 0) >= 5000,
            "unique_request": lambda msg: any(kw in msg for kw in ["独一无二", "定制", "特别定制", "bespoke"]),
            "special_event": lambda msg: any(kw in msg for kw in ["婚礼", "红毯", "颁奖典礼", "重要场合", "婚礼伴娘"]),
        }

        def detect(self, message: str, context) -> Optional[str]:
            """Returns signal type if triggered, None otherwise."""
            # Check budget signals from slots
            slots = context.slots.model_dump() if hasattr(context.slots, 'model_dump') else {}
            for signal_name in ["premium_budget", "luxury_budget"]:
                if self.SIGNAL_PATTERNS[signal_name](slots):
                    return signal_name

            # Check rejection count
            if getattr(context, 'negative_feedback_count', 0) >= 3:
                return "multiple_rejections"

            # Check message-based signals
            for signal_name in ["unique_request", "special_event"]:
                if self.SIGNAL_PATTERNS[signal_name](message):
                    return signal_name

            return None

        def get_recommendation_message(self, signal: str) -> str:
            MESSAGES = {
                "premium_budget": "你的预算挺充裕的，其实可以考虑工作室定制，能更贴合你的想法。",
                "luxury_budget": "这个预算的话，我认识几家工作室，做出的效果会比网购好很多。",
                "multiple_rejections": "看来线上挑不到完全满意的？要不试试工作室定制？",
                "unique_request": "你想要独一无二的效果，这个找工作室定制会更好实现。",
                "special_event": "这种重要场合，工作室的造型师可以给你做整体造型，比单挑衣服更靠谱。",
            }
            return MESSAGES.get(signal, "要不要看看工作室推荐？")
    ```

    **Create ml/data/studio_directory.json:**
    JSON array with 6 studio entries. Each entry:
    ```json
    {
      "id": "studio_001",
      "name": "织造社",
      "city": "上海",
      "specialty": "职场穿搭定制",
      "price_range": { "min": 2000, "max": 8000 },
      "contact": "微信: zhizaoshe_studio",
      "style_tags": ["极简", "轻正式"],
      "occasions": ["interview", "work"],
      "description": "专注职场女性形象定制，擅长用高品质面料打造专业又不失个性的穿搭"
    }
    ```
    Create 6 studios covering: 职场定制, 婚礼造型, 日常穿搭, 设计师品牌, 国风定制, 街头潮流. Vary cities (上海, 北京, 杭州, 成都, 深圳).

    **Write test file ml/tests/test_rule_loader.py** covering all behaviors.

    **After tests pass**, wire FashionRuleLoader into full_outfit_engine.py:
    - Add `self.rule_loader = FashionRuleLoader()` in FullOutfitEngine.__init__ (if it has one) or at module level
    - In the outfit generation method, call `self.rule_loader.get_filtered_rules(body_type=..., occasion=..., color_season=...)` to inject rules into scoring context
    - This makes RUL-01 and RUL-02 operational

  </action>
  <verify>
    <automated>cd C:/AiNeed && python -m pytest ml/tests/test_rule_loader.py -x -v</automated>
  </verify>
  <done>
    - FashionRuleLoader loads 7 JSON files from ml/data/fashion_rules/
    - get_filtered_rules() filters by body_type, occasion, color_season
    - StudioSignalDetector detects all 5 signal types
    - studio_directory.json has 6 entries with varied specialties and cities
    - full_outfit_engine.py uses FashionRuleLoader for rule injection
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                        |
| ----------------- | ---------------------------------------------------------------------------------- |
| LLM API call      | External GLM API may return unexpected content; all responses validated before use |
| JSON file loading | Local file system; rules are trusted data but validate JSON structure on load      |

## STRIDE Threat Register

| Threat ID | Category          | Component                    | Disposition | Mitigation Plan                                                                                  |
| --------- | ----------------- | ---------------------------- | ----------- | ------------------------------------------------------------------------------------------------ |
| T-04-01   | Tampering         | DialogEngine LLM output      | mitigate    | Validate LLM JSON response structure before using; fallback to keyword matching on parse failure |
| T-04-02   | Denial of Service | StudioSignalDetector         | accept      | Signal detection is lightweight computation; no external calls                                   |
| T-04-03   | Tampering         | FashionRuleLoader JSON files | accept      | JSON files are bundled with app, not user-controlled                                             |

</threat_model>

<verification>
- All Python tests pass: `cd C:/AiNeed && python -m pytest ml/tests/ -x -v`
- DialogState enum has SCENE, DIRECT, CHAT: `grep -c "SCENE\|DIRECT\|CHAT" ml/services/stylist/dialog_state.py`
- YIYI_PERSONALITY_PROMPT exists: `grep -c "YIYI_PERSONALITY_PROMPT" ml/services/stylist/dialog_engine.py`
- FashionRuleLoader loads 7 files: test verifies count
- studio_directory.json has 6+ entries: `python -c "import json; d=json.load(open('ml/data/studio_directory.json')); print(len(d))"`
</verification>

<success_criteria>

- DialogEngine processes all states: GREET, CONTEXT, SCENE, DIRECT, CHAT, GENERATE, REFINE, ACTION, WRAP
- Interview flow collects company, position, budget before generating
- Yiyi personality prompt present with forbidden phrases list
- Fashion rules loaded from 7 JSON files, filterable by bodyType+occasion+colorSeason
- Studio signal detector triggers on all 5 signal patterns
- All tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/04-yiyi-agent-voice-onboarding-studio/04-01-SUMMARY.md`
</output>
