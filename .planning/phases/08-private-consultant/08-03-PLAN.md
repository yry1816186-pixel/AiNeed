---
phase: 08
plan: 03
type: backend
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/backend/src/modules/consultant/consultant-availability.service.ts
  - apps/backend/src/modules/consultant/consultant-availability.service.spec.ts
  - apps/backend/src/modules/consultant/consultant.controller.ts
  - apps/backend/src/modules/consultant/consultant.service.ts
  - apps/backend/src/modules/consultant/dto/availability.dto.ts
  - apps/backend/src/modules/consultant/dto/booking.dto.ts
  - apps/backend/src/modules/consultant/consultant.module.ts
  - apps/backend/src/modules/payment/payment.service.ts
  - apps/backend/src/modules/payment/payment.controller.ts
autonomous: true
requirements:
  - ADV-04
  - ADV-07
  - ADV-08
---

# Plan 03: 预约排期 + 分阶段付款 + 结算

<objective>
实现顾问排期管理（ConsultantAvailabilityService），用户可通过日历视图查看顾问可用时段并预约。扩展 ServiceBooking 支持分阶段付款（30% 定金 + 70% 尾款），集成现有 PaymentModule 实现定金/尾款支付流程。实现平台抽成结算（15-20% 佣金）和顾问收入管理。
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| 预约时间冲突 | High | 创建预约时二次检查时段冲突，数据库级别唯一约束 |
| 支付金额篡改 | High | 服务端计算定金/尾款金额，客户端不可指定 |
| 双重支付 | High | 幂等性检查：同一 booking 只能支付一次定金/尾款 |
| 结算金额错误 | High | 佣金比例固定配置，结算金额服务端计算 |
</threat_model>

<tasks>

<task id="08-03-01" type="code">
<read_first>
- apps/backend/src/modules/consultant/consultant.service.ts (现有服务模式)
- apps/backend/src/modules/consultant/dto/consultant.dto.ts (现有 DTO 模式)
- apps/backend/prisma/schema.prisma (ConsultantAvailability 模型结构)
</read_first>
<action>
1. 创建排期 DTO `apps/backend/src/modules/consultant/dto/availability.dto.ts`：

```typescript
import { IsString, IsOptional, IsInt, Min, Max, IsBoolean, IsArray, ValidateNested } from "class-validator";
import { Type } from "class-transformer";
import { ApiProperty, ApiPropertyOptional } from "@nestjs/swagger";

export class CreateAvailabilityDto {
  @ApiProperty({ description: "星期几 (0=周日, 1=周一, ..., 6=周六)", example: 1 })
  @IsInt()
  @Min(0)
  @Max(6)
  dayOfWeek!: number;

  @ApiProperty({ description: "开始时间 (HH:mm)", example: "09:00" })
  @IsString()
  startTime!: string;

  @ApiProperty({ description: "结束时间 (HH:mm)", example: "18:00" })
  @IsString()
  endTime!: string;

  @ApiPropertyOptional({ description: "时段时长(分钟)", example: 60 })
  @IsOptional()
  @IsInt()
  @Min(15)
  slotDuration?: number = 60;

  @ApiPropertyOptional({ description: "是否可用", example: true })
  @IsOptional()
  @IsBoolean()
  isAvailable?: boolean = true;
}

export class BatchCreateAvailabilityDto {
  @ApiProperty({ description: "时段模板列表" })
  @IsArray()
  @ValidateNested({ each: true })
  @Type(() => CreateAvailabilityDto)
  items!: CreateAvailabilityDto[];
}

export class AvailabilityQueryDto {
  @ApiPropertyOptional({ description: "顾问ID", example: "consultant-001" })
  @IsOptional()
  @IsString()
  consultantId?: string;

  @ApiPropertyOptional({ description: "星期几筛选", example: 1 })
  @IsOptional()
  @IsInt()
  @Min(0)
  @Max(6)
  dayOfWeek?: number;
}

export class AvailableSlotsQueryDto {
  @ApiProperty({ description: "顾问ID" })
  @IsString()
  consultantId!: string;

  @ApiProperty({ description: "日期 (YYYY-MM-DD)", example: "2026-04-20" })
  @IsString()
  date!: string;
}
```

2. 创建排期服务 `apps/backend/src/modules/consultant/consultant-availability.service.ts`：

```typescript
import { Injectable, Logger, NotFoundException, BadRequestException } from "@nestjs/common";
import { PrismaService } from "../../common/prisma/prisma.service";
import { CreateAvailabilityDto, BatchCreateAvailabilityDto, AvailableSlotsQueryDto } from "./dto";

interface TimeSlot {
  startTime: string;
  endTime: string;
  isAvailable: boolean;
  bookingId?: string;
}

@Injectable()
export class ConsultantAvailabilityService {
  private readonly logger = new Logger(ConsultantAvailabilityService.name);

  constructor(private readonly prisma: PrismaService) {}

  async setAvailability(consultantId: string, userId: string, dto: BatchCreateAvailabilityDto) {
    const consultant = await this.prisma.consultantProfile.findUnique({ where: { id: consultantId } });
    if (!consultant) throw new NotFoundException("顾问不存在");
    if (consultant.userId !== userId) throw new BadRequestException("无权修改此顾问排期");

    // 删除旧时段模板
    await this.prisma.consultantAvailability.deleteMany({ where: { consultantId } });

    // 批量创建新时段模板
    const data = dto.items.map((item) => ({
      consultantId,
      dayOfWeek: item.dayOfWeek,
      startTime: item.startTime,
      endTime: item.endTime,
      slotDuration: item.slotDuration ?? 60,
      isAvailable: item.isAvailable ?? true,
    }));

    return this.prisma.consultantAvailability.createMany({ data, skipDuplicates: true });
  }

  async getAvailability(consultantId: string) {
    return this.prisma.consultantAvailability.findMany({
      where: { consultantId },
      orderBy: { dayOfWeek: "asc" },
    });
  }

  async getAvailableSlots(query: AvailableSlotsQueryDto): Promise<TimeSlot[]> {
    const { consultantId, date } = query;

    const dateObj = new Date(date);
    const dayOfWeek = dateObj.getDay();

    // 获取该顾问当天的时段模板
    const templates = await this.prisma.consultantAvailability.findMany({
      where: { consultantId, dayOfWeek, isAvailable: true },
    });

    if (templates.length === 0) return [];

    // 生成所有时段
    const allSlots: TimeSlot[] = [];
    for (const template of templates) {
      const slots = this.generateSlots(template.startTime, template.endTime, template.slotDuration);
      allSlots.push(...slots);
    }

    // 查询该日期的已有预约
    const dayStart = new Date(dateObj);
    dayStart.setHours(0, 0, 0, 0);
    const dayEnd = new Date(dateObj);
    dayEnd.setHours(23, 59, 59, 999);

    const bookings = await this.prisma.serviceBooking.findMany({
      where: {
        consultantId,
        scheduledAt: { gte: dayStart, lte: dayEnd },
        status: { in: ["pending", "confirmed", "in_progress"] },
      },
      select: { id: true, scheduledAt: true, durationMinutes: true },
    });

    // 标记已预约时段
    for (const slot of allSlots) {
      const slotStart = this.parseTimeToMinutes(slot.startTime);
      const slotEnd = this.parseTimeToMinutes(slot.endTime);

      for (const booking of bookings) {
        const bookingStart = booking.scheduledAt.getHours() * 60 + booking.scheduledAt.getMinutes();
        const bookingEnd = bookingStart + booking.durationMinutes;

        if (slotStart < bookingEnd && slotEnd > bookingStart) {
          slot.isAvailable = false;
          slot.bookingId = booking.id;
          break;
        }
      }
    }

    return allSlots;
  }

  async checkSlotConflict(consultantId: string, scheduledAt: Date, durationMinutes: number): Promise<boolean> {
    const endTime = new Date(scheduledAt.getTime() + durationMinutes * 60000);

    const conflict = await this.prisma.serviceBooking.findFirst({
      where: {
        consultantId,
        status: { in: ["pending", "confirmed", "in_progress"] },
        scheduledAt: { lt: endTime },
        // Prisma 不直接支持范围重叠查询，使用 OR 条件
        OR: [
          { scheduledAt: { gte: scheduledAt, lt: endTime } },
          {
            AND: [
              { scheduledAt: { lt: scheduledAt } },
            ],
          },
        ],
      },
    });

    return !!conflict;
  }

  private generateSlots(startTime: string, endTime: string, duration: number): TimeSlot[] {
    const slots: TimeSlot[] = [];
    let current = this.parseTimeToMinutes(startTime);
    const end = this.parseTimeToMinutes(endTime);

    while (current + duration <= end) {
      slots.push({
        startTime: this.formatMinutesToTime(current),
        endTime: this.formatMinutesToTime(current + duration),
        isAvailable: true,
      });
      current += duration;
    }

    return slots;
  }

  private parseTimeToMinutes(time: string): number {
    const [hours, minutes] = time.split(":").map(Number);
    return hours * 60 + minutes;
  }

  private formatMinutesToTime(minutes: number): string {
    const hours = Math.floor(minutes / 60);
    const mins = minutes % 60;
    return `${hours.toString().padStart(2, "0")}:${mins.toString().padStart(2, "0")}`;
  }
}
```

3. 在 `consultant.controller.ts` 新增排期端点：
```typescript
@Get("availability")
@ApiOperation({ summary: "获取顾问排期", description: "获取顾问每周可用时段模板" })
async getAvailability(@Query("consultantId") consultantId: string) {
  return this.availabilityService.getAvailability(consultantId);
}

@Get("available-slots")
@ApiOperation({ summary: "获取可用时段", description: "获取指定日期的可用时段列表" })
async getAvailableSlots(@Query() query: AvailableSlotsQueryDto) {
  return this.availabilityService.getAvailableSlots(query);
}

@Post("availability")
@UseGuards(AuthGuard)
@ApiBearerAuth()
@ApiOperation({ summary: "设置排期模板", description: "顾问设置每周可用时段模板" })
async setAvailability(
  @Request() req: RequestWithUser,
  @Body() dto: BatchCreateAvailabilityDto,
  @Query("consultantId") consultantId: string,
) {
  return this.availabilityService.setAvailability(consultantId, req.user.id, dto);
}
```

4. 在 `consultant.module.ts` 注册 `ConsultantAvailabilityService`。
</action>
<acceptance_criteria>
- `apps/backend/src/modules/consultant/consultant-availability.service.ts` exists
- File contains `class ConsultantAvailabilityService` with `setAvailability`, `getAvailability`, `getAvailableSlots`, `checkSlotConflict` methods
- `getAvailableSlots` returns `TimeSlot[]` with `startTime`, `endTime`, `isAvailable` fields
- `checkSlotConflict` returns boolean indicating time slot conflict
- `consultant.controller.ts` contains `@Get("availability")`, `@Get("available-slots")`, `@Post("availability")` endpoints
- `consultant.module.ts` providers includes `ConsultantAvailabilityService`
- `dto/availability.dto.ts` exists with `CreateAvailabilityDto`, `BatchCreateAvailabilityDto`, `AvailableSlotsQueryDto`
</acceptance_criteria>
</task>

<task id="08-03-02" type="code">
<read_first>
- apps/backend/src/modules/consultant/consultant.service.ts (现有 createBooking 方法)
- apps/backend/src/modules/payment/payment.service.ts (现有支付服务)
- apps/backend/src/modules/payment/payment.controller.ts (现有支付控制器)
- apps/backend/prisma/schema.prisma (ServiceBooking 新增的 depositAmount/finalPaymentAmount/platformFee/consultantPayout 字段)
</read_first>
<action>
1. 更新 `consultant.service.ts` 的 `createBooking` 方法支持分阶段付款：

在创建预约时自动计算定金和尾款：
```typescript
// 在 createBooking 方法中，创建数据前添加：
const depositRate = 0.30; // 30% 定金
const depositAmount = new Prisma.Decimal(dto.price).mul(depositRate).toFixed(2);
const finalPaymentAmount = new Prisma.Decimal(dto.price).mul(1 - depositRate).toFixed(2);

// 在 create data 中添加：
depositAmount: new Prisma.Decimal(depositAmount),
finalPaymentAmount: new Prisma.Decimal(finalPaymentAmount),
platformFee: new Prisma.Decimal(0), // 服务完成时计算
consultantPayout: new Prisma.Decimal(0), // 服务完成时计算
```

2. 在 `consultant.service.ts` 新增支付相关方法：

```typescript
async payDeposit(userId: string, bookingId: string) {
  const booking = await this.getBookingById(userId, bookingId);
  if (booking.status !== "pending") {
    throw new BadRequestException("预约状态不允许支付定金");
  }
  if (booking.depositPaidAt) {
    throw new BadRequestException("定金已支付");
  }
  // 返回支付信息，由 PaymentModule 处理实际支付
  return {
    bookingId: booking.id,
    amount: Number(booking.depositAmount),
    currency: booking.currency,
    paymentCategory: "consultant_deposit",
  };
}

async payFinalPayment(userId: string, bookingId: string) {
  const booking = await this.getBookingById(userId, bookingId);
  if (booking.status !== "completed") {
    throw new BadRequestException("服务未完成，无法支付尾款");
  }
  if (booking.finalPaidAt) {
    throw new BadRequestException("尾款已支付");
  }
  if (!booking.depositPaidAt) {
    throw new BadRequestException("请先支付定金");
  }
  return {
    bookingId: booking.id,
    amount: Number(booking.finalPaymentAmount),
    currency: booking.currency,
    paymentCategory: "consultant_final",
  };
}

async confirmDepositPayment(bookingId: string) {
  const booking = await this.prisma.serviceBooking.findUnique({ where: { id: bookingId } });
  if (!booking) throw new NotFoundException("预约不存在");

  return this.prisma.serviceBooking.update({
    where: { id: bookingId },
    data: {
      depositPaidAt: new Date(),
      status: "confirmed",
    },
  });
}

async confirmFinalPayment(bookingId: string) {
  const booking = await this.prisma.serviceBooking.findUnique({ where: { id: bookingId } });
  if (!booking) throw new NotFoundException("预约不存在");

  const platformFeeRate = 0.15; // 15% 平台佣金
  const totalPrice = Number(booking.price);
  const platformFee = totalPrice * platformFeeRate;
  const consultantPayout = totalPrice - platformFee;

  return this.prisma.serviceBooking.update({
    where: { id: bookingId },
    data: {
      finalPaidAt: new Date(),
      platformFee: new Prisma.Decimal(platformFee.toFixed(2)),
      consultantPayout: new Prisma.Decimal(consultantPayout.toFixed(2)),
    },
  });
}
```

3. 在 `consultant.controller.ts` 新增支付端点：
```typescript
@Post("bookings/:id/pay-deposit")
@UseGuards(AuthGuard)
@ApiBearerAuth()
async payDeposit(@Request() req: RequestWithUser, @Param("id") id: string) {
  return this.consultantService.payDeposit(req.user.id, id);
}

@Post("bookings/:id/pay-final")
@UseGuards(AuthGuard)
@ApiBearerAuth()
async payFinalPayment(@Request() req: RequestWithUser, @Param("id") id: string) {
  return this.consultantService.payFinalPayment(req.user.id, id);
}
```

4. 更新 `updateBooking` 中的取消逻辑，实现 CC-38 取消规则：
```typescript
// 在取消预约逻辑中添加：
if (dto.status === BookingStatusDto.CANCELLED) {
  if (booking.depositPaidAt) {
    const hoursUntilBooking = (new Date(dto.scheduledAt || booking.scheduledAt).getTime() - Date.now()) / (1000 * 60 * 60);
    if (hoursUntilBooking >= 24) {
      // 提前 24h 取消，全额退定金
      // TODO: 触发退款流程
    } else {
      // 24h 内取消，扣定金 20% 给用户补偿，80% 退回
      // TODO: 触发部分退款流程
    }
  }
}
```
</action>
<acceptance_criteria>
- `consultant.service.ts` contains `payDeposit` method that validates booking status and returns payment info
- `consultant.service.ts` contains `payFinalPayment` method that validates service completion
- `consultant.service.ts` contains `confirmDepositPayment` method that sets depositPaidAt and status=confirmed
- `consultant.service.ts` contains `confirmFinalPayment` method that calculates platformFee (15%) and consultantPayout
- `consultant.controller.ts` contains `@Post("bookings/:id/pay-deposit")` endpoint
- `consultant.controller.ts` contains `@Post("bookings/:id/pay-final")` endpoint
- `createBooking` method calculates depositAmount (30%) and finalPaymentAmount (70%)
- Cancel booking logic includes 24h rule check
</acceptance_criteria>
</task>

<task id="08-03-03" type="code">
<read_first>
- apps/backend/src/modules/consultant/consultant.service.ts (现有服务模式)
- apps/backend/prisma/schema.prisma (ConsultantEarning、ConsultantWithdrawal 模型)
</read_first>
<action>
在 `consultant.service.ts` 新增结算和提现方法：

```typescript
async getEarnings(consultantId: string, userId: string) {
  const consultant = await this.prisma.consultantProfile.findUnique({ where: { id: consultantId } });
  if (!consultant) throw new NotFoundException("顾问不存在");
  if (consultant.userId !== userId) throw new ForbiddenException("无权查看此顾问收入");

  const [earnings, totalEarned, pendingAmount, settledAmount] = await Promise.all([
    this.prisma.consultantEarning.findMany({
      where: { consultantId },
      orderBy: { createdAt: "desc" },
      take: 50,
    }),
    this.prisma.consultantEarning.aggregate({
      where: { consultantId },
      _sum: { netAmount: true },
    }),
    this.prisma.consultantEarning.aggregate({
      where: { consultantId, status: "pending" },
      _sum: { netAmount: true },
    }),
    this.prisma.consultantEarning.aggregate({
      where: { consultantId, status: "settled" },
      _sum: { netAmount: true },
    }),
  ]);

  return {
    earnings,
    summary: {
      totalEarned: totalEarned._sum.netAmount || 0,
      pendingAmount: pendingAmount._sum.netAmount || 0,
      settledAmount: settledAmount._sum.netAmount || 0,
    },
  };
}

async requestWithdrawal(consultantId: string, userId: string, amount: number, bankInfo: { bankName: string; bankAccount: string; accountHolder: string }) {
  const consultant = await this.prisma.consultantProfile.findUnique({ where: { id: consultantId } });
  if (!consultant) throw new NotFoundException("顾问不存在");
  if (consultant.userId !== userId) throw new ForbiddenException("无权操作");

  const pendingAmount = await this.prisma.consultantEarning.aggregate({
    where: { consultantId, status: "pending" },
    _sum: { netAmount: true },
  });

  const available = Number(pendingAmount._sum.netAmount || 0);
  if (amount > available) {
    throw new BadRequestException(`可提现金额不足，当前可提现: ${available}`);
  }

  return this.prisma.consultantWithdrawal.create({
    data: {
      consultantId,
      amount: new Prisma.Decimal(amount),
      status: "pending",
      bankName: bankInfo.bankName,
      bankAccount: bankInfo.bankAccount,
      accountHolder: bankInfo.accountHolder,
    },
  });
}
```

在 `consultant.controller.ts` 新增结算端点：
```typescript
@Get("earnings")
@UseGuards(AuthGuard)
@ApiBearerAuth()
async getEarnings(@Request() req: RequestWithUser, @Query("consultantId") consultantId: string) {
  return this.consultantService.getEarnings(consultantId, req.user.id);
}

@Post("withdrawals")
@UseGuards(AuthGuard)
@ApiBearerAuth()
async requestWithdrawal(
  @Request() req: RequestWithUser,
  @Body() dto: { consultantId: string; amount: number; bankName: string; bankAccount: string; accountHolder: string },
) {
  return this.consultantService.requestWithdrawal(dto.consultantId, req.user.id, dto.amount, {
    bankName: dto.bankName,
    bankAccount: dto.bankAccount,
    accountHolder: dto.accountHolder,
  });
}
```
</action>
<acceptance_criteria>
- `consultant.service.ts` contains `getEarnings` method returning earnings list + summary (totalEarned, pendingAmount, settledAmount)
- `consultant.service.ts` contains `requestWithdrawal` method with available balance check
- `consultant.controller.ts` contains `@Get("earnings")` endpoint
- `consultant.controller.ts` contains `@Post("withdrawals")` endpoint
- `requestWithdrawal` validates available balance before creating withdrawal
</acceptance_criteria>
</task>

<task id="08-03-04" type="test">
<read_first>
- apps/backend/src/modules/consultant/consultant-availability.service.ts (刚创建的排期服务)
- apps/backend/src/modules/consultant/consultant.service.ts (更新的预约服务)
</read_first>
<action>
创建排期服务单元测试 `apps/backend/src/modules/consultant/consultant-availability.service.spec.ts`：

测试用例：
1. `setAvailability` 应删除旧模板并创建新模板
2. `getAvailableSlots` 应根据时段模板生成时段列表
3. `getAvailableSlots` 已预约时段应标记为不可用
4. `getAvailableSlots` 无模板时应返回空数组
5. `checkSlotConflict` 有冲突时应返回 true
6. `checkSlotConflict` 无冲突时应返回 false

创建支付逻辑测试（在 consultant.service.spec.ts 中新增）：
7. `createBooking` 应自动计算 30% 定金和 70% 尾款
8. `payDeposit` 非pending状态应抛出异常
9. `confirmFinalPayment` 应计算 15% 平台佣金
10. 取消预约提前 24h 应全额退定金
</action>
<acceptance_criteria>
- `apps/backend/src/modules/consultant/consultant-availability.service.spec.ts` exists
- File contains at least 6 test cases for availability service
- `cd apps/backend && npx jest --testPathPattern="consultant-availability" --passWithNoTests` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `cd apps/backend && npx tsc --noEmit` — TypeScript 编译无错误
2. `cd apps/backend && npx jest --testPathPattern="consultant" --passWithNoTests` — 顾问相关测试通过
3. GET /consultant/availability — 获取排期模板
4. GET /consultant/available-slots — 获取可用时段
5. POST /consultant/bookings/:id/pay-deposit — 支付定金
6. POST /consultant/bookings/:id/pay-final — 支付尾款
7. GET /consultant/earnings — 查看收入
8. POST /consultant/withdrawals — 申请提现
</verification>

<success_criteria>
- 顾问可设置每周时段模板
- 用户可查看指定日期的可用时段
- 预约创建时自动计算定金(30%)和尾款(70%)
- 定金支付后预约状态变为 confirmed
- 尾款支付后自动计算平台佣金(15%)和顾问结算金额
- 取消预约遵循 24h 规则
- 顾问可查看收入和申请提现
</success_criteria>

<must_haves>
- 时段冲突检测防止双重预约
- 分阶段付款：30% 定金 + 70% 尾款
- 平台佣金 15% 从尾款中扣除
- 取消规则：提前 24h 全额退款，24h 内扣 20%
</must_haves>
