---
phase: 08
plan: 01
type: backend
wave: 1
depends_on: []
files_modified:
  - apps/backend/prisma/schema.prisma
  - apps/backend/src/modules/consultant/consultant-matching.service.ts
  - apps/backend/src/modules/consultant/consultant-matching.service.spec.ts
  - apps/backend/src/modules/consultant/consultant.controller.ts
  - apps/backend/src/modules/consultant/consultant.service.ts
  - apps/backend/src/modules/consultant/dto/consultant.dto.ts
  - apps/backend/src/modules/consultant/dto/match.dto.ts
  - apps/backend/src/modules/consultant/consultant.module.ts
autonomous: true
requirements:
  - ADV-01
  - ADV-02
  - ADV-03
---

# Plan 01: Schema 扩展 + 智能匹配算法 + 需求提交

<objective>
扩展 Prisma Schema 新增 ConsultantReview、ConsultantAvailability、ConsultantEarning、ConsultantWithdrawal 模型，修改 MessageType 枚举新增 proposal 类型，修改 ServiceBooking 新增分阶段付款字段，修改 ConsultantProfile 新增 location/responseTimeAvg 字段。实现四维智能匹配算法（ConsultantMatchingService）和需求提交端点（POST /consultant/match），让用户提交需求后系统自动推荐 3-5 个最合适的顾问。
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| 匹配算法泄露用户画像数据 | Medium | 匹配结果仅返回匹配度百分比+理由，不暴露原始画像数据 |
| 需求提交注入攻击 | Low | DTO class-validator 验证所有输入字段 |
| 匹配结果操纵 | Medium | 匹配权重服务端计算，客户端不可篡改 |
</threat_model>

<tasks>

<task id="08-01-01" type="schema">
<read_first>
- apps/backend/prisma/schema.prisma (L1628-1768: 现有 ConsultantProfile/ServiceBooking/ChatRoom/ChatMessage 模型及枚举)
- apps/backend/src/modules/consultant/dto/consultant.dto.ts (现有 DTO 枚举和验证模式)
</read_first>
<action>
在 schema.prisma 文件中执行以下变更：

1. **MessageType 枚举** (L1717-1722) 新增 `proposal` 值：
```
enum MessageType {
  text
  image
  file
  system
  proposal
}
```

2. **ConsultantProfile 模型** (L1635-1661) 新增字段：
```
location          String?  @db.Text
responseTimeAvg   Int?     // 平均回复时间(分钟)
```
在 `bookings` 关系后新增：
```
reviews           ConsultantReview[]
availabilities    ConsultantAvailability[]
earnings          ConsultantEarning[]
withdrawals       ConsultantWithdrawal[]
```
新增索引：
```
@@index([location])
```

3. **ServiceBooking 模型** (L1680-1710) 新增字段：
```
depositAmount       Decimal?  @db.Decimal(10, 2)
finalPaymentAmount  Decimal?  @db.Decimal(10, 2)
platformFee         Decimal?  @db.Decimal(10, 2) @default(0)
consultantPayout    Decimal?  @db.Decimal(10, 2) @default(0)
depositPaidAt       DateTime?
finalPaidAt         DateTime?
```

4. **新增 ConsultantReview 模型** (在 ChatMessage 模型之后)：
```
model ConsultantReview {
  id            String   @id @default(uuid())
  bookingId     String   @unique
  userId        String
  consultantId  String

  rating        Int      // 1-5
  content       String?  @db.Text
  tags          Json     // string[]
  beforeImages  Json     // string[]
  afterImages   Json     // string[]
  isAnonymous   Boolean  @default(false)

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  user          User              @relation(fields: [userId], references: [id], onDelete: Cascade)
  consultant    ConsultantProfile @relation(fields: [consultantId], references: [id], onDelete: Cascade)
  booking       ServiceBooking    @relation(fields: [bookingId], references: [id], onDelete: Cascade)

  @@index([consultantId])
  @@index([userId])
  @@index([consultantId, rating])
  @@index([createdAt])
}
```

5. **新增 ConsultantAvailability 模型**：
```
model ConsultantAvailability {
  id            String   @id @default(uuid())
  consultantId  String
  dayOfWeek     Int      // 0=周日, 1=周一, ..., 6=周六
  startTime     String   // "09:00"
  endTime       String   // "18:00"
  slotDuration  Int      @default(60) // 每个时段时长(分钟)
  isAvailable   Boolean  @default(true)

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  consultant    ConsultantProfile @relation(fields: [consultantId], references: [id], onDelete: Cascade)

  @@index([consultantId])
  @@index([consultantId, dayOfWeek])
  @@unique([consultantId, dayOfWeek, startTime])
}
```

6. **新增 ConsultantEarning 模型**：
```
model ConsultantEarning {
  id              String   @id @default(uuid())
  consultantId    String
  bookingId       String
  amount          Decimal  @db.Decimal(10, 2)
  platformFee     Decimal  @db.Decimal(10, 2)
  netAmount       Decimal  @db.Decimal(10, 2)
  status          EarningStatus @default(pending)
  settlementDate  DateTime?

  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  consultant      ConsultantProfile @relation(fields: [consultantId], references: [id], onDelete: Cascade)
  booking         ServiceBooking    @relation(fields: [bookingId], references: [id], onDelete: Cascade)

  @@index([consultantId])
  @@index([status])
  @@index([consultantId, status])
}
```

7. **新增 ConsultantWithdrawal 模型**：
```
model ConsultantWithdrawal {
  id              String   @id @default(uuid())
  consultantId    String
  amount          Decimal  @db.Decimal(10, 2)
  status          WithdrawalStatus @default(pending)
  bankName        String?
  bankAccount     String?
  accountHolder   String?
  rejectedReason  String?
  processedAt     DateTime?

  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  consultant      ConsultantProfile @relation(fields: [consultantId], references: [id], onDelete: Cascade)

  @@index([consultantId])
  @@index([status])
  @@index([consultantId, status])
}
```

8. **新增枚举** (在 BookingStatus 枚举之后)：
```
enum EarningStatus {
  pending
  settled
  failed
}

enum WithdrawalStatus {
  pending
  processing
  completed
  rejected
}
```

9. **User 模型** 新增关系 (在现有关系区域添加)：
```
consultantReviews   ConsultantReview[]
consultantEarnings  ConsultantEarning[]
consultantWithdrawals ConsultantWithdrawal[]
```

10. **ServiceBooking** 新增关系：
```
review    ConsultantReview?
earning   ConsultantEarning?
```
</action>
<acceptance_criteria>
- `schema.prisma` contains `enum MessageType { text image file system proposal }`
- `schema.prisma` contains `model ConsultantReview` with fields: id, bookingId, userId, consultantId, rating, content, tags, beforeImages, afterImages, isAnonymous
- `schema.prisma` contains `model ConsultantAvailability` with fields: id, consultantId, dayOfWeek, startTime, endTime, slotDuration, isAvailable
- `schema.prisma` contains `model ConsultantEarning` with fields: id, consultantId, bookingId, amount, platformFee, netAmount, status, settlementDate
- `schema.prisma` contains `model ConsultantWithdrawal` with fields: id, consultantId, amount, status, bankName, bankAccount, accountHolder
- `schema.prisma` contains `enum EarningStatus { pending settled failed }`
- `schema.prisma` contains `enum WithdrawalStatus { pending processing completed rejected }`
- `ConsultantProfile` has `location` and `responseTimeAvg` fields
- `ServiceBooking` has `depositAmount`, `finalPaymentAmount`, `platformFee`, `consultantPayout` fields
- `npx prisma validate` exits 0 (run from apps/backend)
</acceptance_criteria>
</task>

<task id="08-01-02" type="code">
<read_first>
- apps/backend/src/modules/consultant/consultant.service.ts (现有服务模式：PrismaService 注入、asJson 辅助、分页模式)
- apps/backend/src/modules/consultant/dto/consultant.dto.ts (现有 DTO 验证模式)
- apps/backend/src/modules/consultant/consultant.controller.ts (现有控制器模式：@UseGuards(AuthGuard)、@Request() req)
</read_first>
<action>
创建需求提交 DTO 文件 `apps/backend/src/modules/consultant/dto/match.dto.ts`：

```typescript
import { IsString, IsOptional, IsEnum, IsNumber, Min, Max, IsArray } from "class-validator";
import { Type } from "class-transformer";
import { ApiProperty, ApiPropertyOptional } from "@nestjs/swagger";
import { ServiceTypeDto } from "./consultant.dto";

export class ConsultantMatchRequestDto {
  @ApiProperty({ description: "服务类型", enum: ServiceTypeDto })
  @IsEnum(ServiceTypeDto)
  serviceType!: ServiceTypeDto;

  @ApiPropertyOptional({ description: "预算范围下限", example: 200 })
  @IsOptional()
  @IsNumber()
  @Min(0)
  budgetMin?: number;

  @ApiPropertyOptional({ description: "预算范围上限", example: 1000 })
  @IsOptional()
  @IsNumber()
  @Min(0)
  budgetMax?: number;

  @ApiPropertyOptional({ description: "特殊要求", example: "希望擅长职场穿搭的顾问" })
  @IsOptional()
  @IsString()
  notes?: string;

  @ApiPropertyOptional({ description: "是否考虑线上服务", example: true })
  @IsOptional()
  preferOnline?: boolean = true;
}

export class MatchResultDto {
  consultantId!: string;
  studioName!: string;
  avatar!: string | null;
  specialties!: string[];
  rating!: number;
  reviewCount!: number;
  matchPercentage!: number;
  matchReasons!: string[];
  price?: number;
}
```

更新 `dto/index.ts` 导出新 DTO。
</action>
<acceptance_criteria>
- `apps/backend/src/modules/consultant/dto/match.dto.ts` exists
- File contains `class ConsultantMatchRequestDto` with `serviceType`, `budgetMin`, `budgetMax`, `notes`, `preferOnline` fields
- File contains `class MatchResultDto` with `consultantId`, `matchPercentage`, `matchReasons` fields
- `dto/index.ts` exports `ConsultantMatchRequestDto` and `MatchResultDto`
</acceptance_criteria>
</task>

<task id="08-01-03" type="code">
<read_first>
- apps/backend/src/modules/consultant/consultant.service.ts (现有服务模式)
- apps/backend/src/modules/consultant/dto/match.dto.ts (刚创建的匹配 DTO)
- apps/backend/prisma/schema.prisma (ConsultantProfile 模型结构：specialties Json, rating, location)
</read_first>
<action>
创建智能匹配服务 `apps/backend/src/modules/consultant/consultant-matching.service.ts`：

```typescript
import { Injectable, Logger } from "@nestjs/common";
import { PrismaService } from "../../common/prisma/prisma.service";
import { ConsultantMatchRequestDto, MatchResultDto } from "./dto";

const MATCH_WEIGHTS = {
  profile: 0.30,
  keywords: 0.25,
  specialty: 0.25,
  location: 0.20,
} as const;

const MATCH_REASONS = {
  profile: "擅长你的体型与风格",
  keywords: "符合你的需求关键词",
  specialty: "专长领域匹配",
  location: "距离最近",
  topRated: "评分最高",
} as const;

@Injectable()
export class ConsultantMatchingService {
  private readonly logger = new Logger(ConsultantMatchingService.name);

  constructor(private readonly prisma: PrismaService) {}

  async findMatches(userId: string, dto: ConsultantMatchRequestDto): Promise<MatchResultDto[]> {
    const consultants = await this.prisma.consultantProfile.findMany({
      where: { status: "active" },
      include: {
        user: { select: { id: true, nickname: true, avatar: true } },
        _count: { select: { bookings: true } },
      },
    });

    if (consultants.length === 0) {
      return [];
    }

    const userProfile = await this.getUserProfile(userId);
    const scored = consultants.map((consultant) => {
      const scores = {
        profile: this.calcProfileScore(userProfile, consultant),
        keywords: this.calcKeywordScore(dto, consultant),
        specialty: this.calcSpecialtyScore(dto, consultant),
        location: this.calcLocationScore(userProfile, consultant),
      };

      const totalScore =
        scores.profile * MATCH_WEIGHTS.profile +
        scores.keywords * MATCH_WEIGHTS.keywords +
        scores.specialty * MATCH_WEIGHTS.specialty +
        scores.location * MATCH_WEIGHTS.location;

      const matchPercentage = Math.round(totalScore * 100);
      const matchReasons = this.buildMatchReasons(scores, consultant);

      return {
        consultantId: consultant.id,
        studioName: consultant.studioName,
        avatar: consultant.avatar,
        specialties: (consultant.specialties as string[]) || [],
        rating: Number(consultant.rating),
        reviewCount: consultant.reviewCount,
        matchPercentage: Math.min(matchPercentage, 99),
        matchReasons,
      } as MatchResultDto;
    });

    scored.sort((a, b) => b.matchPercentage - a.matchPercentage);
    return scored.slice(0, 5);
  }

  private async getUserProfile(userId: string) {
    const profile = await this.prisma.userProfile.findUnique({
      where: { userId },
      include: { styleProfile: true },
    });
    return profile;
  }

  private calcProfileScore(userProfile: any, consultant: any): number {
    if (!userProfile?.styleProfile) return 0.5;
    const specialties = (consultant.specialties as string[]) || [];
    const stylePrefs = (userProfile.styleProfile.preferredStyles as string[]) || [];
    if (stylePrefs.length === 0) return 0.5;
    const overlap = stylePrefs.filter((s: string) =>
      specialties.some((sp: string) => sp.includes(s) || s.includes(sp))
    );
    return overlap.length / Math.max(stylePrefs.length, 1);
  }

  private calcKeywordScore(dto: ConsultantMatchRequestDto, consultant: any): number {
    if (!dto.notes) return 0.5;
    const specialties = (consultant.specialties as string[]) || [];
    const keywords = dto.notes.split(/[,，\s]+/).filter(Boolean);
    if (keywords.length === 0) return 0.5;
    const matches = keywords.filter((kw) =>
      specialties.some((sp: string) => sp.includes(kw) || kw.includes(sp))
    );
    return matches.length / Math.max(keywords.length, 1);
  }

  private calcSpecialtyScore(dto: ConsultantMatchRequestDto, consultant: any): number {
    const specialties = (consultant.specialties as string[]) || [];
    const serviceTypeMap: Record<string, string[]> = {
      styling_consultation: ["整体造型", "形象设计", "穿搭"],
      wardrobe_audit: ["衣橱管理", "整理", "搭配"],
      shopping_companion: ["购物", "陪购", "选品"],
      color_analysis: ["色彩", "色彩诊断", "配色"],
      special_event: ["场合", "活动", "婚礼", "面试"],
    };
    const targetKeywords = serviceTypeMap[dto.serviceType] || [];
    if (targetKeywords.length === 0) return 0.5;
    const matches = targetKeywords.filter((kw) =>
      specialties.some((sp: string) => sp.includes(kw))
    );
    return matches.length / targetKeywords.length;
  }

  private calcLocationScore(userProfile: any, consultant: any): number {
    if (!userProfile?.location || !consultant.location) return 0.5;
    return userProfile.location === consultant.location ? 1.0 : 0.3;
  }

  private buildMatchReasons(scores: Record<string, number>, consultant: any): string[] {
    const reasons: string[] = [];
    if (scores.profile >= 0.6) reasons.push(MATCH_REASONS.profile);
    if (scores.keywords >= 0.6) reasons.push(MATCH_REASONS.keywords);
    if (scores.specialty >= 0.6) reasons.push(MATCH_REASONS.specialty);
    if (scores.location >= 0.8) reasons.push(MATCH_REASONS.location);
    if (Number(consultant.rating) >= 4.5) reasons.push(MATCH_REASONS.topRated);
    if (reasons.length === 0) reasons.push(MATCH_REASONS.specialty);
    return reasons.slice(0, 3);
  }
}
```
</action>
<acceptance_criteria>
- `apps/backend/src/modules/consultant/consultant-matching.service.ts` exists
- File contains `class ConsultantMatchingService` with `findMatches` method
- `findMatches` accepts `userId: string` and `ConsultantMatchRequestDto`, returns `Promise<MatchResultDto[]>`
- Method queries `consultantProfile.findMany({ where: { status: "active" } })`
- Method calculates four-dimension scores: profile, keywords, specialty, location
- Method returns max 5 results sorted by matchPercentage descending
- Each result includes `matchPercentage` (0-99) and `matchReasons` (string array)
</acceptance_criteria>
</task>

<task id="08-01-04" type="code">
<read_first>
- apps/backend/src/modules/consultant/consultant.controller.ts (现有控制器模式)
- apps/backend/src/modules/consultant/consultant-matching.service.ts (刚创建的匹配服务)
- apps/backend/src/modules/consultant/consultant.module.ts (现有模块注册模式)
</read_first>
<action>
1. 在 `consultant.controller.ts` 新增匹配端点：

```typescript
@Post("match")
@UseGuards(AuthGuard)
@ApiBearerAuth()
@ApiOperation({ summary: "智能匹配顾问", description: "根据用户需求智能匹配最合适的造型顾问" })
@ApiResponse({ status: 200, description: "返回匹配的顾问列表" })
async matchConsultants(
  @Request() req: RequestWithUser,
  @Body() dto: ConsultantMatchRequestDto,
) {
  return this.consultantMatchingService.findMatches(req.user.id, dto);
}
```

2. 在 `consultant.module.ts` 的 providers 中注册 `ConsultantMatchingService`：
```typescript
providers: [ConsultantService, ConsultantMatchingService],
```

3. 在 `consultant.controller.ts` 构造函数注入 `ConsultantMatchingService`：
```typescript
constructor(
  private readonly consultantService: ConsultantService,
  private readonly consultantMatchingService: ConsultantMatchingService,
) {}
```

4. 在 `consultant.controller.ts` 导入 `ConsultantMatchRequestDto`：
```typescript
import { ..., ConsultantMatchRequestDto } from "./dto";
```
</action>
<acceptance_criteria>
- `consultant.controller.ts` contains `@Post("match")` endpoint
- Endpoint has `@UseGuards(AuthGuard)` and `@ApiBearerAuth()` decorators
- Endpoint calls `this.consultantMatchingService.findMatches(req.user.id, dto)`
- `consultant.module.ts` providers array includes `ConsultantMatchingService`
- `consultant.controller.ts` constructor injects `ConsultantMatchingService`
</acceptance_criteria>
</task>

<task id="08-01-05" type="test">
<read_first>
- apps/backend/src/modules/consultant/consultant-matching.service.ts (刚创建的匹配服务)
- apps/backend/src/modules/consultant/consultant.service.ts (现有测试模式参考)
</read_first>
<action>
创建匹配服务单元测试 `apps/backend/src/modules/consultant/consultant-matching.service.spec.ts`：

测试用例：
1. `findMatches` 应返回最多 5 个结果，按 matchPercentage 降序排列
2. `findMatches` 无 active 顾问时应返回空数组
3. `calcProfileScore` 用户画像与顾问专长有重叠时应返回高分
4. `calcKeywordScore` 需求关键词与顾问专长匹配时应返回高分
5. `calcSpecialtyScore` 服务类型与顾问专长匹配时应返回高分
6. `calcLocationScore` 同城顾问应返回 1.0，不同城应返回 0.3
7. `buildMatchReasons` 应根据各维度分数生成匹配理由
8. `findMatches` 每个结果应包含 matchPercentage (0-99) 和 matchReasons 数组

使用 Jest mock PrismaService，mock `consultantProfile.findMany` 返回测试数据，mock `userProfile.findUnique` 返回用户画像数据。
</action>
<acceptance_criteria>
- `apps/backend/src/modules/consultant/consultant-matching.service.spec.ts` exists
- File contains `describe('ConsultantMatchingService')` with at least 6 test cases
- `cd apps/backend && npx jest --testPathPattern="consultant-matching" --passWithNoTests` exits 0
</acceptance_criteria>
</task>

<task id="08-01-06" type="blocking">
<read_first>
- apps/backend/prisma/schema.prisma (确认 schema 变更已保存)
</read_first>
<action>
运行 Prisma schema push 将所有模型变更同步到数据库：

```bash
cd apps/backend && npx prisma db push
```

如果遇到交互式提示（如数据丢失确认），使用：
```bash
cd apps/backend && npx prisma db push --accept-data-loss
```

然后重新生成 Prisma Client：
```bash
cd apps/backend && npx prisma generate
```
</action>
<acceptance_criteria>
- `cd apps/backend && npx prisma db push` exits 0 (or with --accept-data-loss)
- `cd apps/backend && npx prisma generate` exits 0
- `cd apps/backend && npx prisma validate` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `cd apps/backend && npx prisma validate` — Schema 验证通过
2. `cd apps/backend && npx prisma generate` — Prisma Client 生成成功
3. `cd apps/backend && npx jest --testPathPattern="consultant-matching" --passWithNoTests` — 匹配算法测试通过
4. `cd apps/backend && npx tsc --noEmit` — TypeScript 编译无错误
5. `grep -r "POST.*consultant/match" apps/backend/src` — 匹配端点已注册
</verification>

<success_criteria>
- Prisma Schema 包含 ConsultantReview、ConsultantAvailability、ConsultantEarning、ConsultantWithdrawal 四个新模型
- MessageType 枚举新增 proposal 类型
- ServiceBooking 新增分阶段付款字段
- ConsultantProfile 新增 location、responseTimeAvg 字段
- POST /consultant/match 端点可用，返回 3-5 个匹配顾问
- 每个匹配结果包含 matchPercentage 和 matchReasons
- 所有测试通过
</success_criteria>

<must_haves>
- 四维匹配算法正确计算匹配度（画像30%+关键词25%+专长25%+位置20%）
- 匹配结果展示匹配度百分比和匹配理由
- Prisma Schema 变更已 push 到数据库
</must_haves>
