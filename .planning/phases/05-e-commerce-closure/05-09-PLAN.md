---
phase: 05-e-commerce-closure
plan: 09
type: execute
wave: 5
depends_on: ["05-06", "05-07"]
files_modified:
  - apps/mobile/src/screens/MerchantApplyScreen.tsx
  - apps/mobile/src/screens/StockNotificationScreen.tsx
autonomous: true
requirements:
  - COMM-09
  - COMM-10
  - COMM-05

must_haves:
  truths:
    - "User can submit merchant application with auto-validation"
    - "User can view application status (pending/approved/rejected)"
    - "User can manage stock notification subscriptions"
    - "Merchant apply has real-time field validation per D-09"
  artifacts:
    - path: "apps/mobile/src/screens/MerchantApplyScreen.tsx"
      provides: "Merchant application form with auto-validation"
      contains: "营业执照"
    - path: "apps/mobile/src/screens/StockNotificationScreen.tsx"
      provides: "Stock notification subscription management"
      contains: "subscribeStockNotification"
  key_links:
    - from: "apps/mobile/src/screens/MerchantApplyScreen.tsx"
      to: "apps/mobile/src/services/api/commerce.api.ts"
      via: "applyForMerchant, getMerchantApplicationStatus"
      pattern: "applyForMerchant"
---

<objective>
Create MerchantApplyScreen for merchant onboarding with auto-validation (COMM-09/10 per D-09/D-11). Create StockNotificationScreen for managing stock notification subscriptions (COMM-05 per D-10).

Purpose: Merchant onboarding enables the platform + partner merchant model. Stock notification keeps users engaged with out-of-stock items.
Output: 2 new screens
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e-commerce-closure/05-CONTEXT.md
@.planning/phases/05-e-commerce-closure/05-UI-SPEC.md
@apps/mobile/src/services/api/commerce.api.ts
</context>

<interfaces>
<!-- From Plan 05: API layer -->

API methods available:
- `applyForMerchant(data: { brandName, businessLicense, contactName, phone, description })` -- POST /api/v1/merchants/apply
- `getMerchantApplicationStatus()` -- GET /api/v1/merchants/application
- `subscribeStockNotification(itemId, color?, size?)` -- POST /api/v1/stock-notifications
- `unsubscribeStockNotification(id)` -- DELETE /api/v1/stock-notifications/:id
- `getStockNotifications()` -- GET /api/v1/stock-notifications

UI-SPEC Design Contract (Screen 6 - Merchant Application):
- Form fields with inline validation
- Status page: spinner / success icon / error icon
- Copy: "申请已提交，我们将在1-3个工作日内完成审核"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Merchant application screen with auto-validation</name>
  <files>
    apps/mobile/src/screens/MerchantApplyScreen.tsx
  </files>
  <read_first>
    - apps/mobile/src/services/api/commerce.api.ts
    - .planning/phases/05-e-commerce-closure/05-UI-SPEC.md (Screen 6)
    - .planning/phases/05-e-commerce-closure/05-CONTEXT.md (D-09, D-11)
  </read_first>
  <action>
Create `apps/mobile/src/screens/MerchantApplyScreen.tsx`:

1. **Application form** with fields (use TextInput from React Native Paper):
   - 品牌名称: TextInput, required, uniqueness validation (debounced 500ms API call to check existing brands -- call applyForMerchant and catch duplicate error, or add a dedicated check endpoint call)
   - 营业执照号: TextInput, required, 18 chars, format validation (regex `^[0-9A-HJ-NP-RTUW-Y]{2}\d{6}[0-9A-HJ-NP-RTUW-Y]{10}$`). Show checkmark (green) or X (red) icon inline as user types.
   - 联系人: TextInput, required
   - 手机号: TextInput, required, 11 digits, starts with 1 (regex `^1[3-9]\d{9}$`). Show checkmark/X inline.
   - 品牌简介: TextInput multiline, max 500 chars, show character count

2. **Auto-validation per D-09**: Real-time inline validation:
   - 营业执照号: Show green checkmark or red X as user types
   - 手机号: Show green checkmark or red X as user types
   - 品牌名: Show "品牌名已存在" error text if duplicate (debounced)
   - All validation errors shown inline below each field

3. **Submit**: "提交申请" button (Accent #FF4D4F, full width). Disabled until all fields valid. On submit:
   - Call applyForMerchant API
   - Show loading state on button
   - On success: Navigate to status view

4. **Status page** (shown after submission or if user already applied):
   - Call getMerchantApplicationStatus on mount
   - If status is pending: Spinner + "申请审核中" text
   - If status is approved: Checkmark icon (#52C41A) + "恭喜！您的商家申请已通过" + "进入商家后台" button (placeholder for now)
   - If status is rejected: Error icon (#FF4D4F) + "很抱歉，您的申请未通过。原因：{reason}" + "重新申请" button -> clears form

5. **Navigation entry**: Note in code comment that this screen should be accessible from ProfileScreen settings section (actual navigation wiring done separately).
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `MerchantApplyScreen.tsx` exists
    - Form has fields: 品牌名称, 营业执照号, 联系人, 手机号, 品牌简介
    - 营业执照号 has inline validation with checkmark/X icons
    - 手机号 has inline format validation
    - 品牌名 has debounced uniqueness check
    - Submit button disabled until fields valid
    - Status page shows 审核中/已通过/已拒绝 states
    - TypeScript compiles without new errors
  </acceptance_criteria>
  <done>Merchant application screen complete with 5-field form, real-time auto-validation (business license regex, phone format, brand name uniqueness), submission flow, and status display (pending/approved/rejected).</done>
</task>

<task type="auto">
  <name>Task 2: Stock notification subscription screen</name>
  <files>
    apps/mobile/src/screens/StockNotificationScreen.tsx
  </files>
  <read_first>
    - apps/mobile/src/services/api/commerce.api.ts
    - .planning/phases/05-e-commerce-closure/05-CONTEXT.md (D-10)
  </read_first>
  <action>
Create `apps/mobile/src/screens/StockNotificationScreen.tsx`:

1. **Subscription list**: FlatList of user's stock notification subscriptions. Each item shows:
   - Item thumbnail (50px, rounded)
   - Item name (14px/500)
   - Subscribed color/size label (12px, #999)
   - Status badge: 等待中 (yellow #FAAD14) / 已通知 (green #52C41A) / 已取消 (gray #CCCCCC)
   - For PENDING items: "取消通知" text button (red) -> calls unsubscribeStockNotification API -> removes from list

2. **Empty state**: When no subscriptions:
   - Bell-off icon (MaterialCommunityIcons `bell-off-outline`, 64px, #CCCCCC)
   - "暂无到货通知" heading (18px/600)
   - "去逛逛" button (Accent) -> navigate to Home

3. **Data loading**: On mount, call getStockNotifications API. Pull-to-refresh supported.

4. **Navigation entry**: Note in code comment that this screen should be accessible from ProfileScreen or notification settings.
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `StockNotificationScreen.tsx` exists
    - Subscription list shows item thumbnail, name, color/size, status badge
    - "取消通知" button works for PENDING items
    - Empty state shows "暂无到货通知" with "去逛逛" button
    - Pull-to-refresh works
    - TypeScript compiles without new errors
  </acceptance_criteria>
  <done>Stock notification screen complete with subscription list showing status badges, cancel functionality for pending subscriptions, and empty state with navigation to home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> Merchant apply | PII data (business license, phone number) |
| user -> Stock notification | Item subscription data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-29 | I (Info Disclosure) | Merchant PII | accept | PII encrypted at rest by backend EncryptionService |
| T-05-30 | D (Denial of Service) | Notification spam | mitigate | Unique constraint per userId+itemId+color+size enforced by backend |
</threat_model>

<verification>
1. `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` -- TypeScript must compile
2. Verify MerchantApplyScreen has form with validation
3. Verify MerchantApplyScreen has status view (pending/approved/rejected)
4. Verify StockNotificationScreen has subscription list with cancel
5. Verify StockNotificationScreen has empty state
</verification>

<success_criteria>
- MerchantApplyScreen: form with auto-validation, submit flow, status display
- StockNotificationScreen: subscription list with status badges, cancel, empty state
- All TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-e-commerce-closure/05-09-SUMMARY.md`
</output>
