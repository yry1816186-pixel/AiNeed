---
phase: 05-e-commerce-closure
plan: 08
type: execute
wave: 5
depends_on: ["05-06", "05-07"]
files_modified:
  - apps/mobile/src/screens/OrderDetailScreen.tsx
  - apps/mobile/src/screens/OrdersScreen.tsx
  - apps/mobile/src/components/OrderTimeline.tsx
  - apps/mobile/src/components/RefundRequestForm.tsx
autonomous: true
requirements:
  - COMM-08
  - COMM-12
  - COMM-13

must_haves:
  truths:
    - "Order detail shows vertical timeline with status nodes"
    - "User can confirm receipt for shipped orders"
    - "User can request refund (refund-only or return-refund)"
    - "Orders screen has tab navigation for order status filtering"
    - "Logistics tracking is viewable for shipped orders"
  artifacts:
    - path: "apps/mobile/src/screens/OrderDetailScreen.tsx"
      provides: "Order detail with timeline, actions, refund entry"
      contains: "OrderTimeline"
    - path: "apps/mobile/src/screens/OrdersScreen.tsx"
      provides: "Tab-based order listing"
      contains: "退款"
    - path: "apps/mobile/src/components/OrderTimeline.tsx"
      provides: "Vertical timeline with status nodes"
      contains: "timeline"
    - path: "apps/mobile/src/components/RefundRequestForm.tsx"
      provides: "Refund type selection + reason + image upload"
      contains: "REFUND_ONLY"
  key_links:
    - from: "apps/mobile/src/screens/OrderDetailScreen.tsx"
      to: "apps/mobile/src/services/api/commerce.api.ts"
      via: "confirmReceipt, createRefund, getTrackingInfo"
      pattern: "confirmReceipt"
---

<objective>
Enhance OrderDetailScreen with vertical timeline, confirm receipt, refund entry, and logistics tracking (COMM-08 per D-17/D-18, COMM-12, COMM-13). Enhance OrdersScreen with tab navigation and contextual actions.

Purpose: Complete the order lifecycle management from viewing to confirming receipt to requesting refunds.
Output: Enhanced OrderDetailScreen, OrdersScreen, 2 new components
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e-commerce-closure/05-CONTEXT.md
@.planning/phases/05-e-commerce-closure/05-UI-SPEC.md
@apps/mobile/src/screens/OrderDetailScreen.tsx
@apps/mobile/src/screens/OrdersScreen.tsx
@apps/mobile/src/services/api/commerce.api.ts
@apps/mobile/src/stores/orderStore.ts
</context>

<interfaces>
<!-- From Plan 05: API layer + stores -->

API methods available:
- `confirmReceipt(orderId)` -- PATCH /api/v1/orders/:id/confirm
- `softDeleteOrder(orderId)` -- DELETE /api/v1/orders/:id
- `getOrdersByTab(tab, page, limit)` -- GET /api/v1/orders/tab/:tab
- `createRefund(data)` -- POST /api/v1/refund-requests
- `getOrderRefunds(orderId)` -- GET /api/v1/refund-requests/order/:orderId
- `addRefundTracking(id, trackingNumber)` -- PATCH /api/v1/refund-requests/:id/tracking

Store available:
- `useOrderStore` -- fetchOrdersByTab, confirmReceipt, softDeleteOrder, currentTab

UI-SPEC Design Contract (Screen 5 - Order Detail):
- Timeline: vertical line with filled/outlined circles, Accent for current status
- Action buttons change by status: cancel/confirm receipt/refund/buy again
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Order detail with timeline, refund, logistics</name>
  <files>
    apps/mobile/src/screens/OrderDetailScreen.tsx,
    apps/mobile/src/components/OrderTimeline.tsx,
    apps/mobile/src/components/RefundRequestForm.tsx
  </files>
  <read_first>
    - apps/mobile/src/screens/OrderDetailScreen.tsx
    - apps/mobile/src/services/api/commerce.api.ts
    - .planning/phases/05-e-commerce-closure/05-UI-SPEC.md (Screen 5)
    - .planning/phases/05-e-commerce-closure/05-CONTEXT.md (D-17, D-18)
  </read_first>
  <action>
Create supporting components first:

1. **`OrderTimeline.tsx`** per D-18: Vertical timeline component:
   - Props: events [{status, time, description, trackingNumber?, carrier?}]
   - Vertical line (2px, #E0E0E0) on the left
   - Each node: circle + status text + time + description
   - Current status: filled circle (Accent #FF4D4F) + bold text
   - Past statuses: filled circle (#52C41A green checkmark) + normal text
   - Future statuses: outlined circle (#E0E0E0) + light gray text
   - Tracking number: tappable, copy to clipboard with Toast feedback

2. **`RefundRequestForm.tsx`** per D-17: Refund application form:
   - Props: order (with items and amounts), onSubmit callback, onCancel callback
   - Type selector: Two radio-like buttons "仅退款" / "退货退款"
   - Item selection: checkboxes for items to refund (default all selected)
   - Reason dropdown: 不想要了 / 商品质量问题 / 与描述不符 / 其他
   - Description: TextInput multiline, max 200 chars
   - Image upload: Up to 3 images, tap to add from camera/album (required for 退货退款 type)
   - Amount display: Auto-calculated from selected items, not editable (shows "¥XX.XX")
   - Submit: "提交申请" button, calls createRefund API

Enhance `apps/mobile/src/screens/OrderDetailScreen.tsx`:

3. **Vertical timeline** (D-18): Replace current simple status display with OrderTimeline component. Build events array from order tracking data.

4. **Action buttons by status**:
   - PENDING: "取消订单" (destructive, red outlined)
   - PAID: "申请退款" + "申请退货退款" (both outlined)
   - SHIPPED: "确认收货" (primary, Accent filled) + "查看物流" (text link)
   - COMPLETED: "再次购买" (outlined) + "申请退款" (if within 7 days)

5. **Refund entry** (D-17): Tapping "申请退款" or "申请退货退款" opens RefundRequestForm as Modal.

6. **Logistics tracking**: "查看物流" button navigates to logistics detail view (can be inline section expanding below timeline, showing carrier + tracking number + trace history from getTrackingInfo API or order tracking data).

7. **Confirm receipt**: "确认收货" button shows confirmation Dialog: "确认已收到商品？". On confirm, calls confirmReceipt API, updates order status.
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `OrderTimeline.tsx` exists with vertical line and status nodes (current/past/future)
    - `RefundRequestForm.tsx` exists with type selector (REFUND_ONLY/RETURN_REFUND), reason dropdown, image upload
    - `OrderDetailScreen.tsx` uses OrderTimeline for status display
    - `OrderDetailScreen.tsx` has "确认收货" button for SHIPPED orders
    - `OrderDetailScreen.tsx` has "申请退款"/"申请退货退款" for PAID orders
    - `OrderDetailScreen.tsx` has logistics tracking section
    - `RefundRequestForm.tsx` amount is auto-calculated and not editable
    - TypeScript compiles without new errors
  </acceptance_criteria>
  <done>Order detail screen enhanced with vertical status timeline, conditional action buttons, refund request form (dual type), confirm receipt dialog, and logistics tracking display.</done>
</task>

<task type="auto">
  <name>Task 2: Orders screen with tab navigation</name>
  <files>
    apps/mobile/src/screens/OrdersScreen.tsx
  </files>
  <read_first>
    - apps/mobile/src/screens/OrdersScreen.tsx
    - apps/mobile/src/stores/orderStore.ts
    - apps/mobile/src/services/api/commerce.api.ts
    - .planning/phases/05-e-commerce-closure/05-UI-SPEC.md
  </read_first>
  <action>
Enhance `apps/mobile/src/screens/OrdersScreen.tsx`:

1. **Tab navigation**: Add horizontal tabs at top: 全部 / 待支付 / 待发货 / 待收货 / 退款. Use ScrollableTabView or custom horizontal ScrollView with Chip components. Each tab calls getOrdersByTab(tab) from orderStore.

2. **Tab to status mapping**:
   - 全部 -> tab='all'
   - 待支付 -> tab='pending'
   - 待发货 -> tab='paid'
   - 待收货 -> tab='shipped'
   - 退款 -> tab='refund'

3. **Order card contextual actions**: Each order card shows status badge and action buttons based on status:
   - 待支付: "取消订单" + "去支付" buttons
   - 待发货: "提醒发货" (placeholder, just shows toast "已提醒商家")
   - 待收货: "确认收货" + "查看物流" buttons
   - 已完成: "再次购买" + "删除" buttons
   - 退款中: "查看退款详情" button

4. **Delete order**: Swipe left on completed/cancelled orders reveals delete action. Confirm dialog: "确定删除此订单？". Calls softDeleteOrder API.

5. **Pull-to-refresh**: FlatList with RefreshControl, calls fetchOrdersByTab on refresh.

6. **Empty state per tab**: When a tab has no orders, show "暂无相关订单" centered text.
  </action>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `OrdersScreen.tsx` has tab navigation: 全部/待支付/待发货/待收货/退款
    - Each tab filters orders by calling getOrdersByTab
    - Order cards show contextual action buttons based on status
    - "确认收货" button visible for 待收货 orders
    - Swipe-to-delete works on completed/cancelled orders
    - Pull-to-refresh works
    - Empty state shown when tab has no orders
    - TypeScript compiles without new errors
  </acceptance_criteria>
  <done>Orders screen enhanced with 5-tab navigation (全部/待支付/待发货/待收货/退款), contextual action buttons per order status, swipe-to-delete, pull-to-refresh, and empty states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> Order actions | Confirm receipt, refund requests |
| user -> Refund form | Image uploads, text input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-26 | T (Tampering) | Refund amount | mitigate | Amount auto-calculated from order items, not user-editable |
| T-05-27 | D (Denial of Service) | Multiple refund requests | mitigate | Disable refund button after first request submitted |
| T-05-28 | R (Repudiation) | Confirm receipt | mitigate | Server records completedAt timestamp |
</threat_model>

<verification>
1. `cd C:/AiNeed/apps/mobile && npx tsc --noEmit` -- TypeScript must compile
2. Verify OrderTimeline renders with vertical line and status nodes
3. Verify RefundRequestForm has type selector, reason dropdown, image upload
4. Verify OrderDetailScreen has timeline, refund entry, confirm receipt, logistics
5. Verify OrdersScreen has 5 tabs with contextual actions
</verification>

<success_criteria>
- OrderDetailScreen: vertical timeline, conditional actions, refund form, confirm receipt, logistics
- OrdersScreen: 5-tab navigation, swipe-to-delete, pull-to-refresh, contextual actions
- All TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-e-commerce-closure/05-08-SUMMARY.md`
</output>
