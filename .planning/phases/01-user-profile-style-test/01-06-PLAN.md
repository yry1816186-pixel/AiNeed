---
phase: 01-user-profile-style-test
plan: 06
type: execute
wave: 6
depends_on: [5]
files_modified:
  - apps/backend/tests/integration/phase1-auth-flow.integration.test.ts
  - apps/backend/tests/integration/phase1-quiz-flow.integration.test.ts
  - apps/backend/tests/integration/phase1-profile-sync.integration.test.ts
  - apps/mobile/e2e/phase1-onboarding.e2e.test.ts
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06, PROF-07, PROF-08, PROF-09, PROF-10, PROF-11, PROF-12, PROF-13]

must_haves:
  truths:
    - "Phone registration -> login -> profile -> quiz -> analysis -> poster full backend flow passes"
    - "Mobile E2E: register -> onboarding -> profile -> quiz complete flow passes"
    - "Profile change events reach AI stylist and recommendations module subscribers"
  artifacts:
    - path: "apps/backend/tests/integration/phase1-auth-flow.integration.test.ts"
      provides: "End-to-end phone registration and login integration test"
      contains: "phone-login"
    - path: "apps/backend/tests/integration/phase1-quiz-flow.integration.test.ts"
      provides: "End-to-end quiz + color derivation integration test"
      contains: "batchSubmit"
    - path: "apps/backend/tests/integration/phase1-profile-sync.integration.test.ts"
      provides: "Profile change event propagation integration test"
      contains: "profile:updated"
    - path: "apps/mobile/e2e/phase1-onboarding.e2e.test.ts"
      provides: "Mobile E2E test for full onboarding flow"
      contains: "onboarding"
  key_links:
    - from: "apps/backend/tests/integration/phase1-auth-flow.integration.test.ts"
      to: "apps/backend/src/modules/auth/auth.controller.ts"
      via: "Tests all auth endpoints"
      pattern: "auth/phone|auth/wechat"
    - from: "apps/backend/tests/integration/phase1-quiz-flow.integration.test.ts"
      to: "apps/backend/src/modules/style-quiz/style-quiz.controller.ts"
      via: "Tests quiz + progress + color derivation flow"
      pattern: "quizzes|progress|batch"
    - from: "apps/backend/tests/integration/phase1-profile-sync.integration.test.ts"
      to: "apps/backend/src/modules/profile/services/profile-event-emitter.service.ts"
      via: "Verifies event publication and subscription"
      pattern: "profile:updated|quiz:completed"
---

# Plan 06: Integration Testing

<objective>
Write comprehensive integration tests that verify the complete Phase 1 flow
end-to-end. Test the full backend API chain (registration -> login -> profile ->
quiz -> analysis -> poster) and the full mobile onboarding flow (registration ->
basic info -> photo -> quiz -> report). Verify cross-module event propagation.

Purpose: Integration tests prove that all the pieces built in Plans 01-05 work
correctly when connected together. They catch wiring errors, data format mismatches,
and cross-module communication failures that unit tests miss.

Output: 3 backend integration test suites and 1 mobile E2E test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-user-profile-style-test/01-RESEARCH.md
@.planning/phases/01-user-profile-style-test/01-05-SUMMARY.md

<interfaces>
<!-- Full API surface to test -->

POST /api/v1/auth/send-code { phone }
POST /api/v1/auth/phone-register { phone, code, nickname? } -> { accessToken, refreshToken, user }
POST /api/v1/auth/phone-login { phone, code } -> { accessToken, refreshToken, user }
POST /api/v1/auth/wechat { code } -> { accessToken, refreshToken, user }
PATCH /api/v1/profile { gender, birthDate, ... } -> { data: profile }
GET /api/v1/profile/completeness -> { data: { percentage, missingFields } }
GET /api/v1/profile/body-analysis -> { data: bodyAnalysis }
GET /api/v1/profile/color-analysis -> { data: colorAnalysis }
POST /api/v1/profile/share-poster -> { data: { imageUrl } }
GET /api/v1/quizzes/:quizId -> { data: quiz }
GET /api/v1/quizzes/:quizId/progress -> { data: progress }
POST /api/v1/quizzes/:quizId/progress { questionIndex, answers }
POST /api/v1/quizzes/:quizId/batch-answers { answers[] } -> { data: result }
GET /api/v1/quizzes/:quizId/result -> { data: result }
POST /api/v1/photos/upload (multipart) -> { data: photo }

<!-- Redis Pub/Sub channels to verify -->
profile:updated -> { userId, changedFields, timestamp }
quiz:completed -> { userId, quizId, timestamp }
</interfaces>
</context>

<tasks>

### Task 1: Backend API Integration Tests

<type>create</type>
<files>
- apps/backend/tests/integration/phase1-auth-flow.integration.test.ts
- apps/backend/tests/integration/phase1-quiz-flow.integration.test.ts
- apps/backend/tests/integration/phase1-profile-sync.integration.test.ts
</files>
<read_first>
- apps/backend/tests/ (existing test patterns and setup)
- apps/backend/jest.config.js
- apps/backend/src/modules/auth/auth.controller.ts
- apps/backend/src/modules/style-quiz/style-quiz.controller.ts
- apps/backend/src/modules/profile/profile.controller.ts
</read_first>
<action>
Read existing integration test patterns in the backend test directory.

**File 1: `tests/integration/phase1-auth-flow.integration.test.ts`**

Test suite covering the full phone registration -> login -> profile flow:
1. `POST /auth/send-code` with valid phone -> returns 201, code stored in Redis
2. `POST /auth/phone-register` with phone + code -> returns 201 with accessToken + refreshToken + user
3. `POST /auth/phone-login` with phone + code -> returns 200 with tokens
4. New user has authProvider='phone' in database
5. Register with same phone again -> returns 409 Conflict
6. `POST /auth/phone-login` with wrong code -> returns 401
7. Rate limiting: 6th request within 60 seconds -> returns 429
8. `POST /auth/wechat` with mock WeChat code -> returns 200 with tokens
9. New WeChat user has wechatOpenId set in database
10. Access protected endpoint with phone user token -> returns 200

Use test database (Prisma test instance) and mock Redis for SMS codes.
Mock WeChat API responses for WeChat login tests.

**File 2: `tests/integration/phase1-quiz-flow.integration.test.ts`**

Test suite covering the full quiz flow:
1. `GET /quizzes/default` -> returns quiz with 6 questions, each with 4-6 options
2. `POST /quizzes/default/progress` with questionIndex=0, answers={} -> returns 201
3. `GET /quizzes/default/progress` -> returns { questionIndex: 0, answers: {} }
4. For each question: select an option, save progress, verify progress updated
5. `POST /quizzes/default/batch-answers` with all 6 answers -> returns 200 with result
6. Result contains topStyles (array with style + score) and colorPalette (array with hex + name + category)
7. Color palette has at least 4 entries
8. `GET /quizzes/default/result` -> returns same result as batch submit
9. Progress is cleared after batch submit (quiz completed)

Use seeded quiz data from Plan 01 Task 3.

**File 3: `tests/integration/phase1-profile-sync.integration.test.ts`**

Test suite covering cross-module event propagation:
1. `PATCH /profile` with { nickname: 'test' } -> profile updated
2. Verify `profile:updated` event published to Redis with correct userId and changedFields=['nickname']
3. `POST /quizzes/default/batch-answers` with valid answers -> result saved
4. Verify `quiz:completed` event published to Redis with correct userId and quizId
5. Verify AI Stylist subscriber receives profile:updated event (mock subscriber)
6. Verify Recommendations subscriber receives quiz:completed event (mock subscriber)
7. `GET /profile/completeness` after basic registration -> returns percentage < 100% with missingFields
8. `GET /profile/completeness` after filling all data -> returns 100%

Use Redis subscriber mock to capture published events.
</action>
<acceptance_criteria>
- `apps/backend/tests/integration/phase1-auth-flow.integration.test.ts` exists with 10+ test cases
- Tests cover phone register, phone login, rate limiting, WeChat login, protected endpoint access
- `apps/backend/tests/integration/phase1-quiz-flow.integration.test.ts` exists with 9+ test cases
- Tests cover quiz retrieval, progress save/restore, batch submit, color derivation, result retrieval
- `apps/backend/tests/integration/phase1-profile-sync.integration.test.ts` exists with 8+ test cases
- Tests cover profile:updated event, quiz:completed event, subscriber notification, completeness calculation
- All integration tests pass: `cd apps/backend && npx jest --testPathPattern=integration/phase1 --maxWorkers=2`
</acceptance_criteria>

### Task 2: Mobile E2E Test

<type>create</type>
<files>
- apps/mobile/e2e/phase1-onboarding.e2e.test.ts
</files>
<read_first>
- apps/mobile/e2e/ (existing E2E test patterns, if any)
- apps/mobile/src/screens/OnboardingScreen.tsx
- apps/mobile/src/screens/LoginScreen.tsx
- apps/mobile/src/screens/StyleQuizScreen.tsx
</read_first>
<action>
Create an E2E test for the full mobile onboarding flow.
Use Detox (per CC-24) or the project's existing E2E framework.

**Test flow:**

1. Launch app
2. On LoginScreen: tap "手机号登录" button
3. Enter phone number "13800138000"
4. Tap "获取验证码" button
5. Enter verification code (use test code from mock SMS service)
6. Tap "登录/注册" button
7. On OnboardingScreen Step 0 (BASIC_INFO):
   - Select gender "女"
   - Select age range "25-30"
   - Tap "下一步"
   - Verify skip button is NOT visible on this step
8. On OnboardingScreen Step 1 (PHOTO):
   - Verify PhotoGuideOverlay is NOT visible (not in camera mode by default)
   - Verify PrivacyConsentModal appears on photo selection attempt
   - Tap "跳过这一步"
9. On OnboardingScreen Step 2 (QUIZ):
   - Tap "跳过" to skip quiz
10. Verify navigation to HomeScreen
11. On HomeScreen: verify ProfileCompletenessBar is visible (completeness < 80%)
12. Navigate to ProfileScreen
13. Verify completeness indicator shows
14. Verify "编辑画像" link visible
15. Navigate to StyleQuizScreen
16. Verify quiz loads with questions
17. Select an option, verify auto-advance to next question
18. Complete all questions (or skip remaining)
19. Verify color derivation result displayed

If Detox is not configured, write the test as a test plan with explicit manual steps
and automated component render tests using React Testing Library instead.
</action>
<acceptance_criteria>
- `apps/mobile/e2e/phase1-onboarding.e2e.test.ts` exists
- Test covers: login -> basic info (required) -> photo (skip) -> quiz (skip) -> home -> profile
- Test verifies: gender/age required, photo skip available, quiz skip available, completeness banner
- If Detox configured: `cd apps/mobile && npx detox test phase1-onboarding` passes
- If not Detox: component render tests exist for each screen state
</acceptance_criteria>

</tasks>

<verification>
1. `cd apps/backend && npx jest --testPathPattern=integration/phase1 --maxWorkers=2` -- all backend integration tests pass
2. `cd apps/backend && npx jest --coverage --testPathPattern=modules --maxWorkers=2` -- coverage maintained
3. Verify auth flow: register -> login -> access protected endpoint
4. Verify quiz flow: get quiz -> answer questions -> get result with color derivation
5. Verify event flow: profile update -> event published -> subscribers notified
6. Mobile E2E test exists and can run (or manual test plan documented)
</verification>

<success_criteria>
- [ ] Phone registration -> login -> profile creation full backend flow passes
- [ ] Rate limiting on auth endpoints verified (5/min)
- [ ] Quiz retrieval -> progress save -> batch submit -> color derivation flow passes
- [ ] Profile completeness returns correct percentage and missing fields
- [ ] Share poster generation returns valid image URL
- [ ] Profile update publishes profile:updated event
- [ ] Quiz completion publishes quiz:completed event
- [ ] AI Stylist subscriber receives profile:updated events
- [ ] Recommendations subscriber receives quiz:completed events
- [ ] Mobile E2E: onboarding full flow (register -> basic info -> skip photo -> skip quiz -> home)
- [ ] HomeScreen shows completeness banner when profile incomplete
- [ ] All integration tests pass green
</success_criteria>

<output>
After completion, create `.planning/phases/01-user-profile-style-test/01-06-SUMMARY.md`
</output>
