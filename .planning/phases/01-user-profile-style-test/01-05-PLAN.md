---
phase: 01-user-profile-style-test
plan: 05
type: execute
wave: 5
depends_on: [4]
files_modified:
  - apps/mobile/src/screens/OnboardingScreen.tsx
  - apps/mobile/src/screens/LoginScreen.tsx
  - apps/mobile/src/screens/ProfileScreen.tsx
  - apps/mobile/src/screens/HomeScreen.tsx
  - apps/mobile/src/screens/ProfileEditScreen.tsx
  - apps/mobile/src/screens/BodyAnalysisScreen.tsx
  - apps/mobile/src/screens/ColorAnalysisScreen.tsx
  - apps/mobile/src/screens/StyleQuizScreen.tsx
  - apps/mobile/src/screens/SharePosterScreen.tsx
  - apps/mobile/src/navigation/ (route additions)
autonomous: false
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06, PROF-07, PROF-08, PROF-09, PROF-10, PROF-11, PROF-12]

must_haves:
  truths:
    - "OnboardingScreen has 3-step flow: basic info (required) -> photo upload (optional) -> style quiz (optional)"
    - "Basic info step has gender selection (3 options) and age range selection, cannot be skipped"
    - "LoginScreen has WeChat login button and phone login entry"
    - "StyleQuizScreen shows image-based questions with card layout and auto-save per question"
    - "BodyAnalysisScreen shows body type classification + body proportion SVG + outfit advice"
    - "ColorAnalysisScreen shows 8-subtype color season + palette + suitable/unsuitable colors"
    - "SharePosterScreen shows poster preview + save to album + share"
    - "ProfileEditScreen has basic info editing + body data form + style preference editing"
    - "HomeScreen shows profile completeness banner when < 80%"
    - "ProfileScreen shows completeness indicator + edit entry + learning tip"
    - "Photo upload shows PhotoGuideOverlay in camera mode and PrivacyConsentModal before upload"
  artifacts:
    - path: "apps/mobile/src/screens/OnboardingScreen.tsx"
      provides: "3-step onboarding: basic info -> photo -> style quiz"
      contains: "WELCOME|BASIC_INFO|PHOTO|QUIZ"
    - path: "apps/mobile/src/screens/LoginScreen.tsx"
      provides: "Login with WeChat button + phone number login"
      contains: "wechatLogin|phoneLogin"
    - path: "apps/mobile/src/screens/StyleQuizScreen.tsx"
      provides: "Image-based style quiz with card layout"
      contains: "styleQuizStore|ImageChoiceCard"
    - path: "apps/mobile/src/screens/BodyAnalysisScreen.tsx"
      provides: "Body analysis visualization report"
      contains: "bodyAnalysis|BodyShape"
    - path: "apps/mobile/src/screens/ColorAnalysisScreen.tsx"
      provides: "Color season analysis with palette"
      contains: "colorAnalysis|ColorSeason"
    - path: "apps/mobile/src/screens/SharePosterScreen.tsx"
      provides: "Share poster preview and sharing"
      contains: "sharePoster|viewShot"
  key_links:
    - from: "apps/mobile/src/screens/OnboardingScreen.tsx"
      to: "apps/mobile/src/stores/index.ts"
      via: "AuthStore for registration state"
      pattern: "useAuthStore|authStore"
    - from: "apps/mobile/src/screens/StyleQuizScreen.tsx"
      to: "apps/mobile/src/stores/styleQuizStore.ts"
      via: "StyleQuizStore for quiz state and progress"
      pattern: "useStyleQuizStore|styleQuizStore"
    - from: "apps/mobile/src/screens/OnboardingScreen.tsx"
      to: "apps/mobile/src/components/photo/PhotoGuideOverlay.tsx"
      via: "PhotoGuideOverlay in camera mode"
      pattern: "PhotoGuideOverlay"
    - from: "apps/mobile/src/screens/OnboardingScreen.tsx"
      to: "apps/mobile/src/components/privacy/PrivacyConsentModal.tsx"
      via: "PrivacyConsentModal before photo upload"
      pattern: "PrivacyConsentModal"
---

# Plan 05: Mobile Pages

<objective>
Build all mobile screens for Phase 1: onboarding flow, login extensions,
profile visualization, style quiz, and share poster. Screens consume the API
layers, components, and stores created in Plan 04.

Purpose: This is the user-facing layer. Every screen a new user interacts with
during registration, onboarding, photo upload, style quiz, and profile viewing.

Output: 9 screens extended or created, with navigation routes configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-user-profile-style-test/01-RESEARCH.md
@.planning/phases/01-user-profile-style-test/01-UI-SPEC.md
@.planning/phases/01-user-profile-style-test/01-04-SUMMARY.md

<interfaces>
<!-- Components from Plan 04 available for use -->

From apps/mobile/src/components/profile/ProfileCompletenessBar.tsx:
- Props: { percentage: number; missingFields: string[]; onPress?: () => void }

From apps/mobile/src/components/photo/PhotoGuideOverlay.tsx:
- Props: { visible: boolean; dimensions?: { width: number; height: number } }

From apps/mobile/src/components/photo/PhotoQualityIndicator.tsx:
- Props: { quality: PhotoQualityReport; onRetake?: () => void }

From apps/mobile/src/components/privacy/PrivacyConsentModal.tsx:
- Props: { visible: boolean; onConfirm: () => void; onCancel: () => void }

<!-- Stores from Plan 04 -->

From apps/mobile/src/stores/index.ts (AuthStore extended):
- wechatLogin(), phoneLogin(phone, code), phoneRegister(phone, code, nickname?)

From apps/mobile/src/stores/styleQuizStore.ts:
- loadQuiz(quizId), selectAnswer(questionId, optionId), submitAll(quizId), loadProgress(quizId), reset()

From apps/mobile/src/stores/profileStore.ts:
- loadProfile(), loadCompleteness(), loadBodyAnalysis(), loadColorAnalysis(), updateProfile(data), refreshAll()

<!-- API layers from Plan 04 -->

From apps/mobile/src/services/api/sms.api.ts:
- sendCode(phone), loginWithPhone(phone, code), registerWithPhone(phone, code, nickname?)

From apps/mobile/src/services/auth/wechat.ts:
- authorize(), getAuthCode(), isWechatInstalled()

From apps/mobile/src/services/api/style-quiz.api.ts:
- getQuiz(quizId), submitAnswer(quizId, questionId, optionId), batchSubmit(quizId, answers[]), getProgress(quizId), saveProgress(quizId, questionIndex, answers), getResult(quizId)

<!-- Existing screens to extend -->

From apps/mobile/src/screens/OnboardingScreen.tsx:
- Existing onboarding screen with style/color/body steps
- Uses OnboardingSteps component

From apps/mobile/src/screens/LoginScreen.tsx:
- Existing email/password login
- Uses AuthStore

From apps/mobile/src/screens/ProfileScreen.tsx:
- Existing profile display
- Uses profileApi

From apps/mobile/src/screens/HomeScreen.tsx:
- Existing home screen with bottom tab navigation

From apps/mobile/src/navigation/:
- Stack + Bottom Tabs navigation structure
- Lazy loaded tabs
</interfaces>
</context>

<tasks>

### Task 1: Restructure OnboardingScreen and Extend LoginScreen

<type>extend</type>
<files>
- apps/mobile/src/screens/OnboardingScreen.tsx
- apps/mobile/src/screens/LoginScreen.tsx
</files>
<read_first>
- apps/mobile/src/screens/OnboardingScreen.tsx
- apps/mobile/src/screens/LoginScreen.tsx
- apps/mobile/src/components/onboarding/OnboardingSteps.tsx
- .planning/phases/01-user-profile-style-test/01-UI-SPEC.md
</read_first>
<action>
Read existing OnboardingScreen.tsx and LoginScreen.tsx to understand current structure.

**Restructure OnboardingScreen.tsx** to 3-step flow:

Step 0 (BASIC_INFO - required):
- Title: "基本信息" (Heading 20px bold)
- Subtitle: "帮助我们更好地了解你" (Body secondary 16px)
- Gender selector: 3 horizontal pill buttons ("女"/"男"/"其他"), single-select, primary border on selected (#a855f7)
- Age range selector: horizontal scrollable pills ("18-24", "25-30", "31-40", "40+"), single-select
- CTA button: "下一步" (primary color, full width)
- Validation: cannot proceed without selecting both gender and age range
- NO skip button on this step
- Per D-02 and D-06

Step 1 (PHOTO - optional):
- Title: "上传你的照片" (Heading)
- Subtitle: "让风格分析更精准" (Body secondary)
- Upload area: dashed border rectangle with camera icon + "点击上传" label
- When camera mode active: show PhotoGuideOverlay (body outline SVG + "请正面站立")
- Show PrivacyConsentModal before upload begins
- Skip option: "跳过这一步" link in textSecondary color
- CTA: "下一步" (disabled until photo selected or skip pressed)
- Per D-04, D-06, D-07

Step 2 (QUIZ - optional):
- Title: "风格测试" (Heading)
- Subtitle: "发现你的专属风格" (Body secondary)
- Embedded StyleQuizScreen component (or navigate to it)
- Skip option: "跳过" link
- Per D-08

Navigation between steps:
- Use state variable currentStep: 'BASIC_INFO' | 'PHOTO' | 'QUIZ' | 'COMPLETE'
- Progress indicator at top showing current step
- After quiz completion or skip, navigate to HomeScreen
- Per D-03 and D-15

**Extend LoginScreen.tsx**:
- Add WeChat login button: "微信一键登录" (green background, white text)
  - Only show if isWechatInstalled() returns true
  - On press: call authStore.wechatLogin()
  - Use WeChat icon (SVG or text)
- Add phone login entry: "手机号登录" (outline button, primary border)
  - Navigate to new phone login flow (inline or separate screen)
  - Phone input with +86 prefix, numeric keyboard
  - Verification code input (6 digits) with "获取验证码" button
  - 60-second countdown timer after code sent
  - CTA: "登录/注册" button
- Keep existing email/password login as fallback
- Per D-01
- All copy per UI-SPEC Copywriting Contract
</action>
<acceptance_criteria>
- `apps/mobile/src/screens/OnboardingScreen.tsx` has 3-step flow: BASIC_INFO, PHOTO, QUIZ
- BASIC_INFO step has gender selector with 3 options ("女"/"男"/"其他")
- BASIC_INFO step has age range selector with 4 options ("18-24", "25-30", "31-40", "40+")
- BASIC_INFO step has NO skip button
- PHOTO step shows PhotoGuideOverlay in camera mode
- PHOTO step shows PrivacyConsentModal before upload
- PHOTO step has skip option "跳过这一步"
- QUIZ step has skip option
- `apps/mobile/src/screens/LoginScreen.tsx` has WeChat login button "微信一键登录"
- LoginScreen has phone number login entry "手机号登录"
- Phone login has verification code input with 60s countdown
- `cd apps/mobile && npx tsc --noEmit` exits 0
</acceptance_criteria>

### Task 2: Create Analysis Screens and SharePosterScreen

<type>create</type>
<files>
- apps/mobile/src/screens/ProfileEditScreen.tsx
- apps/mobile/src/screens/BodyAnalysisScreen.tsx
- apps/mobile/src/screens/ColorAnalysisScreen.tsx
- apps/mobile/src/screens/SharePosterScreen.tsx
</files>
<read_first>
- apps/mobile/src/screens/ProfileScreen.tsx
- apps/mobile/src/stores/profileStore.ts
- apps/mobile/src/components/layout/ScreenLayout.tsx
- .planning/phases/01-user-profile-style-test/01-UI-SPEC.md
</read_first>
<action>
Create four new screens following the ScreenLayout pattern and UI-SPEC guidelines.

**File 1: `apps/mobile/src/screens/ProfileEditScreen.tsx`**
- Title: "编辑画像" (Heading)
- Sections:
  - Basic info: gender picker (same as onboarding), age range picker, nickname input
  - Body data: form with inputs for height (cm), weight (kg), shoulder width, bust, waist, hip, inseam
  - Style preferences: tag selector for style keywords
- Each section in a card (surface background, 12px border-radius, md padding)
- CTA: "保存" button (primary color)
- On save: call profileStore.updateProfile()
- Uses useForm hook from existing utils
- Per PROF-01

**File 2: `apps/mobile/src/screens/BodyAnalysisScreen.tsx`**
- Load data from profileStore.bodyAnalysis
- Body type classification card: large card with primary gradient background
  - Shows body type name in Chinese (沙漏型/梨形/苹果形/矩形/倒三角)
  - Shows match percentage
- Body proportion SVG visualization: react-native-svg radar chart showing proportions
  - Axes: shoulder, bust, waist, hip
  - Actual proportions vs ideal proportions overlay
- Outfit advice summary: 3-4 bullet points in Body text
- Uses ScreenLayout wrapper
- Skeleton loading state while data loads
- Per PROF-04

**File 3: `apps/mobile/src/screens/ColorAnalysisScreen.tsx`**
- Load data from profileStore.colorAnalysis
- Color season card: shows 8-subtype season name (e.g., "暖春型 warm_spring")
  - Primary gradient background
- Color palette display: horizontal row of 8 color circles (40px diameter)
  - Hex code + Chinese name below each
  - Mark suitable (green check) vs unsuitable (red X) colors
- Recommendation reasons: "基于你的肤色分析" text
- Per PROF-10 (8 subtypes: spring_warm, spring_light, summer_cool, etc.)

**File 4: `apps/mobile/src/screens/SharePosterScreen.tsx`**
- Uses react-native-view-shot for poster capture
- Uses react-native-share for native share sheet
- Poster content wrapped in ViewShot ref:
  - Style type name + match percentage
  - Color palette circles
  - User avatar + nickname
  - App branding
- Bottom bar: "分享我的风格" CTA button (primary color, full width)
- On share press: capture ViewShot -> open Share.open({ url, title })
- Per PROF-04

All screens:
- Use ScreenLayout component wrapper
- Skeleton loading states (per CC-12)
- Error state with retry button (per CC-14, CC-18)
- accessibilityLabel on all interactive elements (per CC-17)
- Light mode only (per CC-13)
</action>
<acceptance_criteria>
- `apps/mobile/src/screens/ProfileEditScreen.tsx` exists with basic info + body data + style preference editing
- Has gender picker, age range picker, nickname input
- Has body measurement form fields (height, weight, shoulder, bust, waist, hip, inseam)
- `apps/mobile/src/screens/BodyAnalysisScreen.tsx` exists with body type card + SVG visualization
- Shows body type name in Chinese
- Has body proportion radar chart via react-native-svg
- `apps/mobile/src/screens/ColorAnalysisScreen.tsx` exists with color season card + palette
- Shows 8-subtype color season
- Has color circles with hex codes and Chinese names
- `apps/mobile/src/screens/SharePosterScreen.tsx` exists with ViewShot + Share integration
- Has "分享我的风格" CTA button
- All screens use ScreenLayout wrapper
- All screens have loading skeleton states
- All screens have error state with retry
- `cd apps/mobile && npx tsc --noEmit` exits 0
</acceptance_criteria>

### Task 3: Create StyleQuizScreen and Extend HomeScreen/ProfileScreen

<type>create</type>
<files>
- apps/mobile/src/screens/StyleQuizScreen.tsx
- apps/mobile/src/screens/HomeScreen.tsx
- apps/mobile/src/screens/ProfileScreen.tsx
- apps/mobile/src/navigation/ (route additions for new screens)
</files>
<read_first>
- apps/mobile/src/screens/HomeScreen.tsx
- apps/mobile/src/screens/ProfileScreen.tsx
- apps/mobile/src/stores/styleQuizStore.ts
- apps/mobile/src/stores/profileStore.ts
- apps/mobile/src/navigation/ (existing route definitions)
</read_first>
<action>
Read existing screens and navigation to understand current structure.

**Create `screens/StyleQuizScreen.tsx`**:
- Load quiz via styleQuizStore.loadQuiz('default')
- Restore progress via styleQuizStore.loadProgress('default')
- Progress bar at top: full-width thin bar, primary fill, "{current}/{total}" text
- Question display: "Q{number}. {text}" in Heading, hint in Small normal
- Options: 2x2 grid of image cards (each with image + label)
  - Selected state: primary border (3px), scale 1.02
  - Auto-advance after selection with 300ms delay
- Per question selection: call styleQuizStore.selectAnswer() (auto-saves to backend)
- Last question CTA: "查看我的风格报告" instead of "下一题"
- After all questions: call styleQuizStore.submitAll()
- Show loading: "正在生成你的风格报告..." with ActivityIndicator
- After result: show color derivation result as palette
- Skip button: "跳过" top-right
- Per D-08, D-09, D-15, PROF-08, PROF-09

**Extend HomeScreen.tsx**:
- Add ProfileCompletenessBar at top (below header, above content)
- Load completeness from profileStore.loadCompleteness()
- Only show banner when completeness.percentage < 80%
- Banner text: "完善画像解锁个性化推荐" with CTA to ProfileEditScreen
- Per PROF-07

**Extend ProfileScreen.tsx**:
- Add ProfileCompletenessBar below user avatar
- Add "编辑画像" button linking to ProfileEditScreen
- Add "体型分析" link to BodyAnalysisScreen
- Add "色彩分析" link to ColorAnalysisScreen
- Add "风格测试" link to StyleQuizScreen
- Add "分享我的风格" link to SharePosterScreen
- Add learning tip text at bottom: "你的画像会随使用越来越精准" (per PROF-11)
- Per PROF-07, PROF-11

**Update navigation routes**:
- Add routes: ProfileEdit, BodyAnalysis, ColorAnalysis, StyleQuiz, SharePoster
- Add to appropriate stack navigator
- Ensure lazy loading for new routes
</action>
<acceptance_criteria>
- `apps/mobile/src/screens/StyleQuizScreen.tsx` exists with image-based quiz UI
- Has progress bar showing current/total
- Has 2x2 image grid for question options
- Auto-advances after selection with 300ms delay
- Calls styleQuizStore.selectAnswer per question (auto-saves)
- Shows color derivation result as palette after completion
- Has skip button "跳过" in top-right
- `apps/mobile/src/screens/HomeScreen.tsx` shows ProfileCompletenessBar when completeness < 80%
- Banner has "完善画像解锁个性化推荐" text
- `apps/mobile/src/screens/ProfileScreen.tsx` has completeness indicator
- Has "编辑画像" link to ProfileEditScreen
- Has links to BodyAnalysis, ColorAnalysis, StyleQuiz, SharePoster screens
- Has learning tip "你的画像会随使用越来越精准"
- Navigation routes exist for all new screens
- `cd apps/mobile && npx tsc --noEmit` exits 0
</acceptance_criteria>

</tasks>

<verification>
1. `cd apps/mobile && npx tsc --noEmit` -- all TypeScript compiles
2. `grep "BASIC_INFO\|PHOTO\|QUIZ" apps/mobile/src/screens/OnboardingScreen.tsx` -- 3-step flow
3. `grep "微信一键登录" apps/mobile/src/screens/LoginScreen.tsx` -- WeChat button
4. `grep "PhoneGuideOverlay" apps/mobile/src/screens/OnboardingScreen.tsx` -- guide overlay integrated
5. `grep "PrivacyConsentModal" apps/mobile/src/screens/OnboardingScreen.tsx` -- privacy modal integrated
6. `grep "styleQuizStore" apps/mobile/src/screens/StyleQuizScreen.tsx` -- quiz store used
7. `grep "ProfileCompletenessBar" apps/mobile/src/screens/HomeScreen.tsx` -- banner on home
8. `grep "ProfileCompletenessBar" apps/mobile/src/screens/ProfileScreen.tsx` -- indicator on profile
9. Visual verification: run app and test onboarding flow, quiz flow, share poster
</verification>

<success_criteria>
- [ ] OnboardingScreen: 3-step flow (basic info required -> photo optional -> quiz optional)
- [ ] Gender (3 options) and age range (4 options) selection, no skip on basic info
- [ ] PhotoGuideOverlay shown in camera mode during photo upload
- [ ] PrivacyConsentModal shown before photo upload
- [ ] LoginScreen: WeChat login button + phone login with verification code
- [ ] StyleQuizScreen: image-based quiz with auto-save and progress restore
- [ ] BodyAnalysisScreen: body type card + SVG proportion chart + outfit advice
- [ ] ColorAnalysisScreen: 8-subtype color season + palette + suitable/unsuitable colors
- [ ] SharePosterScreen: poster preview + capture + share
- [ ] ProfileEditScreen: basic info + body data + style preference editing
- [ ] HomeScreen: completeness banner when < 80%
- [ ] ProfileScreen: completeness + edit + analysis links + learning tip
- [ ] All new screens in navigation with lazy loading
- [ ] All screens have skeleton loading, error retry, and accessibility labels
</success_criteria>

<output>
After completion, create `.planning/phases/01-user-profile-style-test/01-05-SUMMARY.md`
</output>
