---
phase: 21-yi-dong-duan-week-mei-zhou-tui-jian-duan-dao-duan-yan-zheng
plan: 02
type: execute
wave: 2
depends_on:
  - "21-01"
files_modified: []
autonomous: false
requirements:
  - WEEK-E2E-05
  - WEEK-E2E-06
  - WEEK-E2E-07

must_haves:
  truths:
    - "Week tab is clickable and shows 7-day plan structure"
    - "Each day shows: scene tag, outfit suggestion (with coverImage or placeholder), wardrobe reuse stats"
    - "Loading → data rendering → error retry, all three states work"
    - "OutfitDiary and WeeklyReport are reachable from Week tab via navigation"
    - "No zodiac/constellation strings in any Week-related file"
  artifacts:
    - path: "apps/mobile/src/features/week/screens/WeekScreen.tsx"
      provides: "Complete Week tab with 3-state rendering"
  key_links:
    - from: "WeekScreen.tsx"
      to: "WeekStack Navigator"
      via: "WeekMain route registered in WeekStack"
      pattern: 'name="WeekMain"'
    - from: "WeekScreen.tsx"
      to: "OutfitDiaryScreen"
      via: "navigation.navigate('OutfitDiary')"
      pattern: "navigate.*OutfitDiary"
    - from: "WeekScreen.tsx"
      to: "WeeklyReportScreen"
      via: "navigation.navigate('WeeklyReport')"
      pattern: "navigate.*WeeklyReport"
---

<objective>
End-to-end verification of the Week tab's complete call chain: API path matching, navigation reachability, three-state rendering, and anti-fraud compliance.

Purpose: Confirm that all fixes from Plan 01 work together as a complete user-facing flow, and that no zodiac/constellation code or misleading AI copy exists.

Output: Verification report confirming Week tab works end-to-end.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-yi-dong-duan-week-mei-zhou-tui-jian-duan-dao-duan-yan-zheng/21-01-SUMMARY.md

@apps/mobile/src/features/week/screens/WeekScreen.tsx
@apps/mobile/src/services/api/calendar-plan.api.ts
@apps/mobile/src/navigation/MainStackNavigator.tsx
@apps/mobile/src/navigation/RootNavigator.tsx
@apps/mobile/src/navigation/types.ts
@apps/mobile/src/features/home/components/OutfitDiaryScreen.tsx
@apps/mobile/src/features/home/components/WeeklyReportScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated grep verification of acceptance criteria</name>
  <files></files>
  <action>
Run the following automated checks and record results. Each check MUST pass. If any fails, fix the issue and re-run.

**Check 1 — API Path Match:**
Verify frontend API paths match backend controller routes:

- Grep `"/calendar/weekly-plan"` in `calendar-plan.api.ts` → must find both GET and POST paths
- Grep `"/calendar/plan/"` in `calendar-plan.api.ts` → must find PATCH path with date template
- Grep `"weekly-plan"` in `calendar-plan.controller.ts` → must match @Get and @Post decorators
- Grep `"plan/:date"` in `calendar-plan.controller.ts` → must match @Patch decorator

**Check 2 — Frontend uses correct field names:**

- Grep `\.plans` in `WeekScreen.tsx` → must find (iterating plan.plans array)
- Grep `\.plannedDate` in `WeekScreen.tsx` → must find (reading day date)
- Grep `\.weatherContext` in `WeekScreen.tsx` → must find (reading weather data)
- Grep `\.sceneTag` in `WeekScreen.tsx` → must find (reading scene)
- Grep `plan\.days` in `WeekScreen.tsx` → must NOT find (old field name)
- Grep `plan\.stats` in `WeekScreen.tsx` → must NOT find (old field name, stats computed inline)

**Check 3 — Navigation registration:**

- Grep `WeekMain` in `MainStackNavigator.tsx` → must find WeekStack.Screen registration
- Grep `OutfitDiary` in `MainStackNavigator.tsx` → must find WeekStack.Screen registration
- Grep `WeeklyReport` in `MainStackNavigator.tsx` → must find WeekStack.Screen registration
- Grep `Week.*WeekStackNavigator` in `RootNavigator.tsx` → must find Tab.Screen registration
- Grep `WeekStackParamList` in `types.ts` → must have WeekMain, OutfitDiary, WeeklyReport keys

**Check 4 — Three-state rendering in WeekScreen:**

- Grep `isLoading` in `WeekScreen.tsx` → must find loading state branch
- Grep `isError` in `WeekScreen.tsx` → must find error state branch with retry
- Grep `refetch` in `WeekScreen.tsx` → must find retry action
- Grep `ShimmerSkeleton` in `WeekScreen.tsx` → must find loading skeleton

**Check 5 — Three-state rendering in OutfitDiaryScreen:**

- Grep `isLoading` in `OutfitDiaryScreen.tsx` → must find loading state
- Grep `isError` in `OutfitDiaryScreen.tsx` → must find error state with retry
- Grep `entries.length === 0` in `OutfitDiaryScreen.tsx` → must find empty state

**Check 6 — Three-state rendering in WeeklyReportScreen:**

- Grep `isLoading` in `WeeklyReportScreen.tsx` → must find loading state
- Grep `isError` in `WeeklyReportScreen.tsx` → must find error state with retry
- Grep `hasReport` in `WeeklyReportScreen.tsx` → must find empty/missing report state

**Check 7 — Anti-fraud: No zodiac/constellation:**

- Grep `constellation|zodiac|horoscope` in ALL files under `apps/mobile/src/features/week/` → must return 0 matches
- Grep `星座` in ALL files under `apps/mobile/src/features/week/` → must return 0 matches
- Grep `constellation|zodiac|horoscope|星座` in `WeeklyCalendarView.tsx` → must return 0 matches

**Check 8 — Anti-fraud: No misleading AI copy:**

- Grep `AI深度学习|深度学习推荐|AI智能推荐` in Week-related files → must return 0 matches
- The backend `source` field uses `"ai_generated"` which is truthful (it IS AI-generated via rule matching, no deep learning claimed)

**Check 9 — Weather fallback disclosure:**

- Grep `provider.*fallback|fallback.*provider` in `WeekScreen.tsx` → must find fallback check
- Grep `天气数据不可用` in `WeekScreen.tsx` → must find warning text

**Check 10 — TypeScript compilation:**

- Run `npx tsc --noEmit --project apps/mobile/tsconfig.json` → must return 0 errors

Print a summary table of all 10 checks with PASS/FAIL status.
</action>
<verify>
<automated>echo "Verification task runs inline checks — see action section"</automated>
</verify>
<done>
All 10 automated checks pass with PASS status. Any failures have been fixed and re-verified.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Week tab end-to-end flow</name>
  <files></files>
  <action>
Human tester verifies the complete Week tab flow on device/emulator. No code changes — pure verification.
  </action>
  <what-built>
Complete Week tab with fixed API contract, three-state rendering, weather fallback disclosure, and navigation to OutfitDiary/WeeklyReport.
  </what-built>
  <how-to-verify>
1. Start the app (expo start / yarn mobile)
2. Navigate to the **Week** tab (second tab in bottom navigation)
3. **Loading state**: On first load, verify ShimmerSkeleton appears briefly
4. **Data rendering**: After load, verify:
   - Stats bar shows "X/7 已规划", "100% 已有单品", "7 总搭配数"
   - 7 day cards appear in a row, each showing date, weather icon + temp, status dot
   - If weather data is fallback, verify yellow info banner: "天气数据不可用，使用通用季节建议"
5. **Error state**: If no data, verify "加载失败" message with "重新加载" button appears
6. **Empty state**: If no plans exist, verify "还没有周计划" with "生成周计划" button
7. Tap the **book icon** (穿搭日记) → should navigate to OutfitDiaryScreen
8. Go back, tap the **document icon** (穿搭周报) → should navigate to WeeklyReportScreen
9. Tap the **穿搭周报** card at bottom → should also navigate to WeeklyReportScreen
10. Tap the **穿搭日记** card at bottom → should also navigate to OutfitDiaryScreen
11. Pull down to refresh → data should reload
12. Verify NO zodiac/constellation/星座 content appears anywhere
  </how-to-verify>
  <verify>
    <automated>MISSING — Human verification required</automated>
  </verify>
  <done>Human tester confirms all 12 verification steps pass with no issues.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                             |
| ------------------ | ------------------------------------------------------- |
| Human verification | Confirming UI correctness requires human eyes on device |

## STRIDE Threat Register

| Threat ID | Category               | Component         | Disposition | Mitigation Plan                                                                    |
| --------- | ---------------------- | ----------------- | ----------- | ---------------------------------------------------------------------------------- |
| T-21-05   | Elevation of Privilege | withGuard wrapper | accept      | OutfitDiary/WeeklyReport screens use auth guard — unauthenticated users redirected |

</threat_model>

<verification>
All 10 automated checks from Task 1 pass + human verification from Task 2 approved.
</verification>

<success_criteria>

1. Week tab shows 7-day plan with correct data from backend
2. Each day card shows scene tag, outfit suggestion, weather context
3. Loading → data → error retry states all work
4. OutfitDiary and WeeklyReport reachable via navigation
5. Weather fallback banner visible when appropriate
6. Zero zodiac/constellation code or references
7. No misleading "AI deep learning" copy
8. Human tester approves the experience
   </success_criteria>

<output>
After completion, create `.planning/phases/21-yi-dong-duan-week-mei-zhou-tui-jian-duan-dao-duan-yan-zheng/21-02-SUMMARY.md`
</output>
