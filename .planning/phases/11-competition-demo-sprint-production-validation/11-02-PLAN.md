---
phase: 11-competition-demo-sprint-production-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ml/services/stylist/ai_service_router.py
  - ml/services/stylist/ai_service_router_test.py
  - ml/services/stylist/intelligent_stylist_service.py
  - ml/api/config.py
  - apps/backend/src/domains/ai-core/ai-stylist/tts.service.ts
  - scripts/tts-precache.py
autonomous: true
requirements: [D-08, D-09, D-10, D-11, D-15]

must_haves:
  truths:
    - "GLM-4-Flash 请求超时 5 秒自动 fallback 到 GLM-5"
    - "GLM-4-Flash 返回错误时自动重试 1 次后 fallback"
    - "前端无感知 fallback 发生"
    - "Edge-TTS 常见回复语音已预缓存"
    - "演示全程走实时 AI，不预设回复"
  artifacts:
    - path: "ml/services/stylist/ai_service_router.py"
      provides: "AIServiceRouter: GLM-4-Flash -> retry -> GLM-5 fallback"
      exports: ["AIServiceRouter"]
      contains: "GLM_5"
    - path: "ml/services/stylist/ai_service_router_test.py"
      provides: "Router 单元测试"
      min_lines: 80
    - path: "scripts/tts-precache.py"
      provides: "Edge-TTS 预缓存脚本"
      contains: "edge_tts"
  key_links:
    - from: "ml/services/stylist/intelligent_stylist_service.py"
      to: "ml/services/stylist/ai_service_router.py"
      via: "import AIServiceRouter"
      pattern: "ai_service_router"
    - from: "ml/api/config.py"
      to: "ml/services/stylist/ai_service_router.py"
      via: "GLM_MODEL, GLM_API_ENDPOINT settings"
      pattern: "GLM_MODEL"
---

<objective>
AIServiceRouter GLM Fallback + Edge-TTS 预缓存

Purpose: 比赛现场 AI 稳定性双保险。GLM-4-Flash 超时/报错时自动切换 GLM-5，确保演示不中断。预缓存常见回复语音减少实时 TTS 延迟。

Output:

- AIServiceRouter 实现 GLM-4-Flash -> retry -> GLM-5 自动 fallback
- Edge-TTS 预缓存脚本和常见话术列表
- Router 单元测试覆盖 timeout/error/fallback 路径
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-competition-demo-sprint-production-validation/11-CONTEXT.md

@ml/services/stylist/intelligent_stylist_service.py
@ml/api/config.py
@apps/backend/src/domains/ai-core/ai-stylist/tts.service.ts

<interfaces>
<!-- Current GLM calling pattern in intelligent_stylist_service.py -->

From ml/api/config.py:

```python
class Settings(BaseSettings):
    GLM_API_KEY: str = ""
    ZHIPU_API_KEY: str = ""
    GLM_API_ENDPOINT: str = "https://open.bigmodel.cn/api/paas/v4"
    GLM_MODEL: str = "glm-5"
```

From ml/services/stylist/intelligent_stylist_service.py (GLMStylistEngine):

```python
class GLMStylistEngine:
    def __init__(self):
        self._key_manager = SecureAPIKeyManager()
        self.api_endpoint = os.getenv("GLM_API_ENDPOINT", "https://open.bigmodel.cn/api/paas/v4")
        self.model = os.getenv("GLM_MODEL", "glm-5")
        self.circuit_breaker = CircuitBreaker(failure_threshold=5, recovery_timeout=60, ...)
    # Uses aiohttp to call GLM API
    # Has _call_api_glm method with retry logic
```

From apps/backend/src/domains/ai-core/ai-stylist/tts.service.ts:

```typescript
export class EdgeTTSService {
  private readonly pythonBaseUrl = process.env.PYTHON_AI_URL || "http://localhost:8000";
  async synthesizeAndUpload(text: string, voice = "zh-CN-XiaoxiaoNeural"): Promise<string | null>;
}
```

Fallback chain per D-08/D-09/D-10:

- Primary: GLM-4-Flash (fast, free tier)
- On 5s timeout or error: retry 1 time
- Still failing: fallback to GLM-5 (same Zhipu ecosystem, per D-10)
- Frontend unaware (backend-only change)
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 AIServiceRouter (GLM-4-Flash -> retry -> GLM-5 fallback)</name>
  <files>ml/services/stylist/ai_service_router.py, ml/services/stylist/ai_service_router_test.py, ml/services/stylist/intelligent_stylist_service.py</files>
  <read_first>
    - ml/services/stylist/intelligent_stylist_service.py (当前 GLMStylistEngine 实现)
    - ml/api/config.py (当前 Settings 配置)
    - ml/services/common/secure_api_key.py (API key 管理)
  </read_first>
  <behavior>
    - Test 1: Primary GLM-4-Flash 成功调用时返回结果，无 fallback
    - Test 2: Primary 超时 5 秒触发 fallback 到 GLM-5
    - Test 3: Primary 返回 HTTP 5xx 错误时 retry 1 次后 fallback
    - Test 4: Primary 返回 HTTP 429 时直接 fallback 不 retry
    - Test 5: Primary 和 fallback 都失败时抛出明确异常
    - Test 6: Fallback 成功时返回结果带 `_fallback: true` 标记
  </behavior>
  <action>
Per D-08: 后端自动 fallback，新建 AIServiceRouter。
Per D-09: 5 秒超时触发 fallback，用户体感延迟 < 8 秒。
Per D-10: Fallback 模型为 GLM-5，保持智谱生态一致。
Per D-15: 演示全部走实时 AI，不预设回复。

1. **创建 `ml/services/stylist/ai_service_router.py`**:

```python
"""AIServiceRouter - GLM model fallback router for competition demo stability.

Per D-08: Backend-only automatic fallback.
Per D-09: 5-second timeout triggers fallback.
Per D-10: Fallback to GLM-5 (same Zhipu ecosystem).
"""
import os
import time
import logging
import asyncio
import aiohttp
from typing import Optional, Dict, Any, List
from dataclasses import dataclass, field

logger = logging.getLogger(__name__)

@dataclass
class RouterResult:
    """Result from AIServiceRouter call."""
    text: str
    model_used: str
    fallback_triggered: bool = False
    latency_ms: float = 0.0
    retry_count: int = 0

class AIServiceRouter:
    """Routes LLM calls through primary -> retry -> fallback chain."""

    PRIMARY_MODEL = "glm-4-flash"
    FALLBACK_MODEL = "glm-5"  # Per D-10
    TIMEOUT_SECONDS = 5.0     # Per D-09
    MAX_RETRIES = 1           # Per D-08: retry 1 time

    def __init__(self):
        self._api_key = os.getenv("GLM_API_KEY", "") or os.getenv("ZHIPU_API_KEY", "")
        self._endpoint = os.getenv("GLM_API_ENDPOINT", "https://open.bigmodel.cn/api/paas/v4")
        # Session reuse for performance
        self._session: Optional[aiohttp.ClientSession] = None

    async def _get_session(self) -> aiohttp.ClientSession:
        if self._session is None or self._session.closed:
            self._session = aiohttp.ClientSession()
        return self._session

    async def close(self):
        if self._session and not self._session.closed:
            await self._session.close()

    async def call(
        self,
        messages: List[Dict[str, str]],
        max_tokens: int = 200,
        model: Optional[str] = None,
    ) -> RouterResult:
        """Call LLM with automatic fallback.

        Args:
            messages: Chat messages list
            max_tokens: Max output tokens
            model: Override model (skip fallback logic)
        Returns:
            RouterResult with text, model_used, fallback info
        """
        start = time.monotonic()

        # Direct model call if specified
        if model:
            text = await self._raw_call(model, messages, max_tokens)
            return RouterResult(
                text=text,
                model_used=model,
                latency_ms=(time.monotonic() - start) * 1000,
            )

        # Primary -> retry -> fallback chain
        try:
            text = await self._raw_call(self.PRIMARY_MODEL, messages, max_tokens)
            return RouterResult(
                text=text,
                model_used=self.PRIMARY_MODEL,
                latency_ms=(time.monotonic() - start) * 1000,
            )
        except Exception as primary_error:
            logger.warning(f"Primary model {self.PRIMARY_MODEL} failed: {primary_error}")

            # Retry once (per D-08) unless 429
            if not self._is_rate_limit(primary_error):
                try:
                    text = await self._raw_call(self.PRIMARY_MODEL, messages, max_tokens)
                    return RouterResult(
                        text=text,
                        model_used=self.PRIMARY_MODEL,
                        latency_ms=(time.monotonic() - start) * 1000,
                        retry_count=1,
                    )
                except Exception:
                    pass

            # Fallback to GLM-5 (per D-10)
            try:
                text = await self._raw_call(self.FALLBACK_MODEL, messages, max_tokens)
                logger.info(f"Fallback to {self.FALLBACK_MODEL} succeeded")
                return RouterResult(
                    text=text,
                    model_used=self.FALLBACK_MODEL,
                    fallback_triggered=True,
                    latency_ms=(time.monotonic() - start) * 1000,
                    retry_count=1,
                )
            except Exception as fallback_error:
                total_ms = (time.monotonic() - start) * 1000
                logger.error(f"Both models failed. Total latency: {total_ms:.0f}ms")
                raise RuntimeError(
                    f"Primary ({self.PRIMARY_MODEL}) and fallback ({self.FALLBACK_MODEL}) both failed"
                ) from fallback_error

    async def _raw_call(
        self, model: str, messages: List[Dict[str, str]], max_tokens: int
    ) -> str:
        """Raw API call to Zhipu GLM endpoint."""
        session = await self._get_session()
        url = f"{self._endpoint}/chat/completions"
        payload = {
            "model": model,
            "messages": messages,
            "max_tokens": max_tokens,
            "temperature": 0.7,
        }
        headers = {"Authorization": f"Bearer {self._api_key}", "Content-Type": "application/json"}

        async with session.post(
            url, json=payload, headers=headers, timeout=aiohttp.ClientTimeout(total=self.TIMEOUT_SECONDS)
        ) as response:
            if response.status == 429:
                raise RateLimitError(f"Rate limited on {model}")
            if response.status >= 400:
                text = await response.text()
                raise APIError(f"HTTP {response.status}: {text[:200]}")
            data = await response.json()
            return data["choices"][0]["message"]["content"].strip()

    def _is_rate_limit(self, error: Exception) -> bool:
        return isinstance(error, RateLimitError) or "429" in str(error)

class RateLimitError(Exception):
    pass

class APIError(Exception):
    pass
```

2. **创建 `ml/services/stylist/ai_service_router_test.py`** -- pytest 测试:

   - Mock aiohttp calls to simulate success/timeout/error
   - 测试 6 个 behavior 场景 (见上方 behavior 块)
   - 使用 `pytest-asyncio` 进行 async 测试

3. **修改 `ml/services/stylist/intelligent_stylist_service.py`**:

   - 在 `GLMStylistEngine.__init__` 中初始化 `self._router = AIServiceRouter()`
   - 在 `_call_api_glm` 方法中: 先尝试直接调用，失败时使用 router fallback
   - 或者更简单: 在 `IntelligentStylistService` 层面，将 `_call_api_glm` 替换为调用 `AIServiceRouter.call()`
   - 保持向后兼容 -- 如果 router 未初始化则回退到原逻辑
   - 添加 `GLM_FALLBACK_ENABLED` 环境变量控制 (默认 true)

4. **修改 `ml/api/config.py`** -- 添加新配置项:
   - `GLM_PRIMARY_MODEL: str = "glm-4-flash"`
   - `GLM_FALLBACK_MODEL: str = "glm-5"`
   - `GLM_TIMEOUT_SECONDS: float = 5.0`
   - `GLM_FALLBACK_ENABLED: bool = True`
     </action>
     <acceptance_criteria>
   - `ml/services/stylist/ai_service_router.py` 文件存在
   - 包含 `class AIServiceRouter` 定义
   - 包含 `PRIMARY_MODEL = "glm-4-flash"` 和 `FALLBACK_MODEL = "glm-5"`
   - 包含 `TIMEOUT_SECONDS = 5.0`
   - `ml/services/stylist/ai_service_router_test.py` 包含 >= 5 个 test 函数
   - `ml/services/stylist/intelligent_stylist_service.py` 中引用 `ai_service_router`
   - `ml/api/config.py` 包含 `GLM_PRIMARY_MODEL` 和 `GLM_FALLBACK_MODEL`
     </acceptance_criteria>
     <verify>
     <automated>cd C:/AiNeed && python -c "from ml.services.stylist.ai_service_router import AIServiceRouter; r = AIServiceRouter(); print(f'Primary: {r.PRIMARY_MODEL}, Fallback: {r.FALLBACK_MODEL}, Timeout: {r.TIMEOUT_SECONDS}s')" 2>&1 || echo "Import check"</automated>
     </verify>
     <done>AIServiceRouter 实现 GLM-4-Flash -> retry -> GLM-5 fallback，5 秒超时触发，测试覆盖 6 个场景</done>
     </task>

<task type="auto">
  <name>Task 2: Edge-TTS 预缓存脚本 + 话术列表</name>
  <files>scripts/tts-precache.py, apps/backend/src/domains/ai-core/ai-stylist/tts.service.ts</files>
  <read_first>
    - apps/backend/src/domains/ai-core/ai-stylist/tts.service.ts (现有 Edge-TTS 服务)
  </read_first>
  <action>
Per D-11: 预缓存常见回复语音，减少实时 TTS 延迟。
优先级: 问候语 -> 面试固定话术 -> 常见推荐解释。

1. **创建 `scripts/tts-precache.py`** -- Edge-TTS 预缓存脚本:
   - 使用 `edge-tts` Python 库生成语音文件
   - 预缓存话术列表 (硬编码在脚本中):

```python
PRECACHE_PHRASES = {
    "greeting_1": "嘿！有什么穿搭问题都可以问我哦",
    "greeting_2": "你好呀！今天想聊什么风格？",
    "greeting_3": "早上好！今天有什么安排吗？",
    "interview_ask_company": "什么类型的公司？互联网、金融、还是外企？",
    "interview_ask_position": "什么岗位呢？",
    "interview_ask_budget": "预算大概多少？",
    "interview_confirm": "好的，我帮你搭一套 Smart Casual，互联网公司面试刚刚好",
    "outfit_intro": "为你找到了几套搭配方案，看看哪个更合心意？",
    "outfit_recommend_a": "方案 A 深蓝休闲西装配白 T，利落又不会太正式",
    "try_on_loading": "正在为你生成试穿效果图，请稍等...",
    "try_on_ready": "效果出来了！感觉怎么样？",
    "fallback_studio": "看来线上挑不到完全满意的？要不试试工作室定制？",
    "wrap_up": "很高兴能帮到你！下次需要穿搭建议随时找我",
    "style_memory": "你说过不喜欢高领，我记住了",
}
```

- 每个话术调用 `edge-tts` 生成 MP3
- 输出到 `ml/data/tts-cache/` 目录
- 文件名格式: `{key}_{voice}_{hash}.mp3`
- 语音: `zh-CN-XiaoxiaoNeural` (与 tts.service.ts 一致)
- 添加 `--output-dir` 和 `--voice` 参数
- 打印缓存结果统计

2. **修改 `apps/backend/src/domains/ai-core/ai-stylist/tts.service.ts`**:
   - 添加 `getCachedAudio(textKey: string)` 方法
   - 检查 `ml/data/tts-cache/` 目录下是否有预缓存文件
   - 如果命中预缓存直接返回音频 URL，不调用 Python 端点
   - 如果未命中走原有 `synthesizeAndUpload` 流程
   - 预缓存路径通过环境变量 `TTS_CACHE_DIR` 配置
     </action>
     <acceptance_criteria>
   - `scripts/tts-precache.py` 文件存在
   - 包含 `PRECACHE_PHRASES` 字典，key 数量 >= 13
   - 包含 `edge_tts` import 或 `edge-tts` 调用
   - `tts.service.ts` 添加 `getCachedAudio` 方法
   - `tts.service.ts` 引用 `TTS_CACHE_DIR` 环境变量
     </acceptance_criteria>
     <verify>
     <automated>cd C:/AiNeed && python -c "
     import ast, sys
     with open('scripts/tts-precache.py') as f:
     content = f.read()
     assert 'PRECACHE_PHRASES' in content, 'Missing PRECACHE_PHRASES'
     assert 'edge_tts' in content or 'edge-tts' in content, 'Missing edge_tts'
     print('TTS precache script OK')
     " && grep -c 'getCachedAudio' apps/backend/src/domains/ai-core/ai-stylist/tts.service.ts</automated>
     </verify>
     <done>Edge-TTS 预缓存脚本创建完成，包含 13+ 条常见话术；tts.service.ts 支持缓存优先查找</done>
     </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                         |
| -------------------------- | ----------------------------------- |
| Backend -> GLM API         | 出站 HTTPS 调用，API key 在请求头中 |
| TTS precache -> filesystem | 本地文件写入，需确保输出目录安全    |

## STRIDE Threat Register

| Threat ID | Category | Component               | Disposition | Mitigation Plan                                                    |
| --------- | -------- | ----------------------- | ----------- | ------------------------------------------------------------------ |
| T-11-01   | S        | GLM API calls           | mitigate    | API key via SecureAPIKeyManager, not hardcoded                     |
| T-11-02   | I        | TTS cache files         | accept      | Cache files are non-sensitive audio, no PII                        |
| T-11-03   | D        | AIServiceRouter timeout | mitigate    | 5s timeout prevents indefinite hang, fallback ensures availability |

</threat_model>

<verification>
1. `AIServiceRouter` 类可导入且 PRIMARY_MODEL="glm-4-flash", FALLBACK_MODEL="glm-5"
2. Router 测试文件包含 >= 5 个测试函数
3. `intelligent_stylist_service.py` 引用 `ai_service_router`
4. `tts-precache.py` 包含 13+ 条预缓存话术
5. `tts.service.ts` 添加 `getCachedAudio` 方法
6. `ml/api/config.py` 包含 `GLM_PRIMARY_MODEL` 和 `GLM_FALLBACK_MODEL`
</verification>

<success_criteria>

- AIServiceRouter 实现 GLM-4-Flash -> retry -> GLM-5 fallback
- 5 秒超时触发 fallback，用户体感 < 8 秒
- 测试覆盖 timeout/error/rate-limit/fallback 路径
- Edge-TTS 预缓存 13+ 条常见话术
- tts.service.ts 支持缓存优先查找
  </success_criteria>

<output>
After completion, create `.planning/phases/11-competition-demo-sprint-production-validation/11-02-SUMMARY.md`
</output>
