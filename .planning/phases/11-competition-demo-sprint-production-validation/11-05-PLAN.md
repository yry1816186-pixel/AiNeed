---
phase: 11-competition-demo-sprint-production-validation
plan: 05
type: execute
wave: 2
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - docs/PRESENTATION/XUNO-DEMO-SCRIPT.md
  - docs/PRESENTATION/DEMO-RECORDING-GUIDE.md
  - scripts/demo-script-verify.py
autonomous: true
requirements: [D-05, D-18, D-20]

must_haves:
  truths:
    - "Demo Script 已根据当前代码实现状态校准"
    - "FashionCLIP 引用已替换为 FashionSigLIP"
    - "匹配度雷达图引用与实际实现一致"
    - "录屏指南已更新为 OBS + 模拟器方案"
  artifacts:
    - path: "docs/PRESENTATION/XUNO-DEMO-SCRIPT.md"
      provides: "校准后的 2:20 逐秒演示脚本"
      contains: "FashionSigLIP"
    - path: "docs/PRESENTATION/DEMO-RECORDING-GUIDE.md"
      provides: "OBS 录屏 + 后期剪辑指南"
      contains: "OBS"
    - path: "scripts/demo-script-verify.py"
      provides: "Demo Script 可执行性验证脚本"
      contains: "localhost"
  key_links:
    - from: "docs/PRESENTATION/XUNO-DEMO-SCRIPT.md"
      to: "实际代码实现"
      via: "技术名称校准"
      pattern: "FashionSigLIP"
---

<objective>
Demo Script 校准 + 预录 Backup 视频录屏指南

Purpose: 确保 Demo Script 与实际代码实现一致，避免演示时出现"说了但没做"的尴尬。预录 backup 视频作为 Live demo 失败时的零延迟切换方案。

Output:

- 校准后的 Demo Script (FashionCLIP -> FashionSigLIP, 雷达图状态确认等)
- OBS 录屏 + 后期剪辑指南
- Demo Script 可执行性验证脚本
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-competition-demo-sprint-production-validation/11-CONTEXT.md

@docs/PRESENTATION/XUNO-DEMO-SCRIPT.md
@docs/PRESENTATION/DEMO-RECORDING-GUIDE.md

<interfaces>
<!-- Demo Script structure: 8 acts, 3 "wow" moments -->

From XUNO-DEMO-SCRIPT.md:

- 第一幕: 钩子 (0:00-0:08)
- 第二幕: 打开 App 场景入口 (0:08-0:20)
- 第三幕: 第一个"哇" AI 推荐 (0:20-0:35)
- 第四幕: 第二个"哇" 匹配度雷达图 (0:35-0:50)
- 第五幕: 第三个"哇" 跨场景记忆 (0:50-1:05)
- 第六幕: 包容性设计 (1:05-1:25)
- 第七幕: 技术深度 (1:25-1:50)
- 第八幕: 收尾 (1:50-2:20)

Known calibration points:

1. FashionCLIP -> FashionSigLIP (已替换 in Phase 6)
2. 匹配度雷达图 -- 需确认是否已实现
3. "换一件"实时优化 -- 需确认 ItemReplacementService 是否工作
4. "你说过不喜欢高领" 跨场景记忆 -- 需确认 preference memory 实现
   </interfaces>
   </context>

<tasks>

<task type="auto">
  <name>Task 1: Demo Script 根据实际代码校准</name>
  <files>docs/PRESENTATION/XUNO-DEMO-SCRIPT.md, scripts/demo-script-verify.py</files>
  <read_first>
    - docs/PRESENTATION/XUNO-DEMO-SCRIPT.md (完整演示脚本)
    - apps/backend/src/domains/ai-core/ai-stylist/services/item-replacement.service.ts (换一件功能)
    - apps/backend/src/domains/ai-core/ai-stylist/decision-score.service.ts (匹配度评分)
  </read_first>
  <action>
Per D-18: Demo Script 根据实际代码校准。校准 FashionCLIP -> FashionSigLIP、匹配度雷达图是否已实现等。

**Step 1: 扫描代码确认功能实现状态**

需要确认的功能点:

1. FashionSigLIP 替换完成 (Phase 6 已完成，确认即可)
2. 匹配度雷达图/五维评分 (搜索 decision-score, RecommendationBreakdown 等)
3. "换一件" 实时优化 (搜索 item-replacement, ItemReplacementService)
4. 跨场景记忆 "你说过不喜欢高领" (搜索 preference-memory, PreferenceMemoryService)
5. 语音按钮 STT/TTS (Phase 4 已完成，确认即可)
6. 3 套搭配方案同时展示 (确认 recommendation 输出包含 3 套)

```bash
grep -r "FashionSigLIP\|Marqo" ml/ --include="*.py" | head -5
grep -r "decision.*score\|breakdown" apps/backend/src/ --include="*.ts" | head -10
grep -r "ItemReplacement\|item.*replacement" apps/backend/src/ --include="*.ts" | head -5
grep -r "preference.*memory\|PreferenceMemory" apps/backend/src/ --include="*.ts" | head -5
```

**Step 2: 校准 Demo Script**

对 `XUNO-DEMO-SCRIPT.md` 进行以下修改:

1. **全局替换**: `FashionCLIP` -> `FashionSigLIP` (如果有的话)
2. **第四幕 (雷达图)**:
   - 如果五维雷达图已实现: 保留原描述，确认维度名称与代码一致
   - 如果未实现: 修改为"匹配度评分"文字展示，删除雷达图动画描述
   - 修改描述使其与 `decision-score.service.ts` 的实际维度匹配
3. **第五幕 (跨场景记忆)**:
   - 如果 preference memory 已实现: 保留原描述
   - 如果部分实现: 调整为已实现的部分 (如只记住颜色偏好)
4. **第七幕 (技术深度)**:
   - 确认技术栈列表与当前 package.json/requirements.txt 一致
   - 将 "FashionCLIP 微调" 改为 "FashionSigLIP 中文微调"
   - 添加 GLM fallback 路由作为新亮点 (per D-08)
5. **添加校准备注**: 在文件顶部添加校准日期和变更摘要

**Step 3: 创建 `scripts/demo-script-verify.py`** -- Demo Script 可执行性验证:

```python
"""
验证 Demo Script 中描述的每个功能点在本地环境中可用。
运行前需先启动 Docker 全栈 (demo-local.sh)。
"""
import requests
import json
import time

BASE_URL = "http://localhost:3001/api/v1"

# Demo Script 各幕对应的功能检查
CHECKS = [
    {"act": "第一幕", "name": "Backend Health", "url": f"{BASE_URL}/health", "expect_status": 200},
    {"act": "第二幕", "name": "User Login", "method": "POST", "url": f"{BASE_URL}/auth/login", ...},
    {"act": "第三幕", "name": "AI Recommendation", "method": "POST", "url": f"{BASE_URL}/dialog/process", ...},
    {"act": "第四幕", "name": "Score Breakdown", ...},
    {"act": "第五幕", "name": "Preference Memory", ...},
    {"act": "第六幕", "name": "Body-Positive Language", ...},
]
```

脚本对 Demo Script 中的每一幕进行 API 级别验证，输出:

- 各幕功能是否可用
- 响应时间是否在可接受范围 (< 8 秒 per D-09)
- 最终汇总表 (幕 | 功能 | 可用 | 延迟)
  </action>
  <acceptance_criteria> - `XUNO-DEMO-SCRIPT.md` 中无 `FashionCLIP` 字符串 (已替换为 `FashionSigLIP`) - `XUNO-DEMO-SCRIPT.md` 顶部包含校准日期备注 - `XUNO-DEMO-SCRIPT.md` 中技术栈描述与当前代码一致 - `scripts/demo-script-verify.py` 文件存在 - 验证脚本包含至少 5 个功能检查项 - 验证脚本包含 `localhost:3001` 引用
  </acceptance_criteria>
  <verify>
  <automated>cd C:/AiNeed && grep -c "FashionCLIP" docs/PRESENTATION/XUNO-DEMO-SCRIPT.md && grep -c "校准" docs/PRESENTATION/XUNO-DEMO-SCRIPT.md && test -f scripts/demo-script-verify.py && echo "DEMO SCRIPT CALIBRATED"</automated>
  </verify>
  <done>Demo Script 校准完成，所有技术描述与实际代码一致；可执行性验证脚本覆盖 5+ 功能点</done>
  </task>

<task type="auto">
  <name>Task 2: 更新录屏指南 (OBS + 模拟器方案)</name>
  <files>docs/PRESENTATION/DEMO-RECORDING-GUIDE.md</files>
  <read_first>
    - docs/PRESENTATION/DEMO-RECORDING-GUIDE.md (现有录屏指南)
    - docs/PRESENTATION/VIDEO-SCRIPT.md (视频脚本)
  </read_first>
  <action>
Per D-05: 必须预录 backup 视频。录制一段完美 3 分钟视频。
Per D-20: OBS 录屏 + 后期剪辑。模拟器投屏 + 旁白配音，产出 2-3 分钟 MP4。

更新 `docs/PRESENTATION/DEMO-RECORDING-GUIDE.md`，包含以下内容:

### 1. 硬件/软件准备

- OBS Studio 30+ (免费录屏软件)
- Android Studio 模拟器 (API 34, 1080x2400 分辨率)
- 麦克风 (笔记本内置或 USB 麦克风)
- 耳机 (监听旁白质量)

### 2. OBS 配置

- 场景 1: "模拟器投屏" -- 窗口捕获 Android Emulator
- 场景 2: "画中画" -- 模拟器全屏 + 摄像头小窗 (可选)
- 录制格式: MKV (防崩溃), 后期转 MP4
- 分辨率: 1920x1080
- 帧率: 30fps
- 码率: 8000 Kbps

### 3. 录制步骤

1. 启动 Docker 全栈 (demo-local.sh)
2. 启动 Android 模拟器 + App
3. 执行 demo-warmup.sh 预热
4. 使用测试账号登录 (seed profile #1)
5. 按 Demo Script 逐幕操作
6. 每幕之间暂停 2 秒
7. 完成后重播检查

### 4. 后期剪辑 (使用剪映或 DaVinci Resolve)

- 添加旁白配音 (按 VIDEO-SCRIPT.md 的 [配音] 标注)
- 添加字幕 (自动生成 + 手动校对)
- 添加片头片尾 (XUNO logo + 联系方式)
- 背景音乐 (轻柔钢琴，音量 20%)
- 导出: 1080p MP4, H.264

### 5. Backup 方案

- 将最终 MP4 拷贝到手机和演示 U 盘
- 演示设备预装 VLC 播放器
- Live demo 失败时零延迟切换: 按空格键暂停当前画面 -> VLC 全屏播放 backup 视频
  </action>
  <acceptance_criteria> - `DEMO-RECORDING-GUIDE.md` 包含 "OBS" 字符串 - 包含 "模拟器" 或 "Emulator" 引用 - 包含录制参数 (分辨率、帧率、码率) - 包含后期剪辑步骤 - 包含 backup 切换方案 - 包含 "MKV" 和 "MP4" 格式说明
  </acceptance_criteria>
  <verify>
  <automated>cd C:/AiNeed && grep -c "OBS" docs/PRESENTATION/DEMO-RECORDING-GUIDE.md && grep -c "MP4\|MKV" docs/PRESENTATION/DEMO-RECORDING-GUIDE.md && grep -c "backup\|Backup\|备份" docs/PRESENTATION/DEMO-RECORDING-GUIDE.md && echo "RECORDING GUIDE OK"</automated>
  </verify>
  <done>录屏指南更新为 OBS + 模拟器方案，包含完整录制参数、后期步骤和 backup 切换方案</done>
  </task>

</tasks>

<verification>
1. `XUNO-DEMO-SCRIPT.md` 无 `FashionCLIP` 字符串
2. `demo-script-verify.py` 包含 5+ 功能检查
3. `DEMO-RECORDING-GUIDE.md` 包含 OBS 配置和 backup 方案
</verification>

<success_criteria>

- Demo Script 所有技术描述与当前代码实现一致
- 录屏指南覆盖 OBS 配置 + 录制 + 后期 + backup 切换
- 可执行性验证脚本可自动检查 Demo 各幕功能
  </success_criteria>

<output>
After completion, create `.planning/phases/11-competition-demo-sprint-production-validation/11-05-SUMMARY.md`
</output>
