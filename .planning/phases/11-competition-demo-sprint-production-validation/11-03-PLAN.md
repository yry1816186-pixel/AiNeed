---
phase: 11-competition-demo-sprint-production-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/features/home/components/StyleEvolutionChart.tsx
  - (其他由 tsc --noEmit 扫描发现的错误文件)
autonomous: true
requirements: [D-16]

must_haves:
  truths:
    - "tsc --noEmit 在 apps/mobile 目录下零错误"
    - "tsc --noEmit 在 apps/backend 目录下零错误"
    - "StyleEvolutionChart.tsx 的 9 个 TS 错误全部修复"
  artifacts:
    - path: "apps/mobile/src/features/home/components/StyleEvolutionChart.tsx"
      provides: "修复后的风格进化图表组件"
  key_links:
    - from: "tsc --noEmit"
      to: "全部 .tsx/.ts 文件"
      via: "TypeScript compiler"
      pattern: "error TS"
---

<objective>
全局 tsc --noEmit 一次性修复所有 TypeScript 编译错误

Purpose: 比赛演示零崩溃的基础。TS 编译错误可能在运行时导致白屏或异常，修复它们确保演示流畅。

Output: 全项目 TypeScript 零编译错误
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-competition-demo-sprint-production-validation/11-CONTEXT.md

@apps/mobile/src/features/home/components/StyleEvolutionChart.tsx

<interfaces>
<!-- Known problem file: StyleEvolutionChart.tsx -->

From apps/mobile/src/features/home/components/StyleEvolutionChart.tsx:

- Line 39: `style?: Animated.AnimatedProps<ViewStyle> | ViewStyle` -- potential type mismatch
- Uses `useStyles(colors)` where `colors` is `flatColors` -- may have typing issues
- Uses `DesignTokens.typography.sizes.xs` -- may have typing issues if sizes not properly typed
- `generatePathData` function defined but return value unused in rendering

Current imports:

```typescript
import { DesignTokens } from "../../../design-system/theme/tokens/design-tokens";
import { createStyles } from "../../../shared/contexts/ThemeContext";
import { flatColors as colors } from "../../../design-system/theme";
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全局 tsc --noEmit 错误扫描与修复</name>
  <files>apps/mobile/src/features/home/components/StyleEvolutionChart.tsx, (其他由扫描发现的错误文件)</files>
  <read_first>
    - apps/mobile/src/features/home/components/StyleEvolutionChart.tsx (已知 9 个错误)
    - apps/mobile/tsconfig.json (TypeScript 配置)
    - apps/backend/tsconfig.json (后端 TypeScript 配置)
  </read_first>
  <action>
Per D-16: 全局 tsc --noEmit 一次性修复。不仅修 StyleEvolutionChart.tsx 的 9 个错误，扫描全部编译错误一次性修复。

**Step 1: 全面扫描**

```bash
cd apps/mobile && npx tsc --noEmit 2>&1 | head -200
cd apps/backend && npx tsc --noEmit 2>&1 | head -200
```

收集所有错误输出，按文件分组。

**Step 2: 按文件修复 (逐个击破)**

已知 StyleEvolutionChart.tsx 问题 (基于代码审查):

1. `style` prop 类型: `Animated.AnimatedProps<ViewStyle> | ViewStyle` 可能不兼容 -- 改为 `ViewStyle | undefined`
2. `useStyles(colors)` 中 `flatColors` 类型可能不匹配 `createStyles` 期望的 theme 类型 -- 检查 ThemeContext 的类型定义
3. `DesignTokens.typography.sizes.xs` 可能返回 `undefined` -- 添加 fallback 值如 `|| 12`
4. `lineAnimations` 的 `useMemo` 缺少正确的依赖数组 -- 将 `data` 添加到依赖
5. `transformOrigin` 属性在 React Native 中不支持 (Web only) -- 移除该属性
6. `generatePathData` 返回值未被使用 -- 移除或使用它
7. `Animated.View` 包裹非动画子元素的类型问题

修复原则:

- 不改变组件功能和视觉效果
- 优先使用 type assertion (`as`) 而非重构，保持改动最小化
- 不引入新依赖
- 对每个文件: 修复后立即验证 `tsc --noEmit` 确认该文件错误清零

**Step 3: 验证零错误**

```bash
cd apps/mobile && npx tsc --noEmit 2>&1 | grep "error TS" | wc -l  # 期望 0
cd apps/backend && npx tsc --noEmit 2>&1 | grep "error TS" | wc -l  # 期望 0
```

  </action>
  <acceptance_criteria>
    - `cd apps/mobile && npx tsc --noEmit 2>&1 | grep -c "error TS"` 输出 0
    - `cd apps/backend && npx tsc --noEmit 2>&1 | grep -c "error TS"` 输出 0
    - StyleEvolutionChart.tsx 不再有 TS 编译错误
  </acceptance_criteria>
  <verify>
    <automated>cd C:/AiNeed/apps/mobile && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0"</automated>
  </verify>
  <done>apps/mobile 和 apps/backend tsc --noEmit 零错误输出</done>
</task>

</tasks>

<verification>
1. `cd apps/mobile && npx tsc --noEmit` -- 零 error TS 输出
2. `cd apps/backend && npx tsc --noEmit` -- 零 error TS 输出
3. 所有修复文件 lint 通过
</verification>

<success_criteria>

- apps/mobile tsc --noEmit 零错误
- apps/backend tsc --noEmit 零错误
- 修复不改变组件功能和视觉效果
  </success_criteria>

<output>
After completion, create `.planning/phases/11-competition-demo-sprint-production-validation/11-03-SUMMARY.md`
</output>
