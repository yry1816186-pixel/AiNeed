---
wave: 3
depends_on: [02-02]
files_modified:
  - apps/mobile/src/stores/aiStylistStore.ts
  - apps/mobile/src/stores/index.ts
  - apps/mobile/src/services/api/ai-stylist.api.ts
  - apps/mobile/src/components/aistylist/OutfitPlanView.tsx
  - apps/mobile/src/components/aistylist/ReasoningCard.tsx
  - apps/mobile/src/components/aistylist/ItemReplacementModal.tsx
  - apps/mobile/src/components/aistylist/FeedbackModal.tsx
  - apps/mobile/src/components/aistylist/SceneQuickButtons.tsx
  - apps/mobile/src/components/aistylist/PresetQuestionsModal.tsx
  - apps/mobile/src/components/aistylist/WeatherBadge.tsx
  - apps/mobile/src/components/aistylist/index.ts
requirements: [AIS-01, AIS-02, AIS-04, AIS-05, AIS-06, AIS-10, AIS-11, AIS-12]
autonomous: true
---

# Plan 02-03: 移动端组件 + Store

## 目标

创建 AI 造型师专用的 Zustand Store、扩展 API 客户端、新建 7 个 UI 组件。本 Plan 不涉及页面集成（Plan 02-04 处理），仅构建可复用的组件和状态管理。

## 任务

### Task 1: 新建 aiStylistStore — Zustand 状态管理 (D-27)

<read_first>
- apps/mobile/src/stores/index.ts (existing store exports pattern)
- apps/mobile/src/stores/homeStore.ts (reference pattern for Zustand store)
- apps/mobile/src/services/api/ai-stylist.api.ts (API types)
</read_first>

<action>
创建文件 `apps/mobile/src/stores/aiStylistStore.ts`：

```typescript
import { create } from "zustand";
import {
  aiStylistApi,
  type AiStylistSessionResponse,
  type AiStylistResolution,
  type AiStylistOutfitItem,
} from "../services/api/ai-stylist.api";

/**
 * AI 造型师 Store — D-27
 * 管理当前方案/历史/偏好，替代组件内 useState
 */

// 方案页数据
export interface OutfitPlanDetail {
  sessionId: string;
  lookSummary: string;
  whyItFits: string[];
  weatherInfo?: {
    temperature: number;
    condition: string;
    suggestion: string;
  };
  outfits: Array<{
    title: string;
    items: AiStylistOutfitItem[];
    styleExplanation: string[];
    estimatedTotalPrice?: number;
  }>;
  createdAt: string;
}

// 同类替代商品
export interface AlternativeItem {
  id: string;
  name: string;
  category: string;
  imageUrl: string | null;
  price: number | null;
  brand: string | null;
  tags: string[];
  matchScore: number;
}

// 预设引导问题
export interface PresetQuestion {
  id: string;
  text: string;
  icon: string;
  category: "daily" | "style" | "occasion" | "weather";
}

// 日历日期
export interface CalendarDay {
  date: string;
  sessionCount: number;
  hasOutfitPlan: boolean;
}

// 归档会话
export interface ArchivedSession {
  id: string;
  status: string;
  goal?: string;
  hasOutfitPlan: boolean;
  createdAt: string;
  updatedAt: string;
}

interface AiStylistState {
  // 当前会话
  currentSessionId: string | null;
  currentOutfitPlan: OutfitPlanDetail | null;

  // 加载状态
  isLoading: boolean;
  isGenerating: boolean;
  error: string | null;

  // 预设问题
  presetQuestions: PresetQuestion[];
  isNewUser: boolean;

  // 日历归档
  calendarDays: CalendarDay[];
  archivedSessions: ArchivedSession[];

  // 替换相关
  alternatives: AlternativeItem[];
  isAlternativesLoading: boolean;

  // Actions
  createSession: (entry?: string, goal?: string) => Promise<string | null>;
  sendMessage: (message: string, latitude?: number, longitude?: number) => Promise<AiStylistSessionResponse | null>;
  fetchOutfitPlan: (sessionId: string) => Promise<void>;
  fetchAlternatives: (sessionId: string, outfitIndex: number, itemIndex: number) => Promise<void>;
  replaceItem: (sessionId: string, outfitIndex: number, itemIndex: number, newItemId: string) => Promise<boolean>;
  submitFeedback: (sessionId: string, outfitIndex: number, action: "like" | "dislike", itemId?: string, rating?: number, dislikeReason?: string) => Promise<boolean>;
  fetchPresetQuestions: () => Promise<void>;
  fetchCalendarDays: (year: number, month: number) => Promise<void>;
  fetchArchivedSessions: (date: string) => Promise<void>;
  setCurrentSessionId: (id: string | null) => void;
  clearError: () => void;
  reset: () => void;
}

const initialState = {
  currentSessionId: null,
  currentOutfitPlan: null,
  isLoading: false,
  isGenerating: false,
  error: null,
  presetQuestions: [],
  isNewUser: false,
  calendarDays: [],
  archivedSessions: [],
  alternatives: [],
  isAlternativesLoading: false,
};

export const useAiStylistStore = create<AiStylistState>((set, get) => ({
  ...initialState,

  createSession: async (entry?: string, goal?: string) => {
    set({ isLoading: true, error: null });
    try {
      const response = await aiStylistApi.createSession({ entry, goal });
      if (response.success && response.data) {
        const sessionId = response.data.sessionId;
        set({ currentSessionId: sessionId, isLoading: false });
        return sessionId ?? null;
      }
      set({ error: response.error?.message || "创建会话失败", isLoading: false });
      return null;
    } catch (e: any) {
      set({ error: e.message || "创建会话失败", isLoading: false });
      return null;
    }
  },

  sendMessage: async (message: string, latitude?: number, longitude?: number) => {
    const { currentSessionId } = get();
    if (!currentSessionId) return null;
    set({ isGenerating: true, error: null });
    try {
      const response = await aiStylistApi.sendMessage(currentSessionId, message, latitude, longitude);
      if (response.success && response.data) {
        set({ isGenerating: false });
        return response.data;
      }
      set({ error: response.error?.message || "发送消息失败", isGenerating: false });
      return null;
    } catch (e: any) {
      set({ error: e.message || "发送消息失败", isGenerating: false });
      return null;
    }
  },

  fetchOutfitPlan: async (sessionId: string) => {
    set({ isLoading: true, error: null });
    try {
      const response = await aiStylistApi.getOutfitPlan(sessionId);
      if (response.success && response.data) {
        set({ currentOutfitPlan: response.data as OutfitPlanDetail, isLoading: false });
      } else {
        set({ error: response.error?.message || "获取方案失败", isLoading: false });
      }
    } catch (e: any) {
      set({ error: e.message || "获取方案失败", isLoading: false });
    }
  },

  fetchAlternatives: async (sessionId: string, outfitIndex: number, itemIndex: number) => {
    set({ isAlternativesLoading: true, error: null });
    try {
      const response = await aiStylistApi.getAlternatives(sessionId, outfitIndex, itemIndex);
      if (response.success && response.data) {
        set({ alternatives: (response.data as any).items || response.data as unknown as AlternativeItem[], isAlternativesLoading: false });
      } else {
        set({ isAlternativesLoading: false });
      }
    } catch {
      set({ isAlternativesLoading: false });
    }
  },

  replaceItem: async (sessionId: string, outfitIndex: number, itemIndex: number, newItemId: string) => {
    try {
      const response = await aiStylistApi.replaceItem(sessionId, outfitIndex, itemIndex, newItemId);
      if (response.success) {
        // 刷新方案数据
        await get().fetchOutfitPlan(sessionId);
        return true;
      }
      return false;
    } catch {
      return false;
    }
  },

  submitFeedback: async (sessionId: string, outfitIndex: number, action: "like" | "dislike", itemId?: string, rating?: number, dislikeReason?: string) => {
    try {
      const response = await aiStylistApi.submitFeedback(sessionId, { outfitIndex, action, itemId, rating, dislikeReason });
      return response.success;
    } catch {
      return false;
    }
  },

  fetchPresetQuestions: async () => {
    try {
      const response = await aiStylistApi.getPresetQuestions();
      if (response.success && response.data) {
        const data = response.data as any;
        set({
          presetQuestions: data.questions || [],
          isNewUser: data.isNewUser ?? false,
        });
      }
    } catch {
      // 静默失败
    }
  },

  fetchCalendarDays: async (year: number, month: number) => {
    try {
      const response = await aiStylistApi.getCalendarDays(year, month);
      if (response.success && response.data) {
        set({ calendarDays: response.data as unknown as CalendarDay[] });
      }
    } catch {
      // 静默失败
    }
  },

  fetchArchivedSessions: async (date: string) => {
    try {
      const response = await aiStylistApi.getSessionsByDate(date);
      if (response.success && response.data) {
        set({ archivedSessions: response.data as unknown as ArchivedSession[] });
      }
    } catch {
      // 静默失败
    }
  },

  setCurrentSessionId: (id) => set({ currentSessionId: id }),
  clearError: () => set({ error: null }),
  reset: () => set(initialState),
}));
```
</action>

<acceptance_criteria>
- 文件 `apps/mobile/src/stores/aiStylistStore.ts` 存在
- 文件导出 `useAiStylistStore` hook
- Store 包含 `currentOutfitPlan`, `presetQuestions`, `isNewUser`, `calendarDays`, `alternatives` 状态
- Store 包含 `createSession`, `sendMessage`, `fetchOutfitPlan`, `fetchAlternatives`, `replaceItem`, `submitFeedback`, `fetchPresetQuestions`, `fetchCalendarDays`, `fetchArchivedSessions` actions
- TypeScript 编译通过
</acceptance_criteria>

---

### Task 2: 更新 stores/index.ts — 导出 aiStylistStore

<read_first>
- apps/mobile/src/stores/index.ts (line 1-20, export pattern)
</read_first>

<action>
在 `stores/index.ts` 的 export 列表中添加：
```typescript
export * from "./aiStylistStore";
```

在 `export * from "./app.store";` 之后添加。
</action>

<acceptance_criteria>
- `stores/index.ts` 包含 `export * from "./aiStylistStore";` 行
</acceptance_criteria>

---

### Task 3: 扩展 ai-stylist.api.ts — 新增 API 方法

<read_first>
- apps/mobile/src/services/api/ai-stylist.api.ts (full file, 183 lines)
</read_first>

<action>
在 `aiStylistApi` 对象中添加以下方法（在 `getSuggestions` 之后）：

```typescript
  getOutfitPlan: (
    sessionId: string,
  ): Promise<ApiResponse<unknown>> =>
    apiClient.get(`/ai-stylist/sessions/${sessionId}/outfit-plan`),

  getAlternatives: (
    sessionId: string,
    outfitIndex: number,
    itemIndex: number,
    limit: number = 10,
  ): Promise<ApiResponse<unknown>> =>
    apiClient.get(
      `/ai-stylist/sessions/${sessionId}/items/alternatives?outfitIndex=${outfitIndex}&itemIndex=${itemIndex}&limit=${limit}`,
    ),

  replaceItem: (
    sessionId: string,
    outfitIndex: number,
    itemIndex: number,
    newItemId: string,
  ): Promise<ApiResponse<unknown>> =>
    apiClient.post(
      `/ai-stylist/sessions/${sessionId}/items/replace`,
      { outfitIndex, itemIndex, newItemId },
    ),

  submitFeedback: (
    sessionId: string,
    data: {
      outfitIndex: number;
      action: "like" | "dislike";
      itemId?: string;
      rating?: number;
      dislikeReason?: string;
    },
  ): Promise<ApiResponse<{ success: boolean; message: string }>> =>
    apiClient.post(
      `/ai-stylist/sessions/${sessionId}/feedback`,
      data,
    ),

  getPresetQuestions: (): Promise<ApiResponse<unknown>> =>
    apiClient.get("/ai-stylist/preset-questions"),

  getCalendarDays: (
    year: number,
    month: number,
  ): Promise<ApiResponse<unknown>> =>
    apiClient.get(`/ai-stylist/sessions/calendar?year=${year}&month=${month}`),

  getSessionsByDate: (
    date: string,
  ): Promise<ApiResponse<unknown>> =>
    apiClient.get(`/ai-stylist/sessions/date/${date}`),
```

同时修改 `sendMessage` 方法签名，添加可选的 latitude/longitude 参数：

```typescript
  sendMessage: (
    sessionId: string,
    message: string,
    latitude?: number,
    longitude?: number,
  ): Promise<ApiResponse<AiStylistSessionResponse>> => {
    const payload: Record<string, unknown> = { message };
    if (latitude !== undefined && longitude !== undefined) {
      payload.latitude = latitude;
      payload.longitude = longitude;
    }
    return apiClient.post<AiStylistSessionResponse>(
      `/ai-stylist/sessions/${sessionId}/messages`,
      payload,
    );
  },
```
</action>

<acceptance_criteria>
- `ai-stylist.api.ts` 的 `aiStylistApi` 对象包含 `getOutfitPlan` 方法
- `ai-stylist.api.ts` 的 `aiStylistApi` 对象包含 `getAlternatives` 方法
- `ai-stylist.api.ts` 的 `aiStylistApi` 对象包含 `replaceItem` 方法
- `ai-stylist.api.ts` 的 `aiStylistApi` 对象包含 `submitFeedback` 方法（带 rating/dislikeReason）
- `ai-stylist.api.ts` 的 `aiStylistApi` 对象包含 `getPresetQuestions` 方法
- `ai-stylist.api.ts` 的 `aiStylistApi` 对象包含 `getCalendarDays` 方法
- `ai-stylist.api.ts` 的 `aiStylistApi` 对象包含 `getSessionsByDate` 方法
- `sendMessage` 方法接受 `latitude?` 和 `longitude?` 参数
- TypeScript 编译通过
</acceptance_criteria>

---

### Task 4: 新建 OutfitPlanView 组件 — 方案页核心视图 (AIS-01)

<read_first>
- apps/mobile/src/components/aicompanion/OutfitCard.tsx (existing OutfitCard pattern)
- apps/mobile/src/theme/tokens/design-tokens.ts (DesignTokens)
- apps/mobile/src/stores/aiStylistStore.ts (OutfitPlanDetail type from Task 1)
</read_first>

<action>
创建文件 `apps/mobile/src/components/aistylist/OutfitPlanView.tsx`：

方案页核心视图组件，布局：
- 顶部：方案标题 + 天气徽章
- 中部：单品卡片列表（横向滚动），每个卡片有"替换"按钮
- 底部：可折叠推荐理由卡片

组件接口：
```typescript
interface OutfitPlanViewProps {
  plan: OutfitPlanDetail;
  onItemReplace: (outfitIndex: number, itemIndex: number) => void;
  onItemPress: (item: AiStylistOutfitItem) => void;
  onFeedback: (action: "like" | "dislike") => void;
}
```

实现要点：
1. 使用 `ScrollView` 横向展示多套方案（方案 Tab 切换）
2. 每个单品卡片显示：图片 + 名称 + 价格 + 品牌标签 + "替换"按钮
3. 底部集成 `ReasoningCard` 组件（可折叠推荐理由）
4. 使用 `DesignTokens` 主题令牌
5. 使用 `react-native-reanimated` 实现入场动画

单品卡片样式参考现有 `OutfitCard.tsx` 的 `OutfitItemCard`，但增加"替换"按钮。
</action>

<acceptance_criteria>
- 文件 `apps/mobile/src/components/aistylist/OutfitPlanView.tsx` 存在
- 组件导出 `OutfitPlanView`
- 组件接受 `OutfitPlanDetail` 类型的 `plan` prop
- 组件包含方案 Tab 切换（多套方案时）
- 每个单品卡片包含"替换"按钮
- 底部包含推荐理由区域
- TypeScript 编译通过
</acceptance_criteria>

---

### Task 5: 新建 ReasoningCard 组件 — 可折叠推荐理由 (AIS-04)

<read_first>
- apps/mobile/src/theme/tokens/design-tokens.ts (DesignTokens)
- apps/mobile/src/components/aicompanion/OutfitCard.tsx (whySection pattern)
</read_first>

<action>
创建文件 `apps/mobile/src/components/aistylist/ReasoningCard.tsx`：

可折叠推荐理由卡片组件：
- 默认收起，点击展开
- 展示三段理由：体型适配 + 色彩搭配 + 场合适合度
- 使用 Reanimated 实现折叠/展开动画（height 从 0 到 contentHeight）

```typescript
interface ReasoningCardProps {
  reasons: string[];  // whyItFits 数组
  defaultExpanded?: boolean;
}
```

实现要点：
1. 使用 `useSharedValue` + `useAnimatedStyle` 实现高度动画
2. 使用 `measure` 或 `onLayout` 获取内容高度
3. 收起时显示第一行理由 + "展开更多"按钮
4. 展开时显示全部理由 + "收起"按钮
5. 箭头图标旋转动画（0deg -> 180deg）
</action>

<acceptance_criteria>
- 文件 `apps/mobile/src/components/aistylist/ReasoningCard.tsx` 存在
- 组件导出 `ReasoningCard`
- 组件接受 `reasons: string[]` prop
- 默认收起状态，点击可展开
- 使用 Reanimated 动画实现折叠/展开
- TypeScript 编译通过
</acceptance_criteria>

---

### Task 6: 新建 ItemReplacementModal 组件 — 单品替换弹窗 (AIS-05)

<read_first>
- apps/mobile/src/stores/aiStylistStore.ts (AlternativeItem type)
- apps/mobile/src/theme/tokens/design-tokens.ts (DesignTokens)
</read_first>

<action>
创建文件 `apps/mobile/src/components/aistylist/ItemReplacementModal.tsx`：

单品替换弹窗组件：
- Modal 弹窗，从底部滑入
- 顶部标题："替换单品" + 原商品名称
- 商品列表（FlatList），每个商品卡片显示：图片 + 名称 + 价格 + 品牌 + 匹配分
- 点击商品触发替换
- 支持加载状态（骨架屏）

```typescript
interface ItemReplacementModalProps {
  visible: boolean;
  originalItemName: string;
  alternatives: AlternativeItem[];
  isLoading: boolean;
  onSelect: (itemId: string) => void;
  onClose: () => void;
}
```

实现要点：
1. 使用 React Native `Modal` 组件
2. `FlatList` 渲染替代商品列表
3. 每个商品卡片右侧显示匹配分百分比
4. 加载中显示 3 个骨架屏占位
5. 空状态显示"暂无替代商品"
</action>

<acceptance_criteria>
- 文件 `apps/mobile/src/components/aistylist/ItemReplacementModal.tsx` 存在
- 组件导出 `ItemReplacementModal`
- 组件接受 `alternatives: AlternativeItem[]` prop
- 组件包含 `FlatList` 渲染替代商品
- 组件包含加载状态和空状态
- TypeScript 编译通过
</acceptance_criteria>

---

### Task 7: 新建 FeedbackModal 组件 — 反馈弹窗 (AIS-10)

<read_first>
- apps/mobile/src/theme/tokens/design-tokens.ts (DesignTokens)
</read_first>

<action>
创建文件 `apps/mobile/src/components/aistylist/FeedbackModal.tsx`：

反馈弹窗组件：
- Modal 弹窗，从底部滑入
- 顶部：喜欢/不喜欢 两个大按钮
- 如果选"不喜欢"：展示原因选择（太贵/不适合/颜色不对/风格不符/其他）
- 评分区域：1-5 星评分
- 提交按钮

```typescript
interface FeedbackModalProps {
  visible: boolean;
  onSubmit: (data: {
    action: "like" | "dislike";
    rating?: number;
    dislikeReason?: string;
  }) => void;
  onClose: () => void;
}
```

不喜欢原因枚举：
```typescript
const DISLIKE_REASONS = [
  { value: "too_expensive", label: "太贵了" },
  { value: "not_suitable", label: "不适合我" },
  { value: "wrong_color", label: "颜色不对" },
  { value: "wrong_style", label: "风格不符" },
  { value: "other", label: "其他原因" },
];
```

实现要点：
1. 喜欢/不喜欢按钮使用图标 + 文字，选中高亮
2. 星级评分使用 5 个星星图标，支持点击选择
3. 不喜欢原因使用 Chip/Tag 选择样式，多选转单选
4. 提交后自动关闭弹窗
</action>

<acceptance_criteria>
- 文件 `apps/mobile/src/components/aistylist/FeedbackModal.tsx` 存在
- 组件导出 `FeedbackModal`
- 组件包含喜欢/不喜欢选择
- 组件包含 1-5 星评分
- 组件包含不喜欢原因选择（5 个选项）
- TypeScript 编译通过
</acceptance_criteria>

---

### Task 8: 新建 SceneQuickButtons 组件 — 场景快捷按钮 (AIS-02, AIS-11)

<read_first>
- apps/mobile/src/theme/tokens/design-tokens.ts (DesignTokens)
</read_first>

<action>
创建文件 `apps/mobile/src/components/aistylist/SceneQuickButtons.tsx`：

场景快捷按钮组件：
- 6 个固定场景按钮：通勤/约会/运动/面试/休闲/旅行
- 横向滚动展示
- 每个按钮包含图标 + 文字
- 点击触发对应场景推荐

```typescript
interface SceneQuickButtonsProps {
  onSceneSelect: (scene: string, message: string) => void;
  disabled?: boolean;
}

const SCENES = [
  { id: "commute", label: "通勤", icon: "briefcase-outline", message: "帮我搭一套通勤穿搭" },
  { id: "date", label: "约会", icon: "heart-outline", message: "约会怎么穿" },
  { id: "sport", label: "运动", icon: "fitness-outline", message: "运动穿搭推荐" },
  { id: "interview", label: "面试", icon: "ribbon-outline", message: "我要一套面试穿搭" },
  { id: "casual", label: "休闲", icon: "sunny-outline", message: "今天休闲穿搭" },
  { id: "travel", label: "旅行", icon: "airplane-outline", message: "旅行穿搭推荐" },
];
```

实现要点：
1. 使用 `ScrollView` 横向滚动
2. 每个按钮使用 `TouchableOpacity` + 图标 + 文字
3. 按钮样式：圆角胶囊形，浅色背景 + 品牌色边框
4. 点击时显示短暂按压效果
5. disabled 状态下降低透明度
</action>

<acceptance_criteria>
- 文件 `apps/mobile/src/components/aistylist/SceneQuickButtons.tsx` 存在
- 组件导出 `SceneQuickButtons`
- 组件包含 6 个场景按钮（通勤/约会/运动/面试/休闲/旅行）
- 每个按钮包含图标和文字
- 点击回调传递 `scene` id 和 `message` 文本
- TypeScript 编译通过
</acceptance_criteria>

---

### Task 9: 新建 PresetQuestionsModal 组件 — 预设引导问题 (AIS-12)

<read_first>
- apps/mobile/src/stores/aiStylistStore.ts (PresetQuestion type)
</read_first>

<action>
创建文件 `apps/mobile/src/components/aistylist/PresetQuestionsModal.tsx`：

预设引导问题弹窗组件：
- Modal 弹窗，从底部滑入
- 顶部标题："你想了解什么？"
- 问题列表，每个问题是一个大按钮（图标 + 文字）
- 点击问题触发发送
- 可关闭（跳过引导）

```typescript
interface PresetQuestionsModalProps {
  visible: boolean;
  questions: PresetQuestion[];
  onSelect: (question: PresetQuestion) => void;
  onClose: () => void;
}
```

实现要点：
1. 半屏 Modal，背景半透明遮罩
2. 问题按钮使用 `TouchableOpacity`，大尺寸方便点击
3. 每个按钮左侧图标 + 右侧文字
4. 底部"跳过"链接
5. 入场动画：从底部滑入 + 淡入
</action>

<acceptance_criteria>
- 文件 `apps/mobile/src/components/aistylist/PresetQuestionsModal.tsx` 存在
- 组件导出 `PresetQuestionsModal`
- 组件接受 `questions: PresetQuestion[]` prop
- 组件包含"跳过"关闭选项
- TypeScript 编译通过
</acceptance_criteria>

---

### Task 10: 新建 WeatherBadge 组件 — 天气信息徽章 (AIS-06)

<read_first>
- apps/mobile/src/stores/aiStylistStore.ts (OutfitPlanDetail.weatherInfo type)
</read_first>

<action>
创建文件 `apps/mobile/src/components/aistylist/WeatherBadge.tsx`：

天气信息徽章组件：
- 小型徽章，显示在方案页顶部
- 内容：天气图标 + 温度 + 天气状况 + 位置
- 点击展开显示详细天气建议

```typescript
interface WeatherBadgeProps {
  temperature: number;
  condition: string;
  location?: string;
  suggestion?: string;
}
```

实现要点：
1. 紧凑布局：图标 + 温度 + 天气文字
2. 背景使用浅色渐变
3. 天气图标映射：晴 -> sunny-outline, 雨 -> rainy-outline, 雪 -> snow-outline, 云 -> cloudy-outline
4. 点击展开显示 `suggestion` 文本
5. 无天气数据时不渲染（return null）
</action>

<acceptance_criteria>
- 文件 `apps/mobile/src/components/aistylist/WeatherBadge.tsx` 存在
- 组件导出 `WeatherBadge`
- 组件接受 `temperature`, `condition`, `location?`, `suggestion?` props
- 无天气数据时返回 null
- TypeScript 编译通过
</acceptance_criteria>

---

### Task 11: 新建组件索引文件

<read_first>
- apps/mobile/src/components/aicompanion/index.ts (existing index pattern)
</read_first>

<action>
创建文件 `apps/mobile/src/components/aistylist/index.ts`：

```typescript
export { OutfitPlanView } from "./OutfitPlanView";
export { ReasoningCard } from "./ReasoningCard";
export { ItemReplacementModal } from "./ItemReplacementModal";
export { FeedbackModal } from "./FeedbackModal";
export { SceneQuickButtons } from "./SceneQuickButtons";
export { PresetQuestionsModal } from "./PresetQuestionsModal";
export { WeatherBadge } from "./WeatherBadge";
```
</action>

<acceptance_criteria>
- 文件 `apps/mobile/src/components/aistylist/index.ts` 存在
- 文件导出所有 7 个组件
- TypeScript 编译通过
</acceptance_criteria>

---

## 验证标准

1. TypeScript 编译通过：`cd apps/mobile && npx tsc --noEmit` 退出码 0
2. aiStylistStore 包含所有必要状态和 actions
3. API 客户端包含所有新端点方法
4. 7 个组件文件创建完成
5. 组件索引文件正确导出所有组件

## must_haves

- [ ] aiStylistStore 创建完成，包含 OutfitPlanDetail/AlternativeItem/PresetQuestion/CalendarDay 类型
- [ ] ai-stylist.api.ts 扩展了 7 个新 API 方法
- [ ] 7 个组件创建完成（OutfitPlanView, ReasoningCard, ItemReplacementModal, FeedbackModal, SceneQuickButtons, PresetQuestionsModal, WeatherBadge）
- [ ] stores/index.ts 导出 aiStylistStore
- [ ] 组件索引文件正确导出
- [ ] TypeScript 编译零错误
