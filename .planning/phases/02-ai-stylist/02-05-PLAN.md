---
wave: 5
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - apps/backend/prisma/schema.prisma (schema push only)
requirements: [AIS-01, AIS-02, AIS-03, AIS-04, AIS-05, AIS-06, AIS-07, AIS-08, AIS-09, AIS-10, AIS-11, AIS-12]
autonomous: true
---

# Plan 02-05: 集成验证 + Schema Push

## 目标

执行数据库 Schema Push，验证所有 12 个需求（AIS-01 ~ AIS-12）的实现完整性，确保后端和移动端编译通过，端到端流程可运行。

## 任务

### Task 1: [BLOCKING] 执行 Prisma Schema Push

<read_first>
- apps/backend/prisma/schema.prisma (verify AiStylistSession.status field exists)
- apps/backend/.env (DATABASE_URL must be configured)
</read_first>

<action>
执行数据库 Schema Push 命令：

```bash
cd apps/backend && npx prisma db push --accept-data-loss
```

如果命令失败（如数据库未配置），记录错误并标记此任务需要手动执行。

验证 Schema Push 成功：
```bash
cd apps/backend && npx prisma db pull --print
```
检查输出中 `AiStylistSession` 表是否包含 `status` 列。
</action>

<acceptance_criteria>
- `npx prisma db push` 命令执行成功（退出码 0），或记录了需要手动执行的说明
- `AiStylistSession` 表包含 `status` 列（如果数据库可用）
- `AiStylistSession` 表包含 `@@index([userId, status])` 和 `@@index([userId, createdAt])` 索引
</acceptance_criteria>

---

### Task 2: 后端 TypeScript 编译验证

<read_first>
- apps/backend/tsconfig.json
</read_first>

<action>
执行后端 TypeScript 编译检查：

```bash
cd apps/backend && npx tsc --noEmit
```

如果有编译错误，逐一修复：
1. 检查新服务文件的 import 路径是否正确
2. 检查 DTO 的装饰器是否完整
3. 检查 Controller 的方法签名是否与 Service 匹配
4. 检查类型定义是否一致

修复后重新编译直到零错误。
</action>

<acceptance_criteria>
- `npx tsc --noEmit` 在 `apps/backend` 目录执行退出码 0
- 零 TypeScript 编译错误
</acceptance_criteria>

---

### Task 3: 移动端 TypeScript 编译验证

<read_first>
- apps/mobile/tsconfig.json
</read_first>

<action>
执行移动端 TypeScript 编译检查：

```bash
cd apps/mobile && npx tsc --noEmit
```

如果有编译错误，逐一修复：
1. 检查 aiStylistStore 的类型导入是否正确
2. 检查新组件的 props 类型是否匹配
3. 检查导航类型是否包含新路由
4. 检查 API 客户端方法签名是否与 Store 调用匹配

修复后重新编译直到零错误。
</action>

<acceptance_criteria>
- `npx tsc --noEmit` 在 `apps/mobile` 目录执行退出码 0
- 零 TypeScript 编译错误
</acceptance_criteria>

---

### Task 4: 需求覆盖验证 — 逐条检查 AIS-01 ~ AIS-12

<read_first>
- apps/backend/src/modules/ai-stylist/ (all new and modified files)
- apps/mobile/src/screens/AiStylistScreen.tsx
- apps/mobile/src/components/aistylist/ (all components)
- apps/mobile/src/stores/aiStylistStore.ts
</read_first>

<action>
逐条验证 12 个需求的实现：

| 需求 | 验证方法 | 预期结果 |
|------|----------|----------|
| AIS-01 方案页交互 | grep `OutfitPlanView` in AiStylistScreen.tsx | 组件已集成 |
| AIS-02 MVP 输入方式 | grep `SceneQuickButtons` in AiStylistScreen.tsx | 6 个场景按钮已集成 |
| AIS-03 画像驱动推荐 | grep `buildUserContext` in context.service.ts | 已有实现，无需修改 |
| AIS-04 可折叠推荐理由 | grep `ReasoningCard` in OutfitPlanView.tsx | 组件已集成 |
| AIS-05 单品替换交互 | grep `ItemReplacementModal` in AiStylistScreen.tsx + grep `replaceItem` in item-replacement.service.ts | 弹窗 + 后端服务已实现 |
| AIS-06 天气智能集成 | grep `WeatherIntegrationService` in ai-stylist.module.ts + grep `WeatherBadge` in OutfitPlanView.tsx | 后端服务 + 前端徽章已实现 |
| AIS-07 方案历史归档 | grep `SessionCalendarScreen` in navigation + grep `SessionArchiveService` in module | 日历页面 + 后端服务已实现 |
| AIS-08 AI 安全过滤 | grep `STYLIST_SYSTEM_PROMPT` in system-prompt.ts | 已有安全约束，复用 |
| AIS-09 API 限流降级 | grep `AiQuotaGuard` in controller + grep `rate_limiter` in ml/ | 已有限流降级机制 |
| AIS-10 推荐反馈机制 | grep `FeedbackModal` in AiStylistScreen.tsx + grep `rating` in SubmitFeedbackDto | 反馈弹窗 + 评分 DTO 已实现 |
| AIS-11 场景化快捷推荐 | grep `SceneQuickButtons` + grep `onSceneSelect` | 场景按钮触发一键推荐 |
| AIS-12 预设引导问题 | grep `PresetQuestionsModal` in AiStylistScreen.tsx + grep `PresetQuestionsService` | 引导弹窗 + 后端服务已实现 |

对每条需求执行 grep 验证，记录通过/未通过状态。
</action>

<acceptance_criteria>
- AIS-01: `AiStylistScreen.tsx` 包含 `OutfitPlanView`
- AIS-02: `AiStylistScreen.tsx` 包含 `SceneQuickButtons`
- AIS-03: `context.service.ts` 包含 `buildUserContext`
- AIS-04: `OutfitPlanView.tsx` 包含 `ReasoningCard`
- AIS-05: `AiStylistScreen.tsx` 包含 `ItemReplacementModal` 且 `item-replacement.service.ts` 包含 `replaceItem`
- AIS-06: `ai-stylist.module.ts` 包含 `WeatherIntegrationService` 且 `OutfitPlanView.tsx` 包含 `WeatherBadge`
- AIS-07: 导航配置包含 `SessionCalendar` 且 `session-archive.service.ts` 包含 `getCalendarDays`
- AIS-08: `system-prompt.ts` 包含安全约束内容
- AIS-09: `ai-stylist.controller.ts` 包含 `AiQuotaGuard`
- AIS-10: `AiStylistScreen.tsx` 包含 `FeedbackModal` 且 `ai-stylist.dto.ts` 包含 `rating` 字段
- AIS-11: `SceneQuickButtons` 组件包含 6 个场景且 `onSceneSelect` 回调触发推荐
- AIS-12: `AiStylistScreen.tsx` 包含 `PresetQuestionsModal` 且 `preset-questions.service.ts` 存在
</acceptance_criteria>

---

### Task 5: 后端现有测试验证

<read_first>
- apps/backend/src/modules/ai-stylist/ (test files)
</read_first>

<action>
运行后端现有测试，确保无回归：

```bash
cd apps/backend && npx jest --testPathPattern="ai-stylist" --passWithNoTests
```

如果有测试失败，分析原因：
1. 如果是新增服务导致的 mock 缺失，更新测试文件的 mock
2. 如果是方法签名变更导致的调用不匹配，更新测试调用
3. 记录所有修复

然后运行完整后端测试套件：
```bash
cd apps/backend && npx jest --passWithNoTests 2>&1 | tail -20
```
</action>

<acceptance_criteria>
- `ai-stylist` 相关测试通过（或无测试文件）
- 后端测试套件整体通过率不低于 Plan 开始前的水平（94.7%）
</acceptance_criteria>

---

### Task 6: API 端点完整性验证

<read_first>
- apps/backend/src/modules/ai-stylist/ai-stylist.controller.ts
</read_first>

<action>
验证所有 API 端点已注册：

```bash
cd apps/backend && grep -n "@Get\|@Post\|@Delete\|@Put" src/modules/ai-stylist/ai-stylist.controller.ts
```

预期端点列表（17 个）：
1. `POST sessions` - 创建会话
2. `GET sessions` - 会话列表
3. `GET sessions/:sessionId` - 会话状态
4. `POST sessions/:sessionId/messages` - 发送消息
5. `POST sessions/:sessionId/photo` - 上传照片
6. `POST sessions/:sessionId/resolve` - 生成方案
7. `DELETE sessions/:sessionId` - 删除会话
8. `GET sessions/:sessionId/outfit-plan` - **新增** 方案页数据
9. `GET sessions/:sessionId/items/alternatives` - **新增** 同类替代
10. `POST sessions/:sessionId/items/replace` - **新增** 替换单品
11. `GET sessions/calendar` - **新增** 日历视图
12. `GET sessions/date/:date` - **新增** 日期方案列表
13. `POST sessions/:sessionId/feedback` - 提交反馈（已扩展）
14. `GET sessions/:sessionId/feedback` - 获取反馈
15. `POST chat` - 旧版聊天（deprecated）
16. `GET quota` - 配额查询
17. `GET suggestions` - 快捷建议
18. `GET options/styles` - 风格选项
19. `GET options/occasions` - 场景选项
20. `GET preset-questions` - **新增** 预设问题
21. `GET system-context` - 系统上下文

验证新增的 6 个端点都存在。
</action>

<acceptance_criteria>
- Controller 包含 `GET sessions/:sessionId/outfit-plan` 端点
- Controller 包含 `GET sessions/:sessionId/items/alternatives` 端点
- Controller 包含 `POST sessions/:sessionId/items/replace` 端点
- Controller 包含 `GET sessions/calendar` 端点
- Controller 包含 `GET sessions/date/:date` 端点
- Controller 包含 `GET preset-questions` 端点
- `sendMessage` 端点接受 `latitude`/`longitude` 参数
- `submitFeedback` 端点接受 `rating`/`dislikeReason`/`dislikeDetail` 参数
</acceptance_criteria>

---

## 验证标准

1. Prisma Schema Push 成功执行
2. 后端 TypeScript 编译零错误
3. 移动端 TypeScript 编译零错误
4. 12 个需求（AIS-01 ~ AIS-12）全部覆盖验证通过
5. 后端测试无回归
6. 6 个新增 API 端点 + 2 个修改端点全部验证通过

## must_haves

- [ ] Prisma Schema Push 执行成功（或记录手动执行说明）
- [ ] 后端 TypeScript 编译零错误
- [ ] 移动端 TypeScript 编译零错误
- [ ] AIS-01 ~ AIS-12 全部需求覆盖
- [ ] 6 个新增 API 端点注册完成
- [ ] 后端测试通过率不低于 94.7%
