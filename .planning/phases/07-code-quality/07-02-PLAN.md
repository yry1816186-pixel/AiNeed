---
phase: 7
plan: 02
type: any-elimination
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/components/heartrecommend/AnimatedHeartButton.tsx
  - apps/mobile/src/components/tryon/TryOnProgress.tsx
  - apps/mobile/src/components/aistylist/TypewriterMessage.tsx
  - apps/mobile/src/components/social/SocialInteractions.tsx
  - apps/mobile/src/components/recommendation/AlgorithmVisualization.tsx
  - apps/mobile/src/screens/AiStylistChatScreen.tsx
  - apps/mobile/src/screens/CartScreen.tsx
  - apps/mobile/src/screens/CommunityDetailScreen.tsx
  - apps/mobile/src/screens/ProfileScreen.tsx
  - apps/mobile/src/navigation/navigation.d.ts
  - apps/mobile/src/services/stylist.api.ts
  - apps/mobile/src/services/notification.api.ts
  - apps/mobile/src/services/feature-flag.api.ts
  - apps/mobile/.eslintrc.json
autonomous: true
requirements:
  - QUAL-03
---

# Plan 07-02: 移动端 `any` 类型消除

<objective>
消除移动端生产代码中的 `any` 类型，从 ~67 处降至 < 10 处，并将 ESLint `no-explicit-any` 从 "warn" 升级为 "error"。
</objective>

<threat_model>
**Threat:** `(response.data as any).reason` 绕过类型检查，可能导致未处理字段访问运行时错误
**Mitigation:** 使用 @xuno/types 定义 API 响应类型
**Severity:** Low
</threat_model>

<tasks>

### Task 7-02-01: 修复组件 props/事件中的 `any`

<read_first>
- apps/mobile/src/components/heartrecommend/AnimatedHeartButton.tsx
- apps/mobile/src/components/tryon/TryOnProgress.tsx
- apps/mobile/src/components/aistylist/TypewriterMessage.tsx
- apps/mobile/src/components/social/SocialInteractions.tsx
- apps/mobile/src/components/recommendation/AlgorithmVisualization.tsx
- apps/mobile/src/components/heartrecommend/SwipeCard.tsx
</read_first>

<action>
1. 所有组件中 `style?: any` → `style?: ViewStyle`，添加 `import { ViewStyle } from 'react-native'`
2. 事件处理器 `(e: any)` → `(e: GestureResponderEvent)` 或 `(e: NativeSyntheticEvent<TextInputChangeEventData>)`
3. `Ionicons name={icon as any}` → 定义 `IoniconName` 类型并替换
4. `SwipeCard.tsx` 中 `_index` prop 类型修复
</action>

<acceptance_criteria>
- 所有组件中不包含 `style?: any`
- 所有组件中不包含 `(e: any)` 或 `(event: any)`
- `SwipeCard.tsx` 不包含 `_index` 相关的 TS 错误
- `cd apps/mobile && npx tsc --noEmit` 通过
</acceptance_criteria>

### Task 7-02-02: 修复导航和 API 中的 `any`

<read_first>
- apps/mobile/src/navigation/navigation.d.ts
- apps/mobile/src/screens/AiStylistChatScreen.tsx
- apps/mobile/src/screens/CartScreen.tsx
- apps/mobile/src/screens/CommunityDetailScreen.tsx
- apps/mobile/src/screens/ProfileScreen.tsx
- apps/mobile/src/services/stylist.api.ts
- packages/types/src/index.ts
</read_first>

<action>
1. 扩展 `navigation.d.ts` 路由类型定义，为每个路由添加 `params` 类型
2. 消除所有 `(navigation as any).navigate(...)` → `navigation.navigate('RouteName', params)`
3. 消除所有 `(route.params as any)?.xxx` → `route.params.xxx`
4. 消除 API 服务中的 `(response.data as any).xxx` → 使用 @xuno/types 定义响应类型
5. 修复 `keyExtractor={(item: any) => ...}` → `keyExtractor={(item: ItemType) => ...}`
</action>

<acceptance_criteria>
- `navigation.d.ts` 包含 `RootStackParamList` 类型定义
- `grep -r "navigation as any" apps/mobile/src/` 返回 0 结果
- `grep -r "route.params as any" apps/mobile/src/` 返回 0 结果
- `grep -r "response.data as any" apps/mobile/src/` 返回 0 结果
- `cd apps/mobile && npx tsc --noEmit` 通过
</acceptance_criteria>

### Task 7-02-03: 修复测试文件 + 升级 ESLint

<read_first>
- apps/mobile/.eslintrc.json
- apps/mobile/src/screens/__tests__/HomeScreen.test.tsx
</read_first>

<action>
1. 修复测试文件中的 `any` 模式：FlashList mock、navigation mock 等
2. 将 `"@typescript-eslint/no-explicit-any": "warn"` 改为 `"@typescript-eslint/no-explicit-any": "error"`
3. 统计修复后剩余的 `any` 数量，确保生产代码 < 10 处
</action>

<acceptance_criteria>
- `apps/mobile/.eslintrc.json` 包含 `"@typescript-eslint/no-explicit-any": "error"`
- `cd apps/mobile && grep -r "as any\|: any" src/ --include="*.ts" --include="*.tsx" | grep -v "spec\|test\|eslint-disable\|polyfills" | wc -l` 返回 < 10
- `cd apps/mobile && npx tsc --noEmit` 通过
</acceptance_criteria>

</tasks>

<verification>
1. `cd apps/mobile && npx tsc --noEmit` — TypeScript 编译通过
2. `cd apps/mobile && npx jest --passWithNoTests` — 所有现有测试通过
3. `cd apps/mobile && grep -r "as any\|: any" src/ --include="*.ts" --include="*.tsx" | grep -v "spec\|test\|eslint-disable\|polyfills" | wc -l` — 生产代码 any < 10 处
</verification>

<success_criteria>
- 移动端生产代码 `any` 类型 < 10 处
- ESLint `no-explicit-any` 设为 "error"
- 所有现有测试通过
</success_criteria>

<must_haves>
- 移动端生产代码 `any` 数量显著减少
- ESLint 阻止新增 `any` 类型
- 不改变任何业务逻辑
- 导航类型安全
</must_haves>
