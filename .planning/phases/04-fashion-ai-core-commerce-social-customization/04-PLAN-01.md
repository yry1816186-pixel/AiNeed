---
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/domains/fashion/fashion.module.ts
  - apps/backend/src/domains/fashion/clothing/clothing.module.ts
  - apps/backend/src/domains/fashion/brands/brands.module.ts
  - apps/backend/src/domains/fashion/brands/brand-portal/brand-portal.module.ts
  - apps/backend/src/domains/fashion/search/search.module.ts
  - apps/backend/src/domains/fashion/favorites/favorites.module.ts
  - apps/backend/src/domains/fashion/wardrobe-collection/wardrobe-collection.module.ts
  - apps/backend/src/domains/fashion/style-profiles/style-profiles.module.ts
  - apps/backend/src/domains/fashion/style-quiz/style-quiz.module.ts
  - apps/backend/src/domains/fashion/weather/weather.module.ts
  - apps/backend/src/app.module.ts
requirements:
  - ARCH-01
  - ARCH-08
  - ARCH-09
autonomous: true
---

# Plan 01: Fashion 域迁移

**Objective:** 创建 fashion 域目录结构，迁移 8 个模块到 fashion 域，合并 style-profiles + style-quiz 为统一风格评估模块，合并 wardrobe-collection + favorites 为统一衣橱管理模块

## must_haves

- fashion 域目录结构创建完成
- 8 个模块正确迁移到 `src/domains/fashion/` 下
- style-profiles + style-quiz 合并为 style-assessment 模块
- wardrobe-collection + favorites 合并为 wardrobe 模块
- `nest build` 编译通过
- 所有 fashion 域 API 端点正常工作

---

## Task 01: 创建 fashion 域目录结构

<read_first>
- apps/backend/src/app.module.ts
- apps/backend/src/domains/ (检查 Phase 3 是否已创建 domains 目录)
</read_first>

<action>
1. 创建目录结构：
   ```
   src/domains/fashion/
   ├── fashion.module.ts
   ├── clothing/
   ├── brands/
   │   └── brand-portal/
   ├── search/
   ├── favorites/
   ├── wardrobe-collection/
   ├── style-profiles/
   ├── style-quiz/
   ├── weather/
   ├── style-assessment/    (合并后)
   └── wardrobe/            (合并后)
   ```

2. 创建 `src/domains/fashion/fashion.module.ts`：
   ```typescript
   import { Module } from '@nestjs/common';
   import { ClothingModule } from './clothing/clothing.module';
   import { BrandsModule } from './brands/brands.module';
   import { SearchModule } from './search/search.module';
   import { FavoritesModule } from './favorites/favorites.module';
   import { WardrobeCollectionModule } from './wardrobe-collection/wardrobe-collection.module';
   import { StyleProfilesModule } from './style-profiles/style-profiles.module';
   import { StyleQuizModule } from './style-quiz/style-quiz.module';
   import { WeatherModule } from './weather/weather.module';

   @Module({
     imports: [
       ClothingModule,
       BrandsModule,
       SearchModule,
       FavoritesModule,
       WardrobeCollectionModule,
       StyleProfilesModule,
       StyleQuizModule,
       WeatherModule,
     ],
     exports: [
       ClothingModule,
       BrandsModule,
       SearchModule,
       FavoritesModule,
       WardrobeCollectionModule,
       StyleProfilesModule,
       StyleQuizModule,
       WeatherModule,
     ],
   })
   export class FashionModule {}
   ```
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/fashion/` 返回 0
- `apps/backend/src/domains/fashion/fashion.module.ts` 文件存在
- `fashion.module.ts` 包含 `export class FashionModule`
- `fashion.module.ts` 导入所有 8 个子模块
</acceptance_criteria>

---

## Task 02: 迁移 clothing 模块到 fashion 域

<read_first>
- apps/backend/src/modules/clothing/clothing.module.ts
- apps/backend/src/modules/clothing/ (所有文件)
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 复制 `src/modules/clothing/` 整个目录到 `src/domains/fashion/clothing/`
2. 更新 `src/domains/fashion/clothing/clothing.module.ts` 中的导入路径：
   - 将 `../common/` 或 `../../common/` 开头的导入改为指向 `src/common/` 的正确相对路径
   - 例如：`import { PrismaService } from '../../common/prisma/prisma.service'` → 保持不变（相对路径仍然正确）
3. 在 `src/app.module.ts` 中：
   - 将 `import { ClothingModule } from './modules/clothing/clothing.module'` 改为 `import { ClothingModule } from './domains/fashion/clothing/clothing.module'`
4. 删除 `src/modules/clothing/` 目录
5. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/fashion/clothing/` 返回 0
- `test ! -d apps/backend/src/modules/clothing/` 返回 0（旧目录已删除）
- `apps/backend/src/app.module.ts` 包含 `./domains/fashion/clothing/clothing.module`
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 03: 迁移 brands + brand-portal 模块到 fashion 域

<read_first>
- apps/backend/src/modules/brands/brands.module.ts
- apps/backend/src/modules/brands/brand-portal/brand-portal.module.ts
- apps/backend/src/modules/brands/ (所有文件)
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 复制 `src/modules/brands/` 整个目录到 `src/domains/fashion/brands/`
2. 更新 `src/domains/fashion/brands/brands.module.ts` 中的导入路径：
   - BrandPortalModule 的导入路径从 `./brand-portal/brand-portal.module` 保持不变（相对路径正确）
   - 任何 `../../common/` 路径保持不变
3. 在 `src/app.module.ts` 中：
   - 将 `import { BrandsModule } from './modules/brands/brands.module'` 改为 `import { BrandsModule } from './domains/fashion/brands/brands.module'`
4. 删除 `src/modules/brands/` 目录
5. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/fashion/brands/` 返回 0
- `test -d apps/backend/src/domains/fashion/brands/brand-portal/` 返回 0
- `test ! -d apps/backend/src/modules/brands/` 返回 0
- `apps/backend/src/app.module.ts` 包含 `./domains/fashion/brands/brands.module`
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 04: 迁移 search 模块到 fashion 域

<read_first>
- apps/backend/src/modules/search/search.module.ts
- apps/backend/src/modules/search/ (所有文件)
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 复制 `src/modules/search/` 整个目录到 `src/domains/fashion/search/`
2. 更新 `src/domains/fashion/search/search.module.ts` 中的导入路径：
   - `RecommendationsModule` 导入路径需要更新为指向 platform 域（Phase 3 已迁移）或保持从 app.module 间接导入
   - 如果 Phase 3 已将 Recommendations 迁移到 `src/domains/platform/recommendations/`，更新为 `../../platform/recommendations/recommendations.module`
3. 在 `src/app.module.ts` 中：
   - 将 `import { SearchModule } from './modules/search/search.module'` 改为 `import { SearchModule } from './domains/fashion/search/search.module'`
4. 删除 `src/modules/search/` 目录
5. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/fashion/search/` 返回 0
- `test ! -d apps/backend/src/modules/search/` 返回 0
- `apps/backend/src/app.module.ts` 包含 `./domains/fashion/search/search.module`
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 05: 迁移 weather 模块到 fashion 域

<read_first>
- apps/backend/src/modules/weather/weather.module.ts
- apps/backend/src/modules/weather/ (所有文件)
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 复制 `src/modules/weather/` 整个目录到 `src/domains/fashion/weather/`
2. 更新 `src/domains/fashion/weather/weather.module.ts` 中的导入路径（如有跨模块引用）
3. 在 `src/app.module.ts` 中：
   - 将 WeatherModule 的导入路径改为 `./domains/fashion/weather/weather.module`
4. 删除 `src/modules/weather/` 目录
5. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/fashion/weather/` 返回 0
- `test ! -d apps/backend/src/modules/weather/` 返回 0
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 06: 合并 style-profiles + style-quiz 为 style-assessment 模块

<read_first>
- apps/backend/src/modules/style-profiles/style-profiles.module.ts
- apps/backend/src/modules/style-profiles/ (所有文件)
- apps/backend/src/modules/style-quiz/style-quiz.module.ts
- apps/backend/src/modules/style-quiz/ (所有文件)
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 创建 `src/domains/fashion/style-assessment/` 目录
2. 将 `src/modules/style-profiles/` 所有文件复制到 `src/domains/fashion/style-assessment/profiles/`
3. 将 `src/modules/style-quiz/` 所有文件复制到 `src/domains/fashion/style-assessment/quiz/`
4. 创建 `src/domains/fashion/style-assessment/style-assessment.module.ts`：
   ```typescript
   import { Module, forwardRef } from '@nestjs/common';
   import { StyleProfilesModule } from './profiles/style-profiles.module';
   import { StyleQuizModule } from './quiz/style-quiz.module';

   @Module({
     imports: [StyleProfilesModule, StyleQuizModule],
     exports: [StyleProfilesModule, StyleQuizModule],
   })
   export class StyleAssessmentModule {}
   ```
5. 更新 `src/domains/fashion/style-assessment/quiz/style-quiz.module.ts`：
   - 移除 `forwardRef` 到 ProfileModule（因为现在在同一模块内）
   - 将 `ProfileModule` 的导入改为直接导入：`import { StyleProfilesModule } from '../profiles/style-profiles.module'`
6. 更新 `src/domains/fashion/style-assessment/profiles/style-profiles.module.ts`：
   - 如果有反向引用 style-quiz，同样移除 forwardRef
7. 更新 `src/app.module.ts`：
   - 移除 `StyleProfilesModule` 和 `StyleQuizModule` 的单独导入
   - 添加 `import { StyleAssessmentModule } from './domains/fashion/style-assessment/style-assessment.module'`
8. 更新 `src/domains/fashion/fashion.module.ts`：
   - 移除 `StyleProfilesModule` 和 `StyleQuizModule` 的单独导入
   - 添加 `StyleAssessmentModule` 导入
9. 删除 `src/modules/style-profiles/` 和 `src/modules/style-quiz/` 目录
10. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/fashion/style-assessment/` 返回 0
- `test -d apps/backend/src/domains/fashion/style-assessment/profiles/` 返回 0
- `test -d apps/backend/src/domains/fashion/style-assessment/quiz/` 返回 0
- `apps/backend/src/domains/fashion/style-assessment/style-assessment.module.ts` 包含 `export class StyleAssessmentModule`
- `apps/backend/src/app.module.ts` 包含 `./domains/fashion/style-assessment/style-assessment.module`
- `test ! -d apps/backend/src/modules/style-profiles/` 返回 0
- `test ! -d apps/backend/src/modules/style-quiz/` 返回 0
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 07: 合并 wardrobe-collection + favorites 为 wardrobe 模块

<read_first>
- apps/backend/src/modules/wardrobe-collection/wardrobe-collection.module.ts
- apps/backend/src/modules/wardrobe-collection/ (所有文件)
- apps/backend/src/modules/favorites/favorites.module.ts
- apps/backend/src/modules/favorites/favorites.service.ts
- apps/backend/src/modules/favorites/ (所有文件)
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 创建 `src/domains/fashion/wardrobe/` 目录
2. 将 `src/modules/wardrobe-collection/` 所有文件复制到 `src/domains/fashion/wardrobe/collection/`
3. 将 `src/modules/favorites/` 所有文件复制到 `src/domains/fashion/wardrobe/favorites/`
4. 创建 `src/domains/fashion/wardrobe/wardrobe.module.ts`：
   ```typescript
   import { Module } from '@nestjs/common';
   import { WardrobeCollectionModule } from './collection/wardrobe-collection.module';
   import { FavoritesModule } from './favorites/favorites.module';

   @Module({
     imports: [WardrobeCollectionModule, FavoritesModule],
     exports: [WardrobeCollectionModule, FavoritesModule],
   })
   export class WardrobeModule {}
   ```
5. 更新 `src/app.module.ts`：
   - 移除 `WardrobeCollectionModule` 和 `FavoritesModule` 的单独导入
   - 添加 `import { WardrobeModule } from './domains/fashion/wardrobe/wardrobe.module'`
6. 更新 `src/domains/fashion/fashion.module.ts`：
   - 移除 `WardrobeCollectionModule` 和 `FavoritesModule` 的单独导入
   - 添加 `WardrobeModule` 导入
7. 删除 `src/modules/wardrobe-collection/` 和 `src/modules/favorites/` 目录
8. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/fashion/wardrobe/` 返回 0
- `test -d apps/backend/src/domains/fashion/wardrobe/collection/` 返回 0
- `test -d apps/backend/src/domains/fashion/wardrobe/favorites/` 返回 0
- `apps/backend/src/domains/fashion/wardrobe/wardrobe.module.ts` 包含 `export class WardrobeModule`
- `apps/backend/src/app.module.ts` 包含 `./domains/fashion/wardrobe/wardrobe.module`
- `test ! -d apps/backend/src/modules/wardrobe-collection/` 返回 0
- `test ! -d apps/backend/src/modules/favorites/` 返回 0
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 08: 更新所有跨模块导入路径引用

<read_first>
- apps/backend/src/domains/fashion/ (所有模块文件)
- apps/backend/src/modules/ai-stylist/ai-stylist.module.ts (引用 WeatherModule)
- apps/backend/src/modules/ai-stylist/ (引用 WeatherModule 的文件)
</read_first>

<action>
1. 搜索所有引用已迁移模块的文件：
   ```bash
   grep -r "from.*modules/clothing" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/brands" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/search" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/weather" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/style-profiles" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/style-quiz" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/wardrobe-collection" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/favorites" apps/backend/src/ --include="*.ts" -l
   ```
2. 对每个匹配文件，将导入路径从 `./modules/xxx` 更新为 `./domains/fashion/xxx`
3. 特别注意 `ai-stylist` 模块引用 `WeatherModule` 的路径更新
4. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `grep -r "from.*modules/clothing" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "from.*modules/brands" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "from.*modules/search" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "from.*modules/weather" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "from.*modules/style-profiles" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "from.*modules/style-quiz" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "from.*modules/wardrobe-collection" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "from.*modules/favorites" apps/backend/src/ --include="*.ts"` 返回空
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Verification

```bash
cd apps/backend && npx nest build && npx tsc --noEmit
```

所有 fashion 域模块迁移完成后，编译必须通过，无 TypeScript 错误。
