---
wave: 4
depends_on:
  - 04-PLAN-03
files_modified:
  - apps/backend/src/domains/social/social.module.ts
  - apps/backend/src/domains/social/community/community.module.ts
  - apps/backend/src/domains/social/blogger/blogger.module.ts
  - apps/backend/src/domains/social/consultant/consultant.module.ts
  - apps/backend/src/domains/social/chat/chat.module.ts
  - apps/backend/src/domains/customization/customization.module.ts
  - apps/backend/src/domains/customization/customization/customization.module.ts
  - apps/backend/src/domains/customization/share-template/share-template.module.ts
  - apps/backend/src/modules/queue/queue.module.ts
  - apps/backend/src/modules/admin/admin.module.ts
  - apps/backend/src/modules/admin/admin-community.controller.ts
  - apps/backend/src/app.module.ts
requirements:
  - ARCH-01
  - ARCH-03
autonomous: true
---

# Plan 04: Social + Customization 域迁移 + 消除剩余循环依赖

**Objective:** 创建 social 和 customization 域目录结构，迁移 6 个模块，消除 QueueModule ↔ CommunityModule、QueueModule ↔ TryOnModule、NotificationModule ↔ GatewayModule、AdminModule ↔ CommunityModule、BrandsModule ↔ BrandPortalModule 的循环依赖

## must_haves

- social 域目录结构创建完成（4 个模块）
- customization 域目录结构创建完成（2 个模块）
- QueueModule ↔ CommunityModule 循环依赖已消除
- QueueModule ↔ TryOnModule 循环依赖已消除
- NotificationModule ↔ GatewayModule 循环依赖已消除
- AdminModule ↔ CommunityModule 循环依赖已消除
- BrandsModule ↔ BrandPortalModule 循环依赖已消除
- `nest build` 编译通过
- 所有 social 和 customization 域 API 端点正常工作

---

## Task 01: 创建 social 域目录结构

<read_first>
- apps/backend/src/app.module.ts
- apps/backend/src/domains/ (检查现有域目录)
</read_first>

<action>
1. 创建目录结构：
   ```
   src/domains/social/
   ├── social.module.ts
   ├── community/
   ├── blogger/
   ├── consultant/
   └── chat/
   ```

2. 创建 `src/domains/social/social.module.ts`：
   ```typescript
   import { Module } from '@nestjs/common';
   import { CommunityModule } from './community/community.module';
   import { BloggerModule } from './blogger/blogger.module';
   import { ConsultantModule } from './consultant/consultant.module';
   import { ChatModule } from './chat/chat.module';

   @Module({
     imports: [CommunityModule, BloggerModule, ConsultantModule, ChatModule],
     exports: [CommunityModule, BloggerModule, ConsultantModule, ChatModule],
   })
   export class SocialModule {}
   ```
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/social/` 返回 0
- `apps/backend/src/domains/social/social.module.ts` 包含 `export class SocialModule`
</acceptance_criteria>

---

## Task 02: 创建 customization 域目录结构

<read_first>
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 创建目录结构：
   ```
   src/domains/customization/
   ├── customization.module.ts
   ├── customization/
   └── share-template/
   ```

2. 创建 `src/domains/customization/customization.module.ts`：
   ```typescript
   import { Module } from '@nestjs/common';
   import { CustomizationModule } from './customization/customization.module';
   import { ShareTemplateModule } from './share-template/share-template.module';

   @Module({
     imports: [CustomizationModule, ShareTemplateModule],
     exports: [CustomizationModule, ShareTemplateModule],
   })
   export class CustomizationDomainModule {}
   ```
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/customization/` 返回 0
- `apps/backend/src/domains/customization/customization.module.ts` 包含 `export class CustomizationDomainModule`
</acceptance_criteria>

---

## Task 03: 消除 QueueModule ↔ CommunityModule 循环依赖

<read_first>
- apps/backend/src/modules/queue/queue.module.ts
- apps/backend/src/modules/community/community.module.ts
- apps/backend/src/modules/queue/ (所有文件)
- apps/backend/src/modules/community/ (所有文件)
</read_first>

<action>
1. **解耦策略**：将 QueueModule 中的 ContentModerationProcessor 提取到 Community 模块内部

2. 修改 `src/modules/queue/queue.module.ts`：
   - 移除 `forwardRef(() => CommunityModule)` 导入
   - 移除 `CommunityModule` 从 imports 数组
   - 将 ContentModerationProcessor 移到 Community 模块中

3. 修改 `src/modules/community/community.module.ts`：
   - 移除 `forwardRef(() => QueueModule)` 导入（如果有）
   - 将 ContentModerationProcessor 注册为 Community 模块的 provider
   - 如果 CommunityModule 需要队列功能，通过 EventEmitter2 发送事件

4. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `grep -r "forwardRef.*Queue\|forwardRef.*Community" apps/backend/src/modules/ --include="*.ts"` 返回空
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 04: 消除 QueueModule ↔ TryOnModule 循环依赖

<read_first>
- apps/backend/src/modules/queue/queue.module.ts
- apps/backend/src/domains/ai-core/try-on/try-on.module.ts
- apps/backend/src/modules/queue/ (所有文件)
</read_first>

<action>
1. **解耦策略**：将 QueueModule 中的 VirtualTryOnProcessor 提取到 TryOn 模块内部

2. 修改 `src/modules/queue/queue.module.ts`：
   - 移除 `forwardRef(() => TryOnModule)` 导入
   - 移除 `TryOnModule` 从 imports 数组
   - 将 VirtualTryOnProcessor 移到 TryOn 模块中

3. 修改 `src/domains/ai-core/try-on/try-on.module.ts`：
   - 注册 VirtualTryOnProcessor 为 provider
   - 如果 TryOnModule 需要队列功能，通过 EventEmitter2 发送事件

4. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `grep -r "forwardRef.*TryOn" apps/backend/src/ --include="*.ts"` 返回空
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 05: 消除 NotificationModule ↔ GatewayModule 循环依赖

<read_first>
- apps/backend/src/modules/notification/notification.module.ts (已迁移到 commerce 域)
- apps/backend/src/common/gateway/gateway.module.ts
- apps/backend/src/modules/notification/services/notification.service.ts
</read_first>

<action>
1. **解耦策略**：GatewayModule 是 @Global 模块，移除 forwardRef，直接注入

2. 修改 `src/domains/commerce/notification/core/notification-core.module.ts`（已迁移）：
   - 移除 `forwardRef(() => GatewayModule)` 导入
   - 移除 `GatewayModule` 从 imports 数组
   - 因为 GatewayModule 是 @Global，NotificationService 可以直接注入 WebSocketNotificationService

3. 修改 `src/domains/commerce/notification/core/services/notification.service.ts`：
   - 移除 `forwardRef(() => WebSocketNotificationService)` 注入
   - 改为直接注入：`@Inject() private wsNotificationService: WebSocketNotificationService`

4. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `grep -r "forwardRef.*Gateway\|forwardRef.*WebSocketNotification" apps/backend/src/ --include="*.ts"` 返回空
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 06: 消除 AdminModule ↔ CommunityModule 循环依赖

<read_first>
- apps/backend/src/modules/admin/admin.module.ts
- apps/backend/src/modules/admin/admin-community.controller.ts
- apps/backend/src/modules/community/community.module.ts
</read_first>

<action>
1. **解耦策略**：将 AdminCommunityController 移入 Community 模块

2. 移动 `src/modules/admin/admin-community.controller.ts` 到 `src/modules/community/controllers/admin-community.controller.ts`

3. 修改 `src/modules/admin/admin.module.ts`：
   - 移除 `forwardRef(() => CommunityModule)` 导入
   - 移除 `CommunityModule` 从 imports 数组
   - 移除 AdminCommunityController 的注册（已移到 Community 模块）

4. 修改 `src/modules/community/community.module.ts`：
   - 添加 AdminCommunityController 到 controllers 数组
   - 添加必要的依赖（AdminGuard 等）到 providers

5. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `grep -r "forwardRef.*Admin\|forwardRef.*Community" apps/backend/src/modules/admin/ --include="*.ts"` 返回空
- `test -f apps/backend/src/modules/community/controllers/admin-community.controller.ts` 返回 0
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 07: 消除 BrandsModule ↔ BrandPortalModule 循环依赖

<read_first>
- apps/backend/src/domains/fashion/brands/brands.module.ts
- apps/backend/src/domains/fashion/brands/brand-portal/brand-portal.module.ts
</read_first>

<action>
1. **解耦策略**：BrandPortalModule 移入 BrandsModule 内部作为子模块

2. 修改 `src/domains/fashion/brands/brands.module.ts`：
   - 移除 `forwardRef(() => BrandPortalModule)` 导入
   - 改为直接导入：`import { BrandPortalModule } from './brand-portal/brand-portal.module'`
   - BrandPortalModule 作为 BrandsModule 的子模块直接导入

3. 修改 `src/domains/fashion/brands/brand-portal/brand-portal.module.ts`：
   - 移除 `forwardRef(() => BrandsModule)` 导入
   - 如果 BrandPortalModule 需要 BrandsModule 的服务，通过相对路径直接导入 BrandsService

4. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `grep -r "forwardRef.*Brand" apps/backend/src/ --include="*.ts"` 返回空
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 08: 迁移 community 模块到 social 域

<read_first>
- apps/backend/src/modules/community/community.module.ts
- apps/backend/src/modules/community/ (所有文件)
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 复制 `src/modules/community/` 整个目录到 `src/domains/social/community/`
2. 更新 `src/domains/social/community/community.module.ts` 中的导入路径：
   - QueueModule → 从 platform 域导入（Phase 3 已迁移）
   - AISafetyService → @Global，无需导入
3. 在 `src/app.module.ts` 中更新导入路径
4. 删除 `src/modules/community/` 目录
5. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/social/community/` 返回 0
- `test ! -d apps/backend/src/modules/community/` 返回 0
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 09: 迁移 blogger, consultant, chat 模块到 social 域

<read_first>
- apps/backend/src/modules/blogger/blogger.module.ts
- apps/backend/src/modules/consultant/consultant.module.ts
- apps/backend/src/modules/chat/chat.module.ts
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 复制 `src/modules/blogger/` 到 `src/domains/social/blogger/`
2. 复制 `src/modules/consultant/` 到 `src/domains/social/consultant/`
3. 复制 `src/modules/chat/` 到 `src/domains/social/chat/`
4. 更新各模块中的导入路径：
   - BloggerModule 引用 NotificationModule → `../../commerce/notification/notification.module`
   - ConsultantModule 引用 PaymentModule → `../../commerce/payment/payment.module`
   - ChatModule 引用 WSModule → 保持从 common 层导入
5. 在 `src/app.module.ts` 中更新导入路径
6. 删除 `src/modules/blogger/`、`src/modules/consultant/`、`src/modules/chat/` 目录
7. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/social/blogger/` 返回 0
- `test -d apps/backend/src/domains/social/consultant/` 返回 0
- `test -d apps/backend/src/domains/social/chat/` 返回 0
- `test ! -d apps/backend/src/modules/blogger/` 返回 0
- `test ! -d apps/backend/src/modules/consultant/` 返回 0
- `test ! -d apps/backend/src/modules/chat/` 返回 0
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 10: 迁移 customization 和 share-template 模块到 customization 域

<read_first>
- apps/backend/src/modules/customization/customization.module.ts
- apps/backend/src/modules/share-template/share-template.module.ts
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 复制 `src/modules/customization/` 到 `src/domains/customization/customization/`
2. 复制 `src/modules/share-template/` 到 `src/domains/customization/share-template/`
3. 更新 `src/domains/customization/customization/customization.module.ts` 中的导入路径：
   - PaymentModule → `../../commerce/payment/payment.module`
4. 在 `src/app.module.ts` 中更新导入路径
5. 删除 `src/modules/customization/` 和 `src/modules/share-template/` 目录
6. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/customization/customization/` 返回 0
- `test -d apps/backend/src/domains/customization/share-template/` 返回 0
- `test ! -d apps/backend/src/modules/customization/` 返回 0
- `test ! -d apps/backend/src/modules/share-template/` 返回 0
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 11: 更新所有跨模块导入路径引用

<read_first>
- apps/backend/src/domains/social/ (所有模块文件)
- apps/backend/src/domains/customization/ (所有模块文件)
- apps/backend/src/modules/ (检查是否有文件引用已迁移模块)
</read_first>

<action>
1. 搜索所有引用已迁移 social/customization 模块的文件：
   ```bash
   grep -r "from.*modules/community" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/blogger" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/consultant" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/chat" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/customization" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/share-template" apps/backend/src/ --include="*.ts" -l
   ```
2. 对每个匹配文件，将导入路径从 `./modules/xxx` 更新为正确的域路径
3. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `grep -r "from.*modules/community\|from.*modules/blogger\|from.*modules/consultant\|from.*modules/chat\|from.*modules/customization\|from.*modules/share-template" apps/backend/src/ --include="*.ts"` 返回空
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Verification

```bash
cd apps/backend && npx nest build && npx tsc --noEmit
grep -r "forwardRef" apps/backend/src/ --include="*.ts"
```

编译通过且无任何 forwardRef 残留。
