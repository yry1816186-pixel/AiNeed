---
wave: 2
depends_on:
  - 04-PLAN-01
files_modified:
  - apps/backend/src/domains/ai-core/ai-core.module.ts
  - apps/backend/src/domains/ai-core/ai-stylist/ai-stylist.module.ts
  - apps/backend/src/domains/ai-core/try-on/try-on.module.ts
  - apps/backend/src/domains/ai-core/ai/ai.module.ts
  - apps/backend/src/domains/ai-core/ai-safety/ai-safety.module.ts
  - apps/backend/src/domains/ai-core/photos/photos.module.ts
  - apps/backend/src/app.module.ts
requirements:
  - ARCH-01
  - ARCH-02
autonomous: true
---

# Plan 02: AI-Core 域迁移 + 消除 AiStylistModule ↔ RecommendationsModule 循环依赖

**Objective:** 创建 ai-core 域目录结构，迁移 5 个 AI 相关模块到 ai-core 域，消除 AiStylistModule ↔ RecommendationsModule 的 forwardRef 循环依赖

## must_haves

- ai-core 域目录结构创建完成
- 5 个模块正确迁移到 `src/domains/ai-core/` 下
- AiStylistModule ↔ RecommendationsModule 的 forwardRef 循环依赖已消除
- RecommendationsModule 已在 Phase 3 降级为 platform 层共享服务
- `nest build` 编译通过
- 所有 AI 相关 API 端点正常工作

---

## Task 01: 创建 ai-core 域目录结构

<read_first>
- apps/backend/src/app.module.ts
- apps/backend/src/domains/ (检查现有域目录)
</read_first>

<action>
1. 创建目录结构：
   ```
   src/domains/ai-core/
   ├── ai-core.module.ts
   ├── ai-stylist/
   ├── try-on/
   ├── ai/
   ├── ai-safety/
   └── photos/
   ```

2. 创建 `src/domains/ai-core/ai-core.module.ts`：
   ```typescript
   import { Module } from '@nestjs/common';
   import { AiStylistModule } from './ai-stylist/ai-stylist.module';
   import { TryOnModule } from './try-on/try-on.module';
   import { AiModule } from './ai/ai.module';
   import { AiSafetyModule } from './ai-safety/ai-safety.module';
   import { PhotosModule } from './photos/photos.module';

   @Module({
     imports: [
       AiStylistModule,
       TryOnModule,
       AiModule,
       AiSafetyModule,
       PhotosModule,
     ],
     exports: [
       AiStylistModule,
       TryOnModule,
       AiModule,
       AiSafetyModule,
       PhotosModule,
     ],
   })
   export class AiCoreModule {}
   ```
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/ai-core/` 返回 0
- `apps/backend/src/domains/ai-core/ai-core.module.ts` 包含 `export class AiCoreModule`
- `ai-core.module.ts` 导入所有 5 个子模块
</acceptance_criteria>

---

## Task 02: 迁移 ai 模块到 ai-core 域

<read_first>
- apps/backend/src/modules/ai/ai.module.ts
- apps/backend/src/modules/ai/ (所有文件)
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 复制 `src/modules/ai/` 整个目录到 `src/domains/ai-core/ai/`
2. 更新 `src/domains/ai-core/ai/ai.module.ts` 中的导入路径（如有跨模块引用）
3. 在 `src/app.module.ts` 中：
   - 将 `import { AiModule } from './modules/ai/ai.module'` 改为 `import { AiModule } from './domains/ai-core/ai/ai.module'`
4. 删除 `src/modules/ai/` 目录
5. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/ai-core/ai/` 返回 0
- `test ! -d apps/backend/src/modules/ai/` 返回 0
- `apps/backend/src/app.module.ts` 包含 `./domains/ai-core/ai/ai.module`
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 03: 迁移 ai-safety 模块到 ai-core 域

<read_first>
- apps/backend/src/modules/ai-safety/ai-safety.module.ts
- apps/backend/src/modules/ai-safety/ (所有文件)
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 复制 `src/modules/ai-safety/` 整个目录到 `src/domains/ai-core/ai-safety/`
2. 更新 `src/domains/ai-core/ai-safety/ai-safety.module.ts`：
   - 如果 AiSafetyModule 是 @Global 装饰器，保持 @Global 不变
   - 更新任何跨模块导入路径
3. 在 `src/app.module.ts` 中：
   - 将 AiSafetyModule 的导入路径改为 `./domains/ai-core/ai-safety/ai-safety.module`
4. 删除 `src/modules/ai-safety/` 目录
5. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/ai-core/ai-safety/` 返回 0
- `test ! -d apps/backend/src/modules/ai-safety/` 返回 0
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 04: 迁移 photos 模块到 ai-core 域

<read_first>
- apps/backend/src/modules/photos/photos.module.ts
- apps/backend/src/modules/photos/ (所有文件)
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 复制 `src/modules/photos/` 整个目录到 `src/domains/ai-core/photos/`
2. 更新 `src/domains/ai-core/photos/photos.module.ts` 中的导入路径：
   - 如果导入了 OnboardingModule，更新为正确路径（OnboardingModule 在 identity 域）
3. 在 `src/app.module.ts` 中：
   - 将 PhotosModule 的导入路径改为 `./domains/ai-core/photos/photos.module`
4. 删除 `src/modules/photos/` 目录
5. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/ai-core/photos/` 返回 0
- `test ! -d apps/backend/src/modules/photos/` 返回 0
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 05: 消除 AiStylistModule ↔ RecommendationsModule 循环依赖并迁移 ai-stylist

<read_first>
- apps/backend/src/modules/ai-stylist/ai-stylist.module.ts
- apps/backend/src/modules/ai-stylist/ (所有文件)
- apps/backend/src/modules/recommendations/recommendations.module.ts
- apps/backend/src/app.module.ts
</read_first>

<action>
1. **解耦策略**：AiStylistModule 通过事件驱动与 RecommendationsModule 通信，不再直接导入

2. 修改 `src/modules/ai-stylist/ai-stylist.module.ts`：
   - 移除 `forwardRef(() => RecommendationsModule)` 导入
   - 移除 `RecommendationsModule` 从 imports 数组
   - 如果 AiStylistService 直接注入 RecommendationsService，改为通过 EventEmitter2 发送事件

3. 修改 `src/modules/recommendations/recommendations.module.ts`（如果还在 modules/ 下）：
   - 移除 `forwardRef(() => AiStylistModule)` 导入
   - 移除 `AiStylistModule` 从 imports 数组
   - 添加事件监听器，监听 AiStylist 发出的事件

4. 复制 `src/modules/ai-stylist/` 整个目录到 `src/domains/ai-core/ai-stylist/`

5. 更新 `src/domains/ai-core/ai-stylist/ai-stylist.module.ts` 中的导入路径：
   - WeatherModule → `../../fashion/weather/weather.module`（已在 fashion 域）
   - PhotosModule → `../photos/photos.module`（已在 ai-core 域）
   - AIModule → `../ai/ai.module`（已在 ai-core 域）

6. 在 `src/app.module.ts` 中：
   - 将 AiStylistModule 的导入路径改为 `./domains/ai-core/ai-stylist/ai-stylist.module`

7. 删除 `src/modules/ai-stylist/` 目录

8. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/ai-core/ai-stylist/` 返回 0
- `test ! -d apps/backend/src/modules/ai-stylist/` 返回 0
- `grep -r "forwardRef.*AiStylist" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "forwardRef.*Recommendations" apps/backend/src/ --include="*.ts"` 返回空
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 06: 迁移 try-on 模块到 ai-core 域

<read_first>
- apps/backend/src/modules/try-on/try-on.module.ts
- apps/backend/src/modules/try-on/ (所有文件)
- apps/backend/src/app.module.ts
</read_first>

<action>
1. 复制 `src/modules/try-on/` 整个目录到 `src/domains/ai-core/try-on/`
2. 更新 `src/domains/ai-core/try-on/try-on.module.ts` 中的导入路径：
   - QueueModule → 保持从 platform 域导入（Phase 3 已迁移）或从 app.module 间接导入
   - GatewayModule → 保持 @Global 引用不变
   - AIModule → `../ai/ai.module`（已在 ai-core 域）
3. 在 `src/app.module.ts` 中：
   - 将 TryOnModule 的导入路径改为 `./domains/ai-core/try-on/try-on.module`
4. 删除 `src/modules/try-on/` 目录
5. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `test -d apps/backend/src/domains/ai-core/try-on/` 返回 0
- `test ! -d apps/backend/src/modules/try-on/` 返回 0
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Task 07: 更新所有跨模块导入路径引用

<read_first>
- apps/backend/src/domains/ai-core/ (所有模块文件)
- apps/backend/src/modules/ (检查是否有文件引用已迁移模块)
</read_first>

<action>
1. 搜索所有引用已迁移 AI 模块的文件：
   ```bash
   grep -r "from.*modules/ai-stylist" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/try-on" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/ai/" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/ai-safety" apps/backend/src/ --include="*.ts" -l
   grep -r "from.*modules/photos" apps/backend/src/ --include="*.ts" -l
   ```
2. 对每个匹配文件，将导入路径从 `./modules/xxx` 更新为 `./domains/ai-core/xxx`
3. 特别注意 queue 模块引用 TryOnModule 的路径
4. 运行 `cd apps/backend && npx nest build` 验证编译通过
</action>

<acceptance_criteria>
- `grep -r "from.*modules/ai-stylist" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "from.*modules/try-on" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "from.*modules/ai/" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "from.*modules/ai-safety" apps/backend/src/ --include="*.ts"` 返回空
- `grep -r "from.*modules/photos" apps/backend/src/ --include="*.ts"` 返回空
- `cd apps/backend && npx nest build` 退出码为 0
</acceptance_criteria>

---

## Verification

```bash
cd apps/backend && npx nest build && npx tsc --noEmit
grep -r "forwardRef.*AiStylist\|forwardRef.*Recommendations" apps/backend/src/ --include="*.ts"
```

编译通过且无 AiStylist ↔ Recommendations 的 forwardRef 残留。
