---
wave: 3
depends_on: [20-01, 20-02]
files_modified:
  - scripts/health-check.sh
  - scripts/health-check.ps1
  - docs/local-setup.md
  - README.md
autonomous: true
---

# Plan 20-03: Health Check 脚本 + 本地开发文档

## Objective

创建自动化健康检查脚本和完整的本地开发启动文档，确保 `docker-compose up` 后可以一键验证所有服务状态。

## Tasks

<task id="03-01" type="execute">
<name>Create health-check scripts, local-setup docs, and README section</name>
<read_first>
- docker-compose.local.yml (服务端口和 health endpoint 定义)
- apps/backend/src/domains/platform/health/health.controller.ts (NestJS health endpoints)
- ml/api/routes/health.py (FastAPI health endpoint)
- docker-compose.dev.yml (端口映射: postgres 5432, redis 6379, minio 9000, qdrant 6333)
- README.md (现有内容，追加 section)
</read_first>
<action>
1. **Create `scripts/health-check.sh`** (Linux/macOS/Git Bash):

```bash
#!/usr/bin/env bash
set -euo pipefail

SERVICES_OK=0
SERVICES_FAIL=0

check_service() {
  local name="$1"
  local url="$2"
  local expected="$3"

  if command -v curl &>/dev/null; then
    if curl -sf -o /dev/null -m 5 "$url" 2>/dev/null; then
      echo "  ✅ $name — healthy"
      ((SERVICES_OK++))
    else
      echo "  ❌ $name — unhealthy ($url)"
      ((SERVICES_FAIL++))
    fi
  else
    echo "  ⚠️  $name — skipped (curl not found)"
  fi
}

check_tcp() {
  local name="$1"
  local host="$2"
  local port="$3"

  if bash -c "echo >/dev/tcp/$host/$port" 2>/dev/null; then
    echo "  ✅ $name — healthy (port $port)"
    ((SERVICES_OK++))
  else
    echo "  ❌ $name — unhealthy ($host:$port)"
    ((SERVICES_FAIL++))
  fi
}

echo "🔍 XUNO Backend Health Check"
echo "================================"

echo ""
echo "Infrastructure:"
check_tcp "PostgreSQL" "localhost" "5432"
check_tcp "Redis"      "localhost" "6379"
check_tcp "MinIO"      "localhost" "9000"
check_tcp "Qdrant"     "localhost" "6333"

echo ""
echo "Application:"
check_service "FastAPI AI"  "http://localhost:8002/health" "healthy"
check_service "NestJS API"  "http://localhost:3001/api/v1/health" "healthy"

echo ""
echo "================================"
echo "  ✅ $SERVICES_OK healthy"
echo "  ❌ $SERVICES_FAIL unhealthy"
echo ""

if [ "$SERVICES_FAIL" -gt 0 ]; then
  exit 1
fi
exit 0
```

2. **Create `scripts/health-check.ps1`** (Windows PowerShell):

```powershell
$ok = 0
$fail = 0

function Check-Tcp($name, $port) {
  try {
    $tcp = New-Object System.Net.Sockets.TcpClient
    $tcp.Connect("localhost", $port)
    $tcp.Close()
    Write-Host "  OK $name - healthy (port $port)" -ForegroundColor Green
    $script:ok++
  } catch {
    Write-Host "  FAIL $name - unhealthy (localhost:$port)" -ForegroundColor Red
    $script:fail++
  }
}

function Check-Http($name, $url) {
  try {
    $r = Invoke-WebRequest -Uri $url -TimeoutSec 5 -UseBasicParsing
    Write-Host "  OK $name - healthy ($($r.StatusCode))" -ForegroundColor Green
    $script:ok++
  } catch {
    Write-Host "  FAIL $name - unhealthy ($url)" -ForegroundColor Red
    $script:fail++
  }
}

Write-Host "XUNO Backend Health Check" -ForegroundColor Cyan
Write-Host "================================"

Write-Host "`nInfrastructure:" -ForegroundColor Yellow
Check-Tcp "PostgreSQL" 5432
Check-Tcp "Redis" 6379
Check-Tcp "MinIO" 9000
Check-Tcp "Qdrant" 6333

Write-Host "`nApplication:" -ForegroundColor Yellow
Check-Http "FastAPI AI" "http://localhost:8002/health"
Check-Http "NestJS API" "http://localhost:3001/api/v1/health"

Write-Host "`n================================"
Write-Host "  OK: $ok healthy" -ForegroundColor Green
Write-Host "  FAIL: $fail unhealthy" -ForegroundColor Red

if ($fail -gt 0) { exit 1 }
exit 0
```

3. **Create `docs/local-setup.md`**:

```markdown
# XUNO 本地开发环境启动指南

> Seed 数据仅供开发测试，不代表真实用户或真实商品。所有 demo 数据标记 is_demo: true。

## 前置依赖

- Docker Desktop (with Docker Compose v2)
- Node.js 20+
- pnpm 9+
- Python 3.11+ (AI service)
- (可选) GLM API Key — 无 Key 时 AI 服务降级但不阻塞启动

## 快速启动

### 1. 配置环境变量

cp .env.local .env

编辑 .env，填入 API Key（可选）。

### 2. 一键启动所有服务

docker compose -f docker-compose.local.yml up --build

首次启动约需 3-5 分钟（构建镜像）。

### 3. 验证服务状态

# Linux/macOS/Git Bash

bash scripts/health-check.sh

# Windows PowerShell

pwsh scripts/health-check.ps1

所有 6 个服务应显示 healthy。

### 4. 运行数据库迁移 + Seed

docker compose -f docker-compose.local.yml exec backend npx prisma migrate deploy
docker compose -f docker-compose.local.yml exec backend npx prisma db seed

## 服务端口

| 服务       | 端口        | 用途                     |
| ---------- | ----------- | ------------------------ |
| PostgreSQL | 5432        | 数据库                   |
| Redis      | 6379        | 缓存/队列                |
| MinIO      | 9000 / 9001 | 对象存储 / 控制台        |
| Qdrant     | 6333 / 6334 | 向量数据库               |
| FastAPI AI | 8002        | AI 服务 (体型/色彩/推荐) |
| NestJS API | 3001        | 后端 API                 |

## Health Endpoints

| 服务       | URL                                 | 预期响应              |
| ---------- | ----------------------------------- | --------------------- |
| FastAPI AI | http://localhost:8002/health        | {"status": "healthy"} |
| NestJS API | http://localhost:3001/api/v1/health | {"status": "healthy"} |

## Demo/Sandbox 模式说明

以下服务在本地开发环境以 sandbox 或 degraded 模式运行：

- **定制化预览**: sandbox 模式，预览图片为占位符
- **定制化支付**: sandbox 模式，支付 ID 为 mock 值
- **天气服务**: 无 API Key 时返回 fallback mock 数据
- **AI 对话**: 无 GLM API Key 时返回 ServiceUnavailableException
- **体型/色彩分析**: 依赖 FastAPI 服务在线，否则返回 503

## 常见问题

### PostgreSQL 连接失败

docker compose -f docker-compose.local.yml logs postgres

### AI 服务启动失败

检查 Python 依赖是否安装完整：
docker compose -f docker-compose.local.yml logs ai-service

### Seed 失败

确认 backend 已完全启动（health check 通过），然后重新运行 seed。

### 端口冲突

修改 docker-compose.local.yml 中的端口映射。

## 停止服务

docker compose -f docker-compose.local.yml down

# 保留数据

docker compose -f docker-compose.local.yml down

# 清除数据

docker compose -f docker-compose.local.yml down -v
```

4. **Update README.md** — Add a section near the top (after project description):

````markdown
## 本地开发快速启动

一键启动所有后端服务：

```bash
docker compose -f docker-compose.local.yml up --build
```
````

详细步骤请参考 [docs/local-setup.md](docs/local-setup.md)。

> 注意：所有 seed 数据仅供开发测试，标记 is_demo: true，不代表真实用户或商品。

```

</action>
<acceptance_criteria>
- `scripts/health-check.sh` exists and is executable (has shebang `#!/usr/bin/env bash`)
- `scripts/health-check.ps1` exists with PowerShell syntax
- `scripts/health-check.sh` checks 4 TCP services (5432, 6379, 9000, 6333) and 2 HTTP services (8002, 3001)
- `scripts/health-check.sh` exits with code 1 when any service is unhealthy
- `scripts/health-check.sh` exits with code 0 when all services are healthy
- `docs/local-setup.md` exists
- `docs/local-setup.md` contains "Seed 数据仅供开发测试"
- `docs/local-setup.md` contains docker-compose.local.yml command
- `docs/local-setup.md` contains service port table with all 6 services
- `docs/local-setup.md` contains "Demo/Sandbox 模式说明" section
- `docs/local-setup.md` contains health-check script commands for both platforms
- `README.md` contains "本地开发快速启动" section
- `README.md` references docs/local-setup.md
- `README.md` contains "seed 数据仅供开发测试" disclaimer
</acceptance_criteria>
</task>

## Verification

1. `scripts/health-check.sh` runs without syntax error (`bash -n scripts/health-check.sh`)
2. `docs/local-setup.md` covers all required sections
3. `README.md` has local development section
4. All health check endpoints documented correctly

## Must Haves

- [ ] health-check.sh 检查 6 个服务（4 infra + 2 app）
- [ ] health-check.ps1 为 Windows 用户提供等效脚本
- [ ] docs/local-setup.md 包含完整启动步骤
- [ ] docs/local-setup.md 明确标注 demo/sandbox 模式
- [ ] docs/local-setup.md 包含 "Seed 数据仅供开发测试" 免责声明
- [ ] README.md 添加本地开发快速启动 section
- [ ] 脚本 exit code 0=全绿, 1=有失败
```
