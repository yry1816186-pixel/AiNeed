---
wave: 4
depends_on: [02, 03, 04, 05]
files_modified:
  - apps/mobile/tailwind.config.js
  - apps/mobile/nativewind.config.js
  - apps/mobile/postcss.config.js
  - apps/mobile/.eslintrc.json
autonomous: true
requirements: [DSGN-04, DSGN-05]
---

# Plan 06: 移除 NativeWind/Tailwind 死配置 + 最终验证

## Objective

移除 NativeWind/Tailwind 死配置文件，确认 React Native Paper 仅用于 Dialog/BottomSheet，执行最终验证确保所有 UAT 标准满足。

## Tasks

### Task 06-01: 移除 NativeWind/Tailwind 死配置

<objective>
删除 NativeWind/Tailwind 相关的配置文件。研究确认这些文件零源码使用，属于死配置。
</objective>

<read_first>
- apps/mobile/tailwind.config.js
- apps/mobile/nativewind.config.js
- apps/mobile/postcss.config.js
- apps/mobile/package.json
</read_first>

<action>
1. 确认无源码引用：
   - `cd apps/mobile && grep -r "nativewind" src/ --include="*.tsx" --include="*.ts"` — 应为空
   - `cd apps/mobile && grep -r "tailwindcss" src/ --include="*.tsx" --include="*.ts"` — 应为空
   - `cd apps/mobile && grep -r "className=" src/ --include="*.tsx"` — 应为空或仅含非 Tailwind 用法

2. 删除配置文件：
   - `rm apps/mobile/tailwind.config.js`
   - `rm apps/mobile/nativewind.config.js`
   - `rm apps/mobile/postcss.config.js`

3. 检查 package.json 中是否有 nativewind/tailwindcss 依赖，如有则移除：
   - 检查 `dependencies` 和 `devDependencies` 中的 `nativewind`, `tailwindcss`, `postcss` 相关包
   - 如果存在且无其他用途，执行 `cd apps/mobile && pnpm remove nativewind tailwindcss`

4. 运行类型检查和 Metro 验证：`cd apps/mobile && npx tsc --noEmit`
</action>

<acceptance_criteria>
- `test ! -f apps/mobile/tailwind.config.js` — 文件不存在
- `test ! -f apps/mobile/nativewind.config.js` — 文件不存在
- `test ! -f apps/mobile/postcss.config.js` — 文件不存在
- `cd apps/mobile && grep -r "nativewind" src/ --include="*.tsx" --include="*.ts"` — 无输出
- `cd apps/mobile && npx tsc --noEmit` — 无新增类型错误
</acceptance_criteria>

<verification>
确认文件已删除，类型检查通过。
</verification>

---

### Task 06-02: 确认 Paper 仅用于 Dialog/BottomSheet

<objective>
确认 React Native Paper 的使用范围符合 DSGN-05 要求：仅用于 Dialog/BottomSheet 等复杂交互组件。
</objective>

<read_first>
- apps/mobile/src/components/ui/PaperThemeProvider.tsx
- apps/mobile/package.json
</read_first>

<action>
1. 扫描 Paper 组件使用：
   - `cd apps/mobile && grep -r "from 'react-native-paper'" src/ --include="*.tsx" --include="*.ts"`
   - `cd apps/mobile && grep -r "react-native-paper" src/ --include="*.tsx" --include="*.ts"`

2. 预期结果：仅 `PaperThemeProvider.tsx` 导入 Paper

3. 如果发现其他组件使用 Paper：
   - Dialog/BottomSheet 组件：保留，符合 DSGN-05
   - 其他组件（Button, Card, Text 等）：替换为 DesignTokens 样式的自定义组件

4. 确认 PaperThemeProvider 正确从 DesignTokens 创建 MD3 主题
</action>

<acceptance_criteria>
- `cd apps/mobile && grep -r "from 'react-native-paper'" src/ --include="*.tsx" --include="*.ts"` 输出仅包含 PaperThemeProvider.tsx 或 Dialog/BottomSheet 组件
- PaperThemeProvider.tsx 从 DesignTokens 创建 MD3 主题
- 无组件直接使用 Paper 的 Button, Card, Text, Surface 等基础组件
</acceptance_criteria>

<verification>
运行 grep 扫描，确认 Paper 使用范围。
</verification>

---

### Task 06-03: 最终验证 — UAT 标准检查

<objective>
执行最终验证，确保所有 UAT 标准满足。
</objective>

<read_first>
- apps/mobile/scripts/audit-hardcoded-colors.ts
- apps/mobile/scripts/audit-hardcoded-typography.ts
- apps/mobile/scripts/audit-hardcoded-spacing.ts
</read_first>

<action>
1. 运行所有审计脚本：
   - `cd apps/mobile && npx ts-node scripts/audit-hardcoded-colors.ts`
   - `cd apps/mobile && npx ts-node scripts/audit-hardcoded-typography.ts`
   - `cd apps/mobile && npx ts-node scripts/audit-hardcoded-spacing.ts`

2. 验证 UAT 标准：
   - [ ] 0 处硬编码颜色值（用户可见组件）
   - [ ] 0 处硬编码 fontSize（用户可见组件）
   - [ ] 0 处硬编码间距（用户可见组件）
   - [ ] NativeWind/Tailwind 配置已移除
   - [ ] 暗色模式正常工作
   - [ ] 视觉回归测试通过（核心页面截图对比）

3. 运行类型检查：`cd apps/mobile && npx tsc --noEmit`

4. 运行测试：`cd apps/mobile && npx jest`

5. 启动 Metro 验证：`cd apps/mobile && npx react-native start --port 8081`（确认无启动错误）

6. 暗色模式验证（自动 + 手动）：
   - 自动验证：`cd apps/mobile && npx jest --testPathPattern=dark-mode` — 确认 darkTokens 的每个 key 在 DesignTokens 中有对应条目
   - 自动验证：`cd apps/mobile && grep -rl "DesignTokens.colors" src/ --include="*.tsx" | grep -v "theme/tokens/" | grep -v "design-system/"` — 输出为空（所有颜色已迁移到 useThemeStyles）
   - 手动验证：在设备/模拟器上切换暗色模式，检查核心页面（Home, StylistChat, Cart, Profile, Wardrobe）颜色正确切换，无白色文字在白色背景等问题

7. 视觉回归验证（手动截图对比）：
   - 使用设备/模拟器对 5 个核心页面（Home, StylistChat, Cart, Profile, Wardrobe）分别截图
   - 在 light 和 dark 模式下各截一张
   - 与迁移前基线截图对比，确认无非预期视觉变化
   - 如有 Detox 环境：`cd apps/mobile && npx detox test -c ios.sim.release --take-screenshots failing`

8. 将 ESLint 规则从 `warn` 改为 `error`：
   - 在 `.eslintrc.json` 中将 `"no-hardcoded-styles": "warn"` 改为 `"no-hardcoded-styles": "error"`
</action>

<acceptance_criteria>
- 审计脚本报告用户可见组件中硬编码颜色 = 0
- 审计脚本报告用户可见组件中硬编码 fontSize = 0
- 审计脚本报告用户可见组件中硬编码间距 = 0（排除布局值）
- `test ! -f apps/mobile/tailwind.config.js` — NativeWind/Tailwind 已移除
- `cd apps/mobile && npx tsc --noEmit` — 无类型错误
- `cd apps/mobile && npx jest` — 所有测试通过
- `cd apps/mobile && grep -rl "DesignTokens.colors" src/ --include="*.tsx" | grep -v "theme/tokens/" | grep -v "design-system/"` — 输出为空（暗色模式自动验证代理）
- `.eslintrc.json` 中 `"no-hardcoded-styles": "error"` — 阻止回退
- 手动暗色模式验证通过（5 个核心页面）
- 手动视觉回归验证通过（5 个核心页面 light+dark 截图对比）
</acceptance_criteria>

<verification>
运行所有审计脚本、类型检查和测试，手动验证暗色模式。
</verification>

---

### Task 06-04: 清理迁移工具和临时文件

<objective>
清理 codemod 脚本和临时审计报告，保留审计脚本作为长期质量工具。
</objective>

<read_first>
- apps/mobile/scripts/
</read_first>

<action>
1. 保留的文件（长期质量工具）：
   - `scripts/audit-hardcoded-colors.ts`
   - `scripts/audit-hardcoded-typography.ts`
   - `scripts/audit-hardcoded-spacing.ts`
   - `eslint-rules/no-hardcoded-styles.js`

2. 可删除的临时文件：
   - `scripts/color-audit-report.json`
   - `scripts/typography-audit-report.json`
   - `scripts/spacing-audit-report.json`
   - `scripts/color-migration-report.json`

3. 保留但归档的 codemod 脚本（可能用于后续阶段）：
   - `scripts/codemod-colors.ts`
   - `scripts/codemod-typography.ts`
   - `scripts/codemod-spacing.ts`
   - `scripts/semantic-color-map.json`
   - `scripts/semantic-typography-map.json`
   - `scripts/semantic-spacing-map.json`

4. 在 package.json 中保留审计脚本，移除 codemod 脚本（或标记为 deprecated）
</action>

<acceptance_criteria>
- 临时审计报告文件已删除
- 审计脚本保留在 scripts/ 目录
- `package.json` 中 `audit:colors`, `audit:typography`, `audit:spacing` 脚本保留
</acceptance_criteria>

<verification>
确认文件清理完成，审计脚本仍可运行。
</verification>

## Verification

1. 所有审计脚本报告 0 处硬编码值（用户可见组件）
2. `cd apps/mobile && npx tsc --noEmit` — 无类型错误
3. `cd apps/mobile && npx jest` — 所有测试通过
4. 暗色模式正常工作
5. NativeWind/Tailwind 配置已移除

## must_haves

- NativeWind/Tailwind 死配置完全移除（DSGN-04）
- Paper 仅用于 Dialog/BottomSheet（DSGN-05）
- 所有 UAT 标准满足
- ESLint 规则设为 error 阻止回退
