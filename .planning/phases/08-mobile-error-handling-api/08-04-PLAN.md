---
wave: 3
depends_on: [08-01-PLAN, 08-02-PLAN]
files_modified:
  - apps/mobile/src/shared/utils/logger.ts
  - apps/mobile/src/utils/logger.ts
  - apps/mobile/src/shared/components/screens/ScreenErrorBoundaries.ts
  - apps/mobile/src/components/screens/ScreenErrorBoundaries.ts
  - apps/mobile/src/shared/services/offline-cache.ts
  - apps/mobile/src/services/offline-cache.ts
  - apps/mobile/src/shared/utils/performanceMonitor.ts
  - apps/mobile/src/utils/performanceMonitor.ts
  - apps/mobile/src/shared/utils/performanceUtils.ts
  - apps/mobile/src/utils/performanceUtils.ts
  - apps/mobile/src/services/auth/wechat.ts
  - apps/mobile/src/services/api/client.ts
  - apps/mobile/src/features/stylist/components/AICompanionProvider.tsx
  - apps/mobile/src/components/aicompanion/AICompanionProvider.tsx
  - apps/mobile/src/features/auth/hooks/useAuth.ts
  - apps/mobile/src/hooks/useAuth.ts
  - apps/mobile/src/features/profile/screens/ProfileScreen.tsx
  - apps/mobile/src/features/commerce/screens/CartScreen.tsx
  - apps/mobile/src/features/wardrobe/stores/index.ts
  - apps/mobile/src/features/style-quiz/stores/index.ts
  - apps/mobile/src/features/onboarding/stores/index.ts
  - apps/mobile/src/features/consultant/stores/index.ts
  - apps/mobile/src/features/commerce/stores/index.ts
requirements_addressed: [QUAL-08]
autonomous: true
---

# Plan 04: console.log 清理与重复 Store 修复

## Objective

清理所有 console.log 残留（替换为 console.error 或移除），修复重复的 auth Store 引用。

## Tasks

### Task 1: logger.ts — 将 console.log 改为条件输出

<read_first>
- apps/mobile/src/shared/utils/logger.ts
- apps/mobile/src/utils/logger.ts
</read_first>

<action>
在两个 logger.ts 文件中：

1. 将 `console.log(...args)` 改为条件输出：
   ```typescript
   log: (...args: unknown[]) => {
     if (__DEV__) {
       console.log('[LOG]', ...args);
     }
   },
   ```
2. 确保 info/warn/error 方法已正确实现
3. 检查是否有其他代码直接使用 `console.log` 而非 logger
</action>

<acceptance_criteria>
- logger.ts 的 log 方法仅在 __DEV__ 下输出
- `grep "console.log" apps/mobile/src/shared/utils/logger.ts` 仅在 log 方法内出现
</acceptance_criteria>

---

### Task 2: ScreenErrorBoundaries.ts — console.log 替换为 console.error

<read_first>
- apps/mobile/src/shared/components/screens/ScreenErrorBoundaries.ts
- apps/mobile/src/components/screens/ScreenErrorBoundaries.ts
</read_first>

<action>
在两个 ScreenErrorBoundaries.ts 文件中：

1. 将所有 onReset 回调中的 `console.log` 替换为 `console.error`
2. 例如：`console.log('HomeScreen reset')` → `console.error('HomeScreen error boundary reset')`
3. 约 26 处需要替换
</action>

<acceptance_criteria>
- `grep -c "console.log" apps/mobile/src/shared/components/screens/ScreenErrorBoundaries.ts` 返回 0
- 所有原 console.log 已替换为 console.error
</acceptance_criteria>

---

### Task 3: offline-cache.ts — console.log 替换为 logger

<read_first>
- apps/mobile/src/shared/services/offline-cache.ts
- apps/mobile/src/services/offline-cache.ts
</read_first>

<action>
在两个 offline-cache.ts 文件中：

1. 导入 logger：`import { logger } from '../utils/logger';`（使用相对路径适配文件位置）
2. 将所有 `console.log(...)` 替换为 `logger.info(...)`
3. 保留 `console.error` 不变（这些已经是正确的错误日志）
</action>

<acceptance_criteria>
- `grep -c "console.log" apps/mobile/src/shared/services/offline-cache.ts` 返回 0
- console.error 保持不变
</acceptance_criteria>

---

### Task 4: performanceMonitor.ts / performanceUtils.ts — console.log 替换

<read_first>
- apps/mobile/src/shared/utils/performanceMonitor.ts
- apps/mobile/src/utils/performanceMonitor.ts
- apps/mobile/src/shared/utils/performanceUtils.ts
- apps/mobile/src/utils/performanceUtils.ts
</read_first>

<action>
在 4 个文件中：

1. 将所有 `console.log(...)` 替换为 `__DEV__` 条件输出：
   ```typescript
   if (__DEV__) {
     console.log('[Performance]', ...);
   }
   ```
2. 不导入 logger，保持性能工具的独立性
</action>

<acceptance_criteria>
- `grep -c "console.log" apps/mobile/src/shared/utils/performanceMonitor.ts` 返回 0
- `grep -c "console.log" apps/mobile/src/shared/utils/performanceUtils.ts` 返回 0
- 性能日志仅在开发环境输出
</acceptance_criteria>

---

### Task 5: 其他 console.log 清理

<read_first>
- apps/mobile/src/services/auth/wechat.ts
- apps/mobile/src/services/api/client.ts
- apps/mobile/src/features/stylist/components/AICompanionProvider.tsx
- apps/mobile/src/components/aicompanion/AICompanionProvider.tsx
- apps/mobile/src/features/profile/screens/ProfileScreen.tsx
- apps/mobile/src/features/commerce/screens/CartScreen.tsx
</read_first>

<action>
1. **wechat.ts:139** — `console.log('Share to WeChat:')` → `console.error('Share to WeChat failed:')`
2. **client.ts:139,153** — API 请求/响应日志：包裹在 `if (__DEV__) { ... }` 中
3. **AICompanionProvider.tsx:469** — `console.log('AI companion sendMessage failed:')` → `console.error('AI companion sendMessage failed:')`
4. **ProfileScreen.tsx:600** — Error boundary reset → `console.error('[ProfileScreen] Error boundary reset')`
5. **CartScreen.tsx:935** — 同上 → `console.error('[CartScreen] Error boundary reset')`
</action>

<acceptance_criteria>
- `grep -rn "console.log" apps/mobile/src/features/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | grep -v "__DEV__" | wc -l` 返回 0（或极少）
- wechat.ts 无 console.log
- client.ts 的 console.log 在 __DEV__ 条件内
</acceptance_criteria>

---

### Task 6: 修复重复 Auth Store 引用

<read_first>
- apps/mobile/src/features/auth/hooks/useAuth.ts
- apps/mobile/src/hooks/useAuth.ts
- apps/mobile/src/features/auth/stores/authStore.ts
- apps/mobile/src/stores/index.ts
</read_first>

<action>
1. **features/auth/hooks/useAuth.ts** — 更新 import 路径：
   将 `import { useAuthStore } from "../stores/auth.store";` 改为 `import { useAuthStore } from "../stores/authStore";`
2. **hooks/useAuth.ts** — 删除此文件，它是 features/auth/hooks/useAuth.ts 的重复副本。搜索所有引用 `hooks/useAuth` 的文件，更新为 `features/auth/hooks/useAuth`
3. 搜索所有文件中 `from.*auth\.store` 或 `from.*stores/auth\.store` 的引用，全部更新为正确的 authStore 路径
</action>

<acceptance_criteria>
- `grep -rn "auth.store" apps/mobile/src/ --include="*.ts" --include="*.tsx"` 返回 0
- `grep -rn "auth\.store" apps/mobile/src/ --include="*.ts" --include="*.tsx"` 返回 0
- `test -f apps/mobile/src/hooks/useAuth.ts` 返回 false（文件已删除）
- 无 TypeScript 编译错误
</acceptance_criteria>

---

### Task 7: 清理旧版 stores/index.ts 中的 Stub 代码

<read_first>
- apps/mobile/src/features/wardrobe/stores/index.ts
- apps/mobile/src/features/style-quiz/stores/index.ts
- apps/mobile/src/features/onboarding/stores/index.ts
- apps/mobile/src/features/consultant/stores/index.ts
- apps/mobile/src/features/commerce/stores/index.ts
</read_first>

<action>
这些旧版 stores/index.ts 文件包含 Stub 方法和重复的 Store 定义。统一处理：

1. 在每个文件顶部添加 JSDoc deprecated 标记：
   ```typescript
   /**
    * @deprecated 此文件包含旧版 Stub Store 定义。
    * 请使用同目录下的独立 Store 文件（如 orderStore.ts, couponStore.ts 等）。
    * 新版 Store 已连接真实 API 并包含 error 状态管理。
    */
   ```
2. 搜索所有引用这些旧版 index.ts 的 import 语句，更新为独立 Store 文件路径
3. 确保主入口 `stores/index.ts` 不导出这些旧版 Store（仅导出独立 Store 文件中的版本）
4. 不删除文件（避免破坏可能的间接引用），但 deprecated 标记会触发 IDE 警告
</action>

<acceptance_criteria>
- `grep -l "@deprecated" apps/mobile/src/features/wardrobe/stores/index.ts apps/mobile/src/features/style-quiz/stores/index.ts apps/mobile/src/features/onboarding/stores/index.ts apps/mobile/src/features/consultant/stores/index.ts apps/mobile/src/features/commerce/stores/index.ts` 返回 5 个文件
- 主入口 stores/index.ts 不导出旧版 Store
- `grep -rn "from.*stores/index" apps/mobile/src/features/ --include="*.ts" --include="*.tsx" | wc -l` 接近 0
</acceptance_criteria>

---

## Verification

1. `grep -rn "console.log" apps/mobile/src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | grep -v "__DEV__" | grep -v "logger" | wc -l` 接近 0
2. `grep -rn "auth.store" apps/mobile/src/ --include="*.ts" --include="*.tsx"` 返回 0
3. 旧版 stores/index.ts 有 deprecated 标记
4. `cd apps/mobile && npx tsc --noEmit` 无新增错误

## must_haves

- 生产环境无 console.log 输出（除 __DEV__ 条件内的）
- auth Store 引用路径正确
- 旧版 Stub Store 标记为 deprecated
